
(function ($fog$888, $fog$0, $fog$1, $fog$2, $fog$3, $fog$4, $fog$5, $fog$6, $fog$7, $fog$8, $fog$9, $fog$10, $fog$11, $fog$12, $fog$13, $fog$14, $fog$15, $fog$16, $fog$17, $fog$18, $fog$19, $fog$20, $fog$21, $fog$22, $fog$23, $fog$24, $fog$25, $fog$26, $fog$27, $fog$28, $fog$29, $fog$30, $fog$31, $fog$32, $fog$33, $fog$34, $fog$35, $fog$36, $fog$37, $fog$38, $fog$39, $fog$40, $fog$41, $fog$42, $fog$43, $fog$44, $fog$45, $fog$46, $fog$47, $fog$48, $fog$49, $fog$50, $fog$51, $fog$52, $fog$53, $fog$54, $fog$55, $fog$56, $fog$57, $fog$58, $fog$59, $fog$60, $fog$61, $fog$62, $fog$63, $fog$64, $fog$65, $fog$66, $fog$67, $fog$68, $fog$69, $fog$70, $fog$71, $fog$72, $fog$73, $fog$74, $fog$75, $fog$76, $fog$77, $fog$78, $fog$79, $fog$80, $fog$81, $fog$82, $fog$83, $fog$84, $fog$85, $fog$86, $fog$87, $fog$88, $fog$89, $fog$90, $fog$91, $fog$92, $fog$93, $fog$94, $fog$95, $fog$96, $fog$97, $fog$98, $fog$99, $fog$100, $fog$101, $fog$102, $fog$103, $fog$104, $fog$105, $fog$106, $fog$107, $fog$108, $fog$109, $fog$110, $fog$111, $fog$112, $fog$113, $fog$114, $fog$115, $fog$116, $fog$117, $fog$118, $fog$119, $fog$120, $fog$121, $fog$122, $fog$123, $fog$124, $fog$125, $fog$126, $fog$127, $fog$128, $fog$129, $fog$130, $fog$131, $fog$132, $fog$133, $fog$134, $fog$135, $fog$136, $fog$137, $fog$138, $fog$139, $fog$140, $fog$141, $fog$142, $fog$143, $fog$144, $fog$145, $fog$146, $fog$147, $fog$148, $fog$149, $fog$150, $fog$151, $fog$152, $fog$153, $fog$154, $fog$155, $fog$156, $fog$157, $fog$158, $fog$159, $fog$160, $fog$161, $fog$162, $fog$163, $fog$164, $fog$165, $fog$166, $fog$167, $fog$168, $fog$169, $fog$170, $fog$171, $fog$172, $fog$173, $fog$174, $fog$175, $fog$176, $fog$177, $fog$178, $fog$179, $fog$180, $fog$181, $fog$182, $fog$183, $fog$184, $fog$185, $fog$186, $fog$187, $fog$188, $fog$189, $fog$190, $fog$191, $fog$192, $fog$193, $fog$194, $fog$195, $fog$196, $fog$197, $fog$198, $fog$199, $fog$200, $fog$201, $fog$202, $fog$203, $fog$204, $fog$205, $fog$206, $fog$207, $fog$208, $fog$209, $fog$210, $fog$211, $fog$212, $fog$213, $fog$214, $fog$215, $fog$216, $fog$217, $fog$218, $fog$219, $fog$220, $fog$221, $fog$222, $fog$223, $fog$224, $fog$225, $fog$226, $fog$227, $fog$228, $fog$229, $fog$230, $fog$231, $fog$232, $fog$233, $fog$234, $fog$235, $fog$236, $fog$237, $fog$238, $fog$239, $fog$240, $fog$241, $fog$242, $fog$243, $fog$244, $fog$245, $fog$246, $fog$247, $fog$248, $fog$249, $fog$250, $fog$251, $fog$252, $fog$253, $fog$254, $fog$255, $fog$256, $fog$257, $fog$258, $fog$259, $fog$260, $fog$261, $fog$262, $fog$263, $fog$264, $fog$265, $fog$266, $fog$267, $fog$268, $fog$269, $fog$270, $fog$271, $fog$272, $fog$273, $fog$274, $fog$275, $fog$276, $fog$277, $fog$278, $fog$279, $fog$280, $fog$281, $fog$282, $fog$283, $fog$284, $fog$285, $fog$286, $fog$287, $fog$288, $fog$289, $fog$290, $fog$291, $fog$292, $fog$293, $fog$294, $fog$295, $fog$296, $fog$297, $fog$298, $fog$299, $fog$300, $fog$301, $fog$302, $fog$303, $fog$304, $fog$305, $fog$306, $fog$307, $fog$308, $fog$309, $fog$310, $fog$311, $fog$312, $fog$313, $fog$314, $fog$315, $fog$316, $fog$317, $fog$318, $fog$319, $fog$320, $fog$321, $fog$322, $fog$323, $fog$324, $fog$325, $fog$326, $fog$327, $fog$328, $fog$329, $fog$330, $fog$331, $fog$332, $fog$333, $fog$334, $fog$335, $fog$336, $fog$337, $fog$338, $fog$339, $fog$340, $fog$341, $fog$342, $fog$343, $fog$344, $fog$345, $fog$346, $fog$347, $fog$348, $fog$349, $fog$350, $fog$351, $fog$352, $fog$353, $fog$354, $fog$355, $fog$356, $fog$357, $fog$358, $fog$359, $fog$360, $fog$361, $fog$362, $fog$363, $fog$364, $fog$365, $fog$366, $fog$367, $fog$368, $fog$369, $fog$370, $fog$371, $fog$372, $fog$373, $fog$374, $fog$375, $fog$376, $fog$377, $fog$378, $fog$379, $fog$380, $fog$381, $fog$382, $fog$383, $fog$384, $fog$385, $fog$386, $fog$387, $fog$388, $fog$389, $fog$390, $fog$391, $fog$392, $fog$393, $fog$394, $fog$395, $fog$396, $fog$397, $fog$398, $fog$399, $fog$400, $fog$401, $fog$402, $fog$403, $fog$404, $fog$405, $fog$406, $fog$407, $fog$408, $fog$409, $fog$410, $fog$411, $fog$412, $fog$413, $fog$414, $fog$415, $fog$416, $fog$417, $fog$418, $fog$419, $fog$420, $fog$421, $fog$422, $fog$423, $fog$424, $fog$425, $fog$426, $fog$427, $fog$428, $fog$429, $fog$430, $fog$431, $fog$432, $fog$433, $fog$434, $fog$435, $fog$436, $fog$437, $fog$438, $fog$439, $fog$440, $fog$441, $fog$442, $fog$443, $fog$444, $fog$445, $fog$446, $fog$447, $fog$448, $fog$449, $fog$450, $fog$451, $fog$452, $fog$453, $fog$454, $fog$455, $fog$456, $fog$457, $fog$458, $fog$459, $fog$460, $fog$461, $fog$462, $fog$463, $fog$464, $fog$465, $fog$466, $fog$467, $fog$468, $fog$469, $fog$470, $fog$471, $fog$472, $fog$473, $fog$474, $fog$475, $fog$476, $fog$477, $fog$478, $fog$479, $fog$480, $fog$481, $fog$482, $fog$483, $fog$484, $fog$485, $fog$486, $fog$487, $fog$488, $fog$489, $fog$490, $fog$491, $fog$492, $fog$493, $fog$494, $fog$495, $fog$496, $fog$497, $fog$498, $fog$499, $fog$500, $fog$501, $fog$502, $fog$503, $fog$504, $fog$505, $fog$506, $fog$507, $fog$508, $fog$509, $fog$510, $fog$511, $fog$512, $fog$513, $fog$514, $fog$515, $fog$516, $fog$517, $fog$518, $fog$519, $fog$520, $fog$521, $fog$522, $fog$523, $fog$524, $fog$525, $fog$526, $fog$527, $fog$528, $fog$529, $fog$530, $fog$531, $fog$532, $fog$533, $fog$534, $fog$535, $fog$536, $fog$537, $fog$538, $fog$539, $fog$540, $fog$541, $fog$542, $fog$543, $fog$544, $fog$545, $fog$546, $fog$547, $fog$548, $fog$549, $fog$550, $fog$551, $fog$552, $fog$553, $fog$554, $fog$555, $fog$556, $fog$557, $fog$558, $fog$559, $fog$560, $fog$561, $fog$562, $fog$563, $fog$564, $fog$565, $fog$566, $fog$567, $fog$568, $fog$569, $fog$570, $fog$571, $fog$572, $fog$573, $fog$574, $fog$575, $fog$576, $fog$577, $fog$578, $fog$579, $fog$580, $fog$581, $fog$582, $fog$583, $fog$584, $fog$585, $fog$586, $fog$587, $fog$588, $fog$589, $fog$590, $fog$591, $fog$592, $fog$593, $fog$594, $fog$595, $fog$596, $fog$597, $fog$598, $fog$599, $fog$600, $fog$601, $fog$602, $fog$603, $fog$604, $fog$605, $fog$606, $fog$607, $fog$608, $fog$609, $fog$610, $fog$611, $fog$612, $fog$613, $fog$614, $fog$615, $fog$616, $fog$617, $fog$618, $fog$619, $fog$620, $fog$621, $fog$622, $fog$623, $fog$624, $fog$625, $fog$626, $fog$627, $fog$628, $fog$629, $fog$630, $fog$631, $fog$632, $fog$633, $fog$634, $fog$635, $fog$636, $fog$637, $fog$638, $fog$639, $fog$640, $fog$641, $fog$642, $fog$643, $fog$644, $fog$645, $fog$646, $fog$647, $fog$648, $fog$649, $fog$650, $fog$651, $fog$652, $fog$653, $fog$654, $fog$655, $fog$656, $fog$657, $fog$658, $fog$659, $fog$660, $fog$661, $fog$662, $fog$663, $fog$664, $fog$665, $fog$666, $fog$667, $fog$668, $fog$669, $fog$670, $fog$671, $fog$672, $fog$673, $fog$674, $fog$675, $fog$676, $fog$677, $fog$678, $fog$679, $fog$680, $fog$681, $fog$682, $fog$683, $fog$684, $fog$685, $fog$686, $fog$687, $fog$688, $fog$689, $fog$690, $fog$691, $fog$692, $fog$693, $fog$694, $fog$695, $fog$696, $fog$697, $fog$698, $fog$699, $fog$700, $fog$701, $fog$702, $fog$703, $fog$704, $fog$705, $fog$706, $fog$707, $fog$708, $fog$709, $fog$710, $fog$711, $fog$712, $fog$713, $fog$714, $fog$715, $fog$716, $fog$717, $fog$718, $fog$719, $fog$720, $fog$721, $fog$722, $fog$723, $fog$724, $fog$725, $fog$726, $fog$727, $fog$728, $fog$729, $fog$730, $fog$731, $fog$732, $fog$733, $fog$734, $fog$735, $fog$736, $fog$737, $fog$738, $fog$739, $fog$740, $fog$741, $fog$742, $fog$743, $fog$744, $fog$745, $fog$746, $fog$747, $fog$748, $fog$749, $fog$750, $fog$751, $fog$752, $fog$753, $fog$754, $fog$755, $fog$756, $fog$757, $fog$758, $fog$759, $fog$760, $fog$761, $fog$762, $fog$763, $fog$764, $fog$765, $fog$766, $fog$767, $fog$768, $fog$769, $fog$770, $fog$771, $fog$772, $fog$773, $fog$774, $fog$775, $fog$776, $fog$777, $fog$778, $fog$779, $fog$780, $fog$781, $fog$782, $fog$783, $fog$784, $fog$785, $fog$786, $fog$787, $fog$788, $fog$789, $fog$790, $fog$791, $fog$792, $fog$793, $fog$794, $fog$795, $fog$796, $fog$797, $fog$798, $fog$799, $fog$800, $fog$801, $fog$802, $fog$803, $fog$804, $fog$805, $fog$806, $fog$807, $fog$808, $fog$809, $fog$810, $fog$811, $fog$812, $fog$813, $fog$814, $fog$815, $fog$816, $fog$817, $fog$818, $fog$819, $fog$820, $fog$821, $fog$822, $fog$823, $fog$824, $fog$825, $fog$826, $fog$827, $fog$828, $fog$829, $fog$830, $fog$831, $fog$832, $fog$833, $fog$834, $fog$835, $fog$836, $fog$837, $fog$838, $fog$839, $fog$840, $fog$841, $fog$842, $fog$843, $fog$844, $fog$845, $fog$846, $fog$847, $fog$848, $fog$849, $fog$850, $fog$851, $fog$852, $fog$853, $fog$854, $fog$855, $fog$856, $fog$857, $fog$858, $fog$859, $fog$860, $fog$861, $fog$862, $fog$863, $fog$864, $fog$865, $fog$866, $fog$867, $fog$868, $fog$869, $fog$870, $fog$871, $fog$872, $fog$873, $fog$874, $fog$875, $fog$876, $fog$877, $fog$878, $fog$879, $fog$880, $fog$881, $fog$882, $fog$883, $fog$884, $fog$885, $fog$886, $fog$887) {
  
if ("‮" !== $fog$888) {
  return;
}
        
  (function(){
/*!
 * jQuery JavaScript Library v2.1.4
 * http://jquery.com/
 *
 * Includes Sizzle.js
 * http://sizzlejs.com/
 *
 * Copyright 2005, 2014 jQuery Foundation, Inc. and other contributors
 * Released under the MIT license
 * http://jquery.org/license
 *
 * Date: 2015-04-28T16:01Z
 */

(function( global, factory ) {

  if ( typeof module === $fog$0 && typeof module[$fog$1] === $fog$0 ) {
    // For CommonJS and CommonJS-like environments where a proper `window`
    // is present, execute the factory and get jQuery.
    // For environments that do not have a `window` with a `document`
    // (such as Node.js), expose a factory as module.exports.
    // This accentuates the need for the creation of a real `window`.
    // e.g. var jQuery = require("jquery")(window);
    // See ticket #14549 for more info.
    module[$fog$1] = global[$fog$2] ?
      factory( global, $fog$3 ) :
      function( w ) {
        if ( !w[$fog$2] ) {
          throw new Error( $fog$4 );
        }
        return factory( w );
      };
  } else {
    factory( global );
  }

// Pass this if window is not defined yet
}(typeof window !== $fog$5 ? window : this, function( window, noGlobal ) {

// Support: Firefox 18+
// Can't be in strict mode, several libs including ASP.NET trace
// the stack via arguments.caller.callee and Firefox dies if
// you try to trace through "use strict" call chains. (#13335)
//

var arr = [];

var slice = arr[$fog$6];

var concat = arr[$fog$7];

var push = arr[$fog$8];

var indexOf = arr[$fog$9];

var class2type = {};

var toString = class2type[$fog$10];

var hasOwn = class2type[$fog$11];

var support = {};



var
  // Use the correct document accordingly with window argument (sandbox)
  document = window[$fog$2],

  version = $fog$12,

  // Define a local copy of jQuery
  jQuery = function( selector, context ) {
    // The jQuery object is actually just the init constructor 'enhanced'
    // Need init if jQuery is called (just allow error to be thrown if not included)
    return new jQuery[$fog$14][$fog$13]( selector, context );
  },

  // Support: Android<4.1
  // Make sure we trim BOM and NBSP
  rtrim = $fog$15,

  // Matches dashed string for camelizing
  rmsPrefix = $fog$16,
  rdashAlpha = $fog$17,

  // Used by jQuery.camelCase as callback to replace()
  fcamelCase = function( all, letter ) {
    return letter[$fog$18]();
  };

jQuery[$fog$14] = jQuery[$fog$19] = {
  // The current version of jQuery being used
  jquery: version,

  constructor: jQuery,

  // Start with an empty selector
  selector: $fog$20,

  // The default length of a jQuery object is 0
  length: $fog$21,

  toArray: function() {
    return slice[$fog$22]( this );
  },

  // Get the Nth element in the matched element set OR
  // Get the whole matched element set as a clean array
  get: function( num ) {
    return num != $fog$23 ?

      // Return just the one element from the set
      ( num < $fog$21 ? this[ num + this[$fog$24] ] : this[ num ] ) :

      // Return all the elements in a clean array
      slice[$fog$22]( this );
  },

  // Take an array of elements and push it onto the stack
  // (returning the new matched element set)
  pushStack: function( elems ) {

    // Build a new jQuery matched element set
    var ret = jQuery[$fog$25]( this[$fog$26](), elems );

    // Add the old object onto the stack (as a reference)
    ret[$fog$27] = this;
    ret[$fog$28] = this[$fog$28];

    // Return the newly-formed element set
    return ret;
  },

  // Execute a callback for every element in the matched set.
  // (You can seed the arguments with an array of args, but this is
  // only used internally.)
  each: function( callback, args ) {
    return jQuery[$fog$29]( this, callback, args );
  },

  map: function( callback ) {
    return this[$fog$30]( jQuery[$fog$31](this, function( elem, i ) {
      return callback[$fog$22]( elem, i, elem );
    }));
  },

  slice: function() {
    return this[$fog$30]( slice[$fog$32]( this, arguments ) );
  },

  first: function() {
    return this[$fog$33]( $fog$21 );
  },

  last: function() {
    return this[$fog$33]( -$fog$34 );
  },

  eq: function( i ) {
    var len = this[$fog$24],
      j = +i + ( i < $fog$21 ? len : $fog$21 );
    return this[$fog$30]( j >= $fog$21 && j < len ? [ this[j] ] : [] );
  },

  end: function() {
    return this[$fog$27] || this[$fog$26]($fog$23);
  },

  // For internal use only.
  // Behaves like an Array's method, not like a jQuery method.
  push: push,
  sort: arr[$fog$35],
  splice: arr[$fog$36]
};

jQuery[$fog$37] = jQuery[$fog$14][$fog$37] = function() {
  var options, name, src, copy, copyIsArray, clone,
    target = arguments[$fog$21] || {},
    i = $fog$34,
    length = arguments[$fog$24],
    deep = $fog$38;

  // Handle a deep copy situation
  if ( typeof target === $fog$39 ) {
    deep = target;

    // Skip the boolean and the target
    target = arguments[ i ] || {};
    i++;
  }

  // Handle case when target is a string or something (possible in deep copy)
  if ( typeof target !== $fog$0 && !jQuery[$fog$40](target) ) {
    target = {};
  }

  // Extend jQuery itself if only one argument is passed
  if ( i === length ) {
    target = this;
    i--;
  }

  for ( ; i < length; i++ ) {
    // Only deal with non-null/undefined values
    if ( (options = arguments[ i ]) != $fog$23 ) {
      // Extend the base object
      for ( name in options ) {
        src = target[ name ];
        copy = options[ name ];

        // Prevent never-ending loop
        if ( target === copy ) {
          continue;
        }

        // Recurse if we're merging plain objects or arrays
        if ( deep && copy && ( jQuery[$fog$41](copy) || (copyIsArray = jQuery[$fog$42](copy)) ) ) {
          if ( copyIsArray ) {
            copyIsArray = $fog$38;
            clone = src && jQuery[$fog$42](src) ? src : [];

          } else {
            clone = src && jQuery[$fog$41](src) ? src : {};
          }

          // Never move original objects, clone them
          target[ name ] = jQuery[$fog$37]( deep, clone, copy );

        // Don't bring in undefined values
        } else if ( copy !== undefined ) {
          target[ name ] = copy;
        }
      }
    }
  }

  // Return the modified object
  return target;
};

jQuery[$fog$37]({
  // Unique for each copy of jQuery on the page
  expando: $fog$43 + ( version + Math[$fog$45]() )[$fog$44]( $fog$46, $fog$20 ),

  // Assume jQuery is ready without the ready module
  isReady: $fog$3,

  error: function( msg ) {
    throw new Error( msg );
  },

  noop: function() {},

  isFunction: function( obj ) {
    return jQuery[$fog$47](obj) === $fog$48;
  },

  isArray: Array[$fog$42],

  isWindow: function( obj ) {
    return obj != $fog$23 && obj === obj[$fog$49];
  },

  isNumeric: function( obj ) {
    // parseFloat NaNs numeric-cast false positives (null|true|false|"")
    // ...but misinterprets leading-number strings, particularly hex literals ("0x...")
    // subtraction forces infinities to NaN
    // adding 1 corrects loss of precision from parseFloat (#15100)
    return !jQuery[$fog$42]( obj ) && (obj - parseFloat( obj ) + $fog$34) >= $fog$21;
  },

  isPlainObject: function( obj ) {
    // Not plain objects:
    // - Any object or value whose internal [[Class]] property is not "[object Object]"
    // - DOM nodes
    // - window
    if ( jQuery[$fog$47]( obj ) !== $fog$0 || obj[$fog$50] || jQuery[$fog$51]( obj ) ) {
      return $fog$38;
    }

    if ( obj[$fog$26] &&
        !hasOwn[$fog$22]( obj[$fog$26][$fog$19], $fog$52 ) ) {
      return $fog$38;
    }

    // If the function hasn't returned already, we're confident that
    // |obj| is a plain object, created by {} or constructed with new Object
    return $fog$3;
  },

  isEmptyObject: function( obj ) {
    var name;
    for ( name in obj ) {
      return $fog$38;
    }
    return $fog$3;
  },

  type: function( obj ) {
    if ( obj == $fog$23 ) {
      return obj + $fog$20;
    }
    // Support: Android<4.0, iOS<6 (functionish RegExp)
    return typeof obj === $fog$0 || typeof obj === $fog$48 ?
      class2type[ toString[$fog$22](obj) ] || $fog$0 :
      typeof obj;
  },

  // Evaluates a script in a global context
  globalEval: function( code ) {
    var script,
      indirect = eval;

    code = jQuery[$fog$53]( code );

    if ( code ) {
      // If the code includes a valid, prologue position
      // strict mode pragma, execute code by injecting a
      // script tag into the document.
      if ( code[$fog$9]($fog$54) === $fog$34 ) {
        script = document[$fog$55]($fog$56);
        script[$fog$57] = code;
        document[$fog$61][$fog$60]( script )[$fog$59][$fog$58]( script );
      } else {
      // Otherwise, avoid the DOM node creation, insertion
      // and removal by using an indirect global eval
        indirect( code );
      }
    }
  },

  // Convert dashed to camelCase; used by the css and data modules
  // Support: IE9-11+
  // Microsoft forgot to hump their vendor prefix (#9572)
  camelCase: function( string ) {
    return string[$fog$44]( rmsPrefix, $fog$62 )[$fog$44]( rdashAlpha, fcamelCase );
  },

  nodeName: function( elem, name ) {
    return elem[$fog$63] && elem[$fog$63][$fog$64]() === name[$fog$64]();
  },

  // args is for internal usage only
  each: function( obj, callback, args ) {
    var value,
      i = $fog$21,
      length = obj[$fog$24],
      isArray = isArraylike( obj );

    if ( args ) {
      if ( isArray ) {
        for ( ; i < length; i++ ) {
          value = callback[$fog$32]( obj[ i ], args );

          if ( value === $fog$38 ) {
            break;
          }
        }
      } else {
        for ( i in obj ) {
          value = callback[$fog$32]( obj[ i ], args );

          if ( value === $fog$38 ) {
            break;
          }
        }
      }

    // A special, fast, case for the most common use of each
    } else {
      if ( isArray ) {
        for ( ; i < length; i++ ) {
          value = callback[$fog$22]( obj[ i ], i, obj[ i ] );

          if ( value === $fog$38 ) {
            break;
          }
        }
      } else {
        for ( i in obj ) {
          value = callback[$fog$22]( obj[ i ], i, obj[ i ] );

          if ( value === $fog$38 ) {
            break;
          }
        }
      }
    }

    return obj;
  },

  // Support: Android<4.1
  trim: function( text ) {
    return text == $fog$23 ?
      $fog$20 :
      ( text + $fog$20 )[$fog$44]( rtrim, $fog$20 );
  },

  // results is for internal usage only
  makeArray: function( arr, results ) {
    var ret = results || [];

    if ( arr != $fog$23 ) {
      if ( isArraylike( Object(arr) ) ) {
        jQuery[$fog$25]( ret,
          typeof arr === $fog$65 ?
          [ arr ] : arr
        );
      } else {
        push[$fog$22]( ret, arr );
      }
    }

    return ret;
  },

  inArray: function( elem, arr, i ) {
    return arr == $fog$23 ? -$fog$34 : indexOf[$fog$22]( arr, elem, i );
  },

  merge: function( first, second ) {
    var len = +second[$fog$24],
      j = $fog$21,
      i = first[$fog$24];

    for ( ; j < len; j++ ) {
      first[ i++ ] = second[ j ];
    }

    first[$fog$24] = i;

    return first;
  },

  grep: function( elems, callback, invert ) {
    var callbackInverse,
      matches = [],
      i = $fog$21,
      length = elems[$fog$24],
      callbackExpect = !invert;

    // Go through the array, only saving the items
    // that pass the validator function
    for ( ; i < length; i++ ) {
      callbackInverse = !callback( elems[ i ], i );
      if ( callbackInverse !== callbackExpect ) {
        matches[$fog$8]( elems[ i ] );
      }
    }

    return matches;
  },

  // arg is for internal usage only
  map: function( elems, callback, arg ) {
    var value,
      i = $fog$21,
      length = elems[$fog$24],
      isArray = isArraylike( elems ),
      ret = [];

    // Go through the array, translating each of the items to their new values
    if ( isArray ) {
      for ( ; i < length; i++ ) {
        value = callback( elems[ i ], i, arg );

        if ( value != $fog$23 ) {
          ret[$fog$8]( value );
        }
      }

    // Go through every key on the object,
    } else {
      for ( i in elems ) {
        value = callback( elems[ i ], i, arg );

        if ( value != $fog$23 ) {
          ret[$fog$8]( value );
        }
      }
    }

    // Flatten any nested arrays
    return concat[$fog$32]( [], ret );
  },

  // A global GUID counter for objects
  guid: $fog$34,

  // Bind a function to a context, optionally partially applying any
  // arguments.
  proxy: function( fn, context ) {
    var tmp, args, proxy;

    if ( typeof context === $fog$65 ) {
      tmp = fn[ context ];
      context = fn;
      fn = tmp;
    }

    // Quick check to determine if target is callable, in the spec
    // this throws a TypeError, but we will just return undefined.
    if ( !jQuery[$fog$40]( fn ) ) {
      return undefined;
    }

    // Simulated bind
    args = slice[$fog$22]( arguments, $fog$66 );
    proxy = function() {
      return fn[$fog$32]( context || this, args[$fog$7]( slice[$fog$22]( arguments ) ) );
    };

    // Set the guid of unique handler to the same of original handler, so it can be removed
    proxy[$fog$67] = fn[$fog$67] = fn[$fog$67] || jQuery[$fog$67]++;

    return proxy;
  },

  now: Date[$fog$68],

  // jQuery.support is not used in Core but other projects attach their
  // properties to it so it needs to exist.
  support: support
});

// Populate the class2type map
jQuery[$fog$29]($fog$70[$fog$69]($fog$71), function(i, name) {
  class2type[ $fog$72 + name + $fog$73 ] = name[$fog$64]();
});

function isArraylike( obj ) {

  // Support: iOS 8.2 (not reproducible in simulator)
  // `in` check used to prevent JIT error (gh-2145)
  // hasOwn isn't used here due to false negatives
  // regarding Nodelist length in IE
  var length = $fog$24 in obj && obj[$fog$24],
    type = jQuery[$fog$47]( obj );

  if ( type === $fog$48 || jQuery[$fog$51]( obj ) ) {
    return $fog$38;
  }

  if ( obj[$fog$50] === $fog$34 && length ) {
    return $fog$3;
  }

  return type === $fog$74 || length === $fog$21 ||
    typeof length === $fog$75 && length > $fog$21 && ( length - $fog$34 ) in obj;
}
var Sizzle =
/*!
 * Sizzle CSS Selector Engine v2.2.0-pre
 * http://sizzlejs.com/
 *
 * Copyright 2008, 2014 jQuery Foundation, Inc. and other contributors
 * Released under the MIT license
 * http://jquery.org/license
 *
 * Date: 2014-12-16
 */
(function( window ) {

var i,
  support,
  Expr,
  getText,
  isXML,
  tokenize,
  compile,
  select,
  outermostContext,
  sortInput,
  hasDuplicate,

  // Local document vars
  setDocument,
  document,
  docElem,
  documentIsHTML,
  rbuggyQSA,
  rbuggyMatches,
  matches,
  contains,

  // Instance-specific data
  expando = $fog$76 + $fog$34 * new Date(),
  preferredDoc = window[$fog$2],
  dirruns = $fog$21,
  done = $fog$21,
  classCache = createCache(),
  tokenCache = createCache(),
  compilerCache = createCache(),
  sortOrder = function( a, b ) {
    if ( a === b ) {
      hasDuplicate = $fog$3;
    }
    return $fog$21;
  },

  // General-purpose constants
  MAX_NEGATIVE = $fog$34 << $fog$77,

  // Instance methods
  hasOwn = ({})[$fog$11],
  arr = [],
  pop = arr[$fog$78],
  push_native = arr[$fog$8],
  push = arr[$fog$8],
  slice = arr[$fog$6],
  // Use a stripped-down indexOf as it's faster than native
  // http://jsperf.com/thor-indexof-vs-for/5
  indexOf = function( list, elem ) {
    var i = $fog$21,
      len = list[$fog$24];
    for ( ; i < len; i++ ) {
      if ( list[i] === elem ) {
        return i;
      }
    }
    return -$fog$34;
  },

  booleans = $fog$79,

  // Regular expressions

  // Whitespace characters http://www.w3.org/TR/css3-selectors/#whitespace
  whitespace = $fog$80,
  // http://www.w3.org/TR/css3-syntax/#characters
  characterEncoding = $fog$81,

  // Loosely modeled on CSS identifier characters
  // An unquoted value should be a CSS identifier http://www.w3.org/TR/css3-selectors/#attribute-selectors
  // Proper syntax: http://www.w3.org/TR/CSS21/syndata.html#value-def-identifier
  identifier = characterEncoding[$fog$44]( $fog$82, $fog$83 ),

  // Attribute selectors: http://www.w3.org/TR/selectors/#attribute-selectors
  attributes = $fog$84 + whitespace + $fog$85 + characterEncoding + $fog$86 + whitespace +
    // Operator (capture 2)
    $fog$87 + whitespace +
    // "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"
    $fog$88 + identifier + $fog$89 + whitespace +
    $fog$90,

  pseudos = $fog$91 + characterEncoding + $fog$92 +
    // To reduce the number of selectors needing tokenize in the preFilter, prefer arguments:
    // 1. quoted (capture 3; capture 4 or capture 5)
    $fog$93 +
    // 2. simple (capture 6)
    $fog$94 + attributes + $fog$95 +
    // 3. anything else (capture 2)
    $fog$96 +
    $fog$97,

  // Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter
  rwhitespace = new RegExp( whitespace + $fog$98, $fog$99 ),
  rtrim = new RegExp( $fog$100 + whitespace + $fog$101 + whitespace + $fog$102, $fog$99 ),

  rcomma = new RegExp( $fog$100 + whitespace + $fog$103 + whitespace + $fog$104 ),
  rcombinators = new RegExp( $fog$100 + whitespace + $fog$105 + whitespace + $fog$106 + whitespace + $fog$104 ),

  rattributeQuotes = new RegExp( $fog$107 + whitespace + $fog$108 + whitespace + $fog$90, $fog$99 ),

  rpseudo = new RegExp( pseudos ),
  ridentifier = new RegExp( $fog$100 + identifier + $fog$109 ),

  matchExpr = {
    "ID": new RegExp( $fog$110 + characterEncoding + $fog$106 ),
    "CLASS": new RegExp( $fog$111 + characterEncoding + $fog$106 ),
    "TAG": new RegExp( $fog$112 + characterEncoding[$fog$44]( $fog$82, $fog$113 ) + $fog$106 ),
    "ATTR": new RegExp( $fog$100 + attributes ),
    "PSEUDO": new RegExp( $fog$100 + pseudos ),
    "CHILD": new RegExp( $fog$114 + whitespace +
      $fog$115 + whitespace + $fog$116 + whitespace +
      $fog$117 + whitespace + $fog$118, $fog$119 ),
    "bool": new RegExp( $fog$120 + booleans + $fog$121, $fog$119 ),
    // For use in libraries implementing .is()
    // We use this for POS matching in `select`
    "needsContext": new RegExp( $fog$100 + whitespace + $fog$122 +
      whitespace + $fog$123 + whitespace + $fog$124, $fog$119 )
  },

  rinputs = $fog$125,
  rheader = $fog$126,

  rnative = $fog$127,

  // Easily-parseable/retrievable ID or TAG or CLASS selectors
  rquickExpr = $fog$128,

  rsibling = $fog$129,
  rescape = $fog$130,

  // CSS escapes http://www.w3.org/TR/CSS21/syndata.html#escaped-characters
  runescape = new RegExp( $fog$131 + whitespace + $fog$132 + whitespace + $fog$133, $fog$134 ),
  funescape = function( _, escaped, escapedWhitespace ) {
    var high = $fog$135 + escaped - $fog$136;
    // NaN means non-codepoint
    // Support: Firefox<24
    // Workaround erroneous numeric interpretation of +"0x"
    return high !== high || escapedWhitespace ?
      escaped :
      high < $fog$21 ?
        // BMP codepoint
        String[$fog$137]( high + $fog$136 ) :
        // Supplemental Plane codepoint (surrogate pair)
        String[$fog$137]( high >> $fog$138 | $fog$139, high & $fog$140 | $fog$141 );
  },

  // Used for iframes
  // See setDocument()
  // Removing the function wrapper causes a "Permission Denied"
  // error in IE
  unloadHandler = function() {
    setDocument();
  };

// Optimize for push.apply( _, NodeList )
try {
  push[$fog$32](
    (arr = slice[$fog$22]( preferredDoc[$fog$142] )),
    preferredDoc[$fog$142]
  );
  // Support: Android<4.0
  // Detect silently failing push.apply
  arr[ preferredDoc[$fog$142][$fog$24] ][$fog$50];
} catch ( e ) {
  push = { apply: arr[$fog$24] ?

    // Leverage slice if possible
    function( target, els ) {
      push_native[$fog$32]( target, slice[$fog$22](els) );
    } :

    // Support: IE<9
    // Otherwise append directly
    function( target, els ) {
      var j = target[$fog$24],
        i = $fog$21;
      // Can't trust NodeList.length
      while ( (target[j++] = els[i++]) ) {}
      target[$fog$24] = j - $fog$34;
    }
  };
}

function Sizzle( selector, context, results, seed ) {
  var match, elem, m, nodeType,
    // QSA vars
    i, groups, old, nid, newContext, newSelector;

  if ( ( context ? context[$fog$143] || context : preferredDoc ) !== document ) {
    setDocument( context );
  }

  context = context || document;
  results = results || [];
  nodeType = context[$fog$50];

  if ( typeof selector !== $fog$65 || !selector ||
    nodeType !== $fog$34 && nodeType !== $fog$144 && nodeType !== $fog$145 ) {

    return results;
  }

  if ( !seed && documentIsHTML ) {

    // Try to shortcut find operations when possible (e.g., not under DocumentFragment)
    if ( nodeType !== $fog$145 && (match = rquickExpr[$fog$146]( selector )) ) {
      // Speed-up: Sizzle("#ID")
      if ( (m = match[$fog$34]) ) {
        if ( nodeType === $fog$144 ) {
          elem = context[$fog$147]( m );
          // Check parentNode to catch when Blackberry 4.6 returns
          // nodes that are no longer in the document (jQuery #6963)
          if ( elem && elem[$fog$59] ) {
            // Handle the case where IE, Opera, and Webkit return items
            // by name instead of ID
            if ( elem[$fog$148] === m ) {
              results[$fog$8]( elem );
              return results;
            }
          } else {
            return results;
          }
        } else {
          // Context is not a document
          if ( context[$fog$143] && (elem = context[$fog$143][$fog$147]( m )) &&
            contains( context, elem ) && elem[$fog$148] === m ) {
            results[$fog$8]( elem );
            return results;
          }
        }

      // Speed-up: Sizzle("TAG")
      } else if ( match[$fog$66] ) {
        push[$fog$32]( results, context[$fog$149]( selector ) );
        return results;

      // Speed-up: Sizzle(".CLASS")
      } else if ( (m = match[$fog$150]) && support[$fog$151] ) {
        push[$fog$32]( results, context[$fog$151]( m ) );
        return results;
      }
    }

    // QSA path
    if ( support[$fog$152] && (!rbuggyQSA || !rbuggyQSA[$fog$153]( selector )) ) {
      nid = old = expando;
      newContext = context;
      newSelector = nodeType !== $fog$34 && selector;

      // qSA works strangely on Element-rooted queries
      // We can work around this by specifying an extra ID on the root
      // and working up from there (Thanks to Andrew Dupont for the technique)
      // IE 8 doesn't work on object elements
      if ( nodeType === $fog$34 && context[$fog$63][$fog$64]() !== $fog$0 ) {
        groups = tokenize( selector );

        if ( (old = context[$fog$154]($fog$148)) ) {
          nid = old[$fog$44]( rescape, $fog$155 );
        } else {
          context[$fog$156]( $fog$148, nid );
        }
        nid = $fog$157 + nid + $fog$158;

        i = groups[$fog$24];
        while ( i-- ) {
          groups[i] = nid + toSelector( groups[i] );
        }
        newContext = rsibling[$fog$153]( selector ) && testContext( context[$fog$59] ) || context;
        newSelector = groups[$fog$159]($fog$160);
      }

      if ( newSelector ) {
        try {
          push[$fog$32]( results,
            newContext[$fog$161]( newSelector )
          );
          return results;
        } catch(qsaError) {
        } finally {
          if ( !old ) {
            context[$fog$162]($fog$148);
          }
        }
      }
    }
  }

  // All others
  return select( selector[$fog$44]( rtrim, $fog$163 ), context, results, seed );
}

/**
 * Create key-value caches of limited size
 * @returns {Function(string, Object)} Returns the Object data after storing it on itself with
 *  property name the (space-suffixed) string and (if the cache is larger than Expr.cacheLength)
 *  deleting the oldest entry
 */
function createCache() {
  var keys = [];

  function cache( key, value ) {
    // Use (key + " ") to avoid collision with native prototype properties (see Issue #157)
    if ( keys[$fog$8]( key + $fog$71 ) > Expr[$fog$164] ) {
      // Only keep the most recent entries
      delete cache[ keys[$fog$165]() ];
    }
    return (cache[ key + $fog$71 ] = value);
  }
  return cache;
}

/**
 * Mark a function for special use by Sizzle
 * @param {Function} fn The function to mark
 */
function markFunction( fn ) {
  fn[ expando ] = $fog$3;
  return fn;
}

/**
 * Support testing using an element
 * @param {Function} fn Passed the created div and expects a boolean result
 */
function assert( fn ) {
  var div = document[$fog$55]($fog$166);

  try {
    return !!fn( div );
  } catch (e) {
    return $fog$38;
  } finally {
    // Remove from its parent by default
    if ( div[$fog$59] ) {
      div[$fog$59][$fog$58]( div );
    }
    // release memory in IE
    div = $fog$23;
  }
}

/**
 * Adds the same handler for all of the specified attrs
 * @param {String} attrs Pipe-separated list of attributes
 * @param {Function} handler The method that will be applied
 */
function addHandle( attrs, handler ) {
  var arr = attrs[$fog$69]($fog$167),
    i = attrs[$fog$24];

  while ( i-- ) {
    Expr[$fog$168][ arr[i] ] = handler;
  }
}

/**
 * Checks document order of two siblings
 * @param {Element} a
 * @param {Element} b
 * @returns {Number} Returns less than 0 if a precedes b, greater than 0 if a follows b
 */
function siblingCheck( a, b ) {
  var cur = b && a,
    diff = cur && a[$fog$50] === $fog$34 && b[$fog$50] === $fog$34 &&
      ( ~b[$fog$169] || MAX_NEGATIVE ) -
      ( ~a[$fog$169] || MAX_NEGATIVE );

  // Use IE sourceIndex if available on both nodes
  if ( diff ) {
    return diff;
  }

  // Check if b follows a
  if ( cur ) {
    while ( (cur = cur[$fog$170]) ) {
      if ( cur === b ) {
        return -$fog$34;
      }
    }
  }

  return a ? $fog$34 : -$fog$34;
}

/**
 * Returns a function to use in pseudos for input types
 * @param {String} type
 */
function createInputPseudo( type ) {
  return function( elem ) {
    var name = elem[$fog$63][$fog$64]();
    return name === $fog$171 && elem[$fog$47] === type;
  };
}

/**
 * Returns a function to use in pseudos for buttons
 * @param {String} type
 */
function createButtonPseudo( type ) {
  return function( elem ) {
    var name = elem[$fog$63][$fog$64]();
    return (name === $fog$171 || name === $fog$172) && elem[$fog$47] === type;
  };
}

/**
 * Returns a function to use in pseudos for positionals
 * @param {Function} fn
 */
function createPositionalPseudo( fn ) {
  return markFunction(function( argument ) {
    argument = +argument;
    return markFunction(function( seed, matches ) {
      var j,
        matchIndexes = fn( [], seed[$fog$24], argument ),
        i = matchIndexes[$fog$24];

      // Match elements found at the specified indexes
      while ( i-- ) {
        if ( seed[ (j = matchIndexes[i]) ] ) {
          seed[j] = !(matches[j] = seed[j]);
        }
      }
    });
  });
}

/**
 * Checks a node for validity as a Sizzle context
 * @param {Element|Object=} context
 * @returns {Element|Object|Boolean} The input node if acceptable, otherwise a falsy value
 */
function testContext( context ) {
  return context && typeof context[$fog$149] !== $fog$5 && context;
}

// Expose support vars for convenience
support = Sizzle[$fog$173] = {};

/**
 * Detects XML nodes
 * @param {Element|Object} elem An element or a document
 * @returns {Boolean} True iff elem is a non-HTML XML node
 */
isXML = Sizzle[$fog$174] = function( elem ) {
  // documentElement is verified for cases where it doesn't yet exist
  // (such as loading iframes in IE - #4833)
  var documentElement = elem && (elem[$fog$143] || elem)[$fog$175];
  return documentElement ? documentElement[$fog$63] !== $fog$176 : $fog$38;
};

/**
 * Sets document-related variables once based on the current document
 * @param {Element|Object} [doc] An element or document object to use to set the document
 * @returns {Object} Returns the current document
 */
setDocument = Sizzle[$fog$177] = function( node ) {
  var hasCompare, parent,
    doc = node ? node[$fog$143] || node : preferredDoc;

  // If no document and documentElement is available, return
  if ( doc === document || doc[$fog$50] !== $fog$144 || !doc[$fog$175] ) {
    return document;
  }

  // Set our document
  document = doc;
  docElem = doc[$fog$175];
  parent = doc[$fog$178];

  // Support: IE>8
  // If iframe document is assigned to "document" variable and if iframe has been reloaded,
  // IE will throw "permission denied" error when accessing "document" variable, see jQuery #13936
  // IE6-8 do not support the defaultView property so parent will be undefined
  if ( parent && parent !== parent[$fog$179] ) {
    // IE11 does not have attachEvent, so all must suffer
    if ( parent[$fog$180] ) {
      parent[$fog$180]( $fog$181, unloadHandler, $fog$38 );
    } else if ( parent[$fog$182] ) {
      parent[$fog$182]( $fog$183, unloadHandler );
    }
  }

  /* Support tests
  ---------------------------------------------------------------------- */
  documentIsHTML = !isXML( doc );

  /* Attributes
  ---------------------------------------------------------------------- */

  // Support: IE<8
  // Verify that getAttribute really returns attributes and not properties
  // (excepting IE8 booleans)
  support[$fog$184] = assert(function( div ) {
    div[$fog$185] = $fog$119;
    return !div[$fog$154]($fog$185);
  });

  /* getElement(s)By*
  ---------------------------------------------------------------------- */

  // Check if getElementsByTagName("*") returns only elements
  support[$fog$149] = assert(function( div ) {
    div[$fog$60]( doc[$fog$186]($fog$20) );
    return !div[$fog$149]($fog$104)[$fog$24];
  });

  // Support: IE<9
  support[$fog$151] = rnative[$fog$153]( doc[$fog$151] );

  // Support: IE<10
  // Check if getElementById returns elements by name
  // The broken getElementById methods don't pick up programatically-set names,
  // so use a roundabout getElementsByName test
  support[$fog$187] = assert(function( div ) {
    docElem[$fog$60]( div )[$fog$148] = expando;
    return !doc[$fog$188] || !doc[$fog$188]( expando )[$fog$24];
  });

  // ID find and filter
  if ( support[$fog$187] ) {
    Expr[$fog$189][$fog$190] = function( id, context ) {
      if ( typeof context[$fog$147] !== $fog$5 && documentIsHTML ) {
        var m = context[$fog$147]( id );
        // Check parentNode to catch when Blackberry 4.6 returns
        // nodes that are no longer in the document #6963
        return m && m[$fog$59] ? [ m ] : [];
      }
    };
    Expr[$fog$191][$fog$190] = function( id ) {
      var attrId = id[$fog$44]( runescape, funescape );
      return function( elem ) {
        return elem[$fog$154]($fog$148) === attrId;
      };
    };
  } else {
    // Support: IE6/7
    // getElementById is not reliable as a find shortcut
    delete Expr[$fog$189][$fog$190];

    Expr[$fog$191][$fog$190] =  function( id ) {
      var attrId = id[$fog$44]( runescape, funescape );
      return function( elem ) {
        var node = typeof elem[$fog$192] !== $fog$5 && elem[$fog$192]($fog$148);
        return node && node[$fog$193] === attrId;
      };
    };
  }

  // Tag
  Expr[$fog$189][$fog$194] = support[$fog$149] ?
    function( tag, context ) {
      if ( typeof context[$fog$149] !== $fog$5 ) {
        return context[$fog$149]( tag );

      // DocumentFragment nodes don't have gEBTN
      } else if ( support[$fog$152] ) {
        return context[$fog$161]( tag );
      }
    } :

    function( tag, context ) {
      var elem,
        tmp = [],
        i = $fog$21,
        // By happy coincidence, a (broken) gEBTN appears on DocumentFragment nodes too
        results = context[$fog$149]( tag );

      // Filter out possible comments
      if ( tag === $fog$104 ) {
        while ( (elem = results[i++]) ) {
          if ( elem[$fog$50] === $fog$34 ) {
            tmp[$fog$8]( elem );
          }
        }

        return tmp;
      }
      return results;
    };

  // Class
  Expr[$fog$189][$fog$195] = support[$fog$151] && function( className, context ) {
    if ( documentIsHTML ) {
      return context[$fog$151]( className );
    }
  };

  /* QSA/matchesSelector
  ---------------------------------------------------------------------- */

  // QSA and matchesSelector support

  // matchesSelector(:active) reports false when true (IE9/Opera 11.5)
  rbuggyMatches = [];

  // qSa(:focus) reports false when true (Chrome 21)
  // We allow this because of a bug in IE8/9 that throws an error
  // whenever `document.activeElement` is accessed on an iframe
  // So, we allow :focus to pass through QSA all the time to avoid the IE error
  // See http://bugs.jquery.com/ticket/13378
  rbuggyQSA = [];

  if ( (support[$fog$152] = rnative[$fog$153]( doc[$fog$161] )) ) {
    // Build QSA regex
    // Regex strategy adopted from Diego Perini
    assert(function( div ) {
      // Select is set to empty string on purpose
      // This is to test IE's treatment of not explicitly
      // setting a boolean content attribute,
      // since its presence should be enough
      // http://bugs.jquery.com/ticket/12359
      docElem[$fog$60]( div )[$fog$196] = $fog$197 + expando + $fog$198 +
        $fog$199 + expando + $fog$200 +
        $fog$201;

      // Support: IE8, Opera 11-12.16
      // Nothing should be selected when empty strings follow ^= or $= or *=
      // The test attribute must be unknown in Opera but "safe" for WinRT
      // http://msdn.microsoft.com/en-us/library/ie/hh465388.aspx#attribute_section
      if ( div[$fog$161]($fog$202)[$fog$24] ) {
        rbuggyQSA[$fog$8]( $fog$203 + whitespace + $fog$204 );
      }

      // Support: IE8
      // Boolean attributes and "value" are not treated correctly
      if ( !div[$fog$161]($fog$205)[$fog$24] ) {
        rbuggyQSA[$fog$8]( $fog$84 + whitespace + $fog$206 + booleans + $fog$106 );
      }

      // Support: Chrome<29, Android<4.2+, Safari<7.0+, iOS<7.0+, PhantomJS<1.9.7+
      if ( !div[$fog$161]( $fog$207 + expando + $fog$208 )[$fog$24] ) {
        rbuggyQSA[$fog$8]($fog$209);
      }

      // Webkit/Opera - :checked should return selected option elements
      // http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
      // IE8 throws error here and will not see later tests
      if ( !div[$fog$161]($fog$210)[$fog$24] ) {
        rbuggyQSA[$fog$8]($fog$210);
      }

      // Support: Safari 8+, iOS 8+
      // https://bugs.webkit.org/show_bug.cgi?id=136851
      // In-page `selector#id sibing-combinator selector` fails
      if ( !div[$fog$161]( $fog$211 + expando + $fog$212 )[$fog$24] ) {
        rbuggyQSA[$fog$8]($fog$213);
      }
    });

    assert(function( div ) {
      // Support: Windows 8 Native Apps
      // The type and name attributes are restricted during .innerHTML assignment
      var input = doc[$fog$55]($fog$171);
      input[$fog$156]( $fog$47, $fog$214 );
      div[$fog$60]( input )[$fog$156]( $fog$215, $fog$216 );

      // Support: IE8
      // Enforce case-sensitivity of name attribute
      if ( div[$fog$161]($fog$217)[$fog$24] ) {
        rbuggyQSA[$fog$8]( $fog$215 + whitespace + $fog$218 );
      }

      // FF 3.5 - :enabled/:disabled and hidden elements (hidden elements are still enabled)
      // IE8 throws error here and will not see later tests
      if ( !div[$fog$161]($fog$219)[$fog$24] ) {
        rbuggyQSA[$fog$8]( $fog$219, $fog$220 );
      }

      // Opera 10-11 does not throw on post-comma invalid pseudos
      div[$fog$161]($fog$221);
      rbuggyQSA[$fog$8]($fog$222);
    });
  }

  if ( (support[$fog$223] = rnative[$fog$153]( (matches = docElem[$fog$224] ||
    docElem[$fog$225] ||
    docElem[$fog$226] ||
    docElem[$fog$227] ||
    docElem[$fog$228]) )) ) {

    assert(function( div ) {
      // Check to see if it's possible to do matchesSelector
      // on a disconnected node (IE 9)
      support[$fog$229] = matches[$fog$22]( div, $fog$166 );

      // This should fail with an exception
      // Gecko does not error, returns false instead
      matches[$fog$22]( div, $fog$230 );
      rbuggyMatches[$fog$8]( $fog$231, pseudos );
    });
  }

  rbuggyQSA = rbuggyQSA[$fog$24] && new RegExp( rbuggyQSA[$fog$159]($fog$167) );
  rbuggyMatches = rbuggyMatches[$fog$24] && new RegExp( rbuggyMatches[$fog$159]($fog$167) );

  /* Contains
  ---------------------------------------------------------------------- */
  hasCompare = rnative[$fog$153]( docElem[$fog$232] );

  // Element contains another
  // Purposefully does not implement inclusive descendent
  // As in, an element does not contain itself
  contains = hasCompare || rnative[$fog$153]( docElem[$fog$233] ) ?
    function( a, b ) {
      var adown = a[$fog$50] === $fog$144 ? a[$fog$175] : a,
        bup = b && b[$fog$59];
      return a === bup || !!( bup && bup[$fog$50] === $fog$34 && (
        adown[$fog$233] ?
          adown[$fog$233]( bup ) :
          a[$fog$232] && a[$fog$232]( bup ) & $fog$234
      ));
    } :
    function( a, b ) {
      if ( b ) {
        while ( (b = b[$fog$59]) ) {
          if ( b === a ) {
            return $fog$3;
          }
        }
      }
      return $fog$38;
    };

  /* Sorting
  ---------------------------------------------------------------------- */

  // Document order sorting
  sortOrder = hasCompare ?
  function( a, b ) {

    // Flag for duplicate removal
    if ( a === b ) {
      hasDuplicate = $fog$3;
      return $fog$21;
    }

    // Sort on method existence if only one input has compareDocumentPosition
    var compare = !a[$fog$232] - !b[$fog$232];
    if ( compare ) {
      return compare;
    }

    // Calculate position if both inputs belong to the same document
    compare = ( a[$fog$143] || a ) === ( b[$fog$143] || b ) ?
      a[$fog$232]( b ) :

      // Otherwise we know they are disconnected
      $fog$34;

    // Disconnected nodes
    if ( compare & $fog$34 ||
      (!support[$fog$235] && b[$fog$232]( a ) === compare) ) {

      // Choose the first element that is related to our preferred document
      if ( a === doc || a[$fog$143] === preferredDoc && contains(preferredDoc, a) ) {
        return -$fog$34;
      }
      if ( b === doc || b[$fog$143] === preferredDoc && contains(preferredDoc, b) ) {
        return $fog$34;
      }

      // Maintain original order
      return sortInput ?
        ( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :
        $fog$21;
    }

    return compare & $fog$236 ? -$fog$34 : $fog$34;
  } :
  function( a, b ) {
    // Exit early if the nodes are identical
    if ( a === b ) {
      hasDuplicate = $fog$3;
      return $fog$21;
    }

    var cur,
      i = $fog$21,
      aup = a[$fog$59],
      bup = b[$fog$59],
      ap = [ a ],
      bp = [ b ];

    // Parentless nodes are either documents or disconnected
    if ( !aup || !bup ) {
      return a === doc ? -$fog$34 :
        b === doc ? $fog$34 :
        aup ? -$fog$34 :
        bup ? $fog$34 :
        sortInput ?
        ( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :
        $fog$21;

    // If the nodes are siblings, we can do a quick check
    } else if ( aup === bup ) {
      return siblingCheck( a, b );
    }

    // Otherwise we need full lists of their ancestors for comparison
    cur = a;
    while ( (cur = cur[$fog$59]) ) {
      ap[$fog$237]( cur );
    }
    cur = b;
    while ( (cur = cur[$fog$59]) ) {
      bp[$fog$237]( cur );
    }

    // Walk down the tree looking for a discrepancy
    while ( ap[i] === bp[i] ) {
      i++;
    }

    return i ?
      // Do a sibling check if the nodes have a common ancestor
      siblingCheck( ap[i], bp[i] ) :

      // Otherwise nodes in our document sort first
      ap[i] === preferredDoc ? -$fog$34 :
      bp[i] === preferredDoc ? $fog$34 :
      $fog$21;
  };

  return doc;
};

Sizzle[$fog$224] = function( expr, elements ) {
  return Sizzle( expr, $fog$23, $fog$23, elements );
};

Sizzle[$fog$223] = function( elem, expr ) {
  // Set document vars if needed
  if ( ( elem[$fog$143] || elem ) !== document ) {
    setDocument( elem );
  }

  // Make sure that attribute selectors are quoted
  expr = expr[$fog$44]( rattributeQuotes, $fog$238 );

  if ( support[$fog$223] && documentIsHTML &&
    ( !rbuggyMatches || !rbuggyMatches[$fog$153]( expr ) ) &&
    ( !rbuggyQSA     || !rbuggyQSA[$fog$153]( expr ) ) ) {

    try {
      var ret = matches[$fog$22]( elem, expr );

      // IE 9's matchesSelector returns false on disconnected nodes
      if ( ret || support[$fog$229] ||
          // As well, disconnected nodes are said to be in a document
          // fragment in IE 9
          elem[$fog$2] && elem[$fog$2][$fog$50] !== $fog$145 ) {
        return ret;
      }
    } catch (e) {}
  }

  return Sizzle( expr, document, $fog$23, [ elem ] )[$fog$24] > $fog$21;
};

Sizzle[$fog$233] = function( context, elem ) {
  // Set document vars if needed
  if ( ( context[$fog$143] || context ) !== document ) {
    setDocument( context );
  }
  return contains( context, elem );
};

Sizzle[$fog$239] = function( elem, name ) {
  // Set document vars if needed
  if ( ( elem[$fog$143] || elem ) !== document ) {
    setDocument( elem );
  }

  var fn = Expr[$fog$168][ name[$fog$64]() ],
    // Don't get fooled by Object.prototype properties (jQuery #13807)
    val = fn && hasOwn[$fog$22]( Expr[$fog$168], name[$fog$64]() ) ?
      fn( elem, name, !documentIsHTML ) :
      undefined;

  return val !== undefined ?
    val :
    support[$fog$184] || !documentIsHTML ?
      elem[$fog$154]( name ) :
      (val = elem[$fog$192](name)) && val[$fog$240] ?
        val[$fog$193] :
        $fog$23;
};

Sizzle[$fog$241] = function( msg ) {
  throw new Error( $fog$242 + msg );
};

/**
 * Document sorting and removing duplicates
 * @param {ArrayLike} results
 */
Sizzle[$fog$243] = function( results ) {
  var elem,
    duplicates = [],
    j = $fog$21,
    i = $fog$21;

  // Unless we *know* we can detect duplicates, assume their presence
  hasDuplicate = !support[$fog$244];
  sortInput = !support[$fog$245] && results[$fog$6]( $fog$21 );
  results[$fog$35]( sortOrder );

  if ( hasDuplicate ) {
    while ( (elem = results[i++]) ) {
      if ( elem === results[ i ] ) {
        j = duplicates[$fog$8]( i );
      }
    }
    while ( j-- ) {
      results[$fog$36]( duplicates[ j ], $fog$34 );
    }
  }

  // Clear input after sorting to release objects
  // See https://github.com/jquery/sizzle/pull/225
  sortInput = $fog$23;

  return results;
};

/**
 * Utility function for retrieving the text value of an array of DOM nodes
 * @param {Array|Element} elem
 */
getText = Sizzle[$fog$246] = function( elem ) {
  var node,
    ret = $fog$20,
    i = $fog$21,
    nodeType = elem[$fog$50];

  if ( !nodeType ) {
    // If no nodeType, this is expected to be an array
    while ( (node = elem[i++]) ) {
      // Do not traverse comment nodes
      ret += getText( node );
    }
  } else if ( nodeType === $fog$34 || nodeType === $fog$144 || nodeType === $fog$145 ) {
    // Use textContent for elements
    // innerText usage removed for consistency of new lines (jQuery #11153)
    if ( typeof elem[$fog$247] === $fog$65 ) {
      return elem[$fog$247];
    } else {
      // Traverse its children
      for ( elem = elem[$fog$248]; elem; elem = elem[$fog$170] ) {
        ret += getText( elem );
      }
    }
  } else if ( nodeType === $fog$150 || nodeType === $fog$236 ) {
    return elem[$fog$249];
  }
  // Do not include comment or processing instruction nodes

  return ret;
};

Expr = Sizzle[$fog$250] = {

  // Can be adjusted by the user
  cacheLength: $fog$251,

  createPseudo: markFunction,

  match: matchExpr,

  attrHandle: {},

  find: {},

  relative: {
    ">": { dir: $fog$59, first: $fog$3 },
    " ": { dir: $fog$59 },
    "+": { dir: $fog$252, first: $fog$3 },
    "~": { dir: $fog$252 }
  },

  preFilter: {
    "ATTR": function( match ) {
      match[$fog$34] = match[$fog$34][$fog$44]( runescape, funescape );

      // Move the given value to match[3] whether quoted or unquoted
      match[$fog$150] = ( match[$fog$150] || match[$fog$236] || match[$fog$253] || $fog$20 )[$fog$44]( runescape, funescape );

      if ( match[$fog$66] === $fog$209 ) {
        match[$fog$150] = $fog$71 + match[$fog$150] + $fog$71;
      }

      return match[$fog$6]( $fog$21, $fog$236 );
    },

    "CHILD": function( match ) {
      /* matches from matchExpr["CHILD"]
        1 type (only|nth|...)
        2 what (child|of-type)
        3 argument (even|odd|\d*|\d*n([+-]\d+)?|...)
        4 xn-component of xn+y argument ([+-]?\d*n|)
        5 sign of xn-component
        6 x of xn-component
        7 sign of y-component
        8 y of y-component
      */
      match[$fog$34] = match[$fog$34][$fog$64]();

      if ( match[$fog$34][$fog$6]( $fog$21, $fog$150 ) === $fog$254 ) {
        // nth-* requires argument
        if ( !match[$fog$150] ) {
          Sizzle[$fog$241]( match[$fog$21] );
        }

        // numeric x and y parameters for Expr.filter.CHILD
        // remember that false/true cast respectively to 0/1
        match[$fog$236] = +( match[$fog$236] ? match[$fog$253] + (match[$fog$255] || $fog$34) : $fog$66 * ( match[$fog$150] === $fog$256 || match[$fog$150] === $fog$257 ) );
        match[$fog$253] = +( ( match[$fog$258] + match[$fog$259] ) || match[$fog$150] === $fog$257 );

      // other types prohibit arguments
      } else if ( match[$fog$150] ) {
        Sizzle[$fog$241]( match[$fog$21] );
      }

      return match;
    },

    "PSEUDO": function( match ) {
      var excess,
        unquoted = !match[$fog$255] && match[$fog$66];

      if ( matchExpr[$fog$260][$fog$153]( match[$fog$21] ) ) {
        return $fog$23;
      }

      // Accept quoted arguments as-is
      if ( match[$fog$150] ) {
        match[$fog$66] = match[$fog$236] || match[$fog$253] || $fog$20;

      // Strip excess characters from unquoted arguments
      } else if ( unquoted && rpseudo[$fog$153]( unquoted ) &&
        // Get excess from tokenize (recursively)
        (excess = tokenize( unquoted, $fog$3 )) &&
        // advance to the next closing parenthesis
        (excess = unquoted[$fog$9]( $fog$106, unquoted[$fog$24] - excess ) - unquoted[$fog$24]) ) {

        // excess is a negative index
        match[$fog$21] = match[$fog$21][$fog$6]( $fog$21, excess );
        match[$fog$66] = unquoted[$fog$6]( $fog$21, excess );
      }

      // Return only captures needed by the pseudo filter method (type and argument)
      return match[$fog$6]( $fog$21, $fog$150 );
    }
  },

  filter: {

    "TAG": function( nodeNameSelector ) {
      var nodeName = nodeNameSelector[$fog$44]( runescape, funescape )[$fog$64]();
      return nodeNameSelector === $fog$104 ?
        function() { return $fog$3; } :
        function( elem ) {
          return elem[$fog$63] && elem[$fog$63][$fog$64]() === nodeName;
        };
    },

    "CLASS": function( className ) {
      var pattern = classCache[ className + $fog$71 ];

      return pattern ||
        (pattern = new RegExp( $fog$261 + whitespace + $fog$106 + className + $fog$262 + whitespace + $fog$263 )) &&
        classCache( className, function( elem ) {
          return pattern[$fog$153]( typeof elem[$fog$185] === $fog$65 && elem[$fog$185] || typeof elem[$fog$154] !== $fog$5 && elem[$fog$154]($fog$264) || $fog$20 );
        });
    },

    "ATTR": function( name, operator, check ) {
      return function( elem ) {
        var result = Sizzle[$fog$239]( elem, name );

        if ( result == $fog$23 ) {
          return operator === $fog$231;
        }
        if ( !operator ) {
          return $fog$3;
        }

        result += $fog$20;

        return operator === $fog$107 ? result === check :
          operator === $fog$231 ? result !== check :
          operator === $fog$265 ? check && result[$fog$9]( check ) === $fog$21 :
          operator === $fog$266 ? check && result[$fog$9]( check ) > -$fog$34 :
          operator === $fog$267 ? check && result[$fog$6]( -check[$fog$24] ) === check :
          operator === $fog$209 ? ( $fog$71 + result[$fog$44]( rwhitespace, $fog$71 ) + $fog$71 )[$fog$9]( check ) > -$fog$34 :
          operator === $fog$268 ? result === check || result[$fog$6]( $fog$21, check[$fog$24] + $fog$34 ) === check + $fog$269 :
          $fog$38;
      };
    },

    "CHILD": function( type, what, argument, first, last ) {
      var simple = type[$fog$6]( $fog$21, $fog$150 ) !== $fog$254,
        forward = type[$fog$6]( -$fog$236 ) !== $fog$270,
        ofType = what === $fog$271;

      return first === $fog$34 && last === $fog$21 ?

        // Shortcut for :nth-*(n)
        function( elem ) {
          return !!elem[$fog$59];
        } :

        function( elem, context, xml ) {
          var cache, outerCache, node, diff, nodeIndex, start,
            dir = simple !== forward ? $fog$170 : $fog$252,
            parent = elem[$fog$59],
            name = ofType && elem[$fog$63][$fog$64](),
            useCache = !xml && !ofType;

          if ( parent ) {

            // :(first|last|only)-(child|of-type)
            if ( simple ) {
              while ( dir ) {
                node = elem;
                while ( (node = node[ dir ]) ) {
                  if ( ofType ? node[$fog$63][$fog$64]() === name : node[$fog$50] === $fog$34 ) {
                    return $fog$38;
                  }
                }
                // Reverse direction for :only-* (if we haven't yet done so)
                start = dir = type === $fog$272 && !start && $fog$170;
              }
              return $fog$3;
            }

            start = [ forward ? parent[$fog$248] : parent[$fog$273] ];

            // non-xml :nth-child(...) stores cache data on `parent`
            if ( forward && useCache ) {
              // Seek `elem` from a previously-cached index
              outerCache = parent[ expando ] || (parent[ expando ] = {});
              cache = outerCache[ type ] || [];
              nodeIndex = cache[$fog$21] === dirruns && cache[$fog$34];
              diff = cache[$fog$21] === dirruns && cache[$fog$66];
              node = nodeIndex && parent[$fog$142][ nodeIndex ];

              while ( (node = ++nodeIndex && node && node[ dir ] ||

                // Fallback to seeking `elem` from the start
                (diff = nodeIndex = $fog$21) || start[$fog$78]()) ) {

                // When found, cache indexes on `parent` and break
                if ( node[$fog$50] === $fog$34 && ++diff && node === elem ) {
                  outerCache[ type ] = [ dirruns, nodeIndex, diff ];
                  break;
                }
              }

            // Use previously-cached element index if available
            } else if ( useCache && (cache = (elem[ expando ] || (elem[ expando ] = {}))[ type ]) && cache[$fog$21] === dirruns ) {
              diff = cache[$fog$34];

            // xml :nth-child(...) or :nth-last-child(...) or :nth(-last)?-of-type(...)
            } else {
              // Use the same loop as above to seek `elem` from the start
              while ( (node = ++nodeIndex && node && node[ dir ] ||
                (diff = nodeIndex = $fog$21) || start[$fog$78]()) ) {

                if ( ( ofType ? node[$fog$63][$fog$64]() === name : node[$fog$50] === $fog$34 ) && ++diff ) {
                  // Cache the index of each encountered element
                  if ( useCache ) {
                    (node[ expando ] || (node[ expando ] = {}))[ type ] = [ dirruns, diff ];
                  }

                  if ( node === elem ) {
                    break;
                  }
                }
              }
            }

            // Incorporate the offset, then check against cycle size
            diff -= last;
            return diff === first || ( diff % first === $fog$21 && diff / first >= $fog$21 );
          }
        };
    },

    "PSEUDO": function( pseudo, argument ) {
      // pseudo-class names are case-insensitive
      // http://www.w3.org/TR/selectors/#pseudo-classes
      // Prioritize by case sensitivity in case custom pseudos are added with uppercase letters
      // Remember that setFilters inherits from pseudos
      var args,
        fn = Expr[$fog$274][ pseudo ] || Expr[$fog$275][ pseudo[$fog$64]() ] ||
          Sizzle[$fog$241]( $fog$276 + pseudo );

      // The user may use createPseudo to indicate that
      // arguments are needed to create the filter function
      // just as Sizzle does
      if ( fn[ expando ] ) {
        return fn( argument );
      }

      // But maintain support for old signatures
      if ( fn[$fog$24] > $fog$34 ) {
        args = [ pseudo, pseudo, $fog$20, argument ];
        return Expr[$fog$275][$fog$11]( pseudo[$fog$64]() ) ?
          markFunction(function( seed, matches ) {
            var idx,
              matched = fn( seed, argument ),
              i = matched[$fog$24];
            while ( i-- ) {
              idx = indexOf( seed, matched[i] );
              seed[ idx ] = !( matches[ idx ] = matched[i] );
            }
          }) :
          function( elem ) {
            return fn( elem, $fog$21, args );
          };
      }

      return fn;
    }
  },

  pseudos: {
    // Potentially complex pseudos
    "not": markFunction(function( selector ) {
      // Trim the selector passed to compile
      // to avoid treating leading and trailing
      // spaces as combinators
      var input = [],
        results = [],
        matcher = compile( selector[$fog$44]( rtrim, $fog$163 ) );

      return matcher[ expando ] ?
        markFunction(function( seed, matches, context, xml ) {
          var elem,
            unmatched = matcher( seed, $fog$23, xml, [] ),
            i = seed[$fog$24];

          // Match elements unmatched by `matcher`
          while ( i-- ) {
            if ( (elem = unmatched[i]) ) {
              seed[i] = !(matches[i] = elem);
            }
          }
        }) :
        function( elem, context, xml ) {
          input[$fog$21] = elem;
          matcher( input, $fog$23, xml, results );
          // Don't keep the element (issue #299)
          input[$fog$21] = $fog$23;
          return !results[$fog$78]();
        };
    }),

    "has": markFunction(function( selector ) {
      return function( elem ) {
        return Sizzle( selector, elem )[$fog$24] > $fog$21;
      };
    }),

    "contains": markFunction(function( text ) {
      text = text[$fog$44]( runescape, funescape );
      return function( elem ) {
        return ( elem[$fog$247] || elem[$fog$277] || getText( elem ) )[$fog$9]( text ) > -$fog$34;
      };
    }),

    // "Whether an element is represented by a :lang() selector
    // is based solely on the element's language value
    // being equal to the identifier C,
    // or beginning with the identifier C immediately followed by "-".
    // The matching of C against the element's language value is performed case-insensitively.
    // The identifier C does not have to be a valid language name."
    // http://www.w3.org/TR/selectors/#lang-pseudo
    "lang": markFunction( function( lang ) {
      // lang value must be a valid identifier
      if ( !ridentifier[$fog$153](lang || $fog$20) ) {
        Sizzle[$fog$241]( $fog$278 + lang );
      }
      lang = lang[$fog$44]( runescape, funescape )[$fog$64]();
      return function( elem ) {
        var elemLang;
        do {
          if ( (elemLang = documentIsHTML ?
            elem[$fog$279] :
            elem[$fog$154]($fog$280) || elem[$fog$154]($fog$279)) ) {

            elemLang = elemLang[$fog$64]();
            return elemLang === lang || elemLang[$fog$9]( lang + $fog$269 ) === $fog$21;
          }
        } while ( (elem = elem[$fog$59]) && elem[$fog$50] === $fog$34 );
        return $fog$38;
      };
    }),

    // Miscellaneous
    "target": function( elem ) {
      var hash = window[$fog$281] && window[$fog$281][$fog$282];
      return hash && hash[$fog$6]( $fog$34 ) === elem[$fog$148];
    },

    "root": function( elem ) {
      return elem === docElem;
    },

    "focus": function( elem ) {
      return elem === document[$fog$283] && (!document[$fog$284] || document[$fog$284]()) && !!(elem[$fog$47] || elem[$fog$285] || ~elem[$fog$286]);
    },

    // Boolean properties
    "enabled": function( elem ) {
      return elem[$fog$287] === $fog$38;
    },

    "disabled": function( elem ) {
      return elem[$fog$287] === $fog$3;
    },

    "checked": function( elem ) {
      // In CSS3, :checked should return both checked and selected elements
      // http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
      var nodeName = elem[$fog$63][$fog$64]();
      return (nodeName === $fog$171 && !!elem[$fog$288]) || (nodeName === $fog$289 && !!elem[$fog$290]);
    },

    "selected": function( elem ) {
      // Accessing this property makes selected-by-default
      // options in Safari work properly
      if ( elem[$fog$59] ) {
        elem[$fog$59][$fog$291];
      }

      return elem[$fog$290] === $fog$3;
    },

    // Contents
    "empty": function( elem ) {
      // http://www.w3.org/TR/selectors/#empty-pseudo
      // :empty is negated by element (1) or content nodes (text: 3; cdata: 4; entity ref: 5),
      //   but not by others (comment: 8; processing instruction: 7; etc.)
      // nodeType < 6 works because attributes (2) do not appear as children
      for ( elem = elem[$fog$248]; elem; elem = elem[$fog$170] ) {
        if ( elem[$fog$50] < $fog$255 ) {
          return $fog$38;
        }
      }
      return $fog$3;
    },

    "parent": function( elem ) {
      return !Expr[$fog$274][$fog$292]( elem );
    },

    // Element/input types
    "header": function( elem ) {
      return rheader[$fog$153]( elem[$fog$63] );
    },

    "input": function( elem ) {
      return rinputs[$fog$153]( elem[$fog$63] );
    },

    "button": function( elem ) {
      var name = elem[$fog$63][$fog$64]();
      return name === $fog$171 && elem[$fog$47] === $fog$172 || name === $fog$172;
    },

    "text": function( elem ) {
      var attr;
      return elem[$fog$63][$fog$64]() === $fog$171 &&
        elem[$fog$47] === $fog$57 &&

        // Support: IE<8
        // New HTML5 attribute values (e.g., "search") appear with elem.type === "text"
        ( (attr = elem[$fog$154]($fog$47)) == $fog$23 || attr[$fog$64]() === $fog$57 );
    },

    // Position-in-collection
    "first": createPositionalPseudo(function() {
      return [ $fog$21 ];
    }),

    "last": createPositionalPseudo(function( matchIndexes, length ) {
      return [ length - $fog$34 ];
    }),

    "eq": createPositionalPseudo(function( matchIndexes, length, argument ) {
      return [ argument < $fog$21 ? argument + length : argument ];
    }),

    "even": createPositionalPseudo(function( matchIndexes, length ) {
      var i = $fog$21;
      for ( ; i < length; i += $fog$66 ) {
        matchIndexes[$fog$8]( i );
      }
      return matchIndexes;
    }),

    "odd": createPositionalPseudo(function( matchIndexes, length ) {
      var i = $fog$34;
      for ( ; i < length; i += $fog$66 ) {
        matchIndexes[$fog$8]( i );
      }
      return matchIndexes;
    }),

    "lt": createPositionalPseudo(function( matchIndexes, length, argument ) {
      var i = argument < $fog$21 ? argument + length : argument;
      for ( ; --i >= $fog$21; ) {
        matchIndexes[$fog$8]( i );
      }
      return matchIndexes;
    }),

    "gt": createPositionalPseudo(function( matchIndexes, length, argument ) {
      var i = argument < $fog$21 ? argument + length : argument;
      for ( ; ++i < length; ) {
        matchIndexes[$fog$8]( i );
      }
      return matchIndexes;
    })
  }
};

Expr[$fog$274][$fog$254] = Expr[$fog$274][$fog$33];

// Add button/input type pseudos
for ( i in { radio: $fog$3, checkbox: $fog$3, file: $fog$3, password: $fog$3, image: $fog$3 } ) {
  Expr[$fog$274][ i ] = createInputPseudo( i );
}
for ( i in { submit: $fog$3, reset: $fog$3 } ) {
  Expr[$fog$274][ i ] = createButtonPseudo( i );
}

// Easy API for creating new setFilters
function setFilters() {}
setFilters[$fog$19] = Expr[$fog$293] = Expr[$fog$274];
Expr[$fog$275] = new setFilters();

tokenize = Sizzle[$fog$294] = function( selector, parseOnly ) {
  var matched, match, tokens, type,
    soFar, groups, preFilters,
    cached = tokenCache[ selector + $fog$71 ];

  if ( cached ) {
    return parseOnly ? $fog$21 : cached[$fog$6]( $fog$21 );
  }

  soFar = selector;
  groups = [];
  preFilters = Expr[$fog$295];

  while ( soFar ) {

    // Comma and first run
    if ( !matched || (match = rcomma[$fog$146]( soFar )) ) {
      if ( match ) {
        // Don't consume trailing commas as valid
        soFar = soFar[$fog$6]( match[$fog$21][$fog$24] ) || soFar;
      }
      groups[$fog$8]( (tokens = []) );
    }

    matched = $fog$38;

    // Combinators
    if ( (match = rcombinators[$fog$146]( soFar )) ) {
      matched = match[$fog$165]();
      tokens[$fog$8]({
        value: matched,
        // Cast descendant combinators to space
        type: match[$fog$21][$fog$44]( rtrim, $fog$71 )
      });
      soFar = soFar[$fog$6]( matched[$fog$24] );
    }

    // Filters
    for ( type in Expr[$fog$191] ) {
      if ( (match = matchExpr[ type ][$fog$146]( soFar )) && (!preFilters[ type ] ||
        (match = preFilters[ type ]( match ))) ) {
        matched = match[$fog$165]();
        tokens[$fog$8]({
          value: matched,
          type: type,
          matches: match
        });
        soFar = soFar[$fog$6]( matched[$fog$24] );
      }
    }

    if ( !matched ) {
      break;
    }
  }

  // Return the length of the invalid excess
  // if we're just parsing
  // Otherwise, throw an error or return tokens
  return parseOnly ?
    soFar[$fog$24] :
    soFar ?
      Sizzle[$fog$241]( selector ) :
      // Cache the tokens
      tokenCache( selector, groups )[$fog$6]( $fog$21 );
};

function toSelector( tokens ) {
  var i = $fog$21,
    len = tokens[$fog$24],
    selector = $fog$20;
  for ( ; i < len; i++ ) {
    selector += tokens[i][$fog$193];
  }
  return selector;
}

function addCombinator( matcher, combinator, base ) {
  var dir = combinator[$fog$296],
    checkNonElements = base && dir === $fog$59,
    doneName = done++;

  return combinator[$fog$297] ?
    // Check against closest ancestor/preceding element
    function( elem, context, xml ) {
      while ( (elem = elem[ dir ]) ) {
        if ( elem[$fog$50] === $fog$34 || checkNonElements ) {
          return matcher( elem, context, xml );
        }
      }
    } :

    // Check against all ancestor/preceding elements
    function( elem, context, xml ) {
      var oldCache, outerCache,
        newCache = [ dirruns, doneName ];

      // We can't set arbitrary data on XML nodes, so they don't benefit from dir caching
      if ( xml ) {
        while ( (elem = elem[ dir ]) ) {
          if ( elem[$fog$50] === $fog$34 || checkNonElements ) {
            if ( matcher( elem, context, xml ) ) {
              return $fog$3;
            }
          }
        }
      } else {
        while ( (elem = elem[ dir ]) ) {
          if ( elem[$fog$50] === $fog$34 || checkNonElements ) {
            outerCache = elem[ expando ] || (elem[ expando ] = {});
            if ( (oldCache = outerCache[ dir ]) &&
              oldCache[ $fog$21 ] === dirruns && oldCache[ $fog$34 ] === doneName ) {

              // Assign to newCache so results back-propagate to previous elements
              return (newCache[ $fog$66 ] = oldCache[ $fog$66 ]);
            } else {
              // Reuse newcache so results back-propagate to previous elements
              outerCache[ dir ] = newCache;

              // A match means we're done; a fail means we have to keep checking
              if ( (newCache[ $fog$66 ] = matcher( elem, context, xml )) ) {
                return $fog$3;
              }
            }
          }
        }
      }
    };
}

function elementMatcher( matchers ) {
  return matchers[$fog$24] > $fog$34 ?
    function( elem, context, xml ) {
      var i = matchers[$fog$24];
      while ( i-- ) {
        if ( !matchers[i]( elem, context, xml ) ) {
          return $fog$38;
        }
      }
      return $fog$3;
    } :
    matchers[$fog$21];
}

function multipleContexts( selector, contexts, results ) {
  var i = $fog$21,
    len = contexts[$fog$24];
  for ( ; i < len; i++ ) {
    Sizzle( selector, contexts[i], results );
  }
  return results;
}

function condense( unmatched, map, filter, context, xml ) {
  var elem,
    newUnmatched = [],
    i = $fog$21,
    len = unmatched[$fog$24],
    mapped = map != $fog$23;

  for ( ; i < len; i++ ) {
    if ( (elem = unmatched[i]) ) {
      if ( !filter || filter( elem, context, xml ) ) {
        newUnmatched[$fog$8]( elem );
        if ( mapped ) {
          map[$fog$8]( i );
        }
      }
    }
  }

  return newUnmatched;
}

function setMatcher( preFilter, selector, matcher, postFilter, postFinder, postSelector ) {
  if ( postFilter && !postFilter[ expando ] ) {
    postFilter = setMatcher( postFilter );
  }
  if ( postFinder && !postFinder[ expando ] ) {
    postFinder = setMatcher( postFinder, postSelector );
  }
  return markFunction(function( seed, results, context, xml ) {
    var temp, i, elem,
      preMap = [],
      postMap = [],
      preexisting = results[$fog$24],

      // Get initial elements from seed or context
      elems = seed || multipleContexts( selector || $fog$104, context[$fog$50] ? [ context ] : context, [] ),

      // Prefilter to get matcher input, preserving a map for seed-results synchronization
      matcherIn = preFilter && ( seed || !selector ) ?
        condense( elems, preMap, preFilter, context, xml ) :
        elems,

      matcherOut = matcher ?
        // If we have a postFinder, or filtered seed, or non-seed postFilter or preexisting results,
        postFinder || ( seed ? preFilter : preexisting || postFilter ) ?

          // ...intermediate processing is necessary
          [] :

          // ...otherwise use results directly
          results :
        matcherIn;

    // Find primary matches
    if ( matcher ) {
      matcher( matcherIn, matcherOut, context, xml );
    }

    // Apply postFilter
    if ( postFilter ) {
      temp = condense( matcherOut, postMap );
      postFilter( temp, [], context, xml );

      // Un-match failing elements by moving them back to matcherIn
      i = temp[$fog$24];
      while ( i-- ) {
        if ( (elem = temp[i]) ) {
          matcherOut[ postMap[i] ] = !(matcherIn[ postMap[i] ] = elem);
        }
      }
    }

    if ( seed ) {
      if ( postFinder || preFilter ) {
        if ( postFinder ) {
          // Get the final matcherOut by condensing this intermediate into postFinder contexts
          temp = [];
          i = matcherOut[$fog$24];
          while ( i-- ) {
            if ( (elem = matcherOut[i]) ) {
              // Restore matcherIn since elem is not yet a final match
              temp[$fog$8]( (matcherIn[i] = elem) );
            }
          }
          postFinder( $fog$23, (matcherOut = []), temp, xml );
        }

        // Move matched elements from seed to results to keep them synchronized
        i = matcherOut[$fog$24];
        while ( i-- ) {
          if ( (elem = matcherOut[i]) &&
            (temp = postFinder ? indexOf( seed, elem ) : preMap[i]) > -$fog$34 ) {

            seed[temp] = !(results[temp] = elem);
          }
        }
      }

    // Add elements to results, through postFinder if defined
    } else {
      matcherOut = condense(
        matcherOut === results ?
          matcherOut[$fog$36]( preexisting, matcherOut[$fog$24] ) :
          matcherOut
      );
      if ( postFinder ) {
        postFinder( $fog$23, results, matcherOut, xml );
      } else {
        push[$fog$32]( results, matcherOut );
      }
    }
  });
}

function matcherFromTokens( tokens ) {
  var checkContext, matcher, j,
    len = tokens[$fog$24],
    leadingRelative = Expr[$fog$298][ tokens[$fog$21][$fog$47] ],
    implicitRelative = leadingRelative || Expr[$fog$298][$fog$71],
    i = leadingRelative ? $fog$34 : $fog$21,

    // The foundational matcher ensures that elements are reachable from top-level context(s)
    matchContext = addCombinator( function( elem ) {
      return elem === checkContext;
    }, implicitRelative, $fog$3 ),
    matchAnyContext = addCombinator( function( elem ) {
      return indexOf( checkContext, elem ) > -$fog$34;
    }, implicitRelative, $fog$3 ),
    matchers = [ function( elem, context, xml ) {
      var ret = ( !leadingRelative && ( xml || context !== outermostContext ) ) || (
        (checkContext = context)[$fog$50] ?
          matchContext( elem, context, xml ) :
          matchAnyContext( elem, context, xml ) );
      // Avoid hanging onto element (issue #299)
      checkContext = $fog$23;
      return ret;
    } ];

  for ( ; i < len; i++ ) {
    if ( (matcher = Expr[$fog$298][ tokens[i][$fog$47] ]) ) {
      matchers = [ addCombinator(elementMatcher( matchers ), matcher) ];
    } else {
      matcher = Expr[$fog$191][ tokens[i][$fog$47] ][$fog$32]( $fog$23, tokens[i][$fog$224] );

      // Return special upon seeing a positional matcher
      if ( matcher[ expando ] ) {
        // Find the next relative operator (if any) for proper handling
        j = ++i;
        for ( ; j < len; j++ ) {
          if ( Expr[$fog$298][ tokens[j][$fog$47] ] ) {
            break;
          }
        }
        return setMatcher(
          i > $fog$34 && elementMatcher( matchers ),
          i > $fog$34 && toSelector(
            // If the preceding token was a descendant combinator, insert an implicit any-element `*`
            tokens[$fog$6]( $fog$21, i - $fog$34 )[$fog$7]({ value: tokens[ i - $fog$66 ][$fog$47] === $fog$71 ? $fog$104 : $fog$20 })
          )[$fog$44]( rtrim, $fog$163 ),
          matcher,
          i < j && matcherFromTokens( tokens[$fog$6]( i, j ) ),
          j < len && matcherFromTokens( (tokens = tokens[$fog$6]( j )) ),
          j < len && toSelector( tokens )
        );
      }
      matchers[$fog$8]( matcher );
    }
  }

  return elementMatcher( matchers );
}

function matcherFromGroupMatchers( elementMatchers, setMatchers ) {
  var bySet = setMatchers[$fog$24] > $fog$21,
    byElement = elementMatchers[$fog$24] > $fog$21,
    superMatcher = function( seed, context, xml, results, outermost ) {
      var elem, j, matcher,
        matchedCount = $fog$21,
        i = $fog$299,
        unmatched = seed && [],
        setMatched = [],
        contextBackup = outermostContext,
        // We must always have either seed elements or outermost context
        elems = seed || byElement && Expr[$fog$189][$fog$194]( $fog$104, outermost ),
        // Use integer dirruns iff this is the outermost matcher
        dirrunsUnique = (dirruns += contextBackup == $fog$23 ? $fog$34 : Math[$fog$45]() || $fog$300),
        len = elems[$fog$24];

      if ( outermost ) {
        outermostContext = context !== document && context;
      }

      // Add elements passing elementMatchers directly to results
      // Keep `i` a string if there are no elements so `matchedCount` will be "00" below
      // Support: IE<9, Safari
      // Tolerate NodeList properties (IE: "length"; Safari: <number>) matching elements by id
      for ( ; i !== len && (elem = elems[i]) != $fog$23; i++ ) {
        if ( byElement && elem ) {
          j = $fog$21;
          while ( (matcher = elementMatchers[j++]) ) {
            if ( matcher( elem, context, xml ) ) {
              results[$fog$8]( elem );
              break;
            }
          }
          if ( outermost ) {
            dirruns = dirrunsUnique;
          }
        }

        // Track unmatched elements for set filters
        if ( bySet ) {
          // They will have gone through all possible matchers
          if ( (elem = !matcher && elem) ) {
            matchedCount--;
          }

          // Lengthen the array for every element, matched or not
          if ( seed ) {
            unmatched[$fog$8]( elem );
          }
        }
      }

      // Apply set filters to unmatched elements
      matchedCount += i;
      if ( bySet && i !== matchedCount ) {
        j = $fog$21;
        while ( (matcher = setMatchers[j++]) ) {
          matcher( unmatched, setMatched, context, xml );
        }

        if ( seed ) {
          // Reintegrate element matches to eliminate the need for sorting
          if ( matchedCount > $fog$21 ) {
            while ( i-- ) {
              if ( !(unmatched[i] || setMatched[i]) ) {
                setMatched[i] = pop[$fog$22]( results );
              }
            }
          }

          // Discard index placeholder values to get only actual matches
          setMatched = condense( setMatched );
        }

        // Add matches to results
        push[$fog$32]( results, setMatched );

        // Seedless set matches succeeding multiple successful matchers stipulate sorting
        if ( outermost && !seed && setMatched[$fog$24] > $fog$21 &&
          ( matchedCount + setMatchers[$fog$24] ) > $fog$34 ) {

          Sizzle[$fog$243]( results );
        }
      }

      // Override manipulation of globals by nested matchers
      if ( outermost ) {
        dirruns = dirrunsUnique;
        outermostContext = contextBackup;
      }

      return unmatched;
    };

  return bySet ?
    markFunction( superMatcher ) :
    superMatcher;
}

compile = Sizzle[$fog$301] = function( selector, match /* Internal Use Only */ ) {
  var i,
    setMatchers = [],
    elementMatchers = [],
    cached = compilerCache[ selector + $fog$71 ];

  if ( !cached ) {
    // Generate a function of recursive functions that can be used to check each element
    if ( !match ) {
      match = tokenize( selector );
    }
    i = match[$fog$24];
    while ( i-- ) {
      cached = matcherFromTokens( match[i] );
      if ( cached[ expando ] ) {
        setMatchers[$fog$8]( cached );
      } else {
        elementMatchers[$fog$8]( cached );
      }
    }

    // Cache the compiled function
    cached = compilerCache( selector, matcherFromGroupMatchers( elementMatchers, setMatchers ) );

    // Save selector and tokenization
    cached[$fog$302] = selector;
  }
  return cached;
};

/**
 * A low-level selection function that works with Sizzle's compiled
 *  selector functions
 * @param {String|Function} selector A selector or a pre-compiled
 *  selector function built with Sizzle.compile
 * @param {Element} context
 * @param {Array} [results]
 * @param {Array} [seed] A set of elements to match against
 */
select = Sizzle[$fog$303] = function( selector, context, results, seed ) {
  var i, tokens, token, type, find,
    compiled = typeof selector === $fog$48 && selector,
    match = !seed && tokenize( (selector = compiled[$fog$302] || selector) );

  results = results || [];

  // Try to minimize operations if there is no seed and only one group
  if ( match[$fog$24] === $fog$34 ) {

    // Take a shortcut and set the context if the root selector is an ID
    tokens = match[$fog$21] = match[$fog$21][$fog$6]( $fog$21 );
    if ( tokens[$fog$24] > $fog$66 && (token = tokens[$fog$21])[$fog$47] === $fog$190 &&
        support[$fog$187] && context[$fog$50] === $fog$144 && documentIsHTML &&
        Expr[$fog$298][ tokens[$fog$34][$fog$47] ] ) {

      context = ( Expr[$fog$189][$fog$190]( token[$fog$224][$fog$21][$fog$44](runescape, funescape), context ) || [] )[$fog$21];
      if ( !context ) {
        return results;

      // Precompiled matchers will still verify ancestry, so step up a level
      } else if ( compiled ) {
        context = context[$fog$59];
      }

      selector = selector[$fog$6]( tokens[$fog$165]()[$fog$193][$fog$24] );
    }

    // Fetch a seed set for right-to-left matching
    i = matchExpr[$fog$304][$fog$153]( selector ) ? $fog$21 : tokens[$fog$24];
    while ( i-- ) {
      token = tokens[i];

      // Abort if we hit a combinator
      if ( Expr[$fog$298][ (type = token[$fog$47]) ] ) {
        break;
      }
      if ( (find = Expr[$fog$189][ type ]) ) {
        // Search, expanding context for leading sibling combinators
        if ( (seed = find(
          token[$fog$224][$fog$21][$fog$44]( runescape, funescape ),
          rsibling[$fog$153]( tokens[$fog$21][$fog$47] ) && testContext( context[$fog$59] ) || context
        )) ) {

          // If seed is empty or no tokens remain, we can return early
          tokens[$fog$36]( i, $fog$34 );
          selector = seed[$fog$24] && toSelector( tokens );
          if ( !selector ) {
            push[$fog$32]( results, seed );
            return results;
          }

          break;
        }
      }
    }
  }

  // Compile and execute a filtering function if one is not provided
  // Provide `match` to avoid retokenization if we modified the selector above
  ( compiled || compile( selector, match ) )(
    seed,
    context,
    !documentIsHTML,
    results,
    rsibling[$fog$153]( selector ) && testContext( context[$fog$59] ) || context
  );
  return results;
};

// One-time assignments

// Sort stability
support[$fog$245] = expando[$fog$69]($fog$20)[$fog$35]( sortOrder )[$fog$159]($fog$20) === expando;

// Support: Chrome 14-35+
// Always assume duplicates if they aren't passed to the comparison function
support[$fog$244] = !!hasDuplicate;

// Initialize against the default document
setDocument();

// Support: Webkit<537.32 - Safari 6.0.3/Chrome 25 (fixed in Chrome 27)
// Detached nodes confoundingly follow *each other*
support[$fog$235] = assert(function( div1 ) {
  // Should return 1, but returns 4 (following)
  return div1[$fog$232]( document[$fog$55]($fog$166) ) & $fog$34;
});

// Support: IE<8
// Prevent attribute/property "interpolation"
// http://msdn.microsoft.com/en-us/library/ms536429%28VS.85%29.aspx
if ( !assert(function( div ) {
  div[$fog$196] = $fog$305;
  return div[$fog$248][$fog$154]($fog$285) === $fog$306 ;
}) ) {
  addHandle( $fog$307, function( elem, name, isXML ) {
    if ( !isXML ) {
      return elem[$fog$154]( name, name[$fog$64]() === $fog$47 ? $fog$34 : $fog$66 );
    }
  });
}

// Support: IE<9
// Use defaultValue in place of getAttribute("value")
if ( !support[$fog$184] || !assert(function( div ) {
  div[$fog$196] = $fog$308;
  div[$fog$248][$fog$156]( $fog$193, $fog$20 );
  return div[$fog$248][$fog$154]( $fog$193 ) === $fog$20;
}) ) {
  addHandle( $fog$193, function( elem, name, isXML ) {
    if ( !isXML && elem[$fog$63][$fog$64]() === $fog$171 ) {
      return elem[$fog$309];
    }
  });
}

// Support: IE<9
// Use getAttributeNode to fetch booleans when getAttribute lies
if ( !assert(function( div ) {
  return div[$fog$154]($fog$287) == $fog$23;
}) ) {
  addHandle( booleans, function( elem, name, isXML ) {
    var val;
    if ( !isXML ) {
      return elem[ name ] === $fog$3 ? name[$fog$64]() :
          (val = elem[$fog$192]( name )) && val[$fog$240] ?
          val[$fog$193] :
        $fog$23;
    }
  });
}

return Sizzle;

})( window );



jQuery[$fog$189] = Sizzle;
jQuery[$fog$310] = Sizzle[$fog$250];
jQuery[$fog$310][$fog$311] = jQuery[$fog$310][$fog$274];
jQuery[$fog$312] = Sizzle[$fog$243];
jQuery[$fog$57] = Sizzle[$fog$246];
jQuery[$fog$313] = Sizzle[$fog$174];
jQuery[$fog$233] = Sizzle[$fog$233];



var rneedsContext = jQuery[$fog$310][$fog$314][$fog$304];

var rsingleTag = ($fog$315);



var risSimple = $fog$316;

// Implement the identical functionality for filter and not
function winnow( elements, qualifier, not ) {
  if ( jQuery[$fog$40]( qualifier ) ) {
    return jQuery[$fog$317]( elements, function( elem, i ) {
      /* jshint -W018 */
      return !!qualifier[$fog$22]( elem, i, elem ) !== not;
    });

  }

  if ( qualifier[$fog$50] ) {
    return jQuery[$fog$317]( elements, function( elem ) {
      return ( elem === qualifier ) !== not;
    });

  }

  if ( typeof qualifier === $fog$65 ) {
    if ( risSimple[$fog$153]( qualifier ) ) {
      return jQuery[$fog$191]( qualifier, elements, not );
    }

    qualifier = jQuery[$fog$191]( qualifier, elements );
  }

  return jQuery[$fog$317]( elements, function( elem ) {
    return ( indexOf[$fog$22]( qualifier, elem ) >= $fog$21 ) !== not;
  });
}

jQuery[$fog$191] = function( expr, elems, not ) {
  var elem = elems[ $fog$21 ];

  if ( not ) {
    expr = $fog$318 + expr + $fog$106;
  }

  return elems[$fog$24] === $fog$34 && elem[$fog$50] === $fog$34 ?
    jQuery[$fog$189][$fog$223]( elem, expr ) ? [ elem ] : [] :
    jQuery[$fog$189][$fog$224]( expr, jQuery[$fog$317]( elems, function( elem ) {
      return elem[$fog$50] === $fog$34;
    }));
};

jQuery[$fog$14][$fog$37]({
  find: function( selector ) {
    var i,
      len = this[$fog$24],
      ret = [],
      self = this;

    if ( typeof selector !== $fog$65 ) {
      return this[$fog$30]( jQuery( selector )[$fog$191](function() {
        for ( i = $fog$21; i < len; i++ ) {
          if ( jQuery[$fog$233]( self[ i ], this ) ) {
            return $fog$3;
          }
        }
      }) );
    }

    for ( i = $fog$21; i < len; i++ ) {
      jQuery[$fog$189]( selector, self[ i ], ret );
    }

    // Needed because $( selector, context ) becomes $( context ).find( selector )
    ret = this[$fog$30]( len > $fog$34 ? jQuery[$fog$312]( ret ) : ret );
    ret[$fog$302] = this[$fog$302] ? this[$fog$302] + $fog$71 + selector : selector;
    return ret;
  },
  filter: function( selector ) {
    return this[$fog$30]( winnow(this, selector || [], $fog$38) );
  },
  not: function( selector ) {
    return this[$fog$30]( winnow(this, selector || [], $fog$3) );
  },
  is: function( selector ) {
    return !!winnow(
      this,

      // If this is a positional/relative selector, check membership in the returned set
      // so $("p:first").is("p:last") won't return true for a doc with two "p".
      typeof selector === $fog$65 && rneedsContext[$fog$153]( selector ) ?
        jQuery( selector ) :
        selector || [],
      $fog$38
    )[$fog$24];
  }
});


// Initialize a jQuery object


// A central reference to the root jQuery(document)
var rootjQuery,

  // A simple way to check for HTML strings
  // Prioritize #id over <tag> to avoid XSS via location.hash (#9521)
  // Strict HTML recognition (#11290: must start with <)
  rquickExpr = $fog$319,

  init = jQuery[$fog$14][$fog$13] = function( selector, context ) {
    var match, elem;

    // HANDLE: $(""), $(null), $(undefined), $(false)
    if ( !selector ) {
      return this;
    }

    // Handle HTML strings
    if ( typeof selector === $fog$65 ) {
      if ( selector[$fog$21] === $fog$320 && selector[ selector[$fog$24] - $fog$34 ] === $fog$321 && selector[$fog$24] >= $fog$150 ) {
        // Assume that strings that start and end with <> are HTML and skip the regex check
        match = [ $fog$23, selector, $fog$23 ];

      } else {
        match = rquickExpr[$fog$146]( selector );
      }

      // Match html or make sure no context is specified for #id
      if ( match && (match[$fog$34] || !context) ) {

        // HANDLE: $(html) -> $(array)
        if ( match[$fog$34] ) {
          context = context instanceof jQuery ? context[$fog$21] : context;

          // Option to run scripts is true for back-compat
          // Intentionally let the error be thrown if parseHTML is not present
          jQuery[$fog$25]( this, jQuery[$fog$322](
            match[$fog$34],
            context && context[$fog$50] ? context[$fog$143] || context : document,
            $fog$3
          ) );

          // HANDLE: $(html, props)
          if ( rsingleTag[$fog$153]( match[$fog$34] ) && jQuery[$fog$41]( context ) ) {
            for ( match in context ) {
              // Properties of context are called as methods if possible
              if ( jQuery[$fog$40]( this[ match ] ) ) {
                this[ match ]( context[ match ] );

              // ...and otherwise set as attributes
              } else {
                this[$fog$239]( match, context[ match ] );
              }
            }
          }

          return this;

        // HANDLE: $(#id)
        } else {
          elem = document[$fog$147]( match[$fog$66] );

          // Support: Blackberry 4.6
          // gEBID returns nodes no longer in the document (#6963)
          if ( elem && elem[$fog$59] ) {
            // Inject the element directly into the jQuery object
            this[$fog$24] = $fog$34;
            this[$fog$21] = elem;
          }

          this[$fog$28] = document;
          this[$fog$302] = selector;
          return this;
        }

      // HANDLE: $(expr, $(...))
      } else if ( !context || context[$fog$323] ) {
        return ( context || rootjQuery )[$fog$189]( selector );

      // HANDLE: $(expr, context)
      // (which is just equivalent to: $(context).find(expr)
      } else {
        return this[$fog$26]( context )[$fog$189]( selector );
      }

    // HANDLE: $(DOMElement)
    } else if ( selector[$fog$50] ) {
      this[$fog$28] = this[$fog$21] = selector;
      this[$fog$24] = $fog$34;
      return this;

    // HANDLE: $(function)
    // Shortcut for document ready
    } else if ( jQuery[$fog$40]( selector ) ) {
      return typeof rootjQuery[$fog$324] !== $fog$5 ?
        rootjQuery[$fog$324]( selector ) :
        // Execute immediately if ready is not present
        selector( jQuery );
    }

    if ( selector[$fog$302] !== undefined ) {
      this[$fog$302] = selector[$fog$302];
      this[$fog$28] = selector[$fog$28];
    }

    return jQuery[$fog$325]( selector, this );
  };

// Give the init function the jQuery prototype for later instantiation
init[$fog$19] = jQuery[$fog$14];

// Initialize central reference
rootjQuery = jQuery( document );


var rparentsprev = $fog$326,
  // Methods guaranteed to produce a unique set when starting from a unique set
  guaranteedUnique = {
    children: $fog$3,
    contents: $fog$3,
    next: $fog$3,
    prev: $fog$3
  };

jQuery[$fog$37]({
  dir: function( elem, dir, until ) {
    var matched = [],
      truncate = until !== undefined;

    while ( (elem = elem[ dir ]) && elem[$fog$50] !== $fog$144 ) {
      if ( elem[$fog$50] === $fog$34 ) {
        if ( truncate && jQuery( elem )[$fog$327]( until ) ) {
          break;
        }
        matched[$fog$8]( elem );
      }
    }
    return matched;
  },

  sibling: function( n, elem ) {
    var matched = [];

    for ( ; n; n = n[$fog$170] ) {
      if ( n[$fog$50] === $fog$34 && n !== elem ) {
        matched[$fog$8]( n );
      }
    }

    return matched;
  }
});

jQuery[$fog$14][$fog$37]({
  has: function( target ) {
    var targets = jQuery( target, this ),
      l = targets[$fog$24];

    return this[$fog$191](function() {
      var i = $fog$21;
      for ( ; i < l; i++ ) {
        if ( jQuery[$fog$233]( this, targets[i] ) ) {
          return $fog$3;
        }
      }
    });
  },

  closest: function( selectors, context ) {
    var cur,
      i = $fog$21,
      l = this[$fog$24],
      matched = [],
      pos = rneedsContext[$fog$153]( selectors ) || typeof selectors !== $fog$65 ?
        jQuery( selectors, context || this[$fog$28] ) :
        $fog$21;

    for ( ; i < l; i++ ) {
      for ( cur = this[i]; cur && cur !== context; cur = cur[$fog$59] ) {
        // Always skip document fragments
        if ( cur[$fog$50] < $fog$145 && (pos ?
          pos[$fog$328](cur) > -$fog$34 :

          // Don't pass non-elements to Sizzle
          cur[$fog$50] === $fog$34 &&
            jQuery[$fog$189][$fog$223](cur, selectors)) ) {

          matched[$fog$8]( cur );
          break;
        }
      }
    }

    return this[$fog$30]( matched[$fog$24] > $fog$34 ? jQuery[$fog$312]( matched ) : matched );
  },

  // Determine the position of an element within the set
  index: function( elem ) {

    // No argument, return index in parent
    if ( !elem ) {
      return ( this[ $fog$21 ] && this[ $fog$21 ][$fog$59] ) ? this[$fog$297]()[$fog$329]()[$fog$24] : -$fog$34;
    }

    // Index in selector
    if ( typeof elem === $fog$65 ) {
      return indexOf[$fog$22]( jQuery( elem ), this[ $fog$21 ] );
    }

    // Locate the position of the desired element
    return indexOf[$fog$22]( this,

      // If it receives a jQuery object, the first element is used
      elem[$fog$323] ? elem[ $fog$21 ] : elem
    );
  },

  add: function( selector, context ) {
    return this[$fog$30](
      jQuery[$fog$312](
        jQuery[$fog$25]( this[$fog$330](), jQuery( selector, context ) )
      )
    );
  },

  addBack: function( selector ) {
    return this[$fog$331]( selector == $fog$23 ?
      this[$fog$27] : this[$fog$27][$fog$191](selector)
    );
  }
});

function sibling( cur, dir ) {
  while ( (cur = cur[dir]) && cur[$fog$50] !== $fog$34 ) {}
  return cur;
}

jQuery[$fog$29]({
  parent: function( elem ) {
    var parent = elem[$fog$59];
    return parent && parent[$fog$50] !== $fog$145 ? parent : $fog$23;
  },
  parents: function( elem ) {
    return jQuery[$fog$296]( elem, $fog$59 );
  },
  parentsUntil: function( elem, i, until ) {
    return jQuery[$fog$296]( elem, $fog$59, until );
  },
  next: function( elem ) {
    return sibling( elem, $fog$170 );
  },
  prev: function( elem ) {
    return sibling( elem, $fog$252 );
  },
  nextAll: function( elem ) {
    return jQuery[$fog$296]( elem, $fog$170 );
  },
  prevAll: function( elem ) {
    return jQuery[$fog$296]( elem, $fog$252 );
  },
  nextUntil: function( elem, i, until ) {
    return jQuery[$fog$296]( elem, $fog$170, until );
  },
  prevUntil: function( elem, i, until ) {
    return jQuery[$fog$296]( elem, $fog$252, until );
  },
  siblings: function( elem ) {
    return jQuery[$fog$332]( ( elem[$fog$59] || {} )[$fog$248], elem );
  },
  children: function( elem ) {
    return jQuery[$fog$332]( elem[$fog$248] );
  },
  contents: function( elem ) {
    return elem[$fog$333] || jQuery[$fog$25]( [], elem[$fog$142] );
  }
}, function( name, fn ) {
  jQuery[$fog$14][ name ] = function( until, selector ) {
    var matched = jQuery[$fog$31]( this, fn, until );

    if ( name[$fog$6]( -$fog$253 ) !== $fog$334 ) {
      selector = until;
    }

    if ( selector && typeof selector === $fog$65 ) {
      matched = jQuery[$fog$191]( selector, matched );
    }

    if ( this[$fog$24] > $fog$34 ) {
      // Remove duplicates
      if ( !guaranteedUnique[ name ] ) {
        jQuery[$fog$312]( matched );
      }

      // Reverse order for parents* and prev-derivatives
      if ( rparentsprev[$fog$153]( name ) ) {
        matched[$fog$335]();
      }
    }

    return this[$fog$30]( matched );
  };
});
var rnotwhite = ($fog$336);



// String to Object options format cache
var optionsCache = {};

// Convert String-formatted options into Object-formatted ones and store in cache
function createOptions( options ) {
  var object = optionsCache[ options ] = {};
  jQuery[$fog$29]( options[$fog$314]( rnotwhite ) || [], function( _, flag ) {
    object[ flag ] = $fog$3;
  });
  return object;
}

/*
 * Create a callback list using the following parameters:
 *
 *  options: an optional list of space-separated options that will change how
 *      the callback list behaves or a more traditional option object
 *
 * By default a callback list will act like an event callback list and can be
 * "fired" multiple times.
 *
 * Possible options:
 *
 *  once:     will ensure the callback list can only be fired once (like a Deferred)
 *
 *  memory:     will keep track of previous values and will call any callback added
 *          after the list has been fired right away with the latest "memorized"
 *          values (like a Deferred)
 *
 *  unique:     will ensure a callback can only be added once (no duplicate in the list)
 *
 *  stopOnFalse:  interrupt callings when a callback returns false
 *
 */
jQuery[$fog$337] = function( options ) {

  // Convert options from String-formatted to Object-formatted if needed
  // (we check in cache first)
  options = typeof options === $fog$65 ?
    ( optionsCache[ options ] || createOptions( options ) ) :
    jQuery[$fog$37]( {}, options );

  var // Last fire value (for non-forgettable lists)
    memory,
    // Flag to know if list was already fired
    fired,
    // Flag to know if list is currently firing
    firing,
    // First callback to fire (used internally by add and fireWith)
    firingStart,
    // End of the loop when firing
    firingLength,
    // Index of currently firing callback (modified by remove if needed)
    firingIndex,
    // Actual callback list
    list = [],
    // Stack of fire calls for repeatable lists
    stack = !options[$fog$338] && [],
    // Fire callbacks
    fire = function( data ) {
      memory = options[$fog$339] && data;
      fired = $fog$3;
      firingIndex = firingStart || $fog$21;
      firingStart = $fog$21;
      firingLength = list[$fog$24];
      firing = $fog$3;
      for ( ; list && firingIndex < firingLength; firingIndex++ ) {
        if ( list[ firingIndex ][$fog$32]( data[ $fog$21 ], data[ $fog$34 ] ) === $fog$38 && options[$fog$340] ) {
          memory = $fog$38; // To prevent further calls using add
          break;
        }
      }
      firing = $fog$38;
      if ( list ) {
        if ( stack ) {
          if ( stack[$fog$24] ) {
            fire( stack[$fog$165]() );
          }
        } else if ( memory ) {
          list = [];
        } else {
          self[$fog$341]();
        }
      }
    },
    // Actual Callbacks object
    self = {
      // Add a callback or a collection of callbacks to the list
      add: function() {
        if ( list ) {
          // First, we save the current length
          var start = list[$fog$24];
          (function add( args ) {
            jQuery[$fog$29]( args, function( _, arg ) {
              var type = jQuery[$fog$47]( arg );
              if ( type === $fog$48 ) {
                if ( !options[$fog$312] || !self[$fog$342]( arg ) ) {
                  list[$fog$8]( arg );
                }
              } else if ( arg && arg[$fog$24] && type !== $fog$65 ) {
                // Inspect recursively
                add( arg );
              }
            });
          })( arguments );
          // Do we need to add the callbacks to the
          // current firing batch?
          if ( firing ) {
            firingLength = list[$fog$24];
          // With memory, if we're not firing then
          // we should call right away
          } else if ( memory ) {
            firingStart = start;
            fire( memory );
          }
        }
        return this;
      },
      // Remove a callback from the list
      remove: function() {
        if ( list ) {
          jQuery[$fog$29]( arguments, function( _, arg ) {
            var index;
            while ( ( index = jQuery[$fog$343]( arg, list, index ) ) > -$fog$34 ) {
              list[$fog$36]( index, $fog$34 );
              // Handle firing indexes
              if ( firing ) {
                if ( index <= firingLength ) {
                  firingLength--;
                }
                if ( index <= firingIndex ) {
                  firingIndex--;
                }
              }
            }
          });
        }
        return this;
      },
      // Check if a given callback is in the list.
      // If no argument is given, return whether or not list has callbacks attached.
      has: function( fn ) {
        return fn ? jQuery[$fog$343]( fn, list ) > -$fog$34 : !!( list && list[$fog$24] );
      },
      // Remove all callbacks from the list
      empty: function() {
        list = [];
        firingLength = $fog$21;
        return this;
      },
      // Have the list do nothing anymore
      disable: function() {
        list = stack = memory = undefined;
        return this;
      },
      // Is it disabled?
      disabled: function() {
        return !list;
      },
      // Lock the list in its current state
      lock: function() {
        stack = undefined;
        if ( !memory ) {
          self[$fog$341]();
        }
        return this;
      },
      // Is it locked?
      locked: function() {
        return !stack;
      },
      // Call all callbacks with the given context and arguments
      fireWith: function( context, args ) {
        if ( list && ( !fired || stack ) ) {
          args = args || [];
          args = [ context, args[$fog$6] ? args[$fog$6]() : args ];
          if ( firing ) {
            stack[$fog$8]( args );
          } else {
            fire( args );
          }
        }
        return this;
      },
      // Call all the callbacks with the given arguments
      fire: function() {
        self[$fog$344]( this, arguments );
        return this;
      },
      // To know if the callbacks have already been called at least once
      fired: function() {
        return !!fired;
      }
    };

  return self;
};


jQuery[$fog$37]({

  Deferred: function( func ) {
    var tuples = [
        // action, add listener, listener list, final state
        [ $fog$345, $fog$346, jQuery[$fog$337]($fog$347), $fog$348 ],
        [ $fog$349, $fog$350, jQuery[$fog$337]($fog$347), $fog$351 ],
        [ $fog$352, $fog$353, jQuery[$fog$337]($fog$339) ]
      ],
      state = $fog$354,
      promise = {
        state: function() {
          return state;
        },
        always: function() {
          deferred[$fog$346]( arguments )[$fog$350]( arguments );
          return this;
        },
        then: function( /* fnDone, fnFail, fnProgress */ ) {
          var fns = arguments;
          return jQuery[$fog$356](function( newDefer ) {
            jQuery[$fog$29]( tuples, function( i, tuple ) {
              var fn = jQuery[$fog$40]( fns[ i ] ) && fns[ i ];
              // deferred[ done | fail | progress ] for forwarding actions to newDefer
              deferred[ tuple[$fog$34] ](function() {
                var returned = fn && fn[$fog$32]( this, arguments );
                if ( returned && jQuery[$fog$40]( returned[$fog$355] ) ) {
                  returned[$fog$355]()
                    [$fog$346]( newDefer[$fog$345] )
                    [$fog$350]( newDefer[$fog$349] )
                    [$fog$353]( newDefer[$fog$352] );
                } else {
                  newDefer[ tuple[ $fog$21 ] + $fog$357 ]( this === promise ? newDefer[$fog$355]() : this, fn ? [ returned ] : arguments );
                }
              });
            });
            fns = $fog$23;
          })[$fog$355]();
        },
        // Get a promise for this deferred
        // If obj is provided, the promise aspect is added to the object
        promise: function( obj ) {
          return obj != $fog$23 ? jQuery[$fog$37]( obj, promise ) : promise;
        }
      },
      deferred = {};

    // Keep pipe for back-compat
    promise[$fog$358] = promise[$fog$359];

    // Add list-specific methods
    jQuery[$fog$29]( tuples, function( i, tuple ) {
      var list = tuple[ $fog$66 ],
        stateString = tuple[ $fog$150 ];

      // promise[ done | fail | progress ] = list.add
      promise[ tuple[$fog$34] ] = list[$fog$331];

      // Handle state
      if ( stateString ) {
        list[$fog$331](function() {
          // state = [ resolved | rejected ]
          state = stateString;

        // [ reject_list | resolve_list ].disable; progress_list.lock
        }, tuples[ i ^ $fog$34 ][ $fog$66 ][$fog$341], tuples[ $fog$66 ][ $fog$66 ][$fog$360] );
      }

      // deferred[ resolve | reject | notify ]
      deferred[ tuple[$fog$21] ] = function() {
        deferred[ tuple[$fog$21] + $fog$357 ]( this === deferred ? promise : this, arguments );
        return this;
      };
      deferred[ tuple[$fog$21] + $fog$357 ] = list[$fog$344];
    });

    // Make the deferred a promise
    promise[$fog$355]( deferred );

    // Call given func if any
    if ( func ) {
      func[$fog$22]( deferred, deferred );
    }

    // All done!
    return deferred;
  },

  // Deferred helper
  when: function( subordinate /* , ..., subordinateN */ ) {
    var i = $fog$21,
      resolveValues = slice[$fog$22]( arguments ),
      length = resolveValues[$fog$24],

      // the count of uncompleted subordinates
      remaining = length !== $fog$34 || ( subordinate && jQuery[$fog$40]( subordinate[$fog$355] ) ) ? length : $fog$21,

      // the master Deferred. If resolveValues consist of only a single Deferred, just use that.
      deferred = remaining === $fog$34 ? subordinate : jQuery[$fog$356](),

      // Update function for both resolve and progress values
      updateFunc = function( i, contexts, values ) {
        return function( value ) {
          contexts[ i ] = this;
          values[ i ] = arguments[$fog$24] > $fog$34 ? slice[$fog$22]( arguments ) : value;
          if ( values === progressValues ) {
            deferred[$fog$361]( contexts, values );
          } else if ( !( --remaining ) ) {
            deferred[$fog$362]( contexts, values );
          }
        };
      },

      progressValues, progressContexts, resolveContexts;

    // Add listeners to Deferred subordinates; treat others as resolved
    if ( length > $fog$34 ) {
      progressValues = new Array( length );
      progressContexts = new Array( length );
      resolveContexts = new Array( length );
      for ( ; i < length; i++ ) {
        if ( resolveValues[ i ] && jQuery[$fog$40]( resolveValues[ i ][$fog$355] ) ) {
          resolveValues[ i ][$fog$355]()
            [$fog$346]( updateFunc( i, resolveContexts, resolveValues ) )
            [$fog$350]( deferred[$fog$349] )
            [$fog$353]( updateFunc( i, progressContexts, progressValues ) );
        } else {
          --remaining;
        }
      }
    }

    // If we're not waiting on anything, resolve the master
    if ( !remaining ) {
      deferred[$fog$362]( resolveContexts, resolveValues );
    }

    return deferred[$fog$355]();
  }
});


// The deferred used on DOM ready
var readyList;

jQuery[$fog$14][$fog$324] = function( fn ) {
  // Add the callback
  jQuery[$fog$324][$fog$355]()[$fog$346]( fn );

  return this;
};

jQuery[$fog$37]({
  // Is the DOM ready to be used? Set to true once it occurs.
  isReady: $fog$38,

  // A counter to track how many items to wait for before
  // the ready event fires. See #6781
  readyWait: $fog$34,

  // Hold (or release) the ready event
  holdReady: function( hold ) {
    if ( hold ) {
      jQuery[$fog$363]++;
    } else {
      jQuery[$fog$324]( $fog$3 );
    }
  },

  // Handle when the DOM is ready
  ready: function( wait ) {

    // Abort if there are pending holds or we're already ready
    if ( wait === $fog$3 ? --jQuery[$fog$363] : jQuery[$fog$364] ) {
      return;
    }

    // Remember that the DOM is ready
    jQuery[$fog$364] = $fog$3;

    // If a normal DOM Ready event fired, decrement, and wait if need be
    if ( wait !== $fog$3 && --jQuery[$fog$363] > $fog$21 ) {
      return;
    }

    // If there are functions bound, to execute
    readyList[$fog$362]( document, [ jQuery ] );

    // Trigger any bound ready events
    if ( jQuery[$fog$14][$fog$365] ) {
      jQuery( document )[$fog$365]( $fog$324 );
      jQuery( document )[$fog$366]( $fog$324 );
    }
  }
});

/**
 * The ready event handler and self cleanup method
 */
function completed() {
  document[$fog$367]( $fog$368, completed, $fog$38 );
  window[$fog$367]( $fog$369, completed, $fog$38 );
  jQuery[$fog$324]();
}

jQuery[$fog$324][$fog$355] = function( obj ) {
  if ( !readyList ) {

    readyList = jQuery[$fog$356]();

    // Catch cases where $(document).ready() is called after the browser event has already occurred.
    // We once tried to use readyState "interactive" here, but it caused issues like the one
    // discovered by ChrisS here: http://bugs.jquery.com/ticket/12282#comment:15
    if ( document[$fog$370] === $fog$371 ) {
      // Handle it asynchronously to allow scripts the opportunity to delay ready
      setTimeout( jQuery[$fog$324] );

    } else {

      // Use the handy event callback
      document[$fog$180]( $fog$368, completed, $fog$38 );

      // A fallback to window.onload, that will always work
      window[$fog$180]( $fog$369, completed, $fog$38 );
    }
  }
  return readyList[$fog$355]( obj );
};

// Kick off the DOM ready check even if the user does not
jQuery[$fog$324][$fog$355]();




// Multifunctional method to get and set values of a collection
// The value/s can optionally be executed if it's a function
var access = jQuery[$fog$372] = function( elems, fn, key, value, chainable, emptyGet, raw ) {
  var i = $fog$21,
    len = elems[$fog$24],
    bulk = key == $fog$23;

  // Sets many values
  if ( jQuery[$fog$47]( key ) === $fog$0 ) {
    chainable = $fog$3;
    for ( i in key ) {
      jQuery[$fog$372]( elems, fn, i, key[i], $fog$3, emptyGet, raw );
    }

  // Sets one value
  } else if ( value !== undefined ) {
    chainable = $fog$3;

    if ( !jQuery[$fog$40]( value ) ) {
      raw = $fog$3;
    }

    if ( bulk ) {
      // Bulk operations run against the entire set
      if ( raw ) {
        fn[$fog$22]( elems, value );
        fn = $fog$23;

      // ...except when executing function values
      } else {
        bulk = fn;
        fn = function( elem, key, value ) {
          return bulk[$fog$22]( jQuery( elem ), value );
        };
      }
    }

    if ( fn ) {
      for ( ; i < len; i++ ) {
        fn( elems[i], key, raw ? value : value[$fog$22]( elems[i], i, fn( elems[i], key ) ) );
      }
    }
  }

  return chainable ?
    elems :

    // Gets
    bulk ?
      fn[$fog$22]( elems ) :
      len ? fn( elems[$fog$21], key ) : emptyGet;
};


/**
 * Determines whether an object can have data
 */
jQuery[$fog$373] = function( owner ) {
  // Accepts only:
  //  - Node
  //    - Node.ELEMENT_NODE
  //    - Node.DOCUMENT_NODE
  //  - Object
  //    - Any
  /* jshint -W018 */
  return owner[$fog$50] === $fog$34 || owner[$fog$50] === $fog$144 || !( +owner[$fog$50] );
};


function Data() {
  // Support: Android<4,
  // Old WebKit does not have Object.preventExtensions/freeze method,
  // return new empty object instead with no [[set]] accessor
  Object[$fog$374]( this[$fog$375] = {}, $fog$21, {
    get: function() {
      return {};
    }
  });

  this[$fog$376] = jQuery[$fog$376] + Data[$fog$377]++;
}

Data[$fog$377] = $fog$34;
Data[$fog$378] = jQuery[$fog$373];

Data[$fog$19] = {
  key: function( owner ) {
    // We can accept data for non-element nodes in modern browsers,
    // but we should not, see #8335.
    // Always return the key for a frozen object.
    if ( !Data[$fog$378]( owner ) ) {
      return $fog$21;
    }

    var descriptor = {},
      // Check if the owner object already has a cache key
      unlock = owner[ this[$fog$376] ];

    // If not, create one
    if ( !unlock ) {
      unlock = Data[$fog$377]++;

      // Secure it in a non-enumerable, non-writable property
      try {
        descriptor[ this[$fog$376] ] = { value: unlock };
        Object[$fog$379]( owner, descriptor );

      // Support: Android<4
      // Fallback to a less secure definition
      } catch ( e ) {
        descriptor[ this[$fog$376] ] = unlock;
        jQuery[$fog$37]( owner, descriptor );
      }
    }

    // Ensure the cache object
    if ( !this[$fog$375][ unlock ] ) {
      this[$fog$375][ unlock ] = {};
    }

    return unlock;
  },
  set: function( owner, data, value ) {
    var prop,
      // There may be an unlock assigned to this node,
      // if there is no entry for this "owner", create one inline
      // and set the unlock as though an owner entry had always existed
      unlock = this[$fog$380]( owner ),
      cache = this[$fog$375][ unlock ];

    // Handle: [ owner, key, value ] args
    if ( typeof data === $fog$65 ) {
      cache[ data ] = value;

    // Handle: [ owner, { properties } ] args
    } else {
      // Fresh assignments by object are shallow copied
      if ( jQuery[$fog$381]( cache ) ) {
        jQuery[$fog$37]( this[$fog$375][ unlock ], data );
      // Otherwise, copy the properties one-by-one to the cache object
      } else {
        for ( prop in data ) {
          cache[ prop ] = data[ prop ];
        }
      }
    }
    return cache;
  },
  get: function( owner, key ) {
    // Either a valid cache is found, or will be created.
    // New caches will be created and the unlock returned,
    // allowing direct access to the newly created
    // empty data object. A valid owner object must be provided.
    var cache = this[$fog$375][ this[$fog$380]( owner ) ];

    return key === undefined ?
      cache : cache[ key ];
  },
  access: function( owner, key, value ) {
    var stored;
    // In cases where either:
    //
    //   1. No key was specified
    //   2. A string key was specified, but no value provided
    //
    // Take the "read" path and allow the get method to determine
    // which value to return, respectively either:
    //
    //   1. The entire cache object
    //   2. The data stored at the key
    //
    if ( key === undefined ||
        ((key && typeof key === $fog$65) && value === undefined) ) {

      stored = this[$fog$330]( owner, key );

      return stored !== undefined ?
        stored : this[$fog$330]( owner, jQuery[$fog$382](key) );
    }

    // [*]When the key is not a string, or both a key and value
    // are specified, set or extend (existing objects) with either:
    //
    //   1. An object of properties
    //   2. A key and value
    //
    this[$fog$383]( owner, key, value );

    // Since the "set" path can have two possible entry points
    // return the expected data based on which path was taken[*]
    return value !== undefined ? value : key;
  },
  remove: function( owner, key ) {
    var i, name, camel,
      unlock = this[$fog$380]( owner ),
      cache = this[$fog$375][ unlock ];

    if ( key === undefined ) {
      this[$fog$375][ unlock ] = {};

    } else {
      // Support array or space separated string of keys
      if ( jQuery[$fog$42]( key ) ) {
        // If "name" is an array of keys...
        // When data is initially created, via ("key", "val") signature,
        // keys will be converted to camelCase.
        // Since there is no way to tell _how_ a key was added, remove
        // both plain key and camelCase key. #12786
        // This will only penalize the array argument path.
        name = key[$fog$7]( key[$fog$31]( jQuery[$fog$382] ) );
      } else {
        camel = jQuery[$fog$382]( key );
        // Try the string as a key before any manipulation
        if ( key in cache ) {
          name = [ key, camel ];
        } else {
          // If a key with the spaces exists, use it.
          // Otherwise, create an array by matching non-whitespace
          name = camel;
          name = name in cache ?
            [ name ] : ( name[$fog$314]( rnotwhite ) || [] );
        }
      }

      i = name[$fog$24];
      while ( i-- ) {
        delete cache[ name[ i ] ];
      }
    }
  },
  hasData: function( owner ) {
    return !jQuery[$fog$381](
      this[$fog$375][ owner[ this[$fog$376] ] ] || {}
    );
  },
  discard: function( owner ) {
    if ( owner[ this[$fog$376] ] ) {
      delete this[$fog$375][ owner[ this[$fog$376] ] ];
    }
  }
};
var data_priv = new Data();

var data_user = new Data();



//  Implementation Summary
//
//  1. Enforce API surface and semantic compatibility with 1.9.x branch
//  2. Improve the module's maintainability by reducing the storage
//    paths to a single mechanism.
//  3. Use the same single mechanism to support "private" and "user" data.
//  4. _Never_ expose "private" data to user code (TODO: Drop _data, _removeData)
//  5. Avoid exposing implementation details on user objects (eg. expando properties)
//  6. Provide a clear path for implementation upgrade to WeakMap in 2014

var rbrace = $fog$384,
  rmultiDash = $fog$385;

function dataAttr( elem, key, data ) {
  var name;

  // If nothing was found internally, try to fetch any
  // data from the HTML5 data-* attribute
  if ( data === undefined && elem[$fog$50] === $fog$34 ) {
    name = $fog$386 + key[$fog$44]( rmultiDash, $fog$387 )[$fog$64]();
    data = elem[$fog$154]( name );

    if ( typeof data === $fog$65 ) {
      try {
        data = data === $fog$388 ? $fog$3 :
          data === $fog$389 ? $fog$38 :
          data === $fog$390 ? $fog$23 :
          // Only convert to a number if it doesn't change the string
          +data + $fog$20 === data ? +data :
          rbrace[$fog$153]( data ) ? jQuery[$fog$391]( data ) :
          data;
      } catch( e ) {}

      // Make sure we set the data so it isn't changed later
      data_user[$fog$383]( elem, key, data );
    } else {
      data = undefined;
    }
  }
  return data;
}

jQuery[$fog$37]({
  hasData: function( elem ) {
    return data_user[$fog$392]( elem ) || data_priv[$fog$392]( elem );
  },

  data: function( elem, name, data ) {
    return data_user[$fog$372]( elem, name, data );
  },

  removeData: function( elem, name ) {
    data_user[$fog$393]( elem, name );
  },

  // TODO: Now that all calls to _data and _removeData have been replaced
  // with direct calls to data_priv methods, these can be deprecated.
  _data: function( elem, name, data ) {
    return data_priv[$fog$372]( elem, name, data );
  },

  _removeData: function( elem, name ) {
    data_priv[$fog$393]( elem, name );
  }
});

jQuery[$fog$14][$fog$37]({
  data: function( key, value ) {
    var i, name, data,
      elem = this[ $fog$21 ],
      attrs = elem && elem[$fog$184];

    // Gets all values
    if ( key === undefined ) {
      if ( this[$fog$24] ) {
        data = data_user[$fog$330]( elem );

        if ( elem[$fog$50] === $fog$34 && !data_priv[$fog$330]( elem, $fog$394 ) ) {
          i = attrs[$fog$24];
          while ( i-- ) {

            // Support: IE11+
            // The attrs elements can be null (#14894)
            if ( attrs[ i ] ) {
              name = attrs[ i ][$fog$215];
              if ( name[$fog$9]( $fog$386 ) === $fog$21 ) {
                name = jQuery[$fog$382]( name[$fog$6]($fog$253) );
                dataAttr( elem, name, data[ name ] );
              }
            }
          }
          data_priv[$fog$383]( elem, $fog$394, $fog$3 );
        }
      }

      return data;
    }

    // Sets multiple values
    if ( typeof key === $fog$0 ) {
      return this[$fog$29](function() {
        data_user[$fog$383]( this, key );
      });
    }

    return access( this, function( value ) {
      var data,
        camelKey = jQuery[$fog$382]( key );

      // The calling jQuery object (element matches) is not empty
      // (and therefore has an element appears at this[ 0 ]) and the
      // `value` parameter was not undefined. An empty jQuery object
      // will result in `undefined` for elem = this[ 0 ] which will
      // throw an exception if an attempt to read a data cache is made.
      if ( elem && value === undefined ) {
        // Attempt to get data from the cache
        // with the key as-is
        data = data_user[$fog$330]( elem, key );
        if ( data !== undefined ) {
          return data;
        }

        // Attempt to get data from the cache
        // with the key camelized
        data = data_user[$fog$330]( elem, camelKey );
        if ( data !== undefined ) {
          return data;
        }

        // Attempt to "discover" the data in
        // HTML5 custom data-* attrs
        data = dataAttr( elem, camelKey, undefined );
        if ( data !== undefined ) {
          return data;
        }

        // We tried really hard, but the data doesn't exist.
        return;
      }

      // Set the data...
      this[$fog$29](function() {
        // First, attempt to store a copy or reference of any
        // data that might've been store with a camelCased key.
        var data = data_user[$fog$330]( this, camelKey );

        // For HTML5 data-* attribute interop, we have to
        // store property names with dashes in a camelCase form.
        // This might not apply to all properties...*
        data_user[$fog$383]( this, camelKey, value );

        // *... In the case of properties that might _actually_
        // have dashes, we need to also store a copy of that
        // unchanged property.
        if ( key[$fog$9]($fog$269) !== -$fog$34 && data !== undefined ) {
          data_user[$fog$383]( this, key, value );
        }
      });
    }, $fog$23, value, arguments[$fog$24] > $fog$34, $fog$23, $fog$3 );
  },

  removeData: function( key ) {
    return this[$fog$29](function() {
      data_user[$fog$393]( this, key );
    });
  }
});


jQuery[$fog$37]({
  queue: function( elem, type, data ) {
    var queue;

    if ( elem ) {
      type = ( type || $fog$395 ) + $fog$396;
      queue = data_priv[$fog$330]( elem, type );

      // Speed up dequeue by getting out quickly if this is just a lookup
      if ( data ) {
        if ( !queue || jQuery[$fog$42]( data ) ) {
          queue = data_priv[$fog$372]( elem, type, jQuery[$fog$325](data) );
        } else {
          queue[$fog$8]( data );
        }
      }
      return queue || [];
    }
  },

  dequeue: function( elem, type ) {
    type = type || $fog$395;

    var queue = jQuery[$fog$396]( elem, type ),
      startLength = queue[$fog$24],
      fn = queue[$fog$165](),
      hooks = jQuery[$fog$397]( elem, type ),
      next = function() {
        jQuery[$fog$398]( elem, type );
      };

    // If the fx queue is dequeued, always remove the progress sentinel
    if ( fn === $fog$399 ) {
      fn = queue[$fog$165]();
      startLength--;
    }

    if ( fn ) {

      // Add a progress sentinel to prevent the fx queue from being
      // automatically dequeued
      if ( type === $fog$395 ) {
        queue[$fog$237]( $fog$399 );
      }

      // Clear up the last queue stop function
      delete hooks[$fog$400];
      fn[$fog$22]( elem, next, hooks );
    }

    if ( !startLength && hooks ) {
      hooks[$fog$292][$fog$401]();
    }
  },

  // Not public - generate a queueHooks object, or return the current one
  _queueHooks: function( elem, type ) {
    var key = type + $fog$402;
    return data_priv[$fog$330]( elem, key ) || data_priv[$fog$372]( elem, key, {
      empty: jQuery[$fog$337]($fog$347)[$fog$331](function() {
        data_priv[$fog$393]( elem, [ type + $fog$396, key ] );
      })
    });
  }
});

jQuery[$fog$14][$fog$37]({
  queue: function( type, data ) {
    var setter = $fog$66;

    if ( typeof type !== $fog$65 ) {
      data = type;
      type = $fog$395;
      setter--;
    }

    if ( arguments[$fog$24] < setter ) {
      return jQuery[$fog$396]( this[$fog$21], type );
    }

    return data === undefined ?
      this :
      this[$fog$29](function() {
        var queue = jQuery[$fog$396]( this, type, data );

        // Ensure a hooks for this queue
        jQuery[$fog$397]( this, type );

        if ( type === $fog$395 && queue[$fog$21] !== $fog$399 ) {
          jQuery[$fog$398]( this, type );
        }
      });
  },
  dequeue: function( type ) {
    return this[$fog$29](function() {
      jQuery[$fog$398]( this, type );
    });
  },
  clearQueue: function( type ) {
    return this[$fog$396]( type || $fog$395, [] );
  },
  // Get a promise resolved when queues of a certain type
  // are emptied (fx is the type by default)
  promise: function( type, obj ) {
    var tmp,
      count = $fog$34,
      defer = jQuery[$fog$356](),
      elements = this,
      i = this[$fog$24],
      resolve = function() {
        if ( !( --count ) ) {
          defer[$fog$362]( elements, [ elements ] );
        }
      };

    if ( typeof type !== $fog$65 ) {
      obj = type;
      type = undefined;
    }
    type = type || $fog$395;

    while ( i-- ) {
      tmp = data_priv[$fog$330]( elements[ i ], type + $fog$402 );
      if ( tmp && tmp[$fog$292] ) {
        count++;
        tmp[$fog$292][$fog$331]( resolve );
      }
    }
    resolve();
    return defer[$fog$355]( obj );
  }
});
var pnum = ($fog$404)[$fog$403];

var cssExpand = [ $fog$405, $fog$406, $fog$407, $fog$408 ];

var isHidden = function( elem, el ) {
    // isHidden might be called from jQuery#filter function;
    // in that case, element will be second argument
    elem = el || elem;
    return jQuery[$fog$409]( elem, $fog$410 ) === $fog$411 || !jQuery[$fog$233]( elem[$fog$143], elem );
  };

var rcheckableType = ($fog$412);



(function() {
  var fragment = document[$fog$413](),
    div = fragment[$fog$60]( document[$fog$55]( $fog$166 ) ),
    input = document[$fog$55]( $fog$171 );

  // Support: Safari<=5.1
  // Check state lost if the name is set (#11217)
  // Support: Windows Web Apps (WWA)
  // `name` and `type` must use .setAttribute for WWA (#14901)
  input[$fog$156]( $fog$47, $fog$414 );
  input[$fog$156]( $fog$288, $fog$288 );
  input[$fog$156]( $fog$215, $fog$415 );

  div[$fog$60]( input );

  // Support: Safari<=5.1, Android<4.2
  // Older WebKit doesn't clone checked state correctly in fragments
  support[$fog$416] = div[$fog$417]( $fog$3 )[$fog$417]( $fog$3 )[$fog$273][$fog$288];

  // Support: IE<=11+
  // Make sure textarea (and checkbox) defaultValue is properly cloned
  div[$fog$196] = $fog$418;
  support[$fog$419] = !!div[$fog$417]( $fog$3 )[$fog$273][$fog$309];
})();
var strundefined = typeof undefined;



support[$fog$420] = $fog$421 in window;


var
  rkeyEvent = $fog$422,
  rmouseEvent = $fog$423,
  rfocusMorph = $fog$424,
  rtypenamespace = $fog$425;

function returnTrue() {
  return $fog$3;
}

function returnFalse() {
  return $fog$38;
}

function safeActiveElement() {
  try {
    return document[$fog$283];
  } catch ( err ) { }
}

/*
 * Helper functions for managing events -- not part of the public interface.
 * Props to Dean Edwards' addEvent library for many of the ideas.
 */
jQuery[$fog$426] = {

  global: {},

  add: function( elem, types, handler, data, selector ) {

    var handleObjIn, eventHandle, tmp,
      events, t, handleObj,
      special, handlers, type, namespaces, origType,
      elemData = data_priv[$fog$330]( elem );

    // Don't attach events to noData or text/comment nodes (but allow plain objects)
    if ( !elemData ) {
      return;
    }

    // Caller can pass in an object of custom data in lieu of the handler
    if ( handler[$fog$427] ) {
      handleObjIn = handler;
      handler = handleObjIn[$fog$427];
      selector = handleObjIn[$fog$302];
    }

    // Make sure that the handler has a unique ID, used to find/remove it later
    if ( !handler[$fog$67] ) {
      handler[$fog$67] = jQuery[$fog$67]++;
    }

    // Init the element's event structure and main handler, if this is the first
    if ( !(events = elemData[$fog$428]) ) {
      events = elemData[$fog$428] = {};
    }
    if ( !(eventHandle = elemData[$fog$429]) ) {
      eventHandle = elemData[$fog$429] = function( e ) {
        // Discard the second event of a jQuery.event.trigger() and
        // when an event is called after a page has unloaded
        return typeof jQuery !== strundefined && jQuery[$fog$426][$fog$430] !== e[$fog$47] ?
          jQuery[$fog$426][$fog$431][$fog$32]( elem, arguments ) : undefined;
      };
    }

    // Handle multiple events separated by a space
    types = ( types || $fog$20 )[$fog$314]( rnotwhite ) || [ $fog$20 ];
    t = types[$fog$24];
    while ( t-- ) {
      tmp = rtypenamespace[$fog$146]( types[t] ) || [];
      type = origType = tmp[$fog$34];
      namespaces = ( tmp[$fog$66] || $fog$20 )[$fog$69]( $fog$432 )[$fog$35]();

      // There *must* be a type, no attaching namespace-only handlers
      if ( !type ) {
        continue;
      }

      // If event changes its type, use the special event handlers for the changed type
      special = jQuery[$fog$426][$fog$433][ type ] || {};

      // If selector defined, determine special event api type, otherwise given type
      type = ( selector ? special[$fog$434] : special[$fog$435] ) || type;

      // Update special based on newly reset type
      special = jQuery[$fog$426][$fog$433][ type ] || {};

      // handleObj is passed to all event handlers
      handleObj = jQuery[$fog$37]({
        type: type,
        origType: origType,
        data: data,
        handler: handler,
        guid: handler[$fog$67],
        selector: selector,
        needsContext: selector && jQuery[$fog$310][$fog$314][$fog$304][$fog$153]( selector ),
        namespace: namespaces[$fog$159]($fog$432)
      }, handleObjIn );

      // Init the event handler queue if we're the first
      if ( !(handlers = events[ type ]) ) {
        handlers = events[ type ] = [];
        handlers[$fog$436] = $fog$21;

        // Only use addEventListener if the special events handler returns false
        if ( !special[$fog$437] || special[$fog$437][$fog$22]( elem, data, namespaces, eventHandle ) === $fog$38 ) {
          if ( elem[$fog$180] ) {
            elem[$fog$180]( type, eventHandle, $fog$38 );
          }
        }
      }

      if ( special[$fog$331] ) {
        special[$fog$331][$fog$22]( elem, handleObj );

        if ( !handleObj[$fog$427][$fog$67] ) {
          handleObj[$fog$427][$fog$67] = handler[$fog$67];
        }
      }

      // Add to the element's handler list, delegates in front
      if ( selector ) {
        handlers[$fog$36]( handlers[$fog$436]++, $fog$21, handleObj );
      } else {
        handlers[$fog$8]( handleObj );
      }

      // Keep track of which events have ever been used, for event optimization
      jQuery[$fog$426][$fog$438][ type ] = $fog$3;
    }

  },

  // Detach an event or set of events from an element
  remove: function( elem, types, handler, selector, mappedTypes ) {

    var j, origCount, tmp,
      events, t, handleObj,
      special, handlers, type, namespaces, origType,
      elemData = data_priv[$fog$392]( elem ) && data_priv[$fog$330]( elem );

    if ( !elemData || !(events = elemData[$fog$428]) ) {
      return;
    }

    // Once for each type.namespace in types; type may be omitted
    types = ( types || $fog$20 )[$fog$314]( rnotwhite ) || [ $fog$20 ];
    t = types[$fog$24];
    while ( t-- ) {
      tmp = rtypenamespace[$fog$146]( types[t] ) || [];
      type = origType = tmp[$fog$34];
      namespaces = ( tmp[$fog$66] || $fog$20 )[$fog$69]( $fog$432 )[$fog$35]();

      // Unbind all events (on this namespace, if provided) for the element
      if ( !type ) {
        for ( type in events ) {
          jQuery[$fog$426][$fog$393]( elem, type + types[ t ], handler, selector, $fog$3 );
        }
        continue;
      }

      special = jQuery[$fog$426][$fog$433][ type ] || {};
      type = ( selector ? special[$fog$434] : special[$fog$435] ) || type;
      handlers = events[ type ] || [];
      tmp = tmp[$fog$66] && new RegExp( $fog$439 + namespaces[$fog$159]($fog$440) + $fog$441 );

      // Remove matching events
      origCount = j = handlers[$fog$24];
      while ( j-- ) {
        handleObj = handlers[ j ];

        if ( ( mappedTypes || origType === handleObj[$fog$442] ) &&
          ( !handler || handler[$fog$67] === handleObj[$fog$67] ) &&
          ( !tmp || tmp[$fog$153]( handleObj[$fog$443] ) ) &&
          ( !selector || selector === handleObj[$fog$302] || selector === $fog$444 && handleObj[$fog$302] ) ) {
          handlers[$fog$36]( j, $fog$34 );

          if ( handleObj[$fog$302] ) {
            handlers[$fog$436]--;
          }
          if ( special[$fog$393] ) {
            special[$fog$393][$fog$22]( elem, handleObj );
          }
        }
      }

      // Remove generic event handler if we removed something and no more handlers exist
      // (avoids potential for endless recursion during removal of special event handlers)
      if ( origCount && !handlers[$fog$24] ) {
        if ( !special[$fog$445] || special[$fog$445][$fog$22]( elem, namespaces, elemData[$fog$429] ) === $fog$38 ) {
          jQuery[$fog$446]( elem, type, elemData[$fog$429] );
        }

        delete events[ type ];
      }
    }

    // Remove the expando if it's no longer used
    if ( jQuery[$fog$381]( events ) ) {
      delete elemData[$fog$429];
      data_priv[$fog$393]( elem, $fog$428 );
    }
  },

  trigger: function( event, data, elem, onlyHandlers ) {

    var i, cur, tmp, bubbleType, ontype, handle, special,
      eventPath = [ elem || document ],
      type = hasOwn[$fog$22]( event, $fog$47 ) ? event[$fog$47] : event,
      namespaces = hasOwn[$fog$22]( event, $fog$443 ) ? event[$fog$443][$fog$69]($fog$432) : [];

    cur = tmp = elem = elem || document;

    // Don't do events on text and comment nodes
    if ( elem[$fog$50] === $fog$150 || elem[$fog$50] === $fog$259 ) {
      return;
    }

    // focus/blur morphs to focusin/out; ensure we're not firing them right now
    if ( rfocusMorph[$fog$153]( type + jQuery[$fog$426][$fog$430] ) ) {
      return;
    }

    if ( type[$fog$9]($fog$432) >= $fog$21 ) {
      // Namespaced trigger; create a regexp to match event type in handle()
      namespaces = type[$fog$69]($fog$432);
      type = namespaces[$fog$165]();
      namespaces[$fog$35]();
    }
    ontype = type[$fog$9]($fog$311) < $fog$21 && $fog$447 + type;

    // Caller can pass in a jQuery.Event object, Object, or just an event type string
    event = event[ jQuery[$fog$376] ] ?
      event :
      new jQuery[$fog$448]( type, typeof event === $fog$0 && event );

    // Trigger bitmask: & 1 for native handlers; & 2 for jQuery (always true)
    event[$fog$449] = onlyHandlers ? $fog$66 : $fog$150;
    event[$fog$443] = namespaces[$fog$159]($fog$432);
    event[$fog$450] = event[$fog$443] ?
      new RegExp( $fog$439 + namespaces[$fog$159]($fog$440) + $fog$441 ) :
      $fog$23;

    // Clean up the event in case it is being reused
    event[$fog$451] = undefined;
    if ( !event[$fog$452] ) {
      event[$fog$452] = elem;
    }

    // Clone any incoming data and prepend the event, creating the handler arg list
    data = data == $fog$23 ?
      [ event ] :
      jQuery[$fog$325]( data, [ event ] );

    // Allow special events to draw outside the lines
    special = jQuery[$fog$426][$fog$433][ type ] || {};
    if ( !onlyHandlers && special[$fog$453] && special[$fog$453][$fog$32]( elem, data ) === $fog$38 ) {
      return;
    }

    // Determine event propagation path in advance, per W3C events spec (#9951)
    // Bubble up to document, then to window; watch for a global ownerDocument var (#9724)
    if ( !onlyHandlers && !special[$fog$454] && !jQuery[$fog$51]( elem ) ) {

      bubbleType = special[$fog$434] || type;
      if ( !rfocusMorph[$fog$153]( bubbleType + type ) ) {
        cur = cur[$fog$59];
      }
      for ( ; cur; cur = cur[$fog$59] ) {
        eventPath[$fog$8]( cur );
        tmp = cur;
      }

      // Only add window if we got to document (e.g., not plain obj or detached DOM)
      if ( tmp === (elem[$fog$143] || document) ) {
        eventPath[$fog$8]( tmp[$fog$178] || tmp[$fog$455] || window );
      }
    }

    // Fire handlers on the event path
    i = $fog$21;
    while ( (cur = eventPath[i++]) && !event[$fog$456]() ) {

      event[$fog$47] = i > $fog$34 ?
        bubbleType :
        special[$fog$435] || type;

      // jQuery handler
      handle = ( data_priv[$fog$330]( cur, $fog$428 ) || {} )[ event[$fog$47] ] && data_priv[$fog$330]( cur, $fog$429 );
      if ( handle ) {
        handle[$fog$32]( cur, data );
      }

      // Native handler
      handle = ontype && cur[ ontype ];
      if ( handle && handle[$fog$32] && jQuery[$fog$373]( cur ) ) {
        event[$fog$451] = handle[$fog$32]( cur, data );
        if ( event[$fog$451] === $fog$38 ) {
          event[$fog$457]();
        }
      }
    }
    event[$fog$47] = type;

    // If nobody prevented the default action, do it now
    if ( !onlyHandlers && !event[$fog$458]() ) {

      if ( (!special[$fog$459] || special[$fog$459][$fog$32]( eventPath[$fog$78](), data ) === $fog$38) &&
        jQuery[$fog$373]( elem ) ) {

        // Call a native DOM method on the target with the same name name as the event.
        // Don't do default actions on window, that's where global variables be (#6170)
        if ( ontype && jQuery[$fog$40]( elem[ type ] ) && !jQuery[$fog$51]( elem ) ) {

          // Don't re-trigger an onFOO event when we call its FOO() method
          tmp = elem[ ontype ];

          if ( tmp ) {
            elem[ ontype ] = $fog$23;
          }

          // Prevent re-triggering of the same event, since we already bubbled it above
          jQuery[$fog$426][$fog$430] = type;
          elem[ type ]();
          jQuery[$fog$426][$fog$430] = undefined;

          if ( tmp ) {
            elem[ ontype ] = tmp;
          }
        }
      }
    }

    return event[$fog$451];
  },

  dispatch: function( event ) {

    // Make a writable jQuery.Event from the native event object
    event = jQuery[$fog$426][$fog$460]( event );

    var i, j, ret, matched, handleObj,
      handlerQueue = [],
      args = slice[$fog$22]( arguments ),
      handlers = ( data_priv[$fog$330]( this, $fog$428 ) || {} )[ event[$fog$47] ] || [],
      special = jQuery[$fog$426][$fog$433][ event[$fog$47] ] || {};

    // Use the fix-ed jQuery.Event rather than the (read-only) native event
    args[$fog$21] = event;
    event[$fog$461] = this;

    // Call the preDispatch hook for the mapped type, and let it bail if desired
    if ( special[$fog$462] && special[$fog$462][$fog$22]( this, event ) === $fog$38 ) {
      return;
    }

    // Determine handlers
    handlerQueue = jQuery[$fog$426][$fog$463][$fog$22]( this, event, handlers );

    // Run delegates first; they may want to stop propagation beneath us
    i = $fog$21;
    while ( (matched = handlerQueue[ i++ ]) && !event[$fog$456]() ) {
      event[$fog$464] = matched[$fog$465];

      j = $fog$21;
      while ( (handleObj = matched[$fog$463][ j++ ]) && !event[$fog$466]() ) {

        // Triggered event must either 1) have no namespace, or 2) have namespace(s)
        // a subset or equal to those in the bound event (both can have no namespace).
        if ( !event[$fog$450] || event[$fog$450][$fog$153]( handleObj[$fog$443] ) ) {

          event[$fog$467] = handleObj;
          event[$fog$468] = handleObj[$fog$468];

          ret = ( (jQuery[$fog$426][$fog$433][ handleObj[$fog$442] ] || {})[$fog$429] || handleObj[$fog$427] )
              [$fog$32]( matched[$fog$465], args );

          if ( ret !== undefined ) {
            if ( (event[$fog$451] = ret) === $fog$38 ) {
              event[$fog$457]();
              event[$fog$469]();
            }
          }
        }
      }
    }

    // Call the postDispatch hook for the mapped type
    if ( special[$fog$470] ) {
      special[$fog$470][$fog$22]( this, event );
    }

    return event[$fog$451];
  },

  handlers: function( event, handlers ) {
    var i, matches, sel, handleObj,
      handlerQueue = [],
      delegateCount = handlers[$fog$436],
      cur = event[$fog$452];

    // Find delegate handlers
    // Black-hole SVG <use> instance trees (#13180)
    // Avoid non-left-click bubbling in Firefox (#3861)
    if ( delegateCount && cur[$fog$50] && (!event[$fog$172] || event[$fog$47] !== $fog$471) ) {

      for ( ; cur !== this; cur = cur[$fog$59] || this ) {

        // Don't process clicks on disabled elements (#6911, #8165, #11382, #11764)
        if ( cur[$fog$287] !== $fog$3 || event[$fog$47] !== $fog$471 ) {
          matches = [];
          for ( i = $fog$21; i < delegateCount; i++ ) {
            handleObj = handlers[ i ];

            // Don't conflict with Object.prototype properties (#13203)
            sel = handleObj[$fog$302] + $fog$71;

            if ( matches[ sel ] === undefined ) {
              matches[ sel ] = handleObj[$fog$304] ?
                jQuery( sel, this )[$fog$328]( cur ) >= $fog$21 :
                jQuery[$fog$189]( sel, this, $fog$23, [ cur ] )[$fog$24];
            }
            if ( matches[ sel ] ) {
              matches[$fog$8]( handleObj );
            }
          }
          if ( matches[$fog$24] ) {
            handlerQueue[$fog$8]({ elem: cur, handlers: matches });
          }
        }
      }
    }

    // Add the remaining (directly-bound) handlers
    if ( delegateCount < handlers[$fog$24] ) {
      handlerQueue[$fog$8]({ elem: this, handlers: handlers[$fog$6]( delegateCount ) });
    }

    return handlerQueue;
  },

  // Includes some event props shared by KeyEvent and MouseEvent
  props: $fog$472[$fog$69]($fog$71),

  fixHooks: {},

  keyHooks: {
    props: $fog$473[$fog$69]($fog$71),
    filter: function( event, original ) {

      // Add which for key events
      if ( event[$fog$474] == $fog$23 ) {
        event[$fog$474] = original[$fog$475] != $fog$23 ? original[$fog$475] : original[$fog$476];
      }

      return event;
    }
  },

  mouseHooks: {
    props: $fog$477[$fog$69]($fog$71),
    filter: function( event, original ) {
      var eventDoc, doc, body,
        button = original[$fog$172];

      // Calculate pageX/Y if missing and clientX/Y available
      if ( event[$fog$478] == $fog$23 && original[$fog$479] != $fog$23 ) {
        eventDoc = event[$fog$452][$fog$143] || document;
        doc = eventDoc[$fog$175];
        body = eventDoc[$fog$480];

        event[$fog$478] = original[$fog$479] + ( doc && doc[$fog$481] || body && body[$fog$481] || $fog$21 ) - ( doc && doc[$fog$482] || body && body[$fog$482] || $fog$21 );
        event[$fog$483] = original[$fog$484] + ( doc && doc[$fog$485]  || body && body[$fog$485]  || $fog$21 ) - ( doc && doc[$fog$486]  || body && body[$fog$486]  || $fog$21 );
      }

      // Add which for click: 1 === left; 2 === middle; 3 === right
      // Note: button is not normalized, so don't use it
      if ( !event[$fog$474] && button !== undefined ) {
        event[$fog$474] = ( button & $fog$34 ? $fog$34 : ( button & $fog$66 ? $fog$150 : ( button & $fog$236 ? $fog$66 : $fog$21 ) ) );
      }

      return event;
    }
  },

  fix: function( event ) {
    if ( event[ jQuery[$fog$376] ] ) {
      return event;
    }

    // Create a writable copy of the event object and normalize some properties
    var i, prop, copy,
      type = event[$fog$47],
      originalEvent = event,
      fixHook = this[$fog$487][ type ];

    if ( !fixHook ) {
      this[$fog$487][ type ] = fixHook =
        rmouseEvent[$fog$153]( type ) ? this[$fog$488] :
        rkeyEvent[$fog$153]( type ) ? this[$fog$489] :
        {};
    }
    copy = fixHook[$fog$490] ? this[$fog$490][$fog$7]( fixHook[$fog$490] ) : this[$fog$490];

    event = new jQuery[$fog$448]( originalEvent );

    i = copy[$fog$24];
    while ( i-- ) {
      prop = copy[ i ];
      event[ prop ] = originalEvent[ prop ];
    }

    // Support: Cordova 2.5 (WebKit) (#13255)
    // All events should have a target; Cordova deviceready doesn't
    if ( !event[$fog$452] ) {
      event[$fog$452] = document;
    }

    // Support: Safari 6.0+, Chrome<28
    // Target should not be a text node (#504, #13143)
    if ( event[$fog$452][$fog$50] === $fog$150 ) {
      event[$fog$452] = event[$fog$452][$fog$59];
    }

    return fixHook[$fog$191] ? fixHook[$fog$191]( event, originalEvent ) : event;
  },

  special: {
    load: {
      // Prevent triggered image.load events from bubbling to window.load
      noBubble: $fog$3
    },
    focus: {
      // Fire native event if possible so blur/focus sequence is correct
      trigger: function() {
        if ( this !== safeActiveElement() && this[$fog$491] ) {
          this[$fog$491]();
          return $fog$38;
        }
      },
      delegateType: $fog$492
    },
    blur: {
      trigger: function() {
        if ( this === safeActiveElement() && this[$fog$493] ) {
          this[$fog$493]();
          return $fog$38;
        }
      },
      delegateType: $fog$494
    },
    click: {
      // For checkbox, fire native event so checked state will be right
      trigger: function() {
        if ( this[$fog$47] === $fog$495 && this[$fog$471] && jQuery[$fog$63]( this, $fog$171 ) ) {
          this[$fog$471]();
          return $fog$38;
        }
      },

      // For cross-browser consistency, don't fire native .click() on links
      _default: function( event ) {
        return jQuery[$fog$63]( event[$fog$452], $fog$496 );
      }
    },

    beforeunload: {
      postDispatch: function( event ) {

        // Support: Firefox 20+
        // Firefox doesn't alert if the returnValue field is not set.
        if ( event[$fog$451] !== undefined && event[$fog$497] ) {
          event[$fog$497][$fog$498] = event[$fog$451];
        }
      }
    }
  },

  simulate: function( type, elem, event, bubble ) {
    // Piggyback on a donor event to simulate a different one.
    // Fake originalEvent to avoid donor's stopPropagation, but if the
    // simulated event prevents default then we do the same on the donor.
    var e = jQuery[$fog$37](
      new jQuery[$fog$448](),
      event,
      {
        type: type,
        isSimulated: $fog$3,
        originalEvent: {}
      }
    );
    if ( bubble ) {
      jQuery[$fog$426][$fog$453]( e, $fog$23, elem );
    } else {
      jQuery[$fog$426][$fog$431][$fog$22]( elem, e );
    }
    if ( e[$fog$458]() ) {
      event[$fog$457]();
    }
  }
};

jQuery[$fog$446] = function( elem, type, handle ) {
  if ( elem[$fog$367] ) {
    elem[$fog$367]( type, handle, $fog$38 );
  }
};

jQuery[$fog$448] = function( src, props ) {
  // Allow instantiation without the 'new' keyword
  if ( !(this instanceof jQuery[$fog$448]) ) {
    return new jQuery[$fog$448]( src, props );
  }

  // Event object
  if ( src && src[$fog$47] ) {
    this[$fog$497] = src;
    this[$fog$47] = src[$fog$47];

    // Events bubbling up the document may have been marked as prevented
    // by a handler lower down the tree; reflect the correct value.
    this[$fog$458] = src[$fog$499] ||
        src[$fog$499] === undefined &&
        // Support: Android<4.0
        src[$fog$498] === $fog$38 ?
      returnTrue :
      returnFalse;

  // Event type
  } else {
    this[$fog$47] = src;
  }

  // Put explicitly provided properties onto the event object
  if ( props ) {
    jQuery[$fog$37]( this, props );
  }

  // Create a timestamp if incoming event doesn't have one
  this[$fog$500] = src && src[$fog$500] || jQuery[$fog$68]();

  // Mark it as fixed
  this[ jQuery[$fog$376] ] = $fog$3;
};

// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding
// http://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html
jQuery[$fog$448][$fog$19] = {
  isDefaultPrevented: returnFalse,
  isPropagationStopped: returnFalse,
  isImmediatePropagationStopped: returnFalse,

  preventDefault: function() {
    var e = this[$fog$497];

    this[$fog$458] = returnTrue;

    if ( e && e[$fog$457] ) {
      e[$fog$457]();
    }
  },
  stopPropagation: function() {
    var e = this[$fog$497];

    this[$fog$456] = returnTrue;

    if ( e && e[$fog$469] ) {
      e[$fog$469]();
    }
  },
  stopImmediatePropagation: function() {
    var e = this[$fog$497];

    this[$fog$466] = returnTrue;

    if ( e && e[$fog$501] ) {
      e[$fog$501]();
    }

    this[$fog$469]();
  }
};

// Create mouseenter/leave events using mouseover/out and event-time checks
// Support: Chrome 15+
jQuery[$fog$29]({
  mouseenter: $fog$502,
  mouseleave: $fog$503,
  pointerenter: $fog$504,
  pointerleave: $fog$505
}, function( orig, fix ) {
  jQuery[$fog$426][$fog$433][ orig ] = {
    delegateType: fix,
    bindType: fix,

    handle: function( event ) {
      var ret,
        target = this,
        related = event[$fog$506],
        handleObj = event[$fog$467];

      // For mousenter/leave call the handler if related is outside the target.
      // NB: No relatedTarget if the mouse left/entered the browser window
      if ( !related || (related !== target && !jQuery[$fog$233]( target, related )) ) {
        event[$fog$47] = handleObj[$fog$442];
        ret = handleObj[$fog$427][$fog$32]( this, arguments );
        event[$fog$47] = fix;
      }
      return ret;
    }
  };
});

// Support: Firefox, Chrome, Safari
// Create "bubbling" focus and blur events
if ( !support[$fog$420] ) {
  jQuery[$fog$29]({ focus: $fog$492, blur: $fog$494 }, function( orig, fix ) {

    // Attach a single capturing handler on the document while someone wants focusin/focusout
    var handler = function( event ) {
        jQuery[$fog$426][$fog$507]( fix, event[$fog$452], jQuery[$fog$426][$fog$460]( event ), $fog$3 );
      };

    jQuery[$fog$426][$fog$433][ fix ] = {
      setup: function() {
        var doc = this[$fog$143] || this,
          attaches = data_priv[$fog$372]( doc, fix );

        if ( !attaches ) {
          doc[$fog$180]( orig, handler, $fog$3 );
        }
        data_priv[$fog$372]( doc, fix, ( attaches || $fog$21 ) + $fog$34 );
      },
      teardown: function() {
        var doc = this[$fog$143] || this,
          attaches = data_priv[$fog$372]( doc, fix ) - $fog$34;

        if ( !attaches ) {
          doc[$fog$367]( orig, handler, $fog$3 );
          data_priv[$fog$393]( doc, fix );

        } else {
          data_priv[$fog$372]( doc, fix, attaches );
        }
      }
    };
  });
}

jQuery[$fog$14][$fog$37]({

  on: function( types, selector, data, fn, /*INTERNAL*/ one ) {
    var origFn, type;

    // Types can be a map of types/handlers
    if ( typeof types === $fog$0 ) {
      // ( types-Object, selector, data )
      if ( typeof selector !== $fog$65 ) {
        // ( types-Object, data )
        data = data || selector;
        selector = undefined;
      }
      for ( type in types ) {
        this[$fog$447]( type, selector, data, types[ type ], one );
      }
      return this;
    }

    if ( data == $fog$23 && fn == $fog$23 ) {
      // ( types, fn )
      fn = selector;
      data = selector = undefined;
    } else if ( fn == $fog$23 ) {
      if ( typeof selector === $fog$65 ) {
        // ( types, selector, fn )
        fn = data;
        data = undefined;
      } else {
        // ( types, data, fn )
        fn = data;
        data = selector;
        selector = undefined;
      }
    }
    if ( fn === $fog$38 ) {
      fn = returnFalse;
    } else if ( !fn ) {
      return this;
    }

    if ( one === $fog$34 ) {
      origFn = fn;
      fn = function( event ) {
        // Can use an empty set, since event contains the info
        jQuery()[$fog$366]( event );
        return origFn[$fog$32]( this, arguments );
      };
      // Use same guid so caller can remove using origFn
      fn[$fog$67] = origFn[$fog$67] || ( origFn[$fog$67] = jQuery[$fog$67]++ );
    }
    return this[$fog$29]( function() {
      jQuery[$fog$426][$fog$331]( this, types, fn, data, selector );
    });
  },
  one: function( types, selector, data, fn ) {
    return this[$fog$447]( types, selector, data, fn, $fog$34 );
  },
  off: function( types, selector, fn ) {
    var handleObj, type;
    if ( types && types[$fog$457] && types[$fog$467] ) {
      // ( event )  dispatched jQuery.Event
      handleObj = types[$fog$467];
      jQuery( types[$fog$461] )[$fog$366](
        handleObj[$fog$443] ? handleObj[$fog$442] + $fog$432 + handleObj[$fog$443] : handleObj[$fog$442],
        handleObj[$fog$302],
        handleObj[$fog$427]
      );
      return this;
    }
    if ( typeof types === $fog$0 ) {
      // ( types-object [, selector] )
      for ( type in types ) {
        this[$fog$366]( type, selector, types[ type ] );
      }
      return this;
    }
    if ( selector === $fog$38 || typeof selector === $fog$48 ) {
      // ( types [, fn] )
      fn = selector;
      selector = undefined;
    }
    if ( fn === $fog$38 ) {
      fn = returnFalse;
    }
    return this[$fog$29](function() {
      jQuery[$fog$426][$fog$393]( this, types, fn, selector );
    });
  },

  trigger: function( type, data ) {
    return this[$fog$29](function() {
      jQuery[$fog$426][$fog$453]( type, data, this );
    });
  },
  triggerHandler: function( type, data ) {
    var elem = this[$fog$21];
    if ( elem ) {
      return jQuery[$fog$426][$fog$453]( type, data, elem, $fog$3 );
    }
  }
});


var
  rxhtmlTag = $fog$508,
  rtagName = $fog$509,
  rhtml = $fog$510,
  rnoInnerhtml = $fog$511,
  // checked="checked" or checked
  rchecked = $fog$512,
  rscriptType = $fog$513,
  rscriptTypeMasked = $fog$514,
  rcleanScript = $fog$515,

  // We have to close these tags to support XHTML (#13200)
  wrapMap = {

    // Support: IE9
    option: [ $fog$34, $fog$516, $fog$517 ],

    thead: [ $fog$34, $fog$518, $fog$519 ],
    col: [ $fog$66, $fog$520, $fog$521 ],
    tr: [ $fog$66, $fog$522, $fog$523 ],
    td: [ $fog$150, $fog$524, $fog$525 ],

    _default: [ $fog$21, $fog$20, $fog$20 ]
  };

// Support: IE9
wrapMap[$fog$526] = wrapMap[$fog$289];

wrapMap[$fog$527] = wrapMap[$fog$528] = wrapMap[$fog$529] = wrapMap[$fog$530] = wrapMap[$fog$531];
wrapMap[$fog$532] = wrapMap[$fog$533];

// Support: 1.x compatibility
// Manipulating tables requires a tbody
function manipulationTarget( elem, content ) {
  return jQuery[$fog$63]( elem, $fog$534 ) &&
    jQuery[$fog$63]( content[$fog$50] !== $fog$145 ? content : content[$fog$248], $fog$535 ) ?

    elem[$fog$149]($fog$527)[$fog$21] ||
      elem[$fog$60]( elem[$fog$143][$fog$55]($fog$527) ) :
    elem;
}

// Replace/restore the type attribute of script elements for safe DOM manipulation
function disableScript( elem ) {
  elem[$fog$47] = (elem[$fog$154]($fog$47) !== $fog$23) + $fog$536 + elem[$fog$47];
  return elem;
}
function restoreScript( elem ) {
  var match = rscriptTypeMasked[$fog$146]( elem[$fog$47] );

  if ( match ) {
    elem[$fog$47] = match[ $fog$34 ];
  } else {
    elem[$fog$162]($fog$47);
  }

  return elem;
}

// Mark scripts as having already been evaluated
function setGlobalEval( elems, refElements ) {
  var i = $fog$21,
    l = elems[$fog$24];

  for ( ; i < l; i++ ) {
    data_priv[$fog$383](
      elems[ i ], $fog$537, !refElements || data_priv[$fog$330]( refElements[ i ], $fog$537 )
    );
  }
}

function cloneCopyEvent( src, dest ) {
  var i, l, type, pdataOld, pdataCur, udataOld, udataCur, events;

  if ( dest[$fog$50] !== $fog$34 ) {
    return;
  }

  // 1. Copy private data: events, handlers, etc.
  if ( data_priv[$fog$392]( src ) ) {
    pdataOld = data_priv[$fog$372]( src );
    pdataCur = data_priv[$fog$383]( dest, pdataOld );
    events = pdataOld[$fog$428];

    if ( events ) {
      delete pdataCur[$fog$429];
      pdataCur[$fog$428] = {};

      for ( type in events ) {
        for ( i = $fog$21, l = events[ type ][$fog$24]; i < l; i++ ) {
          jQuery[$fog$426][$fog$331]( dest, type, events[ type ][ i ] );
        }
      }
    }
  }

  // 2. Copy user data
  if ( data_user[$fog$392]( src ) ) {
    udataOld = data_user[$fog$372]( src );
    udataCur = jQuery[$fog$37]( {}, udataOld );

    data_user[$fog$383]( dest, udataCur );
  }
}

function getAll( context, tag ) {
  var ret = context[$fog$149] ? context[$fog$149]( tag || $fog$104 ) :
      context[$fog$161] ? context[$fog$161]( tag || $fog$104 ) :
      [];

  return tag === undefined || tag && jQuery[$fog$63]( context, tag ) ?
    jQuery[$fog$25]( [ context ], ret ) :
    ret;
}

// Fix IE bugs, see support tests
function fixInput( src, dest ) {
  var nodeName = dest[$fog$63][$fog$64]();

  // Fails to persist the checked state of a cloned checkbox or radio button.
  if ( nodeName === $fog$171 && rcheckableType[$fog$153]( src[$fog$47] ) ) {
    dest[$fog$288] = src[$fog$288];

  // Fails to return the selected option to the default selected state when cloning options
  } else if ( nodeName === $fog$171 || nodeName === $fog$538 ) {
    dest[$fog$309] = src[$fog$309];
  }
}

jQuery[$fog$37]({
  clone: function( elem, dataAndEvents, deepDataAndEvents ) {
    var i, l, srcElements, destElements,
      clone = elem[$fog$417]( $fog$3 ),
      inPage = jQuery[$fog$233]( elem[$fog$143], elem );

    // Fix IE cloning issues
    if ( !support[$fog$419] && ( elem[$fog$50] === $fog$34 || elem[$fog$50] === $fog$145 ) &&
        !jQuery[$fog$313]( elem ) ) {

      // We eschew Sizzle here for performance reasons: http://jsperf.com/getall-vs-sizzle/2
      destElements = getAll( clone );
      srcElements = getAll( elem );

      for ( i = $fog$21, l = srcElements[$fog$24]; i < l; i++ ) {
        fixInput( srcElements[ i ], destElements[ i ] );
      }
    }

    // Copy the events from the original to the clone
    if ( dataAndEvents ) {
      if ( deepDataAndEvents ) {
        srcElements = srcElements || getAll( elem );
        destElements = destElements || getAll( clone );

        for ( i = $fog$21, l = srcElements[$fog$24]; i < l; i++ ) {
          cloneCopyEvent( srcElements[ i ], destElements[ i ] );
        }
      } else {
        cloneCopyEvent( elem, clone );
      }
    }

    // Preserve script evaluation history
    destElements = getAll( clone, $fog$56 );
    if ( destElements[$fog$24] > $fog$21 ) {
      setGlobalEval( destElements, !inPage && getAll( elem, $fog$56 ) );
    }

    // Return the cloned set
    return clone;
  },

  buildFragment: function( elems, context, scripts, selection ) {
    var elem, tmp, tag, wrap, contains, j,
      fragment = context[$fog$413](),
      nodes = [],
      i = $fog$21,
      l = elems[$fog$24];

    for ( ; i < l; i++ ) {
      elem = elems[ i ];

      if ( elem || elem === $fog$21 ) {

        // Add nodes directly
        if ( jQuery[$fog$47]( elem ) === $fog$0 ) {
          // Support: QtWebKit, PhantomJS
          // push.apply(_, arraylike) throws on ancient WebKit
          jQuery[$fog$25]( nodes, elem[$fog$50] ? [ elem ] : elem );

        // Convert non-html into a text node
        } else if ( !rhtml[$fog$153]( elem ) ) {
          nodes[$fog$8]( context[$fog$539]( elem ) );

        // Convert html into DOM nodes
        } else {
          tmp = tmp || fragment[$fog$60]( context[$fog$55]($fog$166) );

          // Deserialize a standard representation
          tag = ( rtagName[$fog$146]( elem ) || [ $fog$20, $fog$20 ] )[ $fog$34 ][$fog$64]();
          wrap = wrapMap[ tag ] || wrapMap[$fog$459];
          tmp[$fog$196] = wrap[ $fog$34 ] + elem[$fog$44]( rxhtmlTag, $fog$540 ) + wrap[ $fog$66 ];

          // Descend through wrappers to the right content
          j = wrap[ $fog$21 ];
          while ( j-- ) {
            tmp = tmp[$fog$273];
          }

          // Support: QtWebKit, PhantomJS
          // push.apply(_, arraylike) throws on ancient WebKit
          jQuery[$fog$25]( nodes, tmp[$fog$142] );

          // Remember the top-level container
          tmp = fragment[$fog$248];

          // Ensure the created nodes are orphaned (#12392)
          tmp[$fog$247] = $fog$20;
        }
      }
    }

    // Remove wrapper from fragment
    fragment[$fog$247] = $fog$20;

    i = $fog$21;
    while ( (elem = nodes[ i++ ]) ) {

      // #4087 - If origin and destination elements are the same, and this is
      // that element, do not do anything
      if ( selection && jQuery[$fog$343]( elem, selection ) !== -$fog$34 ) {
        continue;
      }

      contains = jQuery[$fog$233]( elem[$fog$143], elem );

      // Append to fragment
      tmp = getAll( fragment[$fog$60]( elem ), $fog$56 );

      // Preserve script evaluation history
      if ( contains ) {
        setGlobalEval( tmp );
      }

      // Capture executables
      if ( scripts ) {
        j = $fog$21;
        while ( (elem = tmp[ j++ ]) ) {
          if ( rscriptType[$fog$153]( elem[$fog$47] || $fog$20 ) ) {
            scripts[$fog$8]( elem );
          }
        }
      }
    }

    return fragment;
  },

  cleanData: function( elems ) {
    var data, elem, type, key,
      special = jQuery[$fog$426][$fog$433],
      i = $fog$21;

    for ( ; (elem = elems[ i ]) !== undefined; i++ ) {
      if ( jQuery[$fog$373]( elem ) ) {
        key = elem[ data_priv[$fog$376] ];

        if ( key && (data = data_priv[$fog$375][ key ]) ) {
          if ( data[$fog$428] ) {
            for ( type in data[$fog$428] ) {
              if ( special[ type ] ) {
                jQuery[$fog$426][$fog$393]( elem, type );

              // This is a shortcut to avoid jQuery.event.remove's overhead
              } else {
                jQuery[$fog$446]( elem, type, data[$fog$429] );
              }
            }
          }
          if ( data_priv[$fog$375][ key ] ) {
            // Discard any remaining `private` data
            delete data_priv[$fog$375][ key ];
          }
        }
      }
      // Discard any remaining `user` data
      delete data_user[$fog$375][ elem[ data_user[$fog$376] ] ];
    }
  }
});

jQuery[$fog$14][$fog$37]({
  text: function( value ) {
    return access( this, function( value ) {
      return value === undefined ?
        jQuery[$fog$57]( this ) :
        this[$fog$292]()[$fog$29](function() {
          if ( this[$fog$50] === $fog$34 || this[$fog$50] === $fog$145 || this[$fog$50] === $fog$144 ) {
            this[$fog$247] = value;
          }
        });
    }, $fog$23, value, arguments[$fog$24] );
  },

  append: function() {
    return this[$fog$541]( arguments, function( elem ) {
      if ( this[$fog$50] === $fog$34 || this[$fog$50] === $fog$145 || this[$fog$50] === $fog$144 ) {
        var target = manipulationTarget( this, elem );
        target[$fog$60]( elem );
      }
    });
  },

  prepend: function() {
    return this[$fog$541]( arguments, function( elem ) {
      if ( this[$fog$50] === $fog$34 || this[$fog$50] === $fog$145 || this[$fog$50] === $fog$144 ) {
        var target = manipulationTarget( this, elem );
        target[$fog$542]( elem, target[$fog$248] );
      }
    });
  },

  before: function() {
    return this[$fog$541]( arguments, function( elem ) {
      if ( this[$fog$59] ) {
        this[$fog$59][$fog$542]( elem, this );
      }
    });
  },

  after: function() {
    return this[$fog$541]( arguments, function( elem ) {
      if ( this[$fog$59] ) {
        this[$fog$59][$fog$542]( elem, this[$fog$170] );
      }
    });
  },

  remove: function( selector, keepData /* Internal Use Only */ ) {
    var elem,
      elems = selector ? jQuery[$fog$191]( selector, this ) : this,
      i = $fog$21;

    for ( ; (elem = elems[i]) != $fog$23; i++ ) {
      if ( !keepData && elem[$fog$50] === $fog$34 ) {
        jQuery[$fog$543]( getAll( elem ) );
      }

      if ( elem[$fog$59] ) {
        if ( keepData && jQuery[$fog$233]( elem[$fog$143], elem ) ) {
          setGlobalEval( getAll( elem, $fog$56 ) );
        }
        elem[$fog$59][$fog$58]( elem );
      }
    }

    return this;
  },

  empty: function() {
    var elem,
      i = $fog$21;

    for ( ; (elem = this[i]) != $fog$23; i++ ) {
      if ( elem[$fog$50] === $fog$34 ) {

        // Prevent memory leaks
        jQuery[$fog$543]( getAll( elem, $fog$38 ) );

        // Remove any remaining nodes
        elem[$fog$247] = $fog$20;
      }
    }

    return this;
  },

  clone: function( dataAndEvents, deepDataAndEvents ) {
    dataAndEvents = dataAndEvents == $fog$23 ? $fog$38 : dataAndEvents;
    deepDataAndEvents = deepDataAndEvents == $fog$23 ? dataAndEvents : deepDataAndEvents;

    return this[$fog$31](function() {
      return jQuery[$fog$544]( this, dataAndEvents, deepDataAndEvents );
    });
  },

  html: function( value ) {
    return access( this, function( value ) {
      var elem = this[ $fog$21 ] || {},
        i = $fog$21,
        l = this[$fog$24];

      if ( value === undefined && elem[$fog$50] === $fog$34 ) {
        return elem[$fog$196];
      }

      // See if we can take a shortcut and just use innerHTML
      if ( typeof value === $fog$65 && !rnoInnerhtml[$fog$153]( value ) &&
        !wrapMap[ ( rtagName[$fog$146]( value ) || [ $fog$20, $fog$20 ] )[ $fog$34 ][$fog$64]() ] ) {

        value = value[$fog$44]( rxhtmlTag, $fog$540 );

        try {
          for ( ; i < l; i++ ) {
            elem = this[ i ] || {};

            // Remove element nodes and prevent memory leaks
            if ( elem[$fog$50] === $fog$34 ) {
              jQuery[$fog$543]( getAll( elem, $fog$38 ) );
              elem[$fog$196] = value;
            }
          }

          elem = $fog$21;

        // If using innerHTML throws an exception, use the fallback method
        } catch( e ) {}
      }

      if ( elem ) {
        this[$fog$292]()[$fog$545]( value );
      }
    }, $fog$23, value, arguments[$fog$24] );
  },

  replaceWith: function() {
    var arg = arguments[ $fog$21 ];

    // Make the changes, replacing each context element with the new content
    this[$fog$541]( arguments, function( elem ) {
      arg = this[$fog$59];

      jQuery[$fog$543]( getAll( this ) );

      if ( arg ) {
        arg[$fog$546]( elem, this );
      }
    });

    // Force removal if there was no new content (e.g., from empty arguments)
    return arg && (arg[$fog$24] || arg[$fog$50]) ? this : this[$fog$393]();
  },

  detach: function( selector ) {
    return this[$fog$393]( selector, $fog$3 );
  },

  domManip: function( args, callback ) {

    // Flatten any nested arrays
    args = concat[$fog$32]( [], args );

    var fragment, first, scripts, hasScripts, node, doc,
      i = $fog$21,
      l = this[$fog$24],
      set = this,
      iNoClone = l - $fog$34,
      value = args[ $fog$21 ],
      isFunction = jQuery[$fog$40]( value );

    // We can't cloneNode fragments that contain checked, in WebKit
    if ( isFunction ||
        ( l > $fog$34 && typeof value === $fog$65 &&
          !support[$fog$416] && rchecked[$fog$153]( value ) ) ) {
      return this[$fog$29](function( index ) {
        var self = set[$fog$33]( index );
        if ( isFunction ) {
          args[ $fog$21 ] = value[$fog$22]( this, index, self[$fog$547]() );
        }
        self[$fog$541]( args, callback );
      });
    }

    if ( l ) {
      fragment = jQuery[$fog$548]( args, this[ $fog$21 ][$fog$143], $fog$38, this );
      first = fragment[$fog$248];

      if ( fragment[$fog$142][$fog$24] === $fog$34 ) {
        fragment = first;
      }

      if ( first ) {
        scripts = jQuery[$fog$31]( getAll( fragment, $fog$56 ), disableScript );
        hasScripts = scripts[$fog$24];

        // Use the original fragment for the last item instead of the first because it can end up
        // being emptied incorrectly in certain situations (#8070).
        for ( ; i < l; i++ ) {
          node = fragment;

          if ( i !== iNoClone ) {
            node = jQuery[$fog$544]( node, $fog$3, $fog$3 );

            // Keep references to cloned scripts for later restoration
            if ( hasScripts ) {
              // Support: QtWebKit
              // jQuery.merge because push.apply(_, arraylike) throws
              jQuery[$fog$25]( scripts, getAll( node, $fog$56 ) );
            }
          }

          callback[$fog$22]( this[ i ], node, i );
        }

        if ( hasScripts ) {
          doc = scripts[ scripts[$fog$24] - $fog$34 ][$fog$143];

          // Reenable scripts
          jQuery[$fog$31]( scripts, restoreScript );

          // Evaluate executable scripts on first document insertion
          for ( i = $fog$21; i < hasScripts; i++ ) {
            node = scripts[ i ];
            if ( rscriptType[$fog$153]( node[$fog$47] || $fog$20 ) &&
              !data_priv[$fog$372]( node, $fog$537 ) && jQuery[$fog$233]( doc, node ) ) {

              if ( node[$fog$549] ) {
                // Optional AJAX dependency, but won't run scripts if not present
                if ( jQuery[$fog$550] ) {
                  jQuery[$fog$550]( node[$fog$549] );
                }
              } else {
                jQuery[$fog$537]( node[$fog$247][$fog$44]( rcleanScript, $fog$20 ) );
              }
            }
          }
        }
      }
    }

    return this;
  }
});

jQuery[$fog$29]({
  appendTo: $fog$545,
  prependTo: $fog$551,
  insertBefore: $fog$552,
  insertAfter: $fog$553,
  replaceAll: $fog$554
}, function( name, original ) {
  jQuery[$fog$14][ name ] = function( selector ) {
    var elems,
      ret = [],
      insert = jQuery( selector ),
      last = insert[$fog$24] - $fog$34,
      i = $fog$21;

    for ( ; i <= last; i++ ) {
      elems = i === last ? this : this[$fog$544]( $fog$3 );
      jQuery( insert[ i ] )[ original ]( elems );

      // Support: QtWebKit
      // .get() because push.apply(_, arraylike) throws
      push[$fog$32]( ret, elems[$fog$330]() );
    }

    return this[$fog$30]( ret );
  };
});


var iframe,
  elemdisplay = {};

/**
 * Retrieve the actual display of a element
 * @param {String} name nodeName of the element
 * @param {Object} doc Document object
 */
// Called only from within defaultDisplay
function actualDisplay( name, doc ) {
  var style,
    elem = jQuery( doc[$fog$55]( name ) )[$fog$555]( doc[$fog$480] ),

    // getDefaultComputedStyle might be reliably used only on attached element
    display = window[$fog$556] && ( style = window[$fog$556]( elem[ $fog$21 ] ) ) ?

      // Use of this method is a temporary fix (more like optimization) until something better comes along,
      // since it was removed from specification and supported only in FF
      style[$fog$410] : jQuery[$fog$409]( elem[ $fog$21 ], $fog$410 );

  // We don't have any data stored on the element,
  // so use "detach" method as fast way to get rid of the element
  elem[$fog$557]();

  return display;
}

/**
 * Try to determine the default display value of an element
 * @param {String} nodeName
 */
function defaultDisplay( nodeName ) {
  var doc = document,
    display = elemdisplay[ nodeName ];

  if ( !display ) {
    display = actualDisplay( nodeName, doc );

    // If the simple way fails, read from inside an iframe
    if ( display === $fog$411 || !display ) {

      // Use the already-created iframe if possible
      iframe = (iframe || jQuery( $fog$558 ))[$fog$555]( doc[$fog$175] );

      // Always write a new HTML skeleton so Webkit and Firefox don't choke on reuse
      doc = iframe[ $fog$21 ][$fog$333];

      // Support: IE
      doc[$fog$559]();
      doc[$fog$560]();

      display = actualDisplay( nodeName, doc );
      iframe[$fog$557]();
    }

    // Store the correct default display
    elemdisplay[ nodeName ] = display;
  }

  return display;
}
var rmargin = ($fog$561);

var rnumnonpx = new RegExp( $fog$112 + pnum + $fog$562, $fog$119 );

var getStyles = function( elem ) {
    // Support: IE<=11+, Firefox<=30+ (#15098, #14150)
    // IE throws on elements created in popups
    // FF meanwhile throws on frame elements through "defaultView.getComputedStyle"
    if ( elem[$fog$143][$fog$178][$fog$563] ) {
      return elem[$fog$143][$fog$178][$fog$564]( elem, $fog$23 );
    }

    return window[$fog$564]( elem, $fog$23 );
  };



function curCSS( elem, name, computed ) {
  var width, minWidth, maxWidth, ret,
    style = elem[$fog$565];

  computed = computed || getStyles( elem );

  // Support: IE9
  // getPropertyValue is only needed for .css('filter') (#12537)
  if ( computed ) {
    ret = computed[$fog$566]( name ) || computed[ name ];
  }

  if ( computed ) {

    if ( ret === $fog$20 && !jQuery[$fog$233]( elem[$fog$143], elem ) ) {
      ret = jQuery[$fog$565]( elem, name );
    }

    // Support: iOS < 6
    // A tribute to the "awesome hack by Dean Edwards"
    // iOS < 6 (at least) returns percentage for a larger set of values, but width seems to be reliably pixels
    // this is against the CSSOM draft spec: http://dev.w3.org/csswg/cssom/#resolved-values
    if ( rnumnonpx[$fog$153]( ret ) && rmargin[$fog$153]( name ) ) {

      // Remember the original values
      width = style[$fog$567];
      minWidth = style[$fog$568];
      maxWidth = style[$fog$569];

      // Put in the new values to get a computed value out
      style[$fog$568] = style[$fog$569] = style[$fog$567] = ret;
      ret = computed[$fog$567];

      // Revert the changed values
      style[$fog$567] = width;
      style[$fog$568] = minWidth;
      style[$fog$569] = maxWidth;
    }
  }

  return ret !== undefined ?
    // Support: IE
    // IE returns zIndex value as an integer.
    ret + $fog$20 :
    ret;
}


function addGetHookIf( conditionFn, hookFn ) {
  // Define the hook, we'll check on the first run if it's really needed.
  return {
    get: function() {
      if ( conditionFn() ) {
        // Hook not needed (or it's not possible to use it due
        // to missing dependency), remove it.
        delete this[$fog$330];
        return;
      }

      // Hook needed; redefine it so that the support test is not executed again.
      return (this[$fog$330] = hookFn)[$fog$32]( this, arguments );
    }
  };
}


(function() {
  var pixelPositionVal, boxSizingReliableVal,
    docElem = document[$fog$175],
    container = document[$fog$55]( $fog$166 ),
    div = document[$fog$55]( $fog$166 );

  if ( !div[$fog$565] ) {
    return;
  }

  // Support: IE9-11+
  // Style of cloned element affects source element cloned (#8908)
  div[$fog$565][$fog$570] = $fog$571;
  div[$fog$417]( $fog$3 )[$fog$565][$fog$570] = $fog$20;
  support[$fog$572] = div[$fog$565][$fog$570] === $fog$571;

  container[$fog$565][$fog$573] = $fog$574 +
    $fog$575;
  container[$fog$60]( div );

  // Executing both pixelPosition & boxSizingReliable tests require only one layout
  // so they're executed at the same time to save the second computation.
  function computePixelPositionAndBoxSizingReliable() {
    div[$fog$565][$fog$573] =
      // Support: Firefox<29, Android 2.3
      // Vendor-prefix box-sizing
      $fog$576 +
      $fog$577 +
      $fog$578;
    div[$fog$196] = $fog$20;
    docElem[$fog$60]( container );

    var divStyle = window[$fog$564]( div, $fog$23 );
    pixelPositionVal = divStyle[$fog$179] !== $fog$579;
    boxSizingReliableVal = divStyle[$fog$567] === $fog$580;

    docElem[$fog$58]( container );
  }

  // Support: node.js jsdom
  // Don't assume that getComputedStyle is a property of the global object
  if ( window[$fog$564] ) {
    jQuery[$fog$37]( support, {
      pixelPosition: function() {

        // This test is executed only once but we still do memoizing
        // since we can use the boxSizingReliable pre-computing.
        // No need to check if the test was already performed, though.
        computePixelPositionAndBoxSizingReliable();
        return pixelPositionVal;
      },
      boxSizingReliable: function() {
        if ( boxSizingReliableVal == $fog$23 ) {
          computePixelPositionAndBoxSizingReliable();
        }
        return boxSizingReliableVal;
      },
      reliableMarginRight: function() {

        // Support: Android 2.3
        // Check if div with explicit width and no margin-right incorrectly
        // gets computed margin-right based on width of container. (#3333)
        // WebKit Bug 13343 - getComputedStyle returns wrong value for margin-right
        // This support function is only executed once so no memoizing is needed.
        var ret,
          marginDiv = div[$fog$60]( document[$fog$55]( $fog$166 ) );

        // Reset CSS: box-sizing; display; margin; border; padding
        marginDiv[$fog$565][$fog$573] = div[$fog$565][$fog$573] =
          // Support: Firefox<29, Android 2.3
          // Vendor-prefix box-sizing
          $fog$581 +
          $fog$582;
        marginDiv[$fog$565][$fog$583] = marginDiv[$fog$565][$fog$567] = $fog$299;
        div[$fog$565][$fog$567] = $fog$584;
        docElem[$fog$60]( container );

        ret = !parseFloat( window[$fog$564]( marginDiv, $fog$23 )[$fog$583] );

        docElem[$fog$58]( container );
        div[$fog$58]( marginDiv );

        return ret;
      }
    });
  }
})();


// A method for quickly swapping in/out CSS properties to get correct calculations.
jQuery[$fog$585] = function( elem, options, callback, args ) {
  var ret, name,
    old = {};

  // Remember the old values, and insert the new ones
  for ( name in options ) {
    old[ name ] = elem[$fog$565][ name ];
    elem[$fog$565][ name ] = options[ name ];
  }

  ret = callback[$fog$32]( elem, args || [] );

  // Revert the old values
  for ( name in options ) {
    elem[$fog$565][ name ] = old[ name ];
  }

  return ret;
};


var
  // Swappable if display is none or starts with table except "table", "table-cell", or "table-caption"
  // See here for display values: https://developer.mozilla.org/en-US/docs/CSS/display
  rdisplayswap = $fog$586,
  rnumsplit = new RegExp( $fog$112 + pnum + $fog$587, $fog$119 ),
  rrelNum = new RegExp( $fog$588 + pnum + $fog$106, $fog$119 ),

  cssShow = { position: $fog$589, visibility: $fog$214, display: $fog$590 },
  cssNormalTransform = {
    letterSpacing: $fog$299,
    fontWeight: $fog$591
  },

  cssPrefixes = [ $fog$592, $fog$593, $fog$594, $fog$595 ];

// Return a css property mapped to a potentially vendor prefixed property
function vendorPropName( style, name ) {

  // Shortcut for names that are not vendor prefixed
  if ( name in style ) {
    return name;
  }

  // Check for vendor prefixed names
  var capName = name[$fog$21][$fog$18]() + name[$fog$6]($fog$34),
    origName = name,
    i = cssPrefixes[$fog$24];

  while ( i-- ) {
    name = cssPrefixes[ i ] + capName;
    if ( name in style ) {
      return name;
    }
  }

  return origName;
}

function setPositiveNumber( elem, value, subtract ) {
  var matches = rnumsplit[$fog$146]( value );
  return matches ?
    // Guard against undefined "subtract", e.g., when used as in cssHooks
    Math[$fog$596]( $fog$21, matches[ $fog$34 ] - ( subtract || $fog$21 ) ) + ( matches[ $fog$66 ] || $fog$597 ) :
    value;
}

function augmentWidthOrHeight( elem, name, extra, isBorderBox, styles ) {
  var i = extra === ( isBorderBox ? $fog$598 : $fog$599 ) ?
    // If we already have the right measurement, avoid augmentation
    $fog$236 :
    // Otherwise initialize for horizontal or vertical properties
    name === $fog$567 ? $fog$34 : $fog$21,

    val = $fog$21;

  for ( ; i < $fog$236; i += $fog$66 ) {
    // Both box models exclude margin, so add it if we want it
    if ( extra === $fog$600 ) {
      val += jQuery[$fog$409]( elem, extra + cssExpand[ i ], $fog$3, styles );
    }

    if ( isBorderBox ) {
      // border-box includes padding, so remove it if we want content
      if ( extra === $fog$599 ) {
        val -= jQuery[$fog$409]( elem, $fog$601 + cssExpand[ i ], $fog$3, styles );
      }

      // At this point, extra isn't border nor margin, so remove border
      if ( extra !== $fog$600 ) {
        val -= jQuery[$fog$409]( elem, $fog$598 + cssExpand[ i ] + $fog$602, $fog$3, styles );
      }
    } else {
      // At this point, extra isn't content, so add padding
      val += jQuery[$fog$409]( elem, $fog$601 + cssExpand[ i ], $fog$3, styles );

      // At this point, extra isn't content nor padding, so add border
      if ( extra !== $fog$601 ) {
        val += jQuery[$fog$409]( elem, $fog$598 + cssExpand[ i ] + $fog$602, $fog$3, styles );
      }
    }
  }

  return val;
}

function getWidthOrHeight( elem, name, extra ) {

  // Start with offset property, which is equivalent to the border-box value
  var valueIsBorderBox = $fog$3,
    val = name === $fog$567 ? elem[$fog$603] : elem[$fog$604],
    styles = getStyles( elem ),
    isBorderBox = jQuery[$fog$409]( elem, $fog$605, $fog$38, styles ) === $fog$606;

  // Some non-html elements return undefined for offsetWidth, so check for null/undefined
  // svg - https://bugzilla.mozilla.org/show_bug.cgi?id=649285
  // MathML - https://bugzilla.mozilla.org/show_bug.cgi?id=491668
  if ( val <= $fog$21 || val == $fog$23 ) {
    // Fall back to computed then uncomputed css if necessary
    val = curCSS( elem, name, styles );
    if ( val < $fog$21 || val == $fog$23 ) {
      val = elem[$fog$565][ name ];
    }

    // Computed unit is not pixels. Stop here and return.
    if ( rnumnonpx[$fog$153](val) ) {
      return val;
    }

    // Check for style in case a browser which returns unreliable values
    // for getComputedStyle silently falls back to the reliable elem.style
    valueIsBorderBox = isBorderBox &&
      ( support[$fog$607]() || val === elem[$fog$565][ name ] );

    // Normalize "", auto, and prepare for extra
    val = parseFloat( val ) || $fog$21;
  }

  // Use the active box-sizing model to add/subtract irrelevant styles
  return ( val +
    augmentWidthOrHeight(
      elem,
      name,
      extra || ( isBorderBox ? $fog$598 : $fog$599 ),
      valueIsBorderBox,
      styles
    )
  ) + $fog$597;
}

function showHide( elements, show ) {
  var display, elem, hidden,
    values = [],
    index = $fog$21,
    length = elements[$fog$24];

  for ( ; index < length; index++ ) {
    elem = elements[ index ];
    if ( !elem[$fog$565] ) {
      continue;
    }

    values[ index ] = data_priv[$fog$330]( elem, $fog$608 );
    display = elem[$fog$565][$fog$410];
    if ( show ) {
      // Reset the inline display of this element to learn if it is
      // being hidden by cascaded rules or not
      if ( !values[ index ] && display === $fog$411 ) {
        elem[$fog$565][$fog$410] = $fog$20;
      }

      // Set elements which have been overridden with display: none
      // in a stylesheet to whatever the default browser style is
      // for such an element
      if ( elem[$fog$565][$fog$410] === $fog$20 && isHidden( elem ) ) {
        values[ index ] = data_priv[$fog$372]( elem, $fog$608, defaultDisplay(elem[$fog$63]) );
      }
    } else {
      hidden = isHidden( elem );

      if ( display !== $fog$411 || !hidden ) {
        data_priv[$fog$383]( elem, $fog$608, hidden ? display : jQuery[$fog$409]( elem, $fog$410 ) );
      }
    }
  }

  // Set the display of most of the elements in a second loop
  // to avoid the constant reflow
  for ( index = $fog$21; index < length; index++ ) {
    elem = elements[ index ];
    if ( !elem[$fog$565] ) {
      continue;
    }
    if ( !show || elem[$fog$565][$fog$410] === $fog$411 || elem[$fog$565][$fog$410] === $fog$20 ) {
      elem[$fog$565][$fog$410] = show ? values[ index ] || $fog$20 : $fog$411;
    }
  }

  return elements;
}

jQuery[$fog$37]({

  // Add in style property hooks for overriding the default
  // behavior of getting and setting a style property
  cssHooks: {
    opacity: {
      get: function( elem, computed ) {
        if ( computed ) {

          // We should always get a number back from opacity
          var ret = curCSS( elem, $fog$609 );
          return ret === $fog$20 ? $fog$610 : ret;
        }
      }
    }
  },

  // Don't automatically add "px" to these possibly-unitless properties
  cssNumber: {
    "columnCount": $fog$3,
    "fillOpacity": $fog$3,
    "flexGrow": $fog$3,
    "flexShrink": $fog$3,
    "fontWeight": $fog$3,
    "lineHeight": $fog$3,
    "opacity": $fog$3,
    "order": $fog$3,
    "orphans": $fog$3,
    "widows": $fog$3,
    "zIndex": $fog$3,
    "zoom": $fog$3
  },

  // Add in properties whose names you wish to fix before
  // setting or getting the value
  cssProps: {
    "float": $fog$611
  },

  // Get and set the style property on a DOM Node
  style: function( elem, name, value, extra ) {

    // Don't set styles on text and comment nodes
    if ( !elem || elem[$fog$50] === $fog$150 || elem[$fog$50] === $fog$259 || !elem[$fog$565] ) {
      return;
    }

    // Make sure that we're working with the right name
    var ret, type, hooks,
      origName = jQuery[$fog$382]( name ),
      style = elem[$fog$565];

    name = jQuery[$fog$612][ origName ] || ( jQuery[$fog$612][ origName ] = vendorPropName( style, origName ) );

    // Gets hook for the prefixed version, then unprefixed version
    hooks = jQuery[$fog$613][ name ] || jQuery[$fog$613][ origName ];

    // Check if we're setting a value
    if ( value !== undefined ) {
      type = typeof value;

      // Convert "+=" or "-=" to relative numbers (#7345)
      if ( type === $fog$65 && (ret = rrelNum[$fog$146]( value )) ) {
        value = ( ret[$fog$34] + $fog$34 ) * ret[$fog$66] + parseFloat( jQuery[$fog$409]( elem, name ) );
        // Fixes bug #9237
        type = $fog$75;
      }

      // Make sure that null and NaN values aren't set (#7116)
      if ( value == $fog$23 || value !== value ) {
        return;
      }

      // If a number, add 'px' to the (except for certain CSS properties)
      if ( type === $fog$75 && !jQuery[$fog$614][ origName ] ) {
        value += $fog$597;
      }

      // Support: IE9-11+
      // background-* props affect original clone's values
      if ( !support[$fog$572] && value === $fog$20 && name[$fog$9]( $fog$615 ) === $fog$21 ) {
        style[ name ] = $fog$616;
      }

      // If a hook was provided, use that value, otherwise just set the specified value
      if ( !hooks || !($fog$383 in hooks) || (value = hooks[$fog$383]( elem, value, extra )) !== undefined ) {
        style[ name ] = value;
      }

    } else {
      // If a hook was provided get the non-computed value from there
      if ( hooks && $fog$330 in hooks && (ret = hooks[$fog$330]( elem, $fog$38, extra )) !== undefined ) {
        return ret;
      }

      // Otherwise just get the value from the style object
      return style[ name ];
    }
  },

  css: function( elem, name, extra, styles ) {
    var val, num, hooks,
      origName = jQuery[$fog$382]( name );

    // Make sure that we're working with the right name
    name = jQuery[$fog$612][ origName ] || ( jQuery[$fog$612][ origName ] = vendorPropName( elem[$fog$565], origName ) );

    // Try prefixed name followed by the unprefixed name
    hooks = jQuery[$fog$613][ name ] || jQuery[$fog$613][ origName ];

    // If a hook was provided get the computed value from there
    if ( hooks && $fog$330 in hooks ) {
      val = hooks[$fog$330]( elem, $fog$3, extra );
    }

    // Otherwise, if a way to get the computed value exists, use that
    if ( val === undefined ) {
      val = curCSS( elem, name, styles );
    }

    // Convert "normal" to computed value
    if ( val === $fog$617 && name in cssNormalTransform ) {
      val = cssNormalTransform[ name ];
    }

    // Make numeric if forced or a qualifier was provided and val looks numeric
    if ( extra === $fog$20 || extra ) {
      num = parseFloat( val );
      return extra === $fog$3 || jQuery[$fog$618]( num ) ? num || $fog$21 : val;
    }
    return val;
  }
});

jQuery[$fog$29]([ $fog$619, $fog$567 ], function( i, name ) {
  jQuery[$fog$613][ name ] = {
    get: function( elem, computed, extra ) {
      if ( computed ) {

        // Certain elements can have dimension info if we invisibly show them
        // but it must have a current display style that would benefit
        return rdisplayswap[$fog$153]( jQuery[$fog$409]( elem, $fog$410 ) ) && elem[$fog$603] === $fog$21 ?
          jQuery[$fog$585]( elem, cssShow, function() {
            return getWidthOrHeight( elem, name, extra );
          }) :
          getWidthOrHeight( elem, name, extra );
      }
    },

    set: function( elem, value, extra ) {
      var styles = extra && getStyles( elem );
      return setPositiveNumber( elem, value, extra ?
        augmentWidthOrHeight(
          elem,
          name,
          extra,
          jQuery[$fog$409]( elem, $fog$605, $fog$38, styles ) === $fog$606,
          styles
        ) : $fog$21
      );
    }
  };
});

// Support: Android 2.3
jQuery[$fog$613][$fog$583] = addGetHookIf( support[$fog$620],
  function( elem, computed ) {
    if ( computed ) {
      return jQuery[$fog$585]( elem, { "display": $fog$621 },
        curCSS, [ elem, $fog$583 ] );
    }
  }
);

// These hooks are used by animate to expand properties
jQuery[$fog$29]({
  margin: $fog$20,
  padding: $fog$20,
  border: $fog$602
}, function( prefix, suffix ) {
  jQuery[$fog$613][ prefix + suffix ] = {
    expand: function( value ) {
      var i = $fog$21,
        expanded = {},

        // Assumes a single number if not a string
        parts = typeof value === $fog$65 ? value[$fog$69]($fog$71) : [ value ];

      for ( ; i < $fog$236; i++ ) {
        expanded[ prefix + cssExpand[ i ] + suffix ] =
          parts[ i ] || parts[ i - $fog$66 ] || parts[ $fog$21 ];
      }

      return expanded;
    }
  };

  if ( !rmargin[$fog$153]( prefix ) ) {
    jQuery[$fog$613][ prefix + suffix ][$fog$383] = setPositiveNumber;
  }
});

jQuery[$fog$14][$fog$37]({
  css: function( name, value ) {
    return access( this, function( elem, name, value ) {
      var styles, len,
        map = {},
        i = $fog$21;

      if ( jQuery[$fog$42]( name ) ) {
        styles = getStyles( elem );
        len = name[$fog$24];

        for ( ; i < len; i++ ) {
          map[ name[ i ] ] = jQuery[$fog$409]( elem, name[ i ], $fog$38, styles );
        }

        return map;
      }

      return value !== undefined ?
        jQuery[$fog$565]( elem, name, value ) :
        jQuery[$fog$409]( elem, name );
    }, name, value, arguments[$fog$24] > $fog$34 );
  },
  show: function() {
    return showHide( this, $fog$3 );
  },
  hide: function() {
    return showHide( this );
  },
  toggle: function( state ) {
    if ( typeof state === $fog$39 ) {
      return state ? this[$fog$622]() : this[$fog$623]();
    }

    return this[$fog$29](function() {
      if ( isHidden( this ) ) {
        jQuery( this )[$fog$622]();
      } else {
        jQuery( this )[$fog$623]();
      }
    });
  }
});


function Tween( elem, options, prop, end, easing ) {
  return new Tween[$fog$19][$fog$13]( elem, options, prop, end, easing );
}
jQuery[$fog$624] = Tween;

Tween[$fog$19] = {
  constructor: Tween,
  init: function( elem, options, prop, end, easing, unit ) {
    this[$fog$465] = elem;
    this[$fog$625] = prop;
    this[$fog$626] = easing || $fog$627;
    this[$fog$628] = options;
    this[$fog$629] = this[$fog$68] = this[$fog$630]();
    this[$fog$631] = end;
    this[$fog$632] = unit || ( jQuery[$fog$614][ prop ] ? $fog$20 : $fog$597 );
  },
  cur: function() {
    var hooks = Tween[$fog$633][ this[$fog$625] ];

    return hooks && hooks[$fog$330] ?
      hooks[$fog$330]( this ) :
      Tween[$fog$633][$fog$459][$fog$330]( this );
  },
  run: function( percent ) {
    var eased,
      hooks = Tween[$fog$633][ this[$fog$625] ];

    if ( this[$fog$628][$fog$634] ) {
      this[$fog$635] = eased = jQuery[$fog$626][ this[$fog$626] ](
        percent, this[$fog$628][$fog$634] * percent, $fog$21, $fog$34, this[$fog$628][$fog$634]
      );
    } else {
      this[$fog$635] = eased = percent;
    }
    this[$fog$68] = ( this[$fog$631] - this[$fog$629] ) * eased + this[$fog$629];

    if ( this[$fog$628][$fog$636] ) {
      this[$fog$628][$fog$636][$fog$22]( this[$fog$465], this[$fog$68], this );
    }

    if ( hooks && hooks[$fog$383] ) {
      hooks[$fog$383]( this );
    } else {
      Tween[$fog$633][$fog$459][$fog$383]( this );
    }
    return this;
  }
};

Tween[$fog$19][$fog$13][$fog$19] = Tween[$fog$19];

Tween[$fog$633] = {
  _default: {
    get: function( tween ) {
      var result;

      if ( tween[$fog$465][ tween[$fog$625] ] != $fog$23 &&
        (!tween[$fog$465][$fog$565] || tween[$fog$465][$fog$565][ tween[$fog$625] ] == $fog$23) ) {
        return tween[$fog$465][ tween[$fog$625] ];
      }

      // Passing an empty string as a 3rd parameter to .css will automatically
      // attempt a parseFloat and fallback to a string if the parse fails.
      // Simple values such as "10px" are parsed to Float;
      // complex values such as "rotate(1rad)" are returned as-is.
      result = jQuery[$fog$409]( tween[$fog$465], tween[$fog$625], $fog$20 );
      // Empty strings, null, undefined and "auto" are converted to 0.
      return !result || result === $fog$637 ? $fog$21 : result;
    },
    set: function( tween ) {
      // Use step hook for back compat.
      // Use cssHook if its there.
      // Use .style if available and use plain properties where available.
      if ( jQuery[$fog$395][$fog$636][ tween[$fog$625] ] ) {
        jQuery[$fog$395][$fog$636][ tween[$fog$625] ]( tween );
      } else if ( tween[$fog$465][$fog$565] && ( tween[$fog$465][$fog$565][ jQuery[$fog$612][ tween[$fog$625] ] ] != $fog$23 || jQuery[$fog$613][ tween[$fog$625] ] ) ) {
        jQuery[$fog$565]( tween[$fog$465], tween[$fog$625], tween[$fog$68] + tween[$fog$632] );
      } else {
        tween[$fog$465][ tween[$fog$625] ] = tween[$fog$68];
      }
    }
  }
};

// Support: IE9
// Panic based approach to setting things on disconnected nodes
Tween[$fog$633][$fog$485] = Tween[$fog$633][$fog$481] = {
  set: function( tween ) {
    if ( tween[$fog$465][$fog$50] && tween[$fog$465][$fog$59] ) {
      tween[$fog$465][ tween[$fog$625] ] = tween[$fog$68];
    }
  }
};

jQuery[$fog$626] = {
  linear: function( p ) {
    return p;
  },
  swing: function( p ) {
    return $fog$638 - Math[$fog$639]( p * Math[$fog$640] ) / $fog$66;
  }
};

jQuery[$fog$395] = Tween[$fog$19][$fog$13];

// Back Compat <1.8 extension point
jQuery[$fog$395][$fog$636] = {};




var
  fxNow, timerId,
  rfxtypes = $fog$641,
  rfxnum = new RegExp( $fog$642 + pnum + $fog$643, $fog$119 ),
  rrun = $fog$644,
  animationPrefilters = [ defaultPrefilter ],
  tweeners = {
    "*": [ function( prop, value ) {
      var tween = this[$fog$645]( prop, value ),
        target = tween[$fog$630](),
        parts = rfxnum[$fog$146]( value ),
        unit = parts && parts[ $fog$150 ] || ( jQuery[$fog$614][ prop ] ? $fog$20 : $fog$597 ),

        // Starting value computation is required for potential unit mismatches
        start = ( jQuery[$fog$614][ prop ] || unit !== $fog$597 && +target ) &&
          rfxnum[$fog$146]( jQuery[$fog$409]( tween[$fog$465], prop ) ),
        scale = $fog$34,
        maxIterations = $fog$646;

      if ( start && start[ $fog$150 ] !== unit ) {
        // Trust units reported by jQuery.css
        unit = unit || start[ $fog$150 ];

        // Make sure we update the tween properties later on
        parts = parts || [];

        // Iteratively approximate from a nonzero starting point
        start = +target || $fog$34;

        do {
          // If previous iteration zeroed out, double until we get *something*.
          // Use string for doubling so we don't accidentally see scale as unchanged below
          scale = scale || $fog$647;

          // Adjust and apply
          start = start / scale;
          jQuery[$fog$565]( tween[$fog$465], prop, start + unit );

        // Update scale, tolerating zero or NaN from tween.cur(),
        // break the loop if scale is unchanged or perfect, or if we've just had enough
        } while ( scale !== (scale = tween[$fog$630]() / target) && scale !== $fog$34 && --maxIterations );
      }

      // Update tween properties
      if ( parts ) {
        start = tween[$fog$629] = +start || +target || $fog$21;
        tween[$fog$632] = unit;
        // If a +=/-= token was provided, we're doing a relative animation
        tween[$fog$631] = parts[ $fog$34 ] ?
          start + ( parts[ $fog$34 ] + $fog$34 ) * parts[ $fog$66 ] :
          +parts[ $fog$66 ];
      }

      return tween;
    } ]
  };

// Animations created synchronously will run synchronously
function createFxNow() {
  setTimeout(function() {
    fxNow = undefined;
  });
  return ( fxNow = jQuery[$fog$68]() );
}

// Generate parameters to create a standard animation
function genFx( type, includeWidth ) {
  var which,
    i = $fog$21,
    attrs = { height: type };

  // If we include width, step value is 1 to do all cssExpand values,
  // otherwise step value is 2 to skip over Left and Right
  includeWidth = includeWidth ? $fog$34 : $fog$21;
  for ( ; i < $fog$236 ; i += $fog$66 - includeWidth ) {
    which = cssExpand[ i ];
    attrs[ $fog$600 + which ] = attrs[ $fog$601 + which ] = type;
  }

  if ( includeWidth ) {
    attrs[$fog$609] = attrs[$fog$567] = type;
  }

  return attrs;
}

function createTween( value, prop, animation ) {
  var tween,
    collection = ( tweeners[ prop ] || [] )[$fog$7]( tweeners[ $fog$104 ] ),
    index = $fog$21,
    length = collection[$fog$24];
  for ( ; index < length; index++ ) {
    if ( (tween = collection[ index ][$fog$22]( animation, prop, value )) ) {

      // We're done with this property
      return tween;
    }
  }
}

function defaultPrefilter( elem, props, opts ) {
  /* jshint validthis: true */
  var prop, value, toggle, tween, hooks, oldfire, display, checkDisplay,
    anim = this,
    orig = {},
    style = elem[$fog$565],
    hidden = elem[$fog$50] && isHidden( elem ),
    dataShow = data_priv[$fog$330]( elem, $fog$648 );

  // Handle queue: false promises
  if ( !opts[$fog$396] ) {
    hooks = jQuery[$fog$397]( elem, $fog$395 );
    if ( hooks[$fog$649] == $fog$23 ) {
      hooks[$fog$649] = $fog$21;
      oldfire = hooks[$fog$292][$fog$401];
      hooks[$fog$292][$fog$401] = function() {
        if ( !hooks[$fog$649] ) {
          oldfire();
        }
      };
    }
    hooks[$fog$649]++;

    anim[$fog$650](function() {
      // Ensure the complete handler is called before this completes
      anim[$fog$650](function() {
        hooks[$fog$649]--;
        if ( !jQuery[$fog$396]( elem, $fog$395 )[$fog$24] ) {
          hooks[$fog$292][$fog$401]();
        }
      });
    });
  }

  // Height/width overflow pass
  if ( elem[$fog$50] === $fog$34 && ( $fog$619 in props || $fog$567 in props ) ) {
    // Make sure that nothing sneaks out
    // Record all 3 overflow attributes because IE9-10 do not
    // change the overflow attribute when overflowX and
    // overflowY are set to the same value
    opts[$fog$651] = [ style[$fog$651], style[$fog$652], style[$fog$653] ];

    // Set display property to inline-block for height/width
    // animations on inline elements that are having width/height animated
    display = jQuery[$fog$409]( elem, $fog$410 );

    // Test default display if display is currently "none"
    checkDisplay = display === $fog$411 ?
      data_priv[$fog$330]( elem, $fog$608 ) || defaultDisplay( elem[$fog$63] ) : display;

    if ( checkDisplay === $fog$654 && jQuery[$fog$409]( elem, $fog$655 ) === $fog$411 ) {
      style[$fog$410] = $fog$621;
    }
  }

  if ( opts[$fog$651] ) {
    style[$fog$651] = $fog$214;
    anim[$fog$650](function() {
      style[$fog$651] = opts[$fog$651][ $fog$21 ];
      style[$fog$652] = opts[$fog$651][ $fog$34 ];
      style[$fog$653] = opts[$fog$651][ $fog$66 ];
    });
  }

  // show/hide pass
  for ( prop in props ) {
    value = props[ prop ];
    if ( rfxtypes[$fog$146]( value ) ) {
      delete props[ prop ];
      toggle = toggle || value === $fog$656;
      if ( value === ( hidden ? $fog$623 : $fog$622 ) ) {

        // If there is dataShow left over from a stopped hide or show and we are going to proceed with show, we should pretend to be hidden
        if ( value === $fog$622 && dataShow && dataShow[ prop ] !== undefined ) {
          hidden = $fog$3;
        } else {
          continue;
        }
      }
      orig[ prop ] = dataShow && dataShow[ prop ] || jQuery[$fog$565]( elem, prop );

    // Any non-fx value stops us from restoring the original display value
    } else {
      display = undefined;
    }
  }

  if ( !jQuery[$fog$381]( orig ) ) {
    if ( dataShow ) {
      if ( $fog$214 in dataShow ) {
        hidden = dataShow[$fog$214];
      }
    } else {
      dataShow = data_priv[$fog$372]( elem, $fog$648, {} );
    }

    // Store state if its toggle - enables .stop().toggle() to "reverse"
    if ( toggle ) {
      dataShow[$fog$214] = !hidden;
    }
    if ( hidden ) {
      jQuery( elem )[$fog$622]();
    } else {
      anim[$fog$346](function() {
        jQuery( elem )[$fog$623]();
      });
    }
    anim[$fog$346](function() {
      var prop;

      data_priv[$fog$393]( elem, $fog$648 );
      for ( prop in orig ) {
        jQuery[$fog$565]( elem, prop, orig[ prop ] );
      }
    });
    for ( prop in orig ) {
      tween = createTween( hidden ? dataShow[ prop ] : $fog$21, prop, anim );

      if ( !( prop in dataShow ) ) {
        dataShow[ prop ] = tween[$fog$629];
        if ( hidden ) {
          tween[$fog$631] = tween[$fog$629];
          tween[$fog$629] = prop === $fog$567 || prop === $fog$619 ? $fog$34 : $fog$21;
        }
      }
    }

  // If this is a noop like .hide().hide(), restore an overwritten display value
  } else if ( (display === $fog$411 ? defaultDisplay( elem[$fog$63] ) : display) === $fog$654 ) {
    style[$fog$410] = display;
  }
}

function propFilter( props, specialEasing ) {
  var index, name, easing, value, hooks;

  // camelCase, specialEasing and expand cssHook pass
  for ( index in props ) {
    name = jQuery[$fog$382]( index );
    easing = specialEasing[ name ];
    value = props[ index ];
    if ( jQuery[$fog$42]( value ) ) {
      easing = value[ $fog$34 ];
      value = props[ index ] = value[ $fog$21 ];
    }

    if ( index !== name ) {
      props[ name ] = value;
      delete props[ index ];
    }

    hooks = jQuery[$fog$613][ name ];
    if ( hooks && $fog$657 in hooks ) {
      value = hooks[$fog$657]( value );
      delete props[ name ];

      // Not quite $.extend, this won't overwrite existing keys.
      // Reusing 'index' because we have the correct "name"
      for ( index in value ) {
        if ( !( index in props ) ) {
          props[ index ] = value[ index ];
          specialEasing[ index ] = easing;
        }
      }
    } else {
      specialEasing[ name ] = easing;
    }
  }
}

function Animation( elem, properties, options ) {
  var result,
    stopped,
    index = $fog$21,
    length = animationPrefilters[$fog$24],
    deferred = jQuery[$fog$356]()[$fog$650]( function() {
      // Don't match elem in the :animated selector
      delete tick[$fog$465];
    }),
    tick = function() {
      if ( stopped ) {
        return $fog$38;
      }
      var currentTime = fxNow || createFxNow(),
        remaining = Math[$fog$596]( $fog$21, animation[$fog$658] + animation[$fog$634] - currentTime ),
        // Support: Android 2.3
        // Archaic crash bug won't allow us to use `1 - ( 0.5 || 0 )` (#12497)
        temp = remaining / animation[$fog$634] || $fog$21,
        percent = $fog$34 - temp,
        index = $fog$21,
        length = animation[$fog$659][$fog$24];

      for ( ; index < length ; index++ ) {
        animation[$fog$659][ index ][$fog$660]( percent );
      }

      deferred[$fog$361]( elem, [ animation, percent, remaining ]);

      if ( percent < $fog$34 && length ) {
        return remaining;
      } else {
        deferred[$fog$362]( elem, [ animation ] );
        return $fog$38;
      }
    },
    animation = deferred[$fog$355]({
      elem: elem,
      props: jQuery[$fog$37]( {}, properties ),
      opts: jQuery[$fog$37]( $fog$3, { specialEasing: {} }, options ),
      originalProperties: properties,
      originalOptions: options,
      startTime: fxNow || createFxNow(),
      duration: options[$fog$634],
      tweens: [],
      createTween: function( prop, end ) {
        var tween = jQuery[$fog$624]( elem, animation[$fog$661], prop, end,
            animation[$fog$661][$fog$662][ prop ] || animation[$fog$661][$fog$626] );
        animation[$fog$659][$fog$8]( tween );
        return tween;
      },
      stop: function( gotoEnd ) {
        var index = $fog$21,
          // If we are going to the end, we want to run all the tweens
          // otherwise we skip this part
          length = gotoEnd ? animation[$fog$659][$fog$24] : $fog$21;
        if ( stopped ) {
          return this;
        }
        stopped = $fog$3;
        for ( ; index < length ; index++ ) {
          animation[$fog$659][ index ][$fog$660]( $fog$34 );
        }

        // Resolve when we played the last frame; otherwise, reject
        if ( gotoEnd ) {
          deferred[$fog$362]( elem, [ animation, gotoEnd ] );
        } else {
          deferred[$fog$663]( elem, [ animation, gotoEnd ] );
        }
        return this;
      }
    }),
    props = animation[$fog$490];

  propFilter( props, animation[$fog$661][$fog$662] );

  for ( ; index < length ; index++ ) {
    result = animationPrefilters[ index ][$fog$22]( animation, elem, props, animation[$fog$661] );
    if ( result ) {
      return result;
    }
  }

  jQuery[$fog$31]( props, createTween, animation );

  if ( jQuery[$fog$40]( animation[$fog$661][$fog$629] ) ) {
    animation[$fog$661][$fog$629][$fog$22]( elem, animation );
  }

  jQuery[$fog$395][$fog$664](
    jQuery[$fog$37]( tick, {
      elem: elem,
      anim: animation,
      queue: animation[$fog$661][$fog$396]
    })
  );

  // attach callbacks from options
  return animation[$fog$353]( animation[$fog$661][$fog$353] )
    [$fog$346]( animation[$fog$661][$fog$346], animation[$fog$661][$fog$371] )
    [$fog$350]( animation[$fog$661][$fog$350] )
    [$fog$650]( animation[$fog$661][$fog$650] );
}

jQuery[$fog$665] = jQuery[$fog$37]( Animation, {

  tweener: function( props, callback ) {
    if ( jQuery[$fog$40]( props ) ) {
      callback = props;
      props = [ $fog$104 ];
    } else {
      props = props[$fog$69]($fog$71);
    }

    var prop,
      index = $fog$21,
      length = props[$fog$24];

    for ( ; index < length ; index++ ) {
      prop = props[ index ];
      tweeners[ prop ] = tweeners[ prop ] || [];
      tweeners[ prop ][$fog$237]( callback );
    }
  },

  prefilter: function( callback, prepend ) {
    if ( prepend ) {
      animationPrefilters[$fog$237]( callback );
    } else {
      animationPrefilters[$fog$8]( callback );
    }
  }
});

jQuery[$fog$666] = function( speed, easing, fn ) {
  var opt = speed && typeof speed === $fog$0 ? jQuery[$fog$37]( {}, speed ) : {
    complete: fn || !fn && easing ||
      jQuery[$fog$40]( speed ) && speed,
    duration: speed,
    easing: fn && easing || easing && !jQuery[$fog$40]( easing ) && easing
  };

  opt[$fog$634] = jQuery[$fog$395][$fog$366] ? $fog$21 : typeof opt[$fog$634] === $fog$75 ? opt[$fog$634] :
    opt[$fog$634] in jQuery[$fog$395][$fog$667] ? jQuery[$fog$395][$fog$667][ opt[$fog$634] ] : jQuery[$fog$395][$fog$667][$fog$459];

  // Normalize opt.queue - true/undefined/null -> "fx"
  if ( opt[$fog$396] == $fog$23 || opt[$fog$396] === $fog$3 ) {
    opt[$fog$396] = $fog$395;
  }

  // Queueing
  opt[$fog$668] = opt[$fog$371];

  opt[$fog$371] = function() {
    if ( jQuery[$fog$40]( opt[$fog$668] ) ) {
      opt[$fog$668][$fog$22]( this );
    }

    if ( opt[$fog$396] ) {
      jQuery[$fog$398]( this, opt[$fog$396] );
    }
  };

  return opt;
};

jQuery[$fog$14][$fog$37]({
  fadeTo: function( speed, to, easing, callback ) {

    // Show any hidden elements after setting opacity to 0
    return this[$fog$191]( isHidden )[$fog$409]( $fog$609, $fog$21 )[$fog$622]()

      // Animate to the value specified
      [$fog$631]()[$fog$669]({ opacity: to }, speed, easing, callback );
  },
  animate: function( prop, speed, easing, callback ) {
    var empty = jQuery[$fog$381]( prop ),
      optall = jQuery[$fog$666]( speed, easing, callback ),
      doAnimation = function() {
        // Operate on a copy of prop so per-property easing won't be lost
        var anim = Animation( this, jQuery[$fog$37]( {}, prop ), optall );

        // Empty animations, or finishing resolves immediately
        if ( empty || data_priv[$fog$330]( this, $fog$670 ) ) {
          anim[$fog$400]( $fog$3 );
        }
      };
      doAnimation[$fog$670] = doAnimation;

    return empty || optall[$fog$396] === $fog$38 ?
      this[$fog$29]( doAnimation ) :
      this[$fog$396]( optall[$fog$396], doAnimation );
  },
  stop: function( type, clearQueue, gotoEnd ) {
    var stopQueue = function( hooks ) {
      var stop = hooks[$fog$400];
      delete hooks[$fog$400];
      stop( gotoEnd );
    };

    if ( typeof type !== $fog$65 ) {
      gotoEnd = clearQueue;
      clearQueue = type;
      type = undefined;
    }
    if ( clearQueue && type !== $fog$38 ) {
      this[$fog$396]( type || $fog$395, [] );
    }

    return this[$fog$29](function() {
      var dequeue = $fog$3,
        index = type != $fog$23 && type + $fog$402,
        timers = jQuery[$fog$671],
        data = data_priv[$fog$330]( this );

      if ( index ) {
        if ( data[ index ] && data[ index ][$fog$400] ) {
          stopQueue( data[ index ] );
        }
      } else {
        for ( index in data ) {
          if ( data[ index ] && data[ index ][$fog$400] && rrun[$fog$153]( index ) ) {
            stopQueue( data[ index ] );
          }
        }
      }

      for ( index = timers[$fog$24]; index--; ) {
        if ( timers[ index ][$fog$465] === this && (type == $fog$23 || timers[ index ][$fog$396] === type) ) {
          timers[ index ][$fog$672][$fog$400]( gotoEnd );
          dequeue = $fog$38;
          timers[$fog$36]( index, $fog$34 );
        }
      }

      // Start the next in the queue if the last step wasn't forced.
      // Timers currently will call their complete callbacks, which
      // will dequeue but only if they were gotoEnd.
      if ( dequeue || !gotoEnd ) {
        jQuery[$fog$398]( this, type );
      }
    });
  },
  finish: function( type ) {
    if ( type !== $fog$38 ) {
      type = type || $fog$395;
    }
    return this[$fog$29](function() {
      var index,
        data = data_priv[$fog$330]( this ),
        queue = data[ type + $fog$396 ],
        hooks = data[ type + $fog$402 ],
        timers = jQuery[$fog$671],
        length = queue ? queue[$fog$24] : $fog$21;

      // Enable finishing flag on private data
      data[$fog$670] = $fog$3;

      // Empty the queue first
      jQuery[$fog$396]( this, type, [] );

      if ( hooks && hooks[$fog$400] ) {
        hooks[$fog$400][$fog$22]( this, $fog$3 );
      }

      // Look for any active animations, and finish them
      for ( index = timers[$fog$24]; index--; ) {
        if ( timers[ index ][$fog$465] === this && timers[ index ][$fog$396] === type ) {
          timers[ index ][$fog$672][$fog$400]( $fog$3 );
          timers[$fog$36]( index, $fog$34 );
        }
      }

      // Look for any animations in the old queue and finish them
      for ( index = $fog$21; index < length; index++ ) {
        if ( queue[ index ] && queue[ index ][$fog$670] ) {
          queue[ index ][$fog$670][$fog$22]( this );
        }
      }

      // Turn off finishing flag
      delete data[$fog$670];
    });
  }
});

jQuery[$fog$29]([ $fog$656, $fog$622, $fog$623 ], function( i, name ) {
  var cssFn = jQuery[$fog$14][ name ];
  jQuery[$fog$14][ name ] = function( speed, easing, callback ) {
    return speed == $fog$23 || typeof speed === $fog$39 ?
      cssFn[$fog$32]( this, arguments ) :
      this[$fog$669]( genFx( name, $fog$3 ), speed, easing, callback );
  };
});

// Generate shortcuts for custom animations
jQuery[$fog$29]({
  slideDown: genFx($fog$622),
  slideUp: genFx($fog$623),
  slideToggle: genFx($fog$656),
  fadeIn: { opacity: $fog$622 },
  fadeOut: { opacity: $fog$623 },
  fadeToggle: { opacity: $fog$656 }
}, function( name, props ) {
  jQuery[$fog$14][ name ] = function( speed, easing, callback ) {
    return this[$fog$669]( props, speed, easing, callback );
  };
});

jQuery[$fog$671] = [];
jQuery[$fog$395][$fog$673] = function() {
  var timer,
    i = $fog$21,
    timers = jQuery[$fog$671];

  fxNow = jQuery[$fog$68]();

  for ( ; i < timers[$fog$24]; i++ ) {
    timer = timers[ i ];
    // Checks the timer has not already been removed
    if ( !timer() && timers[ i ] === timer ) {
      timers[$fog$36]( i--, $fog$34 );
    }
  }

  if ( !timers[$fog$24] ) {
    jQuery[$fog$395][$fog$400]();
  }
  fxNow = undefined;
};

jQuery[$fog$395][$fog$664] = function( timer ) {
  jQuery[$fog$671][$fog$8]( timer );
  if ( timer() ) {
    jQuery[$fog$395][$fog$629]();
  } else {
    jQuery[$fog$671][$fog$78]();
  }
};

jQuery[$fog$395][$fog$674] = $fog$675;

jQuery[$fog$395][$fog$629] = function() {
  if ( !timerId ) {
    timerId = setInterval( jQuery[$fog$395][$fog$673], jQuery[$fog$395][$fog$674] );
  }
};

jQuery[$fog$395][$fog$400] = function() {
  clearInterval( timerId );
  timerId = $fog$23;
};

jQuery[$fog$395][$fog$667] = {
  slow: $fog$676,
  fast: $fog$677,
  // Default speed
  _default: $fog$678
};


// Based off of the plugin by Clint Helfers, with permission.
// http://blindsignals.com/index.php/2009/07/jquery-delay/
jQuery[$fog$14][$fog$679] = function( time, type ) {
  time = jQuery[$fog$395] ? jQuery[$fog$395][$fog$667][ time ] || time : time;
  type = type || $fog$395;

  return this[$fog$396]( type, function( next, hooks ) {
    var timeout = setTimeout( next, time );
    hooks[$fog$400] = function() {
      clearTimeout( timeout );
    };
  });
};


(function() {
  var input = document[$fog$55]( $fog$171 ),
    select = document[$fog$55]( $fog$303 ),
    opt = select[$fog$60]( document[$fog$55]( $fog$289 ) );

  input[$fog$47] = $fog$495;

  // Support: iOS<=5.1, Android<=4.2+
  // Default value for a checkbox should be "on"
  support[$fog$680] = input[$fog$193] !== $fog$20;

  // Support: IE<=11+
  // Must access selectedIndex to make default options select
  support[$fog$681] = opt[$fog$290];

  // Support: Android<=2.3
  // Options inside disabled selects are incorrectly marked as disabled
  select[$fog$287] = $fog$3;
  support[$fog$682] = !opt[$fog$287];

  // Support: IE<=11+
  // An input loses its value after becoming a radio
  input = document[$fog$55]( $fog$171 );
  input[$fog$193] = $fog$415;
  input[$fog$47] = $fog$414;
  support[$fog$683] = input[$fog$193] === $fog$415;
})();


var nodeHook, boolHook,
  attrHandle = jQuery[$fog$310][$fog$168];

jQuery[$fog$14][$fog$37]({
  attr: function( name, value ) {
    return access( this, jQuery[$fog$239], name, value, arguments[$fog$24] > $fog$34 );
  },

  removeAttr: function( name ) {
    return this[$fog$29](function() {
      jQuery[$fog$684]( this, name );
    });
  }
});

jQuery[$fog$37]({
  attr: function( elem, name, value ) {
    var hooks, ret,
      nType = elem[$fog$50];

    // don't get/set attributes on text, comment and attribute nodes
    if ( !elem || nType === $fog$150 || nType === $fog$259 || nType === $fog$66 ) {
      return;
    }

    // Fallback to prop when attributes are not supported
    if ( typeof elem[$fog$154] === strundefined ) {
      return jQuery[$fog$625]( elem, name, value );
    }

    // All attributes are lowercase
    // Grab necessary hook if one is defined
    if ( nType !== $fog$34 || !jQuery[$fog$313]( elem ) ) {
      name = name[$fog$64]();
      hooks = jQuery[$fog$685][ name ] ||
        ( jQuery[$fog$310][$fog$314][$fog$686][$fog$153]( name ) ? boolHook : nodeHook );
    }

    if ( value !== undefined ) {

      if ( value === $fog$23 ) {
        jQuery[$fog$684]( elem, name );

      } else if ( hooks && $fog$383 in hooks && (ret = hooks[$fog$383]( elem, value, name )) !== undefined ) {
        return ret;

      } else {
        elem[$fog$156]( name, value + $fog$20 );
        return value;
      }

    } else if ( hooks && $fog$330 in hooks && (ret = hooks[$fog$330]( elem, name )) !== $fog$23 ) {
      return ret;

    } else {
      ret = jQuery[$fog$189][$fog$239]( elem, name );

      // Non-existent attributes return null, we normalize to undefined
      return ret == $fog$23 ?
        undefined :
        ret;
    }
  },

  removeAttr: function( elem, value ) {
    var name, propName,
      i = $fog$21,
      attrNames = value && value[$fog$314]( rnotwhite );

    if ( attrNames && elem[$fog$50] === $fog$34 ) {
      while ( (name = attrNames[i++]) ) {
        propName = jQuery[$fog$687][ name ] || name;

        // Boolean attributes get special treatment (#10870)
        if ( jQuery[$fog$310][$fog$314][$fog$686][$fog$153]( name ) ) {
          // Set corresponding property to false
          elem[ propName ] = $fog$38;
        }

        elem[$fog$162]( name );
      }
    }
  },

  attrHooks: {
    type: {
      set: function( elem, value ) {
        if ( !support[$fog$683] && value === $fog$414 &&
          jQuery[$fog$63]( elem, $fog$171 ) ) {
          var val = elem[$fog$193];
          elem[$fog$156]( $fog$47, value );
          if ( val ) {
            elem[$fog$193] = val;
          }
          return value;
        }
      }
    }
  }
});

// Hooks for boolean attributes
boolHook = {
  set: function( elem, value, name ) {
    if ( value === $fog$38 ) {
      // Remove boolean attributes when set to false
      jQuery[$fog$684]( elem, name );
    } else {
      elem[$fog$156]( name, name );
    }
    return name;
  }
};
jQuery[$fog$29]( jQuery[$fog$310][$fog$314][$fog$686][$fog$403][$fog$314]( $fog$688 ), function( i, name ) {
  var getter = attrHandle[ name ] || jQuery[$fog$189][$fog$239];

  attrHandle[ name ] = function( elem, name, isXML ) {
    var ret, handle;
    if ( !isXML ) {
      // Avoid an infinite loop by temporarily removing this function from the getter
      handle = attrHandle[ name ];
      attrHandle[ name ] = ret;
      ret = getter( elem, name, isXML ) != $fog$23 ?
        name[$fog$64]() :
        $fog$23;
      attrHandle[ name ] = handle;
    }
    return ret;
  };
});




var rfocusable = $fog$125;

jQuery[$fog$14][$fog$37]({
  prop: function( name, value ) {
    return access( this, jQuery[$fog$625], name, value, arguments[$fog$24] > $fog$34 );
  },

  removeProp: function( name ) {
    return this[$fog$29](function() {
      delete this[ jQuery[$fog$687][ name ] || name ];
    });
  }
});

jQuery[$fog$37]({
  propFix: {
    "for": $fog$689,
    "class": $fog$185
  },

  prop: function( elem, name, value ) {
    var ret, hooks, notxml,
      nType = elem[$fog$50];

    // Don't get/set properties on text, comment and attribute nodes
    if ( !elem || nType === $fog$150 || nType === $fog$259 || nType === $fog$66 ) {
      return;
    }

    notxml = nType !== $fog$34 || !jQuery[$fog$313]( elem );

    if ( notxml ) {
      // Fix name and attach hooks
      name = jQuery[$fog$687][ name ] || name;
      hooks = jQuery[$fog$633][ name ];
    }

    if ( value !== undefined ) {
      return hooks && $fog$383 in hooks && (ret = hooks[$fog$383]( elem, value, name )) !== undefined ?
        ret :
        ( elem[ name ] = value );

    } else {
      return hooks && $fog$330 in hooks && (ret = hooks[$fog$330]( elem, name )) !== $fog$23 ?
        ret :
        elem[ name ];
    }
  },

  propHooks: {
    tabIndex: {
      get: function( elem ) {
        return elem[$fog$690]( $fog$691 ) || rfocusable[$fog$153]( elem[$fog$63] ) || elem[$fog$285] ?
          elem[$fog$286] :
          -$fog$34;
      }
    }
  }
});

if ( !support[$fog$681] ) {
  jQuery[$fog$633][$fog$290] = {
    get: function( elem ) {
      var parent = elem[$fog$59];
      if ( parent && parent[$fog$59] ) {
        parent[$fog$59][$fog$291];
      }
      return $fog$23;
    }
  };
}

jQuery[$fog$29]([
  $fog$286,
  $fog$692,
  $fog$693,
  $fog$694,
  $fog$695,
  $fog$696,
  $fog$697,
  $fog$698,
  $fog$699,
  $fog$700
], function() {
  jQuery[$fog$687][ this[$fog$64]() ] = this;
});




var rclass = $fog$701;

jQuery[$fog$14][$fog$37]({
  addClass: function( value ) {
    var classes, elem, cur, clazz, j, finalValue,
      proceed = typeof value === $fog$65 && value,
      i = $fog$21,
      len = this[$fog$24];

    if ( jQuery[$fog$40]( value ) ) {
      return this[$fog$29](function( j ) {
        jQuery( this )[$fog$702]( value[$fog$22]( this, j, this[$fog$185] ) );
      });
    }

    if ( proceed ) {
      // The disjunction here is for better compressibility (see removeClass)
      classes = ( value || $fog$20 )[$fog$314]( rnotwhite ) || [];

      for ( ; i < len; i++ ) {
        elem = this[ i ];
        cur = elem[$fog$50] === $fog$34 && ( elem[$fog$185] ?
          ( $fog$71 + elem[$fog$185] + $fog$71 )[$fog$44]( rclass, $fog$71 ) :
          $fog$71
        );

        if ( cur ) {
          j = $fog$21;
          while ( (clazz = classes[j++]) ) {
            if ( cur[$fog$9]( $fog$71 + clazz + $fog$71 ) < $fog$21 ) {
              cur += clazz + $fog$71;
            }
          }

          // only assign if different to avoid unneeded rendering.
          finalValue = jQuery[$fog$53]( cur );
          if ( elem[$fog$185] !== finalValue ) {
            elem[$fog$185] = finalValue;
          }
        }
      }
    }

    return this;
  },

  removeClass: function( value ) {
    var classes, elem, cur, clazz, j, finalValue,
      proceed = arguments[$fog$24] === $fog$21 || typeof value === $fog$65 && value,
      i = $fog$21,
      len = this[$fog$24];

    if ( jQuery[$fog$40]( value ) ) {
      return this[$fog$29](function( j ) {
        jQuery( this )[$fog$703]( value[$fog$22]( this, j, this[$fog$185] ) );
      });
    }
    if ( proceed ) {
      classes = ( value || $fog$20 )[$fog$314]( rnotwhite ) || [];

      for ( ; i < len; i++ ) {
        elem = this[ i ];
        // This expression is here for better compressibility (see addClass)
        cur = elem[$fog$50] === $fog$34 && ( elem[$fog$185] ?
          ( $fog$71 + elem[$fog$185] + $fog$71 )[$fog$44]( rclass, $fog$71 ) :
          $fog$20
        );

        if ( cur ) {
          j = $fog$21;
          while ( (clazz = classes[j++]) ) {
            // Remove *all* instances
            while ( cur[$fog$9]( $fog$71 + clazz + $fog$71 ) >= $fog$21 ) {
              cur = cur[$fog$44]( $fog$71 + clazz + $fog$71, $fog$71 );
            }
          }

          // Only assign if different to avoid unneeded rendering.
          finalValue = value ? jQuery[$fog$53]( cur ) : $fog$20;
          if ( elem[$fog$185] !== finalValue ) {
            elem[$fog$185] = finalValue;
          }
        }
      }
    }

    return this;
  },

  toggleClass: function( value, stateVal ) {
    var type = typeof value;

    if ( typeof stateVal === $fog$39 && type === $fog$65 ) {
      return stateVal ? this[$fog$702]( value ) : this[$fog$703]( value );
    }

    if ( jQuery[$fog$40]( value ) ) {
      return this[$fog$29](function( i ) {
        jQuery( this )[$fog$704]( value[$fog$22](this, i, this[$fog$185], stateVal), stateVal );
      });
    }

    return this[$fog$29](function() {
      if ( type === $fog$65 ) {
        // Toggle individual class names
        var className,
          i = $fog$21,
          self = jQuery( this ),
          classNames = value[$fog$314]( rnotwhite ) || [];

        while ( (className = classNames[ i++ ]) ) {
          // Check each className given, space separated list
          if ( self[$fog$705]( className ) ) {
            self[$fog$703]( className );
          } else {
            self[$fog$702]( className );
          }
        }

      // Toggle whole class name
      } else if ( type === strundefined || type === $fog$39 ) {
        if ( this[$fog$185] ) {
          // store className if set
          data_priv[$fog$383]( this, $fog$706, this[$fog$185] );
        }

        // If the element has a class name or if we're passed `false`,
        // then remove the whole classname (if there was one, the above saved it).
        // Otherwise bring back whatever was previously saved (if anything),
        // falling back to the empty string if nothing was stored.
        this[$fog$185] = this[$fog$185] || value === $fog$38 ? $fog$20 : data_priv[$fog$330]( this, $fog$706 ) || $fog$20;
      }
    });
  },

  hasClass: function( selector ) {
    var className = $fog$71 + selector + $fog$71,
      i = $fog$21,
      l = this[$fog$24];
    for ( ; i < l; i++ ) {
      if ( this[i][$fog$50] === $fog$34 && ($fog$71 + this[i][$fog$185] + $fog$71)[$fog$44](rclass, $fog$71)[$fog$9]( className ) >= $fog$21 ) {
        return $fog$3;
      }
    }

    return $fog$38;
  }
});




var rreturn = $fog$707;

jQuery[$fog$14][$fog$37]({
  val: function( value ) {
    var hooks, ret, isFunction,
      elem = this[$fog$21];

    if ( !arguments[$fog$24] ) {
      if ( elem ) {
        hooks = jQuery[$fog$708][ elem[$fog$47] ] || jQuery[$fog$708][ elem[$fog$63][$fog$64]() ];

        if ( hooks && $fog$330 in hooks && (ret = hooks[$fog$330]( elem, $fog$193 )) !== undefined ) {
          return ret;
        }

        ret = elem[$fog$193];

        return typeof ret === $fog$65 ?
          // Handle most common string cases
          ret[$fog$44](rreturn, $fog$20) :
          // Handle cases where value is null/undef or number
          ret == $fog$23 ? $fog$20 : ret;
      }

      return;
    }

    isFunction = jQuery[$fog$40]( value );

    return this[$fog$29](function( i ) {
      var val;

      if ( this[$fog$50] !== $fog$34 ) {
        return;
      }

      if ( isFunction ) {
        val = value[$fog$22]( this, i, jQuery( this )[$fog$709]() );
      } else {
        val = value;
      }

      // Treat null/undefined as ""; convert numbers to string
      if ( val == $fog$23 ) {
        val = $fog$20;

      } else if ( typeof val === $fog$75 ) {
        val += $fog$20;

      } else if ( jQuery[$fog$42]( val ) ) {
        val = jQuery[$fog$31]( val, function( value ) {
          return value == $fog$23 ? $fog$20 : value + $fog$20;
        });
      }

      hooks = jQuery[$fog$708][ this[$fog$47] ] || jQuery[$fog$708][ this[$fog$63][$fog$64]() ];

      // If set returns undefined, fall back to normal setting
      if ( !hooks || !($fog$383 in hooks) || hooks[$fog$383]( this, val, $fog$193 ) === undefined ) {
        this[$fog$193] = val;
      }
    });
  }
});

jQuery[$fog$37]({
  valHooks: {
    option: {
      get: function( elem ) {
        var val = jQuery[$fog$189][$fog$239]( elem, $fog$193 );
        return val != $fog$23 ?
          val :
          // Support: IE10-11+
          // option.text throws exceptions (#14686, #14858)
          jQuery[$fog$53]( jQuery[$fog$57]( elem ) );
      }
    },
    select: {
      get: function( elem ) {
        var value, option,
          options = elem[$fog$628],
          index = elem[$fog$291],
          one = elem[$fog$47] === $fog$710 || index < $fog$21,
          values = one ? $fog$23 : [],
          max = one ? index + $fog$34 : options[$fog$24],
          i = index < $fog$21 ?
            max :
            one ? index : $fog$21;

        // Loop through all the selected options
        for ( ; i < max; i++ ) {
          option = options[ i ];

          // IE6-9 doesn't update selected after form reset (#2551)
          if ( ( option[$fog$290] || i === index ) &&
              // Don't return options that are disabled or in a disabled optgroup
              ( support[$fog$682] ? !option[$fog$287] : option[$fog$154]( $fog$287 ) === $fog$23 ) &&
              ( !option[$fog$59][$fog$287] || !jQuery[$fog$63]( option[$fog$59], $fog$526 ) ) ) {

            // Get the specific value for the option
            value = jQuery( option )[$fog$709]();

            // We don't need an array for one selects
            if ( one ) {
              return value;
            }

            // Multi-Selects return an array
            values[$fog$8]( value );
          }
        }

        return values;
      },

      set: function( elem, value ) {
        var optionSet, option,
          options = elem[$fog$628],
          values = jQuery[$fog$325]( value ),
          i = options[$fog$24];

        while ( i-- ) {
          option = options[ i ];
          if ( (option[$fog$290] = jQuery[$fog$343]( option[$fog$193], values ) >= $fog$21) ) {
            optionSet = $fog$3;
          }
        }

        // Force browsers to behave consistently when non-matching value is set
        if ( !optionSet ) {
          elem[$fog$291] = -$fog$34;
        }
        return values;
      }
    }
  }
});

// Radios and checkboxes getter/setter
jQuery[$fog$29]([ $fog$414, $fog$495 ], function() {
  jQuery[$fog$708][ this ] = {
    set: function( elem, value ) {
      if ( jQuery[$fog$42]( value ) ) {
        return ( elem[$fog$288] = jQuery[$fog$343]( jQuery(elem)[$fog$709](), value ) >= $fog$21 );
      }
    }
  };
  if ( !support[$fog$680] ) {
    jQuery[$fog$708][ this ][$fog$330] = function( elem ) {
      return elem[$fog$154]($fog$193) === $fog$23 ? $fog$447 : elem[$fog$193];
    };
  }
});




// Return jQuery for attributes-only inclusion


jQuery[$fog$29]( ($fog$711 +
  $fog$712 +
  $fog$713)[$fog$69]($fog$71), function( i, name ) {

  // Handle event binding
  jQuery[$fog$14][ name ] = function( data, fn ) {
    return arguments[$fog$24] > $fog$21 ?
      this[$fog$447]( name, $fog$23, data, fn ) :
      this[$fog$453]( name );
  };
});

jQuery[$fog$14][$fog$37]({
  hover: function( fnOver, fnOut ) {
    return this[$fog$715]( fnOver )[$fog$714]( fnOut || fnOver );
  },

  bind: function( types, data, fn ) {
    return this[$fog$447]( types, $fog$23, data, fn );
  },
  unbind: function( types, fn ) {
    return this[$fog$366]( types, $fog$23, fn );
  },

  delegate: function( selector, types, data, fn ) {
    return this[$fog$447]( types, selector, data, fn );
  },
  undelegate: function( selector, types, fn ) {
    // ( namespace ) or ( selector, types [, fn] )
    return arguments[$fog$24] === $fog$34 ? this[$fog$366]( selector, $fog$444 ) : this[$fog$366]( types, selector || $fog$444, fn );
  }
});


var nonce = jQuery[$fog$68]();

var rquery = ($fog$716);



// Support: Android 2.3
// Workaround failure to string-cast null input
jQuery[$fog$391] = function( data ) {
  return JSON[$fog$717]( data + $fog$20 );
};


// Cross-browser xml parsing
jQuery[$fog$718] = function( data ) {
  var xml, tmp;
  if ( !data || typeof data !== $fog$65 ) {
    return $fog$23;
  }

  // Support: IE9
  try {
    tmp = new DOMParser();
    xml = tmp[$fog$719]( data, $fog$720 );
  } catch ( e ) {
    xml = undefined;
  }

  if ( !xml || xml[$fog$149]( $fog$721 )[$fog$24] ) {
    jQuery[$fog$241]( $fog$722 + data );
  }
  return xml;
};


var
  rhash = $fog$723,
  rts = $fog$724,
  rheaders = $fog$725,
  // #7653, #8125, #8152: local protocol detection
  rlocalProtocol = $fog$726,
  rnoContent = $fog$727,
  rprotocol = $fog$728,
  rurl = $fog$729,

  /* Prefilters
   * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)
   * 2) These are called:
   *    - BEFORE asking for a transport
   *    - AFTER param serialization (s.data is a string if s.processData is true)
   * 3) key is the dataType
   * 4) the catchall symbol "*" can be used
   * 5) execution will start with transport dataType and THEN continue down to "*" if needed
   */
  prefilters = {},

  /* Transports bindings
   * 1) key is the dataType
   * 2) the catchall symbol "*" can be used
   * 3) selection will start with transport dataType and THEN go to "*" if needed
   */
  transports = {},

  // Avoid comment-prolog char sequence (#10098); must appease lint and evade compression
  allTypes = $fog$730[$fog$7]( $fog$104 ),

  // Document location
  ajaxLocation = window[$fog$281][$fog$285],

  // Segment location into parts
  ajaxLocParts = rurl[$fog$146]( ajaxLocation[$fog$64]() ) || [];

// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport
function addToPrefiltersOrTransports( structure ) {

  // dataTypeExpression is optional and defaults to "*"
  return function( dataTypeExpression, func ) {

    if ( typeof dataTypeExpression !== $fog$65 ) {
      func = dataTypeExpression;
      dataTypeExpression = $fog$104;
    }

    var dataType,
      i = $fog$21,
      dataTypes = dataTypeExpression[$fog$64]()[$fog$314]( rnotwhite ) || [];

    if ( jQuery[$fog$40]( func ) ) {
      // For each dataType in the dataTypeExpression
      while ( (dataType = dataTypes[i++]) ) {
        // Prepend if requested
        if ( dataType[$fog$21] === $fog$98 ) {
          dataType = dataType[$fog$6]( $fog$34 ) || $fog$104;
          (structure[ dataType ] = structure[ dataType ] || [])[$fog$237]( func );

        // Otherwise append
        } else {
          (structure[ dataType ] = structure[ dataType ] || [])[$fog$8]( func );
        }
      }
    }
  };
}

// Base inspection function for prefilters and transports
function inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR ) {

  var inspected = {},
    seekingTransport = ( structure === transports );

  function inspect( dataType ) {
    var selected;
    inspected[ dataType ] = $fog$3;
    jQuery[$fog$29]( structure[ dataType ] || [], function( _, prefilterOrFactory ) {
      var dataTypeOrTransport = prefilterOrFactory( options, originalOptions, jqXHR );
      if ( typeof dataTypeOrTransport === $fog$65 && !seekingTransport && !inspected[ dataTypeOrTransport ] ) {
        options[$fog$731][$fog$237]( dataTypeOrTransport );
        inspect( dataTypeOrTransport );
        return $fog$38;
      } else if ( seekingTransport ) {
        return !( selected = dataTypeOrTransport );
      }
    });
    return selected;
  }

  return inspect( options[$fog$731][ $fog$21 ] ) || !inspected[ $fog$104 ] && inspect( $fog$104 );
}

// A special extend for ajax options
// that takes "flat" options (not to be deep extended)
// Fixes #9887
function ajaxExtend( target, src ) {
  var key, deep,
    flatOptions = jQuery[$fog$733][$fog$732] || {};

  for ( key in src ) {
    if ( src[ key ] !== undefined ) {
      ( flatOptions[ key ] ? target : ( deep || (deep = {}) ) )[ key ] = src[ key ];
    }
  }
  if ( deep ) {
    jQuery[$fog$37]( $fog$3, target, deep );
  }

  return target;
}

/* Handles responses to an ajax request:
 * - finds the right dataType (mediates between content-type and expected dataType)
 * - returns the corresponding response
 */
function ajaxHandleResponses( s, jqXHR, responses ) {

  var ct, type, finalDataType, firstDataType,
    contents = s[$fog$734],
    dataTypes = s[$fog$731];

  // Remove auto dataType and get content-type in the process
  while ( dataTypes[ $fog$21 ] === $fog$104 ) {
    dataTypes[$fog$165]();
    if ( ct === undefined ) {
      ct = s[$fog$735] || jqXHR[$fog$736]($fog$737);
    }
  }

  // Check if we're dealing with a known content-type
  if ( ct ) {
    for ( type in contents ) {
      if ( contents[ type ] && contents[ type ][$fog$153]( ct ) ) {
        dataTypes[$fog$237]( type );
        break;
      }
    }
  }

  // Check to see if we have a response for the expected dataType
  if ( dataTypes[ $fog$21 ] in responses ) {
    finalDataType = dataTypes[ $fog$21 ];
  } else {
    // Try convertible dataTypes
    for ( type in responses ) {
      if ( !dataTypes[ $fog$21 ] || s[$fog$738][ type + $fog$71 + dataTypes[$fog$21] ] ) {
        finalDataType = type;
        break;
      }
      if ( !firstDataType ) {
        firstDataType = type;
      }
    }
    // Or just use first one
    finalDataType = finalDataType || firstDataType;
  }

  // If we found a dataType
  // We add the dataType to the list if needed
  // and return the corresponding response
  if ( finalDataType ) {
    if ( finalDataType !== dataTypes[ $fog$21 ] ) {
      dataTypes[$fog$237]( finalDataType );
    }
    return responses[ finalDataType ];
  }
}

/* Chain conversions given the request and the original response
 * Also sets the responseXXX fields on the jqXHR instance
 */
function ajaxConvert( s, response, jqXHR, isSuccess ) {
  var conv2, current, conv, tmp, prev,
    converters = {},
    // Work with a copy of dataTypes in case we need to modify it for conversion
    dataTypes = s[$fog$731][$fog$6]();

  // Create converters map with lowercased keys
  if ( dataTypes[ $fog$34 ] ) {
    for ( conv in s[$fog$738] ) {
      converters[ conv[$fog$64]() ] = s[$fog$738][ conv ];
    }
  }

  current = dataTypes[$fog$165]();

  // Convert to each sequential dataType
  while ( current ) {

    if ( s[$fog$739][ current ] ) {
      jqXHR[ s[$fog$739][ current ] ] = response;
    }

    // Apply the dataFilter if provided
    if ( !prev && isSuccess && s[$fog$740] ) {
      response = s[$fog$740]( response, s[$fog$741] );
    }

    prev = current;
    current = dataTypes[$fog$165]();

    if ( current ) {

    // There's only work to do if current dataType is non-auto
      if ( current === $fog$104 ) {

        current = prev;

      // Convert response if prev dataType is non-auto and differs from current
      } else if ( prev !== $fog$104 && prev !== current ) {

        // Seek a direct converter
        conv = converters[ prev + $fog$71 + current ] || converters[ $fog$742 + current ];

        // If none found, seek a pair
        if ( !conv ) {
          for ( conv2 in converters ) {

            // If conv2 outputs current
            tmp = conv2[$fog$69]( $fog$71 );
            if ( tmp[ $fog$34 ] === current ) {

              // If prev can be converted to accepted input
              conv = converters[ prev + $fog$71 + tmp[ $fog$21 ] ] ||
                converters[ $fog$742 + tmp[ $fog$21 ] ];
              if ( conv ) {
                // Condense equivalence converters
                if ( conv === $fog$3 ) {
                  conv = converters[ conv2 ];

                // Otherwise, insert the intermediate dataType
                } else if ( converters[ conv2 ] !== $fog$3 ) {
                  current = tmp[ $fog$21 ];
                  dataTypes[$fog$237]( tmp[ $fog$34 ] );
                }
                break;
              }
            }
          }
        }

        // Apply converter (if not an equivalence)
        if ( conv !== $fog$3 ) {

          // Unless errors are allowed to bubble, catch and return them
          if ( conv && s[ $fog$743 ] ) {
            response = conv( response );
          } else {
            try {
              response = conv( response );
            } catch ( e ) {
              return { state: $fog$721, error: conv ? e : $fog$744 + prev + $fog$745 + current };
            }
          }
        }
      }
    }
  }

  return { state: $fog$746, data: response };
}

jQuery[$fog$37]({

  // Counter for holding the number of active queries
  active: $fog$21,

  // Last-Modified header cache for next request
  lastModified: {},
  etag: {},

  ajaxSettings: {
    url: ajaxLocation,
    type: $fog$747,
    isLocal: rlocalProtocol[$fog$153]( ajaxLocParts[ $fog$34 ] ),
    global: $fog$3,
    processData: $fog$3,
    async: $fog$3,
    contentType: $fog$748,
    /*
    timeout: 0,
    data: null,
    dataType: null,
    username: null,
    password: null,
    cache: null,
    throws: false,
    traditional: false,
    headers: {},
    */

    accepts: {
      "*": allTypes,
      text: $fog$749,
      html: $fog$750,
      xml: $fog$751,
      json: $fog$752
    },

    contents: {
      xml: $fog$753,
      html: $fog$754,
      json: $fog$755
    },

    responseFields: {
      xml: $fog$756,
      text: $fog$757,
      json: $fog$758
    },

    // Data converters
    // Keys separate source (or catchall "*") and destination types with a single space
    converters: {

      // Convert anything to text
      "* text": String,

      // Text to html (true = no transformation)
      "text html": $fog$3,

      // Evaluate text as a json expression
      "text json": jQuery[$fog$391],

      // Parse text as xml
      "text xml": jQuery[$fog$718]
    },

    // For options that shouldn't be deep extended:
    // you can add your own custom options here if
    // and when you create one that shouldn't be
    // deep extended (see ajaxExtend)
    flatOptions: {
      url: $fog$3,
      context: $fog$3
    }
  },

  // Creates a full fledged settings object into target
  // with both ajaxSettings and settings fields.
  // If target is omitted, writes into ajaxSettings.
  ajaxSetup: function( target, settings ) {
    return settings ?

      // Building a settings object
      ajaxExtend( ajaxExtend( target, jQuery[$fog$733] ), settings ) :

      // Extending ajaxSettings
      ajaxExtend( jQuery[$fog$733], target );
  },

  ajaxPrefilter: addToPrefiltersOrTransports( prefilters ),
  ajaxTransport: addToPrefiltersOrTransports( transports ),

  // Main method
  ajax: function( url, options ) {

    // If url is an object, simulate pre-1.5 signature
    if ( typeof url === $fog$0 ) {
      options = url;
      url = undefined;
    }

    // Force options to be an object
    options = options || {};

    var transport,
      // URL without anti-cache param
      cacheURL,
      // Response headers
      responseHeadersString,
      responseHeaders,
      // timeout handle
      timeoutTimer,
      // Cross-domain detection vars
      parts,
      // To know if global events are to be dispatched
      fireGlobals,
      // Loop variable
      i,
      // Create the final options object
      s = jQuery[$fog$759]( {}, options ),
      // Callbacks context
      callbackContext = s[$fog$28] || s,
      // Context for global events is callbackContext if it is a DOM node or jQuery collection
      globalEventContext = s[$fog$28] && ( callbackContext[$fog$50] || callbackContext[$fog$323] ) ?
        jQuery( callbackContext ) :
        jQuery[$fog$426],
      // Deferreds
      deferred = jQuery[$fog$356](),
      completeDeferred = jQuery[$fog$337]($fog$347),
      // Status-dependent callbacks
      statusCode = s[$fog$760] || {},
      // Headers (they are sent all at once)
      requestHeaders = {},
      requestHeadersNames = {},
      // The jqXHR state
      state = $fog$21,
      // Default abort message
      strAbort = $fog$761,
      // Fake xhr
      jqXHR = {
        readyState: $fog$21,

        // Builds headers hashtable if needed
        getResponseHeader: function( key ) {
          var match;
          if ( state === $fog$66 ) {
            if ( !responseHeaders ) {
              responseHeaders = {};
              while ( (match = rheaders[$fog$146]( responseHeadersString )) ) {
                responseHeaders[ match[$fog$34][$fog$64]() ] = match[ $fog$66 ];
              }
            }
            match = responseHeaders[ key[$fog$64]() ];
          }
          return match == $fog$23 ? $fog$23 : match;
        },

        // Raw string
        getAllResponseHeaders: function() {
          return state === $fog$66 ? responseHeadersString : $fog$23;
        },

        // Caches the header
        setRequestHeader: function( name, value ) {
          var lname = name[$fog$64]();
          if ( !state ) {
            name = requestHeadersNames[ lname ] = requestHeadersNames[ lname ] || name;
            requestHeaders[ name ] = value;
          }
          return this;
        },

        // Overrides response content-type header
        overrideMimeType: function( type ) {
          if ( !state ) {
            s[$fog$735] = type;
          }
          return this;
        },

        // Status-dependent callbacks
        statusCode: function( map ) {
          var code;
          if ( map ) {
            if ( state < $fog$66 ) {
              for ( code in map ) {
                // Lazy-add the new callback in a way that preserves old ones
                statusCode[ code ] = [ statusCode[ code ], map[ code ] ];
              }
            } else {
              // Execute the appropriate callbacks
              jqXHR[$fog$650]( map[ jqXHR[$fog$762] ] );
            }
          }
          return this;
        },

        // Cancel the request
        abort: function( statusText ) {
          var finalText = statusText || strAbort;
          if ( transport ) {
            transport[$fog$763]( finalText );
          }
          done( $fog$21, finalText );
          return this;
        }
      };

    // Attach deferreds
    deferred[$fog$355]( jqXHR )[$fog$371] = completeDeferred[$fog$331];
    jqXHR[$fog$746] = jqXHR[$fog$346];
    jqXHR[$fog$241] = jqXHR[$fog$350];

    // Remove hash character (#7531: and string promotion)
    // Add protocol if not provided (prefilters might expect it)
    // Handle falsy url in the settings object (#10093: consistency with old signature)
    // We also use the url parameter if available
    s[$fog$764] = ( ( url || s[$fog$764] || ajaxLocation ) + $fog$20 )[$fog$44]( rhash, $fog$20 )
      [$fog$44]( rprotocol, ajaxLocParts[ $fog$34 ] + $fog$765 );

    // Alias method option to type as per ticket #12004
    s[$fog$47] = options[$fog$766] || options[$fog$47] || s[$fog$766] || s[$fog$47];

    // Extract dataTypes list
    s[$fog$731] = jQuery[$fog$53]( s[$fog$741] || $fog$104 )[$fog$64]()[$fog$314]( rnotwhite ) || [ $fog$20 ];

    // A cross-domain request is in order when we have a protocol:host:port mismatch
    if ( s[$fog$767] == $fog$23 ) {
      parts = rurl[$fog$146]( s[$fog$764][$fog$64]() );
      s[$fog$767] = !!( parts &&
        ( parts[ $fog$34 ] !== ajaxLocParts[ $fog$34 ] || parts[ $fog$66 ] !== ajaxLocParts[ $fog$66 ] ||
          ( parts[ $fog$150 ] || ( parts[ $fog$34 ] === $fog$768 ? $fog$769 : $fog$770 ) ) !==
            ( ajaxLocParts[ $fog$150 ] || ( ajaxLocParts[ $fog$34 ] === $fog$768 ? $fog$769 : $fog$770 ) ) )
      );
    }

    // Convert data if not already a string
    if ( s[$fog$468] && s[$fog$771] && typeof s[$fog$468] !== $fog$65 ) {
      s[$fog$468] = jQuery[$fog$772]( s[$fog$468], s[$fog$773] );
    }

    // Apply prefilters
    inspectPrefiltersOrTransports( prefilters, s, options, jqXHR );

    // If request was aborted inside a prefilter, stop there
    if ( state === $fog$66 ) {
      return jqXHR;
    }

    // We can fire global events as of now if asked to
    // Don't fire events if jQuery.event is undefined in an AMD-usage scenario (#15118)
    fireGlobals = jQuery[$fog$426] && s[$fog$438];

    // Watch for a new set of requests
    if ( fireGlobals && jQuery[$fog$774]++ === $fog$21 ) {
      jQuery[$fog$426][$fog$453]($fog$775);
    }

    // Uppercase the type
    s[$fog$47] = s[$fog$47][$fog$18]();

    // Determine if request has content
    s[$fog$776] = !rnoContent[$fog$153]( s[$fog$47] );

    // Save the URL in case we're toying with the If-Modified-Since
    // and/or If-None-Match header later on
    cacheURL = s[$fog$764];

    // More options handling for requests with no content
    if ( !s[$fog$776] ) {

      // If data is available, append data to url
      if ( s[$fog$468] ) {
        cacheURL = ( s[$fog$764] += ( rquery[$fog$153]( cacheURL ) ? $fog$777 : $fog$778 ) + s[$fog$468] );
        // #9682: remove data so that it's not used in an eventual retry
        delete s[$fog$468];
      }

      // Add anti-cache in url if needed
      if ( s[$fog$375] === $fog$38 ) {
        s[$fog$764] = rts[$fog$153]( cacheURL ) ?

          // If there is already a '_' parameter, set its value
          cacheURL[$fog$44]( rts, $fog$779 + nonce++ ) :

          // Otherwise add one to the end
          cacheURL + ( rquery[$fog$153]( cacheURL ) ? $fog$777 : $fog$778 ) + $fog$780 + nonce++;
      }
    }

    // Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
    if ( s[$fog$781] ) {
      if ( jQuery[$fog$782][ cacheURL ] ) {
        jqXHR[$fog$783]( $fog$784, jQuery[$fog$782][ cacheURL ] );
      }
      if ( jQuery[$fog$785][ cacheURL ] ) {
        jqXHR[$fog$783]( $fog$786, jQuery[$fog$785][ cacheURL ] );
      }
    }

    // Set the correct header, if data is being sent
    if ( s[$fog$468] && s[$fog$776] && s[$fog$787] !== $fog$38 || options[$fog$787] ) {
      jqXHR[$fog$783]( $fog$737, s[$fog$787] );
    }

    // Set the Accepts header for the server, depending on the dataType
    jqXHR[$fog$783](
      $fog$788,
      s[$fog$731][ $fog$21 ] && s[$fog$378][ s[$fog$731][$fog$21] ] ?
        s[$fog$378][ s[$fog$731][$fog$21] ] + ( s[$fog$731][ $fog$21 ] !== $fog$104 ? $fog$789 + allTypes + $fog$790 : $fog$20 ) :
        s[$fog$378][ $fog$104 ]
    );

    // Check for headers option
    for ( i in s[$fog$791] ) {
      jqXHR[$fog$783]( i, s[$fog$791][ i ] );
    }

    // Allow custom headers/mimetypes and early abort
    if ( s[$fog$792] && ( s[$fog$792][$fog$22]( callbackContext, jqXHR, s ) === $fog$38 || state === $fog$66 ) ) {
      // Abort if not done already and return
      return jqXHR[$fog$763]();
    }

    // Aborting is no longer a cancellation
    strAbort = $fog$763;

    // Install callbacks on deferreds
    for ( i in { success: $fog$34, error: $fog$34, complete: $fog$34 } ) {
      jqXHR[ i ]( s[ i ] );
    }

    // Get transport
    transport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );

    // If no transport, we auto-abort
    if ( !transport ) {
      done( -$fog$34, $fog$793 );
    } else {
      jqXHR[$fog$370] = $fog$34;

      // Send global event
      if ( fireGlobals ) {
        globalEventContext[$fog$453]( $fog$794, [ jqXHR, s ] );
      }
      // Timeout
      if ( s[$fog$795] && s[$fog$796] > $fog$21 ) {
        timeoutTimer = setTimeout(function() {
          jqXHR[$fog$763]($fog$796);
        }, s[$fog$796] );
      }

      try {
        state = $fog$34;
        transport[$fog$797]( requestHeaders, done );
      } catch ( e ) {
        // Propagate exception as error if not done
        if ( state < $fog$66 ) {
          done( -$fog$34, e );
        // Simply rethrow otherwise
        } else {
          throw e;
        }
      }
    }

    // Callback for when everything is done
    function done( status, nativeStatusText, responses, headers ) {
      var isSuccess, success, error, response, modified,
        statusText = nativeStatusText;

      // Called once
      if ( state === $fog$66 ) {
        return;
      }

      // State is "done" now
      state = $fog$66;

      // Clear timeout if it exists
      if ( timeoutTimer ) {
        clearTimeout( timeoutTimer );
      }

      // Dereference transport for early garbage collection
      // (no matter how long the jqXHR object will be used)
      transport = undefined;

      // Cache response headers
      responseHeadersString = headers || $fog$20;

      // Set readyState
      jqXHR[$fog$370] = status > $fog$21 ? $fog$236 : $fog$21;

      // Determine if successful
      isSuccess = status >= $fog$677 && status < $fog$798 || status === $fog$799;

      // Get response data
      if ( responses ) {
        response = ajaxHandleResponses( s, jqXHR, responses );
      }

      // Convert no matter what (that way responseXXX fields are always set)
      response = ajaxConvert( s, response, jqXHR, isSuccess );

      // If successful, handle type chaining
      if ( isSuccess ) {

        // Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
        if ( s[$fog$781] ) {
          modified = jqXHR[$fog$736]($fog$800);
          if ( modified ) {
            jQuery[$fog$782][ cacheURL ] = modified;
          }
          modified = jqXHR[$fog$736]($fog$785);
          if ( modified ) {
            jQuery[$fog$785][ cacheURL ] = modified;
          }
        }

        // if no content
        if ( status === $fog$801 || s[$fog$47] === $fog$802 ) {
          statusText = $fog$803;

        // if not modified
        } else if ( status === $fog$799 ) {
          statusText = $fog$804;

        // If we have data, let's convert it
        } else {
          statusText = response[$fog$805];
          success = response[$fog$468];
          error = response[$fog$241];
          isSuccess = !error;
        }
      } else {
        // Extract error from statusText and normalize for non-aborts
        error = statusText;
        if ( status || !statusText ) {
          statusText = $fog$241;
          if ( status < $fog$21 ) {
            status = $fog$21;
          }
        }
      }

      // Set data for the fake xhr object
      jqXHR[$fog$762] = status;
      jqXHR[$fog$806] = ( nativeStatusText || statusText ) + $fog$20;

      // Success/Error
      if ( isSuccess ) {
        deferred[$fog$362]( callbackContext, [ success, statusText, jqXHR ] );
      } else {
        deferred[$fog$663]( callbackContext, [ jqXHR, statusText, error ] );
      }

      // Status-dependent callbacks
      jqXHR[$fog$760]( statusCode );
      statusCode = undefined;

      if ( fireGlobals ) {
        globalEventContext[$fog$453]( isSuccess ? $fog$807 : $fog$808,
          [ jqXHR, s, isSuccess ? success : error ] );
      }

      // Complete
      completeDeferred[$fog$344]( callbackContext, [ jqXHR, statusText ] );

      if ( fireGlobals ) {
        globalEventContext[$fog$453]( $fog$809, [ jqXHR, s ] );
        // Handle the global AJAX counter
        if ( !( --jQuery[$fog$774] ) ) {
          jQuery[$fog$426][$fog$453]($fog$810);
        }
      }
    }

    return jqXHR;
  },

  getJSON: function( url, data, callback ) {
    return jQuery[$fog$330]( url, data, callback, $fog$811 );
  },

  getScript: function( url, callback ) {
    return jQuery[$fog$330]( url, undefined, callback, $fog$56 );
  }
});

jQuery[$fog$29]( [ $fog$330, $fog$812 ], function( i, method ) {
  jQuery[ method ] = function( url, data, callback, type ) {
    // Shift arguments if data argument was omitted
    if ( jQuery[$fog$40]( data ) ) {
      type = type || callback;
      callback = data;
      data = undefined;
    }

    return jQuery[$fog$813]({
      url: url,
      type: method,
      dataType: type,
      data: data,
      success: callback
    });
  };
});


jQuery[$fog$550] = function( url ) {
  return jQuery[$fog$813]({
    url: url,
    type: $fog$747,
    dataType: $fog$56,
    async: $fog$38,
    global: $fog$38,
    "throws": $fog$3
  });
};


jQuery[$fog$14][$fog$37]({
  wrapAll: function( html ) {
    var wrap;

    if ( jQuery[$fog$40]( html ) ) {
      return this[$fog$29](function( i ) {
        jQuery( this )[$fog$814]( html[$fog$22](this, i) );
      });
    }

    if ( this[ $fog$21 ] ) {

      // The elements to wrap the target around
      wrap = jQuery( html, this[ $fog$21 ][$fog$143] )[$fog$33]( $fog$21 )[$fog$544]( $fog$3 );

      if ( this[ $fog$21 ][$fog$59] ) {
        wrap[$fog$542]( this[ $fog$21 ] );
      }

      wrap[$fog$31](function() {
        var elem = this;

        while ( elem[$fog$815] ) {
          elem = elem[$fog$815];
        }

        return elem;
      })[$fog$545]( this );
    }

    return this;
  },

  wrapInner: function( html ) {
    if ( jQuery[$fog$40]( html ) ) {
      return this[$fog$29](function( i ) {
        jQuery( this )[$fog$816]( html[$fog$22](this, i) );
      });
    }

    return this[$fog$29](function() {
      var self = jQuery( this ),
        contents = self[$fog$734]();

      if ( contents[$fog$24] ) {
        contents[$fog$814]( html );

      } else {
        self[$fog$545]( html );
      }
    });
  },

  wrap: function( html ) {
    var isFunction = jQuery[$fog$40]( html );

    return this[$fog$29](function( i ) {
      jQuery( this )[$fog$814]( isFunction ? html[$fog$22](this, i) : html );
    });
  },

  unwrap: function() {
    return this[$fog$817]()[$fog$29](function() {
      if ( !jQuery[$fog$63]( this, $fog$480 ) ) {
        jQuery( this )[$fog$554]( this[$fog$142] );
      }
    })[$fog$631]();
  }
});


jQuery[$fog$310][$fog$293][$fog$214] = function( elem ) {
  // Support: Opera <= 12.12
  // Opera reports offsetWidths and offsetHeights less than zero on some elements
  return elem[$fog$603] <= $fog$21 && elem[$fog$604] <= $fog$21;
};
jQuery[$fog$310][$fog$293][$fog$818] = function( elem ) {
  return !jQuery[$fog$310][$fog$293][$fog$214]( elem );
};




var r20 = $fog$819,
  rbracket = $fog$820,
  rCRLF = $fog$821,
  rsubmitterTypes = $fog$822,
  rsubmittable = $fog$823;

function buildParams( prefix, obj, traditional, add ) {
  var name;

  if ( jQuery[$fog$42]( obj ) ) {
    // Serialize array item.
    jQuery[$fog$29]( obj, function( i, v ) {
      if ( traditional || rbracket[$fog$153]( prefix ) ) {
        // Treat each array item as a scalar.
        add( prefix, v );

      } else {
        // Item is non-scalar (array or object), encode its numeric index.
        buildParams( prefix + $fog$824 + ( typeof v === $fog$0 ? i : $fog$20 ) + $fog$73, v, traditional, add );
      }
    });

  } else if ( !traditional && jQuery[$fog$47]( obj ) === $fog$0 ) {
    // Serialize object item.
    for ( name in obj ) {
      buildParams( prefix + $fog$824 + name + $fog$73, obj[ name ], traditional, add );
    }

  } else {
    // Serialize scalar item.
    add( prefix, obj );
  }
}

// Serialize an array of form elements or a set of
// key/values into a query string
jQuery[$fog$772] = function( a, traditional ) {
  var prefix,
    s = [],
    add = function( key, value ) {
      // If value is a function, invoke it and return its value
      value = jQuery[$fog$40]( value ) ? value() : ( value == $fog$23 ? $fog$20 : value );
      s[ s[$fog$24] ] = encodeURIComponent( key ) + $fog$107 + encodeURIComponent( value );
    };

  // Set traditional to true for jQuery <= 1.3.2 behavior.
  if ( traditional === undefined ) {
    traditional = jQuery[$fog$733] && jQuery[$fog$733][$fog$773];
  }

  // If an array was passed in, assume that it is an array of form elements.
  if ( jQuery[$fog$42]( a ) || ( a[$fog$323] && !jQuery[$fog$41]( a ) ) ) {
    // Serialize the form elements
    jQuery[$fog$29]( a, function() {
      add( this[$fog$215], this[$fog$193] );
    });

  } else {
    // If traditional, encode the "old" way (the way 1.3.2 or older
    // did it), otherwise encode params recursively.
    for ( prefix in a ) {
      buildParams( prefix, a[ prefix ], traditional, add );
    }
  }

  // Return the resulting serialization
  return s[$fog$159]( $fog$777 )[$fog$44]( r20, $fog$98 );
};

jQuery[$fog$14][$fog$37]({
  serialize: function() {
    return jQuery[$fog$772]( this[$fog$825]() );
  },
  serializeArray: function() {
    return this[$fog$31](function() {
      // Can add propHook for "elements" to filter or add form elements
      var elements = jQuery[$fog$625]( this, $fog$826 );
      return elements ? jQuery[$fog$325]( elements ) : this;
    })
    [$fog$191](function() {
      var type = this[$fog$47];

      // Use .is( ":disabled" ) so that fieldset[disabled] works
      return this[$fog$215] && !jQuery( this )[$fog$327]( $fog$220 ) &&
        rsubmittable[$fog$153]( this[$fog$63] ) && !rsubmitterTypes[$fog$153]( type ) &&
        ( this[$fog$288] || !rcheckableType[$fog$153]( type ) );
    })
    [$fog$31](function( i, elem ) {
      var val = jQuery( this )[$fog$709]();

      return val == $fog$23 ?
        $fog$23 :
        jQuery[$fog$42]( val ) ?
          jQuery[$fog$31]( val, function( val ) {
            return { name: elem[$fog$215], value: val[$fog$44]( rCRLF, $fog$827 ) };
          }) :
          { name: elem[$fog$215], value: val[$fog$44]( rCRLF, $fog$827 ) };
    })[$fog$330]();
  }
});


jQuery[$fog$733][$fog$828] = function() {
  try {
    return new XMLHttpRequest();
  } catch( e ) {}
};

var xhrId = $fog$21,
  xhrCallbacks = {},
  xhrSuccessStatus = {
    // file protocol always yields status code 0, assume 200
    $fog$21: $fog$677,
    // Support: IE9
    // #1450: sometimes IE returns 1223 when it should be 204
    $fog$829: $fog$801
  },
  xhrSupported = jQuery[$fog$733][$fog$828]();

// Support: IE9
// Open requests must be manually aborted on unload (#5280)
// See https://support.microsoft.com/kb/2856746 for more info
if ( window[$fog$182] ) {
  window[$fog$182]( $fog$183, function() {
    for ( var key in xhrCallbacks ) {
      xhrCallbacks[ key ]();
    }
  });
}

support[$fog$830] = !!xhrSupported && ( $fog$831 in xhrSupported );
support[$fog$813] = xhrSupported = !!xhrSupported;

jQuery[$fog$832](function( options ) {
  var callback;

  // Cross domain only allowed if supported through XMLHttpRequest
  if ( support[$fog$830] || xhrSupported && !options[$fog$767] ) {
    return {
      send: function( headers, complete ) {
        var i,
          xhr = options[$fog$828](),
          id = ++xhrId;

        xhr[$fog$833]( options[$fog$47], options[$fog$764], options[$fog$795], options[$fog$834], options[$fog$835] );

        // Apply custom fields if provided
        if ( options[$fog$836] ) {
          for ( i in options[$fog$836] ) {
            xhr[ i ] = options[$fog$836][ i ];
          }
        }

        // Override mime type if needed
        if ( options[$fog$735] && xhr[$fog$837] ) {
          xhr[$fog$837]( options[$fog$735] );
        }

        // X-Requested-With header
        // For cross-domain requests, seeing as conditions for a preflight are
        // akin to a jigsaw puzzle, we simply never set it to be sure.
        // (it can always be set on a per-request basis or even using ajaxSetup)
        // For same-domain requests, won't change header if already provided.
        if ( !options[$fog$767] && !headers[$fog$838] ) {
          headers[$fog$838] = $fog$839;
        }

        // Set headers
        for ( i in headers ) {
          xhr[$fog$783]( i, headers[ i ] );
        }

        // Callback
        callback = function( type ) {
          return function() {
            if ( callback ) {
              delete xhrCallbacks[ id ];
              callback = xhr[$fog$840] = xhr[$fog$841] = $fog$23;

              if ( type === $fog$763 ) {
                xhr[$fog$763]();
              } else if ( type === $fog$241 ) {
                complete(
                  // file: protocol always yields status 0; see #8605, #14207
                  xhr[$fog$762],
                  xhr[$fog$806]
                );
              } else {
                complete(
                  xhrSuccessStatus[ xhr[$fog$762] ] || xhr[$fog$762],
                  xhr[$fog$806],
                  // Support: IE9
                  // Accessing binary-data responseText throws an exception
                  // (#11426)
                  typeof xhr[$fog$757] === $fog$65 ? {
                    text: xhr[$fog$757]
                  } : undefined,
                  xhr[$fog$842]()
                );
              }
            }
          };
        };

        // Listen to events
        xhr[$fog$840] = callback();
        xhr[$fog$841] = callback($fog$241);

        // Create the abort callback
        callback = xhrCallbacks[ id ] = callback($fog$763);

        try {
          // Do send the request (this may raise an exception)
          xhr[$fog$797]( options[$fog$776] && options[$fog$468] || $fog$23 );
        } catch ( e ) {
          // #14683: Only rethrow if this hasn't been notified as an error yet
          if ( callback ) {
            throw e;
          }
        }
      },

      abort: function() {
        if ( callback ) {
          callback();
        }
      }
    };
  }
});




// Install script dataType
jQuery[$fog$759]({
  accepts: {
    script: $fog$843
  },
  contents: {
    script: $fog$844
  },
  converters: {
    "text script": function( text ) {
      jQuery[$fog$537]( text );
      return text;
    }
  }
});

// Handle cache's special case and crossDomain
jQuery[$fog$845]( $fog$56, function( s ) {
  if ( s[$fog$375] === undefined ) {
    s[$fog$375] = $fog$38;
  }
  if ( s[$fog$767] ) {
    s[$fog$47] = $fog$747;
  }
});

// Bind script tag hack transport
jQuery[$fog$832]( $fog$56, function( s ) {
  // This transport only deals with cross domain requests
  if ( s[$fog$767] ) {
    var script, callback;
    return {
      send: function( _, complete ) {
        script = jQuery($fog$846)[$fog$625]({
          async: $fog$3,
          charset: s[$fog$847],
          src: s[$fog$764]
        })[$fog$447](
          $fog$848,
          callback = function( evt ) {
            script[$fog$393]();
            callback = $fog$23;
            if ( evt ) {
              complete( evt[$fog$47] === $fog$241 ? $fog$849 : $fog$677, evt[$fog$47] );
            }
          }
        );
        document[$fog$61][$fog$60]( script[ $fog$21 ] );
      },
      abort: function() {
        if ( callback ) {
          callback();
        }
      }
    };
  }
});




var oldCallbacks = [],
  rjsonp = $fog$850;

// Default jsonp settings
jQuery[$fog$759]({
  jsonp: $fog$851,
  jsonpCallback: function() {
    var callback = oldCallbacks[$fog$78]() || ( jQuery[$fog$376] + $fog$852 + ( nonce++ ) );
    this[ callback ] = $fog$3;
    return callback;
  }
});

// Detect, normalize options and install callbacks for jsonp requests
jQuery[$fog$845]( $fog$853, function( s, originalSettings, jqXHR ) {

  var callbackName, overwritten, responseContainer,
    jsonProp = s[$fog$854] !== $fog$38 && ( rjsonp[$fog$153]( s[$fog$764] ) ?
      $fog$764 :
      typeof s[$fog$468] === $fog$65 && !( s[$fog$787] || $fog$20 )[$fog$9]($fog$855) && rjsonp[$fog$153]( s[$fog$468] ) && $fog$468
    );

  // Handle iff the expected data type is "jsonp" or we have a parameter to set
  if ( jsonProp || s[$fog$731][ $fog$21 ] === $fog$854 ) {

    // Get callback name, remembering preexisting value associated with it
    callbackName = s[$fog$856] = jQuery[$fog$40]( s[$fog$856] ) ?
      s[$fog$856]() :
      s[$fog$856];

    // Insert callback into url or form data
    if ( jsonProp ) {
      s[ jsonProp ] = s[ jsonProp ][$fog$44]( rjsonp, $fog$163 + callbackName );
    } else if ( s[$fog$854] !== $fog$38 ) {
      s[$fog$764] += ( rquery[$fog$153]( s[$fog$764] ) ? $fog$777 : $fog$778 ) + s[$fog$854] + $fog$107 + callbackName;
    }

    // Use data converter to retrieve json after script execution
    s[$fog$738][$fog$857] = function() {
      if ( !responseContainer ) {
        jQuery[$fog$241]( callbackName + $fog$858 );
      }
      return responseContainer[ $fog$21 ];
    };

    // force json dataType
    s[$fog$731][ $fog$21 ] = $fog$811;

    // Install callback
    overwritten = window[ callbackName ];
    window[ callbackName ] = function() {
      responseContainer = arguments;
    };

    // Clean-up function (fires after converters)
    jqXHR[$fog$650](function() {
      // Restore preexisting value
      window[ callbackName ] = overwritten;

      // Save back as free
      if ( s[ callbackName ] ) {
        // make sure that re-using the options doesn't screw things around
        s[$fog$856] = originalSettings[$fog$856];

        // save the callback name for future use
        oldCallbacks[$fog$8]( callbackName );
      }

      // Call if it was a function and we have a response
      if ( responseContainer && jQuery[$fog$40]( overwritten ) ) {
        overwritten( responseContainer[ $fog$21 ] );
      }

      responseContainer = overwritten = undefined;
    });

    // Delegate to script
    return $fog$56;
  }
});




// data: string of html
// context (optional): If specified, the fragment will be created in this context, defaults to document
// keepScripts (optional): If true, will include scripts passed in the html string
jQuery[$fog$322] = function( data, context, keepScripts ) {
  if ( !data || typeof data !== $fog$65 ) {
    return $fog$23;
  }
  if ( typeof context === $fog$39 ) {
    keepScripts = context;
    context = $fog$38;
  }
  context = context || document;

  var parsed = rsingleTag[$fog$146]( data ),
    scripts = !keepScripts && [];

  // Single tag
  if ( parsed ) {
    return [ context[$fog$55]( parsed[$fog$34] ) ];
  }

  parsed = jQuery[$fog$548]( [ data ], context, scripts );

  if ( scripts && scripts[$fog$24] ) {
    jQuery( scripts )[$fog$393]();
  }

  return jQuery[$fog$25]( [], parsed[$fog$142] );
};


// Keep a copy of the old load method
var _load = jQuery[$fog$14][$fog$369];

/**
 * Load a url into a page
 */
jQuery[$fog$14][$fog$369] = function( url, params, callback ) {
  if ( typeof url !== $fog$65 && _load ) {
    return _load[$fog$32]( this, arguments );
  }

  var selector, type, response,
    self = this,
    off = url[$fog$9]($fog$71);

  if ( off >= $fog$21 ) {
    selector = jQuery[$fog$53]( url[$fog$6]( off ) );
    url = url[$fog$6]( $fog$21, off );
  }

  // If it's a function
  if ( jQuery[$fog$40]( params ) ) {

    // We assume that it's the callback
    callback = params;
    params = undefined;

  // Otherwise, build a param string
  } else if ( params && typeof params === $fog$0 ) {
    type = $fog$859;
  }

  // If we have elements to modify, make the request
  if ( self[$fog$24] > $fog$21 ) {
    jQuery[$fog$813]({
      url: url,

      // if "type" variable is undefined, then "GET" method will be used
      type: type,
      dataType: $fog$547,
      data: params
    })[$fog$346](function( responseText ) {

      // Save response for use in complete callback
      response = arguments;

      self[$fog$547]( selector ?

        // If a selector was specified, locate the right elements in a dummy div
        // Exclude scripts to avoid IE 'Permission Denied' errors
        jQuery($fog$860)[$fog$545]( jQuery[$fog$322]( responseText ) )[$fog$189]( selector ) :

        // Otherwise use the full result
        responseText );

    })[$fog$371]( callback && function( jqXHR, status ) {
      self[$fog$29]( callback, response || [ jqXHR[$fog$757], status, jqXHR ] );
    });
  }

  return this;
};




// Attach a bunch of functions for handling common AJAX events
jQuery[$fog$29]( [ $fog$775, $fog$810, $fog$809, $fog$808, $fog$807, $fog$794 ], function( i, type ) {
  jQuery[$fog$14][ type ] = function( fn ) {
    return this[$fog$447]( type, fn );
  };
});




jQuery[$fog$310][$fog$293][$fog$861] = function( elem ) {
  return jQuery[$fog$317](jQuery[$fog$671], function( fn ) {
    return elem === fn[$fog$465];
  })[$fog$24];
};




var docElem = window[$fog$2][$fog$175];

/**
 * Gets a window from an element
 */
function getWindow( elem ) {
  return jQuery[$fog$51]( elem ) ? elem : elem[$fog$50] === $fog$144 && elem[$fog$178];
}

jQuery[$fog$862] = {
  setOffset: function( elem, options, i ) {
    var curPosition, curLeft, curCSSTop, curTop, curOffset, curCSSLeft, calculatePosition,
      position = jQuery[$fog$409]( elem, $fog$863 ),
      curElem = jQuery( elem ),
      props = {};

    // Set position first, in-case top/left are set even on static elem
    if ( position === $fog$864 ) {
      elem[$fog$565][$fog$863] = $fog$298;
    }

    curOffset = curElem[$fog$862]();
    curCSSTop = jQuery[$fog$409]( elem, $fog$179 );
    curCSSLeft = jQuery[$fog$409]( elem, $fog$865 );
    calculatePosition = ( position === $fog$589 || position === $fog$866 ) &&
      ( curCSSTop + curCSSLeft )[$fog$9]($fog$637) > -$fog$34;

    // Need to be able to calculate position if either
    // top or left is auto and position is either absolute or fixed
    if ( calculatePosition ) {
      curPosition = curElem[$fog$863]();
      curTop = curPosition[$fog$179];
      curLeft = curPosition[$fog$865];

    } else {
      curTop = parseFloat( curCSSTop ) || $fog$21;
      curLeft = parseFloat( curCSSLeft ) || $fog$21;
    }

    if ( jQuery[$fog$40]( options ) ) {
      options = options[$fog$22]( elem, i, curOffset );
    }

    if ( options[$fog$179] != $fog$23 ) {
      props[$fog$179] = ( options[$fog$179] - curOffset[$fog$179] ) + curTop;
    }
    if ( options[$fog$865] != $fog$23 ) {
      props[$fog$865] = ( options[$fog$865] - curOffset[$fog$865] ) + curLeft;
    }

    if ( $fog$867 in options ) {
      options[$fog$867][$fog$22]( elem, props );

    } else {
      curElem[$fog$409]( props );
    }
  }
};

jQuery[$fog$14][$fog$37]({
  offset: function( options ) {
    if ( arguments[$fog$24] ) {
      return options === undefined ?
        this :
        this[$fog$29](function( i ) {
          jQuery[$fog$862][$fog$868]( this, options, i );
        });
    }

    var docElem, win,
      elem = this[ $fog$21 ],
      box = { top: $fog$21, left: $fog$21 },
      doc = elem && elem[$fog$143];

    if ( !doc ) {
      return;
    }

    docElem = doc[$fog$175];

    // Make sure it's not a disconnected DOM node
    if ( !jQuery[$fog$233]( docElem, elem ) ) {
      return box;
    }

    // Support: BlackBerry 5, iOS 3 (original iPhone)
    // If we don't have gBCR, just use 0,0 rather than error
    if ( typeof elem[$fog$869] !== strundefined ) {
      box = elem[$fog$869]();
    }
    win = getWindow( doc );
    return {
      top: box[$fog$179] + win[$fog$870] - docElem[$fog$486],
      left: box[$fog$865] + win[$fog$871] - docElem[$fog$482]
    };
  },

  position: function() {
    if ( !this[ $fog$21 ] ) {
      return;
    }

    var offsetParent, offset,
      elem = this[ $fog$21 ],
      parentOffset = { top: $fog$21, left: $fog$21 };

    // Fixed elements are offset from window (parentOffset = {top:0, left: 0}, because it is its only offset parent
    if ( jQuery[$fog$409]( elem, $fog$863 ) === $fog$866 ) {
      // Assume getBoundingClientRect is there when computed position is fixed
      offset = elem[$fog$869]();

    } else {
      // Get *real* offsetParent
      offsetParent = this[$fog$872]();

      // Get correct offsets
      offset = this[$fog$862]();
      if ( !jQuery[$fog$63]( offsetParent[ $fog$21 ], $fog$547 ) ) {
        parentOffset = offsetParent[$fog$862]();
      }

      // Add offsetParent borders
      parentOffset[$fog$179] += jQuery[$fog$409]( offsetParent[ $fog$21 ], $fog$873, $fog$3 );
      parentOffset[$fog$865] += jQuery[$fog$409]( offsetParent[ $fog$21 ], $fog$874, $fog$3 );
    }

    // Subtract parent offsets and element margins
    return {
      top: offset[$fog$179] - parentOffset[$fog$179] - jQuery[$fog$409]( elem, $fog$875, $fog$3 ),
      left: offset[$fog$865] - parentOffset[$fog$865] - jQuery[$fog$409]( elem, $fog$876, $fog$3 )
    };
  },

  offsetParent: function() {
    return this[$fog$31](function() {
      var offsetParent = this[$fog$872] || docElem;

      while ( offsetParent && ( !jQuery[$fog$63]( offsetParent, $fog$547 ) && jQuery[$fog$409]( offsetParent, $fog$863 ) === $fog$864 ) ) {
        offsetParent = offsetParent[$fog$872];
      }

      return offsetParent || docElem;
    });
  }
});

// Create scrollLeft and scrollTop methods
jQuery[$fog$29]( { scrollLeft: $fog$871, scrollTop: $fog$870 }, function( method, prop ) {
  var top = $fog$870 === prop;

  jQuery[$fog$14][ method ] = function( val ) {
    return access( this, function( elem, method, val ) {
      var win = getWindow( elem );

      if ( val === undefined ) {
        return win ? win[ prop ] : elem[ method ];
      }

      if ( win ) {
        win[$fog$877](
          !top ? val : window[$fog$871],
          top ? val : window[$fog$870]
        );

      } else {
        elem[ method ] = val;
      }
    }, method, val, arguments[$fog$24], $fog$23 );
  };
});

// Support: Safari<7+, Chrome<37+
// Add the top/left cssHooks using jQuery.fn.position
// Webkit bug: https://bugs.webkit.org/show_bug.cgi?id=29084
// Blink bug: https://code.google.com/p/chromium/issues/detail?id=229280
// getComputedStyle returns percent when specified for top/left/bottom/right;
// rather than make the css module depend on the offset module, just check for it here
jQuery[$fog$29]( [ $fog$179, $fog$865 ], function( i, prop ) {
  jQuery[$fog$613][ prop ] = addGetHookIf( support[$fog$878],
    function( elem, computed ) {
      if ( computed ) {
        computed = curCSS( elem, prop );
        // If curCSS returns percentage, fallback to offset
        return rnumnonpx[$fog$153]( computed ) ?
          jQuery( elem )[$fog$863]()[ prop ] + $fog$597 :
          computed;
      }
    }
  );
});


// Create innerHeight, innerWidth, height, width, outerHeight and outerWidth methods
jQuery[$fog$29]( { Height: $fog$619, Width: $fog$567 }, function( name, type ) {
  jQuery[$fog$29]( { padding: $fog$879 + name, content: type, "": $fog$880 + name }, function( defaultExtra, funcName ) {
    // Margin is only for outerHeight, outerWidth
    jQuery[$fog$14][ funcName ] = function( margin, value ) {
      var chainable = arguments[$fog$24] && ( defaultExtra || typeof margin !== $fog$39 ),
        extra = defaultExtra || ( margin === $fog$3 || value === $fog$3 ? $fog$600 : $fog$598 );

      return access( this, function( elem, type, value ) {
        var doc;

        if ( jQuery[$fog$51]( elem ) ) {
          // As of 5/8/2012 this will yield incorrect results for Mobile Safari, but there
          // isn't a whole lot we can do. See pull request at this URL for discussion:
          // https://github.com/jquery/jquery/pull/764
          return elem[$fog$2][$fog$175][ $fog$881 + name ];
        }

        // Get document width or height
        if ( elem[$fog$50] === $fog$144 ) {
          doc = elem[$fog$175];

          // Either scroll[Width/Height] or offset[Width/Height] or client[Width/Height],
          // whichever is greatest
          return Math[$fog$596](
            elem[$fog$480][ $fog$882 + name ], doc[ $fog$882 + name ],
            elem[$fog$480][ $fog$862 + name ], doc[ $fog$862 + name ],
            doc[ $fog$881 + name ]
          );
        }

        return value === undefined ?
          // Get width or height on the element, requesting but not forcing parseFloat
          jQuery[$fog$409]( elem, type, extra ) :

          // Set width or height on the element
          jQuery[$fog$565]( elem, type, value, extra );
      }, type, chainable ? margin : undefined, chainable, $fog$23 );
    };
  });
});


// The number of elements contained in the matched element set
jQuery[$fog$14][$fog$883] = function() {
  return this[$fog$24];
};

jQuery[$fog$14][$fog$884] = jQuery[$fog$14][$fog$885];




// Register as a named AMD module, since jQuery can be concatenated with other
// files that may use define, but not via a proper concatenation script that
// understands anonymous AMD modules. A named AMD is safest and most robust
// way to register. Lowercase jquery is used because AMD module names are
// derived from file names, and jQuery is normally delivered in a lowercase
// file name. Do this after creating the global so that if an AMD module wants
// to call noConflict to hide this version of jQuery, it will work.

// Note that for maximum portability, libraries that are not jQuery should
// declare themselves as anonymous modules, and avoid setting a global if an
// AMD loader is present. jQuery is a special case. For more information, see
// https://github.com/jrburke/requirejs/wiki/Updating-existing-libraries#wiki-anon

if ( typeof define === $fog$48 && define[$fog$886] ) {
  define( $fog$323, [], function() {
    return jQuery;
  });
}




var
  // Map over jQuery in case of overwrite
  _jQuery = window[$fog$43],

  // Map over the $ in case of overwrite
  _$ = window[$fog$109];

jQuery[$fog$887] = function( deep ) {
  if ( window[$fog$109] === jQuery ) {
    window[$fog$109] = _$;
  }

  if ( deep && window[$fog$43] === jQuery ) {
    window[$fog$43] = _jQuery;
  }

  return jQuery;
};

// Expose jQuery and $ identifiers, even in AMD
// (#7102#comment:10, https://github.com/jquery/jquery/pull/557)
// and CommonJS for browser emulators (#13566)
if ( typeof noGlobal === strundefined ) {
  window[$fog$43] = window[$fog$109] = jQuery;
}




return jQuery;

}));
})();
})("‮", "object", "exports", "document", true, "jQuery requires a window with a document", "undefined", "slice", "concat", "push", "indexOf", "toString", "hasOwnProperty", "2.1.4", "init", "fn", /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, /^-ms-/, /-([\da-z])/gi, "toUpperCase", "prototype", "", 0, "call", null, "length", "merge", "constructor", "prevObject", "context", "each", "pushStack", "map", "apply", "eq", 1, "sort", "splice", "extend", false, "boolean", "isFunction", "isPlainObject", "isArray", "jQuery", "replace", "random", /\D/g, "type", "function", "window", "nodeType", "isWindow", "isPrototypeOf", "trim", "use strict", "createElement", "script", "text", "removeChild", "parentNode", "appendChild", "head", "ms-", "nodeName", "toLowerCase", "string", 2, "guid", "now", "split", "Boolean Number String Function Array Date RegExp Object Error", " ", "[object ", "]", "array", "number", "sizzle", 31, "pop", "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped", "[\\x20\\t\\r\\n\\f]", "(?:\\\\.|[\\w-]|[^\\x00-\\xa0])+", "w", "w#", "\\[", "*(", ")(?:", "*([*^$|!~]?=)", "*(?:'((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\"|(", "))|)", "*\\]", ":(", ")(?:\\((", "('((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\")|", "((?:\\\\.|[^\\\\()[\\]]|", ")*)|", ".*", ")\\)|)", "+", "g", "^", "+|((?:^|[^\\\\])(?:\\\\.)*)", "+$", "*,", "*", "*([>+~]|", ")", "=", "*([^\\]'\"]*?)", "$", "^#(", "^\\.(", "^(", "w*", "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(", "*(even|odd|(([+-]|)(\\d*)n|)", "*(?:([+-]|)", "*(\\d+)|))", "*\\)|)", "i", "^(?:", ")$", "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(", "*((?:-\\d)?\\d*)", "*\\)|)(?=[^-]|$)", /^(?:input|select|textarea|button)$/i, /^h\d$/i, /^[^{]+\{\s*\[native \w/, /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/, /[+~]/, /'|\\/g, "\\\\([\\da-f]{1,6}", "?|(", ")|.)", "ig", "0x", 0x10000, "fromCharCode", 10, 0xD800, 0x3FF, 0xDC00, "childNodes", "ownerDocument", 9, 11, "exec", "getElementById", "id", "getElementsByTagName", 3, "getElementsByClassName", "qsa", "test", "getAttribute", "\\$&", "setAttribute", "[id='", "'] ", "join", ",", "querySelectorAll", "removeAttribute", "$1", "cacheLength", "shift", "div", "|", "attrHandle", "sourceIndex", "nextSibling", "input", "button", "support", "isXML", "documentElement", "HTML", "setDocument", "defaultView", "top", "addEventListener", "unload", "attachEvent", "onunload", "attributes", "className", "createComment", "getById", "getElementsByName", "find", "ID", "filter", "getAttributeNode", "value", "TAG", "CLASS", "innerHTML", "<a id='", "'></a>", "<select id='", "-\f]' msallowcapture=''>", "<option selected=''></option></select>", "[msallowcapture^='']", "[*^$]=", "*(?:''|\"\")", "[selected]", "*(?:value|", "[id~=", "-]", "~=", ":checked", "a#", "+*", ".#.+[+~]", "hidden", "name", "D", "[name=d]", "*[*^$|!~]?=", ":enabled", ":disabled", "*,:x", ",.*:", "matchesSelector", "matches", "webkitMatchesSelector", "mozMatchesSelector", "oMatchesSelector", "msMatchesSelector", "disconnectedMatch", "[s!='']:x", "!=", "compareDocumentPosition", "contains", 16, "sortDetached", 4, "unshift", "='$1']", "attr", "specified", "error", "Syntax error, unrecognized expression: ", "uniqueSort", "detectDuplicates", "sortStable", "getText", "textContent", "firstChild", "nodeValue", "selectors", 50, "previousSibling", 5, "nth", 6, "even", "odd", 7, 8, "CHILD", "(^|", "(", "|$)", "class", "^=", "*=", "$=", "|=", "-", "last", "of-type", "only", "lastChild", "pseudos", "setFilters", "unsupported pseudo: ", "innerText", "unsupported lang: ", "lang", "xml:lang", "location", "hash", "activeElement", "hasFocus", "href", "tabIndex", "disabled", "checked", "option", "selected", "selectedIndex", "empty", "filters", "tokenize", "preFilter", "dir", "first", "relative", "0", 0.1, "compile", "selector", "select", "needsContext", "<a href='#'></a>", "#", "type|href|height|width", "<input/>", "defaultValue", "expr", ":", "unique", "isXMLDoc", "match", /^<(\w+)\s*\/?>(?:<\/\1>|)$/, /^.[^:#\[\.,]*$/, "grep", ":not(", /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]*))$/, "<", ">", "parseHTML", "jquery", "ready", "makeArray", /^(?:parents|prev(?:Until|All))/, "is", "index", "prevAll", "get", "add", "sibling", "contentDocument", "Until", "reverse", /\S+/g, "Callbacks", "once", "memory", "stopOnFalse", "disable", "has", "inArray", "fireWith", "resolve", "done", "once memory", "resolved", "reject", "fail", "rejected", "notify", "progress", "pending", "promise", "Deferred", "With", "pipe", "then", "lock", "notifyWith", "resolveWith", "readyWait", "isReady", "triggerHandler", "off", "removeEventListener", "DOMContentLoaded", "load", "readyState", "complete", "access", "acceptData", "defineProperty", "cache", "expando", "uid", "accepts", "defineProperties", "key", "isEmptyObject", "camelCase", "set", /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/, /([A-Z])/g, "data-", "-$1", "true", "false", "null", "parseJSON", "hasData", "remove", "hasDataAttrs", "fx", "queue", "_queueHooks", "dequeue", "inprogress", "stop", "fire", "queueHooks", "source", /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/, "Top", "Right", "Bottom", "Left", "css", "display", "none", /^(?:checkbox|radio)$/i, "createDocumentFragment", "radio", "t", "checkClone", "cloneNode", "<textarea>x</textarea>", "noCloneChecked", "focusinBubbles", "onfocusin", /^key/, /^(?:mouse|pointer|contextmenu)|click/, /^(?:focusinfocus|focusoutblur)$/, /^([^.]*)(?:\.(.+)|)$/, "event", "handler", "events", "handle", "triggered", "dispatch", ".", "special", "delegateType", "bindType", "delegateCount", "setup", "global", "(^|\\.)", "\\.(?:.*\\.|)", "(\\.|$)", "origType", "namespace", "**", "teardown", "removeEvent", "on", "Event", "isTrigger", "namespace_re", "result", "target", "trigger", "noBubble", "parentWindow", "isPropagationStopped", "preventDefault", "isDefaultPrevented", "_default", "fix", "delegateTarget", "preDispatch", "handlers", "currentTarget", "elem", "isImmediatePropagationStopped", "handleObj", "data", "stopPropagation", "postDispatch", "click", "altKey bubbles cancelable ctrlKey currentTarget eventPhase metaKey relatedTarget shiftKey target timeStamp view which", "char charCode key keyCode", "which", "charCode", "keyCode", "button buttons clientX clientY offsetX offsetY pageX pageY screenX screenY toElement", "pageX", "clientX", "body", "scrollLeft", "clientLeft", "pageY", "clientY", "scrollTop", "clientTop", "fixHooks", "mouseHooks", "keyHooks", "props", "focus", "focusin", "blur", "focusout", "checkbox", "a", "originalEvent", "returnValue", "defaultPrevented", "timeStamp", "stopImmediatePropagation", "mouseover", "mouseout", "pointerover", "pointerout", "relatedTarget", "simulate", /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([\w:]+)[^>]*)\/>/gi, /<([\w:]+)/, /<|&#?\w+;/, /<(?:script|style|link)/i, /checked\s*(?:[^=]|=\s*.checked.)/i, /^$|\/(?:java|ecma)script/i, /^true\/(.*)/, /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g, "<select multiple='multiple'>", "</select>", "<table>", "</table>", "<table><colgroup>", "</colgroup></table>", "<table><tbody>", "</tbody></table>", "<table><tbody><tr>", "</tr></tbody></table>", "optgroup", "tbody", "tfoot", "colgroup", "caption", "thead", "th", "td", "table", "tr", "/", "globalEval", "textarea", "createTextNode", "<$1></$2>", "domManip", "insertBefore", "cleanData", "clone", "append", "replaceChild", "html", "buildFragment", "src", "_evalUrl", "prepend", "before", "after", "replaceWith", "appendTo", "getDefaultComputedStyle", "detach", "<iframe frameborder='0' width='0' height='0'/>", "write", "close", /^margin/, ")(?!px)[a-z%]+$", "opener", "getComputedStyle", "style", "getPropertyValue", "width", "minWidth", "maxWidth", "backgroundClip", "content-box", "clearCloneStyle", "cssText", "border:0;width:0;height:0;top:0;left:-9999px;margin-top:1px;", "position:absolute", "-webkit-box-sizing:border-box;-moz-box-sizing:border-box;", "box-sizing:border-box;display:block;margin-top:1%;top:1%;", "border:1px;padding:1px;width:4px;position:absolute", "1%", "4px", "-webkit-box-sizing:content-box;-moz-box-sizing:content-box;", "box-sizing:content-box;display:block;margin:0;border:0;padding:0", "marginRight", "1px", "swap", /^(none|table(?!-c[ea]).+)/, ")(.*)$", "^([+-])=(", "absolute", "block", "400", "Webkit", "O", "Moz", "ms", "max", "px", "border", "content", "margin", "padding", "Width", "offsetWidth", "offsetHeight", "boxSizing", "border-box", "boxSizingReliable", "olddisplay", "opacity", "1", "cssFloat", "cssProps", "cssHooks", "cssNumber", "background", "inherit", "normal", "isNumeric", "height", "reliableMarginRight", "inline-block", "show", "hide", "Tween", "prop", "easing", "swing", "options", "start", "cur", "end", "unit", "propHooks", "duration", "pos", "step", "auto", 0.5, "cos", "PI", /^(?:toggle|show|hide)$/, "^(?:([+-])=|)(", ")([a-z%]*)$", /queueHooks$/, "createTween", 20, ".5", "fxshow", "unqueued", "always", "overflow", "overflowX", "overflowY", "inline", "float", "toggle", "expand", "startTime", "tweens", "run", "opts", "specialEasing", "rejectWith", "timer", "Animation", "speed", "speeds", "old", "animate", "finish", "timers", "anim", "tick", "interval", 13, 600, 200, 400, "delay", "checkOn", "optSelected", "optDisabled", "radioValue", "removeAttr", "attrHooks", "bool", "propFix", /\w+/g, "htmlFor", "hasAttribute", "tabindex", "readOnly", "maxLength", "cellSpacing", "cellPadding", "rowSpan", "colSpan", "useMap", "frameBorder", "contentEditable", /[\t\r\n\f]/g, "addClass", "removeClass", "toggleClass", "hasClass", "__className__", /\r/g, "valHooks", "val", "select-one", "blur focus focusin focusout load resize scroll unload click dblclick ", "mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave ", "change select submit keydown keypress keyup error contextmenu", "mouseleave", "mouseenter", /\?/, "parse", "parseXML", "parseFromString", "text/xml", "parsererror", "Invalid XML: ", /#.*$/, /([?&])_=[^&]*/, /^(.*?):[ \t]*([^\r\n]*)$/mg, /^(?:about|app|app-storage|.+-extension|file|res|widget):$/, /^(?:GET|HEAD)$/, /^\/\//, /^([\w.+-]+:)(?:\/\/(?:[^\/?#]*@|)([^\/?#:]*)(?::(\d+)|)|)/, "*/", "dataTypes", "flatOptions", "ajaxSettings", "contents", "mimeType", "getResponseHeader", "Content-Type", "converters", "responseFields", "dataFilter", "dataType", "* ", "throws", "No conversion from ", " to ", "success", "GET", "application/x-www-form-urlencoded; charset=UTF-8", "text/plain", "text/html", "application/xml, text/xml", "application/json, text/javascript", /xml/, /html/, /json/, "responseXML", "responseText", "responseJSON", "ajaxSetup", "statusCode", "canceled", "status", "abort", "url", "//", "method", "crossDomain", "http:", "80", "443", "processData", "param", "traditional", "active", "ajaxStart", "hasContent", "&", "?", "$1_=", "_=", "ifModified", "lastModified", "setRequestHeader", "If-Modified-Since", "etag", "If-None-Match", "contentType", "Accept", ", ", "; q=0.01", "headers", "beforeSend", "No Transport", "ajaxSend", "async", "timeout", "send", 300, 304, "Last-Modified", 204, "HEAD", "nocontent", "notmodified", "state", "statusText", "ajaxSuccess", "ajaxError", "ajaxComplete", "ajaxStop", "json", "post", "ajax", "wrapAll", "firstElementChild", "wrapInner", "parent", "visible", /%20/g, /\[\]$/, /\r?\n/g, /^(?:submit|button|image|reset|file)$/i, /^(?:input|select|textarea|keygen)/i, "[", "serializeArray", "elements", "\r\n", "xhr", 1223, "cors", "withCredentials", "ajaxTransport", "open", "username", "password", "xhrFields", "overrideMimeType", "X-Requested-With", "XMLHttpRequest", "onload", "onerror", "getAllResponseHeaders", "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript", /(?:java|ecma)script/, "ajaxPrefilter", "<script>", "scriptCharset", "load error", 404, /(=)\?(?=&|$)|\?\?/, "callback", "_", "json jsonp", "jsonp", "application/x-www-form-urlencoded", "jsonpCallback", "script json", " was not called", "POST", "<div>", "animated", "offset", "position", "static", "left", "fixed", "using", "setOffset", "getBoundingClientRect", "pageYOffset", "pageXOffset", "offsetParent", "borderTopWidth", "borderLeftWidth", "marginTop", "marginLeft", "scrollTo", "pixelPosition", "inner", "outer", "client", "scroll", "size", "andSelf", "addBack", "amd", "noConflict");
     