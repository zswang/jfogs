
(function ($fog$0, $fog$1, $fog$2, $fog$3, $fog$4, $fog$5, $fog$6, $fog$7, $fog$8, $fog$9, $fog$10, $fog$11, $fog$12, $fog$13, $fog$14, $fog$15, $fog$16, $fog$17, $fog$18, $fog$19, $fog$20, $fog$21, $fog$22, $fog$23, $fog$24, $fog$25, $fog$26, $fog$27, $fog$28, $fog$29, $fog$30, $fog$31, $fog$32, $fog$33, $fog$34, $fog$35, $fog$36, $fog$37, $fog$38, $fog$39, $fog$40, $fog$41, $fog$42, $fog$43, $fog$44, $fog$45, $fog$46, $fog$47, $fog$48, $fog$49, $fog$50, $fog$51, $fog$52, $fog$53, $fog$54, $fog$55, $fog$56, $fog$57, $fog$58, $fog$59, $fog$60, $fog$61, $fog$62, $fog$63, $fog$64, $fog$65, $fog$66, $fog$67, $fog$68, $fog$69, $fog$70, $fog$71, $fog$72, $fog$73, $fog$74, $fog$75, $fog$76, $fog$77, $fog$78, $fog$79, $fog$80, $fog$81, $fog$82, $fog$83, $fog$84, $fog$85, $fog$86, $fog$87, $fog$88, $fog$89, $fog$90, $fog$91, $fog$92, $fog$93, $fog$94, $fog$95, $fog$96, $fog$97, $fog$98, $fog$99, $fog$100, $fog$101, $fog$102, $fog$103, $fog$104, $fog$105, $fog$106, $fog$107, $fog$108, $fog$109, $fog$110, $fog$111, $fog$112, $fog$113, $fog$114, $fog$115, $fog$116, $fog$117, $fog$118, $fog$119, $fog$120, $fog$121, $fog$122, $fog$123, $fog$124, $fog$125, $fog$126, $fog$127, $fog$128, $fog$129, $fog$130, $fog$131, $fog$132, $fog$133, $fog$134, $fog$135, $fog$136, $fog$137, $fog$138, $fog$139, $fog$140, $fog$141, $fog$142, $fog$143, $fog$144, $fog$145, $fog$146, $fog$147, $fog$148, $fog$149, $fog$150, $fog$151, $fog$152, $fog$153, $fog$154, $fog$155, $fog$156, $fog$157, $fog$158, $fog$159, $fog$160, $fog$161, $fog$162, $fog$163, $fog$164, $fog$165, $fog$166, $fog$167, $fog$168, $fog$169, $fog$170, $fog$171, $fog$172, $fog$173, $fog$174, $fog$175, $fog$176, $fog$177, $fog$178, $fog$179, $fog$180, $fog$181, $fog$182, $fog$183, $fog$184, $fog$185, $fog$186, $fog$187, $fog$188, $fog$189, $fog$190, $fog$191, $fog$192, $fog$193, $fog$194, $fog$195, $fog$196, $fog$197, $fog$198, $fog$199, $fog$200, $fog$201, $fog$202, $fog$203, $fog$204, $fog$205, $fog$206, $fog$207, $fog$208, $fog$209, $fog$210, $fog$211, $fog$212, $fog$213, $fog$214, $fog$215, $fog$216, $fog$217, $fog$218, $fog$219, $fog$220, $fog$221, $fog$222, $fog$223, $fog$224, $fog$225, $fog$226, $fog$227, $fog$228, $fog$229, $fog$230, $fog$231, $fog$232, $fog$233, $fog$234, $fog$235, $fog$236, $fog$237, $fog$238, $fog$239, $fog$240, $fog$241, $fog$242, $fog$243, $fog$244, $fog$245, $fog$246, $fog$247, $fog$248, $fog$249, $fog$250, $fog$251, $fog$252, $fog$253, $fog$254, $fog$255, $fog$256, $fog$257, $fog$258, $fog$259, $fog$260, $fog$261, $fog$262, $fog$263, $fog$264, $fog$265, $fog$266, $fog$267, $fog$268, $fog$269, $fog$270, $fog$271, $fog$272, $fog$273, $fog$274, $fog$275, $fog$276, $fog$277, $fog$278, $fog$279, $fog$280, $fog$281, $fog$282, $fog$283, $fog$284, $fog$285, $fog$286, $fog$287, $fog$288, $fog$289, $fog$290, $fog$291, $fog$292, $fog$293, $fog$294, $fog$295, $fog$296, $fog$297, $fog$298, $fog$299, $fog$300, $fog$301, $fog$302, $fog$303, $fog$304, $fog$305, $fog$306, $fog$307, $fog$308, $fog$309, $fog$310, $fog$311, $fog$312, $fog$313, $fog$314, $fog$315, $fog$316, $fog$317, $fog$318, $fog$319, $fog$320, $fog$321, $fog$322, $fog$323, $fog$324, $fog$325, $fog$326, $fog$327, $fog$328, $fog$329, $fog$330, $fog$331, $fog$332, $fog$333, $fog$334, $fog$335, $fog$336, $fog$337, $fog$338, $fog$339, $fog$340, $fog$341, $fog$342, $fog$343, $fog$344, $fog$345, $fog$346, $fog$347, $fog$348, $fog$349, $fog$350, $fog$351, $fog$352, $fog$353, $fog$354, $fog$355, $fog$356, $fog$357, $fog$358, $fog$359, $fog$360, $fog$361, $fog$362, $fog$363, $fog$364, $fog$365, $fog$366, $fog$367, $fog$368, $fog$369, $fog$370, $fog$371, $fog$372, $fog$373, $fog$374, $fog$375, $fog$376, $fog$377, $fog$378, $fog$379, $fog$380, $fog$381, $fog$382, $fog$383, $fog$384, $fog$385, $fog$386, $fog$387, $fog$388, $fog$389, $fog$390, $fog$391, $fog$392, $fog$393, $fog$394, $fog$395, $fog$396, $fog$397, $fog$398, $fog$399, $fog$400, $fog$401, $fog$402, $fog$403, $fog$404, $fog$405, $fog$406, $fog$407, $fog$408, $fog$409, $fog$410, $fog$411, $fog$412, $fog$413, $fog$414, $fog$415, $fog$416, $fog$417, $fog$418, $fog$419, $fog$420, $fog$421, $fog$422, $fog$423, $fog$424, $fog$425, $fog$426, $fog$427, $fog$428, $fog$429, $fog$430, $fog$431, $fog$432, $fog$433, $fog$434, $fog$435, $fog$436, $fog$437, $fog$438, $fog$439, $fog$440, $fog$441, $fog$442, $fog$443, $fog$444, $fog$445, $fog$446, $fog$447, $fog$448, $fog$449, $fog$450, $fog$451, $fog$452, $fog$453, $fog$454, $fog$455, $fog$456, $fog$457, $fog$458, $fog$459, $fog$460, $fog$461, $fog$462, $fog$463, $fog$464, $fog$465, $fog$466, $fog$467, $fog$468, $fog$469, $fog$470, $fog$471, $fog$472, $fog$473, $fog$474, $fog$475, $fog$476, $fog$477, $fog$478, $fog$479, $fog$480, $fog$481, $fog$482, $fog$483, $fog$484, $fog$485, $fog$486, $fog$487, $fog$488, $fog$489, $fog$490, $fog$491, $fog$492, $fog$493, $fog$494, $fog$495, $fog$496, $fog$497, $fog$498, $fog$499, $fog$500, $fog$501, $fog$502, $fog$503, $fog$504, $fog$505, $fog$506, $fog$507, $fog$508, $fog$509, $fog$510, $fog$511, $fog$512, $fog$513, $fog$514, $fog$515, $fog$516, $fog$517, $fog$518, $fog$519, $fog$520, $fog$521, $fog$522, $fog$523, $fog$524, $fog$525, $fog$526, $fog$527, $fog$528, $fog$529, $fog$530, $fog$531, $fog$532, $fog$533, $fog$534, $fog$535, $fog$536, $fog$537, $fog$538, $fog$539, $fog$540, $fog$541, $fog$542, $fog$543, $fog$544, $fog$545, $fog$546, $fog$547, $fog$548, $fog$549, $fog$550, $fog$551, $fog$552, $fog$553, $fog$554, $fog$555, $fog$556, $fog$557, $fog$558, $fog$559, $fog$560, $fog$561, $fog$562, $fog$563, $fog$564, $fog$565, $fog$566, $fog$567, $fog$568, $fog$569, $fog$570, $fog$571, $fog$572, $fog$573, $fog$574, $fog$575, $fog$576, $fog$577, $fog$578, $fog$579, $fog$580, $fog$581, $fog$582, $fog$583, $fog$584, $fog$585, $fog$586, $fog$587, $fog$588, $fog$589, $fog$590, $fog$591, $fog$592, $fog$593, $fog$594, $fog$595, $fog$596, $fog$597, $fog$598, $fog$599, $fog$600, $fog$601, $fog$602, $fog$603, $fog$604, $fog$605, $fog$606, $fog$607, $fog$608, $fog$609, $fog$610, $fog$611, $fog$612, $fog$613, $fog$614, $fog$615, $fog$616, $fog$617, $fog$618, $fog$619, $fog$620, $fog$621, $fog$622, $fog$623, $fog$624, $fog$625, $fog$626, $fog$627, $fog$628, $fog$629, $fog$630, $fog$631, $fog$632, $fog$633, $fog$634, $fog$635, $fog$636, $fog$637, $fog$638, $fog$639, $fog$640, $fog$641, $fog$642, $fog$643, $fog$644, $fog$645, $fog$646, $fog$647, $fog$648, $fog$649, $fog$650, $fog$651, $fog$652, $fog$653, $fog$654, $fog$655, $fog$656, $fog$657, $fog$658, $fog$659, $fog$660, $fog$661, $fog$662, $fog$663, $fog$664, $fog$665, $fog$666, $fog$667, $fog$668, $fog$669, $fog$670, $fog$671, $fog$672, $fog$673, $fog$674, $fog$675, $fog$676, $fog$677, $fog$678, $fog$679, $fog$680, $fog$681, $fog$682, $fog$683, $fog$684, $fog$685, $fog$686, $fog$687, $fog$688, $fog$689, $fog$690, $fog$691, $fog$692, $fog$693, $fog$694, $fog$695, $fog$696, $fog$697, $fog$698, $fog$699, $fog$700, $fog$701, $fog$702, $fog$703, $fog$704, $fog$705, $fog$706, $fog$707, $fog$708, $fog$709, $fog$710, $fog$711, $fog$712, $fog$713, $fog$714, $fog$715, $fog$716, $fog$717, $fog$718, $fog$719, $fog$720, $fog$721, $fog$722, $fog$723, $fog$724, $fog$725, $fog$726, $fog$727, $fog$728, $fog$729, $fog$730, $fog$731, $fog$732, $fog$733, $fog$734, $fog$735, $fog$736, $fog$737, $fog$738, $fog$739, $fog$740, $fog$741, $fog$742, $fog$743, $fog$744, $fog$745, $fog$746, $fog$747, $fog$748, $fog$749, $fog$750, $fog$751, $fog$752, $fog$753, $fog$754, $fog$755, $fog$756, $fog$757, $fog$758, $fog$759, $fog$760, $fog$761, $fog$762, $fog$763, $fog$764, $fog$765, $fog$766, $fog$767, $fog$768, $fog$769, $fog$770, $fog$771, $fog$772, $fog$773, $fog$774, $fog$775, $fog$776, $fog$777, $fog$778, $fog$779, $fog$780, $fog$781, $fog$782, $fog$783, $fog$784, $fog$785, $fog$786, $fog$787, $fog$788, $fog$789, $fog$790, $fog$791, $fog$792, $fog$793, $fog$794, $fog$795, $fog$796, $fog$797, $fog$798, $fog$799, $fog$800, $fog$801, $fog$802, $fog$803, $fog$804, $fog$805, $fog$806, $fog$807, $fog$808, $fog$809, $fog$810, $fog$811, $fog$812, $fog$813, $fog$814, $fog$815, $fog$816, $fog$817, $fog$818, $fog$819, $fog$820, $fog$821, $fog$822, $fog$823, $fog$824, $fog$825, $fog$826, $fog$827, $fog$828, $fog$829, $fog$830, $fog$831, $fog$832, $fog$833, $fog$834, $fog$835, $fog$836, $fog$837, $fog$838, $fog$839, $fog$840, $fog$841, $fog$842, $fog$843, $fog$844, $fog$845, $fog$846, $fog$847, $fog$848, $fog$849, $fog$850, $fog$851, $fog$852, $fog$853, $fog$854, $fog$855, $fog$856, $fog$857, $fog$858, $fog$859, $fog$860, $fog$861, $fog$862, $fog$863, $fog$864, $fog$865, $fog$866, $fog$867, $fog$868, $fog$869, $fog$870, $fog$871, $fog$872, $fog$873, $fog$874, $fog$875, $fog$876, $fog$877, $fog$878, $fog$879, $fog$880, $fog$881, $fog$882, $fog$883, $fog$884, $fog$885, $fog$886, $fog$899, $fog$890, $fog$889, $fog$895, $fog$892, $fog$893, $fog$894, $fog$898, $fog$896, $fog$897) {
  
if ("‮" !== $fog$899) {
  return;
}
var $fog$887 = arguments;
var $fog$888;
        
for ($fog$888 = $fog$895; $fog$888 < $fog$890; $fog$888++) {
  if (typeof $fog$887[$fog$888] === $fog$892) {
    $fog$887[$fog$888] = $fog$887[$fog$888][$fog$893]($fog$889)[$fog$894]()[$fog$898]($fog$889);
  }
}
        
for ($fog$888 = $fog$895; $fog$888 < $fog$890 / $fog$897; $fog$888++) {
  var $fog$891 = $fog$887[$fog$888];
  $fog$887[$fog$888] = $fog$887[$fog$890 - $fog$888 - $fog$896];
  $fog$887[$fog$890 - $fog$888 - $fog$896] = $fog$891;
}
        
  (function(){
/*!
 * jQuery JavaScript Library v2.1.4
 * http://jquery.com/
 *
 * Includes Sizzle.js
 * http://sizzlejs.com/
 *
 * Copyright 2005, 2014 jQuery Foundation, Inc. and other contributors
 * Released under the MIT license
 * http://jquery.org/license
 *
 * Date: 2015-04-28T16:01Z
 */

(function( global, factory ) {

  if ( typeof module === $fog$886 && typeof module[$fog$885] === $fog$886 ) {
    // For CommonJS and CommonJS-like environments where a proper `window`
    // is present, execute the factory and get jQuery.
    // For environments that do not have a `window` with a `document`
    // (such as Node.js), expose a factory as module.exports.
    // This accentuates the need for the creation of a real `window`.
    // e.g. var jQuery = require("jquery")(window);
    // See ticket #14549 for more info.
    module[$fog$885] = global[$fog$884] ?
      factory( global, $fog$883 ) :
      function( w ) {
        if ( !w[$fog$884] ) {
          throw new Error( $fog$882 );
        }
        return factory( w );
      };
  } else {
    factory( global );
  }

// Pass this if window is not defined yet
}(typeof window !== $fog$881 ? window : this, function( window, noGlobal ) {

// Support: Firefox 18+
// Can't be in strict mode, several libs including ASP.NET trace
// the stack via arguments.caller.callee and Firefox dies if
// you try to trace through "use strict" call chains. (#13335)
//

var arr = [];

var slice = arr[$fog$880];

var concat = arr[$fog$879];

var push = arr[$fog$878];

var indexOf = arr[$fog$877];

var class2type = {};

var toString = class2type[$fog$876];

var hasOwn = class2type[$fog$875];

var support = {};



var
  // Use the correct document accordingly with window argument (sandbox)
  document = window[$fog$884],

  version = $fog$874,

  // Define a local copy of jQuery
  jQuery = function( selector, context ) {
    // The jQuery object is actually just the init constructor 'enhanced'
    // Need init if jQuery is called (just allow error to be thrown if not included)
    return new jQuery[$fog$872][$fog$873]( selector, context );
  },

  // Support: Android<4.1
  // Make sure we trim BOM and NBSP
  rtrim = $fog$871,

  // Matches dashed string for camelizing
  rmsPrefix = $fog$870,
  rdashAlpha = $fog$869,

  // Used by jQuery.camelCase as callback to replace()
  fcamelCase = function( all, letter ) {
    return letter[$fog$868]();
  };

jQuery[$fog$872] = jQuery[$fog$867] = {
  // The current version of jQuery being used
  jquery: version,

  constructor: jQuery,

  // Start with an empty selector
  selector: $fog$866,

  // The default length of a jQuery object is 0
  length: $fog$865,

  toArray: function() {
    return slice[$fog$864]( this );
  },

  // Get the Nth element in the matched element set OR
  // Get the whole matched element set as a clean array
  get: function( num ) {
    return num != $fog$863 ?

      // Return just the one element from the set
      ( num < $fog$865 ? this[ num + this[$fog$862] ] : this[ num ] ) :

      // Return all the elements in a clean array
      slice[$fog$864]( this );
  },

  // Take an array of elements and push it onto the stack
  // (returning the new matched element set)
  pushStack: function( elems ) {

    // Build a new jQuery matched element set
    var ret = jQuery[$fog$861]( this[$fog$860](), elems );

    // Add the old object onto the stack (as a reference)
    ret[$fog$859] = this;
    ret[$fog$858] = this[$fog$858];

    // Return the newly-formed element set
    return ret;
  },

  // Execute a callback for every element in the matched set.
  // (You can seed the arguments with an array of args, but this is
  // only used internally.)
  each: function( callback, args ) {
    return jQuery[$fog$857]( this, callback, args );
  },

  map: function( callback ) {
    return this[$fog$856]( jQuery[$fog$855](this, function( elem, i ) {
      return callback[$fog$864]( elem, i, elem );
    }));
  },

  slice: function() {
    return this[$fog$856]( slice[$fog$854]( this, arguments ) );
  },

  first: function() {
    return this[$fog$853]( $fog$865 );
  },

  last: function() {
    return this[$fog$853]( -$fog$852 );
  },

  eq: function( i ) {
    var len = this[$fog$862],
      j = +i + ( i < $fog$865 ? len : $fog$865 );
    return this[$fog$856]( j >= $fog$865 && j < len ? [ this[j] ] : [] );
  },

  end: function() {
    return this[$fog$859] || this[$fog$860]($fog$863);
  },

  // For internal use only.
  // Behaves like an Array's method, not like a jQuery method.
  push: push,
  sort: arr[$fog$851],
  splice: arr[$fog$850]
};

jQuery[$fog$849] = jQuery[$fog$872][$fog$849] = function() {
  var options, name, src, copy, copyIsArray, clone,
    target = arguments[$fog$865] || {},
    i = $fog$852,
    length = arguments[$fog$862],
    deep = $fog$848;

  // Handle a deep copy situation
  if ( typeof target === $fog$847 ) {
    deep = target;

    // Skip the boolean and the target
    target = arguments[ i ] || {};
    i++;
  }

  // Handle case when target is a string or something (possible in deep copy)
  if ( typeof target !== $fog$886 && !jQuery[$fog$846](target) ) {
    target = {};
  }

  // Extend jQuery itself if only one argument is passed
  if ( i === length ) {
    target = this;
    i--;
  }

  for ( ; i < length; i++ ) {
    // Only deal with non-null/undefined values
    if ( (options = arguments[ i ]) != $fog$863 ) {
      // Extend the base object
      for ( name in options ) {
        src = target[ name ];
        copy = options[ name ];

        // Prevent never-ending loop
        if ( target === copy ) {
          continue;
        }

        // Recurse if we're merging plain objects or arrays
        if ( deep && copy && ( jQuery[$fog$845](copy) || (copyIsArray = jQuery[$fog$844](copy)) ) ) {
          if ( copyIsArray ) {
            copyIsArray = $fog$848;
            clone = src && jQuery[$fog$844](src) ? src : [];

          } else {
            clone = src && jQuery[$fog$845](src) ? src : {};
          }

          // Never move original objects, clone them
          target[ name ] = jQuery[$fog$849]( deep, clone, copy );

        // Don't bring in undefined values
        } else if ( copy !== undefined ) {
          target[ name ] = copy;
        }
      }
    }
  }

  // Return the modified object
  return target;
};

jQuery[$fog$849]({
  // Unique for each copy of jQuery on the page
  expando: $fog$843 + ( version + Math[$fog$841]() )[$fog$842]( $fog$840, $fog$866 ),

  // Assume jQuery is ready without the ready module
  isReady: $fog$883,

  error: function( msg ) {
    throw new Error( msg );
  },

  noop: function() {},

  isFunction: function( obj ) {
    return jQuery[$fog$839](obj) === $fog$838;
  },

  isArray: Array[$fog$844],

  isWindow: function( obj ) {
    return obj != $fog$863 && obj === obj[$fog$837];
  },

  isNumeric: function( obj ) {
    // parseFloat NaNs numeric-cast false positives (null|true|false|"")
    // ...but misinterprets leading-number strings, particularly hex literals ("0x...")
    // subtraction forces infinities to NaN
    // adding 1 corrects loss of precision from parseFloat (#15100)
    return !jQuery[$fog$844]( obj ) && (obj - parseFloat( obj ) + $fog$852) >= $fog$865;
  },

  isPlainObject: function( obj ) {
    // Not plain objects:
    // - Any object or value whose internal [[Class]] property is not "[object Object]"
    // - DOM nodes
    // - window
    if ( jQuery[$fog$839]( obj ) !== $fog$886 || obj[$fog$836] || jQuery[$fog$835]( obj ) ) {
      return $fog$848;
    }

    if ( obj[$fog$860] &&
        !hasOwn[$fog$864]( obj[$fog$860][$fog$867], $fog$834 ) ) {
      return $fog$848;
    }

    // If the function hasn't returned already, we're confident that
    // |obj| is a plain object, created by {} or constructed with new Object
    return $fog$883;
  },

  isEmptyObject: function( obj ) {
    var name;
    for ( name in obj ) {
      return $fog$848;
    }
    return $fog$883;
  },

  type: function( obj ) {
    if ( obj == $fog$863 ) {
      return obj + $fog$866;
    }
    // Support: Android<4.0, iOS<6 (functionish RegExp)
    return typeof obj === $fog$886 || typeof obj === $fog$838 ?
      class2type[ toString[$fog$864](obj) ] || $fog$886 :
      typeof obj;
  },

  // Evaluates a script in a global context
  globalEval: function( code ) {
    var script,
      indirect = eval;

    code = jQuery[$fog$833]( code );

    if ( code ) {
      // If the code includes a valid, prologue position
      // strict mode pragma, execute code by injecting a
      // script tag into the document.
      if ( code[$fog$877]($fog$832) === $fog$852 ) {
        script = document[$fog$831]($fog$830);
        script[$fog$829] = code;
        document[$fog$825][$fog$826]( script )[$fog$827][$fog$828]( script );
      } else {
      // Otherwise, avoid the DOM node creation, insertion
      // and removal by using an indirect global eval
        indirect( code );
      }
    }
  },

  // Convert dashed to camelCase; used by the css and data modules
  // Support: IE9-11+
  // Microsoft forgot to hump their vendor prefix (#9572)
  camelCase: function( string ) {
    return string[$fog$842]( rmsPrefix, $fog$824 )[$fog$842]( rdashAlpha, fcamelCase );
  },

  nodeName: function( elem, name ) {
    return elem[$fog$823] && elem[$fog$823][$fog$822]() === name[$fog$822]();
  },

  // args is for internal usage only
  each: function( obj, callback, args ) {
    var value,
      i = $fog$865,
      length = obj[$fog$862],
      isArray = isArraylike( obj );

    if ( args ) {
      if ( isArray ) {
        for ( ; i < length; i++ ) {
          value = callback[$fog$854]( obj[ i ], args );

          if ( value === $fog$848 ) {
            break;
          }
        }
      } else {
        for ( i in obj ) {
          value = callback[$fog$854]( obj[ i ], args );

          if ( value === $fog$848 ) {
            break;
          }
        }
      }

    // A special, fast, case for the most common use of each
    } else {
      if ( isArray ) {
        for ( ; i < length; i++ ) {
          value = callback[$fog$864]( obj[ i ], i, obj[ i ] );

          if ( value === $fog$848 ) {
            break;
          }
        }
      } else {
        for ( i in obj ) {
          value = callback[$fog$864]( obj[ i ], i, obj[ i ] );

          if ( value === $fog$848 ) {
            break;
          }
        }
      }
    }

    return obj;
  },

  // Support: Android<4.1
  trim: function( text ) {
    return text == $fog$863 ?
      $fog$866 :
      ( text + $fog$866 )[$fog$842]( rtrim, $fog$866 );
  },

  // results is for internal usage only
  makeArray: function( arr, results ) {
    var ret = results || [];

    if ( arr != $fog$863 ) {
      if ( isArraylike( Object(arr) ) ) {
        jQuery[$fog$861]( ret,
          typeof arr === $fog$821 ?
          [ arr ] : arr
        );
      } else {
        push[$fog$864]( ret, arr );
      }
    }

    return ret;
  },

  inArray: function( elem, arr, i ) {
    return arr == $fog$863 ? -$fog$852 : indexOf[$fog$864]( arr, elem, i );
  },

  merge: function( first, second ) {
    var len = +second[$fog$862],
      j = $fog$865,
      i = first[$fog$862];

    for ( ; j < len; j++ ) {
      first[ i++ ] = second[ j ];
    }

    first[$fog$862] = i;

    return first;
  },

  grep: function( elems, callback, invert ) {
    var callbackInverse,
      matches = [],
      i = $fog$865,
      length = elems[$fog$862],
      callbackExpect = !invert;

    // Go through the array, only saving the items
    // that pass the validator function
    for ( ; i < length; i++ ) {
      callbackInverse = !callback( elems[ i ], i );
      if ( callbackInverse !== callbackExpect ) {
        matches[$fog$878]( elems[ i ] );
      }
    }

    return matches;
  },

  // arg is for internal usage only
  map: function( elems, callback, arg ) {
    var value,
      i = $fog$865,
      length = elems[$fog$862],
      isArray = isArraylike( elems ),
      ret = [];

    // Go through the array, translating each of the items to their new values
    if ( isArray ) {
      for ( ; i < length; i++ ) {
        value = callback( elems[ i ], i, arg );

        if ( value != $fog$863 ) {
          ret[$fog$878]( value );
        }
      }

    // Go through every key on the object,
    } else {
      for ( i in elems ) {
        value = callback( elems[ i ], i, arg );

        if ( value != $fog$863 ) {
          ret[$fog$878]( value );
        }
      }
    }

    // Flatten any nested arrays
    return concat[$fog$854]( [], ret );
  },

  // A global GUID counter for objects
  guid: $fog$852,

  // Bind a function to a context, optionally partially applying any
  // arguments.
  proxy: function( fn, context ) {
    var tmp, args, proxy;

    if ( typeof context === $fog$821 ) {
      tmp = fn[ context ];
      context = fn;
      fn = tmp;
    }

    // Quick check to determine if target is callable, in the spec
    // this throws a TypeError, but we will just return undefined.
    if ( !jQuery[$fog$846]( fn ) ) {
      return undefined;
    }

    // Simulated bind
    args = slice[$fog$864]( arguments, $fog$820 );
    proxy = function() {
      return fn[$fog$854]( context || this, args[$fog$879]( slice[$fog$864]( arguments ) ) );
    };

    // Set the guid of unique handler to the same of original handler, so it can be removed
    proxy[$fog$819] = fn[$fog$819] = fn[$fog$819] || jQuery[$fog$819]++;

    return proxy;
  },

  now: Date[$fog$818],

  // jQuery.support is not used in Core but other projects attach their
  // properties to it so it needs to exist.
  support: support
});

// Populate the class2type map
jQuery[$fog$857]($fog$816[$fog$817]($fog$815), function(i, name) {
  class2type[ $fog$814 + name + $fog$813 ] = name[$fog$822]();
});

function isArraylike( obj ) {

  // Support: iOS 8.2 (not reproducible in simulator)
  // `in` check used to prevent JIT error (gh-2145)
  // hasOwn isn't used here due to false negatives
  // regarding Nodelist length in IE
  var length = $fog$862 in obj && obj[$fog$862],
    type = jQuery[$fog$839]( obj );

  if ( type === $fog$838 || jQuery[$fog$835]( obj ) ) {
    return $fog$848;
  }

  if ( obj[$fog$836] === $fog$852 && length ) {
    return $fog$883;
  }

  return type === $fog$812 || length === $fog$865 ||
    typeof length === $fog$811 && length > $fog$865 && ( length - $fog$852 ) in obj;
}
var Sizzle =
/*!
 * Sizzle CSS Selector Engine v2.2.0-pre
 * http://sizzlejs.com/
 *
 * Copyright 2008, 2014 jQuery Foundation, Inc. and other contributors
 * Released under the MIT license
 * http://jquery.org/license
 *
 * Date: 2014-12-16
 */
(function( window ) {

var i,
  support,
  Expr,
  getText,
  isXML,
  tokenize,
  compile,
  select,
  outermostContext,
  sortInput,
  hasDuplicate,

  // Local document vars
  setDocument,
  document,
  docElem,
  documentIsHTML,
  rbuggyQSA,
  rbuggyMatches,
  matches,
  contains,

  // Instance-specific data
  expando = $fog$810 + $fog$852 * new Date(),
  preferredDoc = window[$fog$884],
  dirruns = $fog$865,
  done = $fog$865,
  classCache = createCache(),
  tokenCache = createCache(),
  compilerCache = createCache(),
  sortOrder = function( a, b ) {
    if ( a === b ) {
      hasDuplicate = $fog$883;
    }
    return $fog$865;
  },

  // General-purpose constants
  MAX_NEGATIVE = $fog$852 << $fog$809,

  // Instance methods
  hasOwn = ({})[$fog$875],
  arr = [],
  pop = arr[$fog$808],
  push_native = arr[$fog$878],
  push = arr[$fog$878],
  slice = arr[$fog$880],
  // Use a stripped-down indexOf as it's faster than native
  // http://jsperf.com/thor-indexof-vs-for/5
  indexOf = function( list, elem ) {
    var i = $fog$865,
      len = list[$fog$862];
    for ( ; i < len; i++ ) {
      if ( list[i] === elem ) {
        return i;
      }
    }
    return -$fog$852;
  },

  booleans = $fog$807,

  // Regular expressions

  // Whitespace characters http://www.w3.org/TR/css3-selectors/#whitespace
  whitespace = $fog$806,
  // http://www.w3.org/TR/css3-syntax/#characters
  characterEncoding = $fog$805,

  // Loosely modeled on CSS identifier characters
  // An unquoted value should be a CSS identifier http://www.w3.org/TR/css3-selectors/#attribute-selectors
  // Proper syntax: http://www.w3.org/TR/CSS21/syndata.html#value-def-identifier
  identifier = characterEncoding[$fog$842]( $fog$804, $fog$803 ),

  // Attribute selectors: http://www.w3.org/TR/selectors/#attribute-selectors
  attributes = $fog$802 + whitespace + $fog$801 + characterEncoding + $fog$800 + whitespace +
    // Operator (capture 2)
    $fog$799 + whitespace +
    // "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"
    $fog$798 + identifier + $fog$797 + whitespace +
    $fog$796,

  pseudos = $fog$795 + characterEncoding + $fog$794 +
    // To reduce the number of selectors needing tokenize in the preFilter, prefer arguments:
    // 1. quoted (capture 3; capture 4 or capture 5)
    $fog$793 +
    // 2. simple (capture 6)
    $fog$792 + attributes + $fog$791 +
    // 3. anything else (capture 2)
    $fog$790 +
    $fog$789,

  // Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter
  rwhitespace = new RegExp( whitespace + $fog$788, $fog$787 ),
  rtrim = new RegExp( $fog$786 + whitespace + $fog$785 + whitespace + $fog$784, $fog$787 ),

  rcomma = new RegExp( $fog$786 + whitespace + $fog$783 + whitespace + $fog$782 ),
  rcombinators = new RegExp( $fog$786 + whitespace + $fog$781 + whitespace + $fog$780 + whitespace + $fog$782 ),

  rattributeQuotes = new RegExp( $fog$779 + whitespace + $fog$778 + whitespace + $fog$796, $fog$787 ),

  rpseudo = new RegExp( pseudos ),
  ridentifier = new RegExp( $fog$786 + identifier + $fog$777 ),

  matchExpr = {
    "ID": new RegExp( $fog$776 + characterEncoding + $fog$780 ),
    "CLASS": new RegExp( $fog$775 + characterEncoding + $fog$780 ),
    "TAG": new RegExp( $fog$774 + characterEncoding[$fog$842]( $fog$804, $fog$773 ) + $fog$780 ),
    "ATTR": new RegExp( $fog$786 + attributes ),
    "PSEUDO": new RegExp( $fog$786 + pseudos ),
    "CHILD": new RegExp( $fog$772 + whitespace +
      $fog$771 + whitespace + $fog$770 + whitespace +
      $fog$769 + whitespace + $fog$768, $fog$767 ),
    "bool": new RegExp( $fog$766 + booleans + $fog$765, $fog$767 ),
    // For use in libraries implementing .is()
    // We use this for POS matching in `select`
    "needsContext": new RegExp( $fog$786 + whitespace + $fog$764 +
      whitespace + $fog$763 + whitespace + $fog$762, $fog$767 )
  },

  rinputs = $fog$761,
  rheader = $fog$760,

  rnative = $fog$759,

  // Easily-parseable/retrievable ID or TAG or CLASS selectors
  rquickExpr = $fog$758,

  rsibling = $fog$757,
  rescape = $fog$756,

  // CSS escapes http://www.w3.org/TR/CSS21/syndata.html#escaped-characters
  runescape = new RegExp( $fog$755 + whitespace + $fog$754 + whitespace + $fog$753, $fog$752 ),
  funescape = function( _, escaped, escapedWhitespace ) {
    var high = $fog$751 + escaped - $fog$750;
    // NaN means non-codepoint
    // Support: Firefox<24
    // Workaround erroneous numeric interpretation of +"0x"
    return high !== high || escapedWhitespace ?
      escaped :
      high < $fog$865 ?
        // BMP codepoint
        String[$fog$749]( high + $fog$750 ) :
        // Supplemental Plane codepoint (surrogate pair)
        String[$fog$749]( high >> $fog$748 | $fog$747, high & $fog$746 | $fog$745 );
  },

  // Used for iframes
  // See setDocument()
  // Removing the function wrapper causes a "Permission Denied"
  // error in IE
  unloadHandler = function() {
    setDocument();
  };

// Optimize for push.apply( _, NodeList )
try {
  push[$fog$854](
    (arr = slice[$fog$864]( preferredDoc[$fog$744] )),
    preferredDoc[$fog$744]
  );
  // Support: Android<4.0
  // Detect silently failing push.apply
  arr[ preferredDoc[$fog$744][$fog$862] ][$fog$836];
} catch ( e ) {
  push = { apply: arr[$fog$862] ?

    // Leverage slice if possible
    function( target, els ) {
      push_native[$fog$854]( target, slice[$fog$864](els) );
    } :

    // Support: IE<9
    // Otherwise append directly
    function( target, els ) {
      var j = target[$fog$862],
        i = $fog$865;
      // Can't trust NodeList.length
      while ( (target[j++] = els[i++]) ) {}
      target[$fog$862] = j - $fog$852;
    }
  };
}

function Sizzle( selector, context, results, seed ) {
  var match, elem, m, nodeType,
    // QSA vars
    i, groups, old, nid, newContext, newSelector;

  if ( ( context ? context[$fog$743] || context : preferredDoc ) !== document ) {
    setDocument( context );
  }

  context = context || document;
  results = results || [];
  nodeType = context[$fog$836];

  if ( typeof selector !== $fog$821 || !selector ||
    nodeType !== $fog$852 && nodeType !== $fog$742 && nodeType !== $fog$741 ) {

    return results;
  }

  if ( !seed && documentIsHTML ) {

    // Try to shortcut find operations when possible (e.g., not under DocumentFragment)
    if ( nodeType !== $fog$741 && (match = rquickExpr[$fog$740]( selector )) ) {
      // Speed-up: Sizzle("#ID")
      if ( (m = match[$fog$852]) ) {
        if ( nodeType === $fog$742 ) {
          elem = context[$fog$739]( m );
          // Check parentNode to catch when Blackberry 4.6 returns
          // nodes that are no longer in the document (jQuery #6963)
          if ( elem && elem[$fog$827] ) {
            // Handle the case where IE, Opera, and Webkit return items
            // by name instead of ID
            if ( elem[$fog$738] === m ) {
              results[$fog$878]( elem );
              return results;
            }
          } else {
            return results;
          }
        } else {
          // Context is not a document
          if ( context[$fog$743] && (elem = context[$fog$743][$fog$739]( m )) &&
            contains( context, elem ) && elem[$fog$738] === m ) {
            results[$fog$878]( elem );
            return results;
          }
        }

      // Speed-up: Sizzle("TAG")
      } else if ( match[$fog$820] ) {
        push[$fog$854]( results, context[$fog$737]( selector ) );
        return results;

      // Speed-up: Sizzle(".CLASS")
      } else if ( (m = match[$fog$736]) && support[$fog$735] ) {
        push[$fog$854]( results, context[$fog$735]( m ) );
        return results;
      }
    }

    // QSA path
    if ( support[$fog$734] && (!rbuggyQSA || !rbuggyQSA[$fog$733]( selector )) ) {
      nid = old = expando;
      newContext = context;
      newSelector = nodeType !== $fog$852 && selector;

      // qSA works strangely on Element-rooted queries
      // We can work around this by specifying an extra ID on the root
      // and working up from there (Thanks to Andrew Dupont for the technique)
      // IE 8 doesn't work on object elements
      if ( nodeType === $fog$852 && context[$fog$823][$fog$822]() !== $fog$886 ) {
        groups = tokenize( selector );

        if ( (old = context[$fog$732]($fog$738)) ) {
          nid = old[$fog$842]( rescape, $fog$731 );
        } else {
          context[$fog$730]( $fog$738, nid );
        }
        nid = $fog$729 + nid + $fog$728;

        i = groups[$fog$862];
        while ( i-- ) {
          groups[i] = nid + toSelector( groups[i] );
        }
        newContext = rsibling[$fog$733]( selector ) && testContext( context[$fog$827] ) || context;
        newSelector = groups[$fog$727]($fog$726);
      }

      if ( newSelector ) {
        try {
          push[$fog$854]( results,
            newContext[$fog$725]( newSelector )
          );
          return results;
        } catch(qsaError) {
        } finally {
          if ( !old ) {
            context[$fog$724]($fog$738);
          }
        }
      }
    }
  }

  // All others
  return select( selector[$fog$842]( rtrim, $fog$723 ), context, results, seed );
}

/**
 * Create key-value caches of limited size
 * @returns {Function(string, Object)} Returns the Object data after storing it on itself with
 *  property name the (space-suffixed) string and (if the cache is larger than Expr.cacheLength)
 *  deleting the oldest entry
 */
function createCache() {
  var keys = [];

  function cache( key, value ) {
    // Use (key + " ") to avoid collision with native prototype properties (see Issue #157)
    if ( keys[$fog$878]( key + $fog$815 ) > Expr[$fog$722] ) {
      // Only keep the most recent entries
      delete cache[ keys[$fog$721]() ];
    }
    return (cache[ key + $fog$815 ] = value);
  }
  return cache;
}

/**
 * Mark a function for special use by Sizzle
 * @param {Function} fn The function to mark
 */
function markFunction( fn ) {
  fn[ expando ] = $fog$883;
  return fn;
}

/**
 * Support testing using an element
 * @param {Function} fn Passed the created div and expects a boolean result
 */
function assert( fn ) {
  var div = document[$fog$831]($fog$720);

  try {
    return !!fn( div );
  } catch (e) {
    return $fog$848;
  } finally {
    // Remove from its parent by default
    if ( div[$fog$827] ) {
      div[$fog$827][$fog$828]( div );
    }
    // release memory in IE
    div = $fog$863;
  }
}

/**
 * Adds the same handler for all of the specified attrs
 * @param {String} attrs Pipe-separated list of attributes
 * @param {Function} handler The method that will be applied
 */
function addHandle( attrs, handler ) {
  var arr = attrs[$fog$817]($fog$719),
    i = attrs[$fog$862];

  while ( i-- ) {
    Expr[$fog$718][ arr[i] ] = handler;
  }
}

/**
 * Checks document order of two siblings
 * @param {Element} a
 * @param {Element} b
 * @returns {Number} Returns less than 0 if a precedes b, greater than 0 if a follows b
 */
function siblingCheck( a, b ) {
  var cur = b && a,
    diff = cur && a[$fog$836] === $fog$852 && b[$fog$836] === $fog$852 &&
      ( ~b[$fog$717] || MAX_NEGATIVE ) -
      ( ~a[$fog$717] || MAX_NEGATIVE );

  // Use IE sourceIndex if available on both nodes
  if ( diff ) {
    return diff;
  }

  // Check if b follows a
  if ( cur ) {
    while ( (cur = cur[$fog$716]) ) {
      if ( cur === b ) {
        return -$fog$852;
      }
    }
  }

  return a ? $fog$852 : -$fog$852;
}

/**
 * Returns a function to use in pseudos for input types
 * @param {String} type
 */
function createInputPseudo( type ) {
  return function( elem ) {
    var name = elem[$fog$823][$fog$822]();
    return name === $fog$715 && elem[$fog$839] === type;
  };
}

/**
 * Returns a function to use in pseudos for buttons
 * @param {String} type
 */
function createButtonPseudo( type ) {
  return function( elem ) {
    var name = elem[$fog$823][$fog$822]();
    return (name === $fog$715 || name === $fog$714) && elem[$fog$839] === type;
  };
}

/**
 * Returns a function to use in pseudos for positionals
 * @param {Function} fn
 */
function createPositionalPseudo( fn ) {
  return markFunction(function( argument ) {
    argument = +argument;
    return markFunction(function( seed, matches ) {
      var j,
        matchIndexes = fn( [], seed[$fog$862], argument ),
        i = matchIndexes[$fog$862];

      // Match elements found at the specified indexes
      while ( i-- ) {
        if ( seed[ (j = matchIndexes[i]) ] ) {
          seed[j] = !(matches[j] = seed[j]);
        }
      }
    });
  });
}

/**
 * Checks a node for validity as a Sizzle context
 * @param {Element|Object=} context
 * @returns {Element|Object|Boolean} The input node if acceptable, otherwise a falsy value
 */
function testContext( context ) {
  return context && typeof context[$fog$737] !== $fog$881 && context;
}

// Expose support vars for convenience
support = Sizzle[$fog$713] = {};

/**
 * Detects XML nodes
 * @param {Element|Object} elem An element or a document
 * @returns {Boolean} True iff elem is a non-HTML XML node
 */
isXML = Sizzle[$fog$712] = function( elem ) {
  // documentElement is verified for cases where it doesn't yet exist
  // (such as loading iframes in IE - #4833)
  var documentElement = elem && (elem[$fog$743] || elem)[$fog$711];
  return documentElement ? documentElement[$fog$823] !== $fog$710 : $fog$848;
};

/**
 * Sets document-related variables once based on the current document
 * @param {Element|Object} [doc] An element or document object to use to set the document
 * @returns {Object} Returns the current document
 */
setDocument = Sizzle[$fog$709] = function( node ) {
  var hasCompare, parent,
    doc = node ? node[$fog$743] || node : preferredDoc;

  // If no document and documentElement is available, return
  if ( doc === document || doc[$fog$836] !== $fog$742 || !doc[$fog$711] ) {
    return document;
  }

  // Set our document
  document = doc;
  docElem = doc[$fog$711];
  parent = doc[$fog$708];

  // Support: IE>8
  // If iframe document is assigned to "document" variable and if iframe has been reloaded,
  // IE will throw "permission denied" error when accessing "document" variable, see jQuery #13936
  // IE6-8 do not support the defaultView property so parent will be undefined
  if ( parent && parent !== parent[$fog$707] ) {
    // IE11 does not have attachEvent, so all must suffer
    if ( parent[$fog$706] ) {
      parent[$fog$706]( $fog$705, unloadHandler, $fog$848 );
    } else if ( parent[$fog$704] ) {
      parent[$fog$704]( $fog$703, unloadHandler );
    }
  }

  /* Support tests
  ---------------------------------------------------------------------- */
  documentIsHTML = !isXML( doc );

  /* Attributes
  ---------------------------------------------------------------------- */

  // Support: IE<8
  // Verify that getAttribute really returns attributes and not properties
  // (excepting IE8 booleans)
  support[$fog$702] = assert(function( div ) {
    div[$fog$701] = $fog$767;
    return !div[$fog$732]($fog$701);
  });

  /* getElement(s)By*
  ---------------------------------------------------------------------- */

  // Check if getElementsByTagName("*") returns only elements
  support[$fog$737] = assert(function( div ) {
    div[$fog$826]( doc[$fog$700]($fog$866) );
    return !div[$fog$737]($fog$782)[$fog$862];
  });

  // Support: IE<9
  support[$fog$735] = rnative[$fog$733]( doc[$fog$735] );

  // Support: IE<10
  // Check if getElementById returns elements by name
  // The broken getElementById methods don't pick up programatically-set names,
  // so use a roundabout getElementsByName test
  support[$fog$699] = assert(function( div ) {
    docElem[$fog$826]( div )[$fog$738] = expando;
    return !doc[$fog$698] || !doc[$fog$698]( expando )[$fog$862];
  });

  // ID find and filter
  if ( support[$fog$699] ) {
    Expr[$fog$697][$fog$696] = function( id, context ) {
      if ( typeof context[$fog$739] !== $fog$881 && documentIsHTML ) {
        var m = context[$fog$739]( id );
        // Check parentNode to catch when Blackberry 4.6 returns
        // nodes that are no longer in the document #6963
        return m && m[$fog$827] ? [ m ] : [];
      }
    };
    Expr[$fog$695][$fog$696] = function( id ) {
      var attrId = id[$fog$842]( runescape, funescape );
      return function( elem ) {
        return elem[$fog$732]($fog$738) === attrId;
      };
    };
  } else {
    // Support: IE6/7
    // getElementById is not reliable as a find shortcut
    delete Expr[$fog$697][$fog$696];

    Expr[$fog$695][$fog$696] =  function( id ) {
      var attrId = id[$fog$842]( runescape, funescape );
      return function( elem ) {
        var node = typeof elem[$fog$694] !== $fog$881 && elem[$fog$694]($fog$738);
        return node && node[$fog$693] === attrId;
      };
    };
  }

  // Tag
  Expr[$fog$697][$fog$692] = support[$fog$737] ?
    function( tag, context ) {
      if ( typeof context[$fog$737] !== $fog$881 ) {
        return context[$fog$737]( tag );

      // DocumentFragment nodes don't have gEBTN
      } else if ( support[$fog$734] ) {
        return context[$fog$725]( tag );
      }
    } :

    function( tag, context ) {
      var elem,
        tmp = [],
        i = $fog$865,
        // By happy coincidence, a (broken) gEBTN appears on DocumentFragment nodes too
        results = context[$fog$737]( tag );

      // Filter out possible comments
      if ( tag === $fog$782 ) {
        while ( (elem = results[i++]) ) {
          if ( elem[$fog$836] === $fog$852 ) {
            tmp[$fog$878]( elem );
          }
        }

        return tmp;
      }
      return results;
    };

  // Class
  Expr[$fog$697][$fog$691] = support[$fog$735] && function( className, context ) {
    if ( documentIsHTML ) {
      return context[$fog$735]( className );
    }
  };

  /* QSA/matchesSelector
  ---------------------------------------------------------------------- */

  // QSA and matchesSelector support

  // matchesSelector(:active) reports false when true (IE9/Opera 11.5)
  rbuggyMatches = [];

  // qSa(:focus) reports false when true (Chrome 21)
  // We allow this because of a bug in IE8/9 that throws an error
  // whenever `document.activeElement` is accessed on an iframe
  // So, we allow :focus to pass through QSA all the time to avoid the IE error
  // See http://bugs.jquery.com/ticket/13378
  rbuggyQSA = [];

  if ( (support[$fog$734] = rnative[$fog$733]( doc[$fog$725] )) ) {
    // Build QSA regex
    // Regex strategy adopted from Diego Perini
    assert(function( div ) {
      // Select is set to empty string on purpose
      // This is to test IE's treatment of not explicitly
      // setting a boolean content attribute,
      // since its presence should be enough
      // http://bugs.jquery.com/ticket/12359
      docElem[$fog$826]( div )[$fog$690] = $fog$689 + expando + $fog$688 +
        $fog$687 + expando + $fog$686 +
        $fog$685;

      // Support: IE8, Opera 11-12.16
      // Nothing should be selected when empty strings follow ^= or $= or *=
      // The test attribute must be unknown in Opera but "safe" for WinRT
      // http://msdn.microsoft.com/en-us/library/ie/hh465388.aspx#attribute_section
      if ( div[$fog$725]($fog$684)[$fog$862] ) {
        rbuggyQSA[$fog$878]( $fog$683 + whitespace + $fog$682 );
      }

      // Support: IE8
      // Boolean attributes and "value" are not treated correctly
      if ( !div[$fog$725]($fog$681)[$fog$862] ) {
        rbuggyQSA[$fog$878]( $fog$802 + whitespace + $fog$680 + booleans + $fog$780 );
      }

      // Support: Chrome<29, Android<4.2+, Safari<7.0+, iOS<7.0+, PhantomJS<1.9.7+
      if ( !div[$fog$725]( $fog$679 + expando + $fog$678 )[$fog$862] ) {
        rbuggyQSA[$fog$878]($fog$677);
      }

      // Webkit/Opera - :checked should return selected option elements
      // http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
      // IE8 throws error here and will not see later tests
      if ( !div[$fog$725]($fog$676)[$fog$862] ) {
        rbuggyQSA[$fog$878]($fog$676);
      }

      // Support: Safari 8+, iOS 8+
      // https://bugs.webkit.org/show_bug.cgi?id=136851
      // In-page `selector#id sibing-combinator selector` fails
      if ( !div[$fog$725]( $fog$675 + expando + $fog$674 )[$fog$862] ) {
        rbuggyQSA[$fog$878]($fog$673);
      }
    });

    assert(function( div ) {
      // Support: Windows 8 Native Apps
      // The type and name attributes are restricted during .innerHTML assignment
      var input = doc[$fog$831]($fog$715);
      input[$fog$730]( $fog$839, $fog$672 );
      div[$fog$826]( input )[$fog$730]( $fog$671, $fog$670 );

      // Support: IE8
      // Enforce case-sensitivity of name attribute
      if ( div[$fog$725]($fog$669)[$fog$862] ) {
        rbuggyQSA[$fog$878]( $fog$671 + whitespace + $fog$668 );
      }

      // FF 3.5 - :enabled/:disabled and hidden elements (hidden elements are still enabled)
      // IE8 throws error here and will not see later tests
      if ( !div[$fog$725]($fog$667)[$fog$862] ) {
        rbuggyQSA[$fog$878]( $fog$667, $fog$666 );
      }

      // Opera 10-11 does not throw on post-comma invalid pseudos
      div[$fog$725]($fog$665);
      rbuggyQSA[$fog$878]($fog$664);
    });
  }

  if ( (support[$fog$663] = rnative[$fog$733]( (matches = docElem[$fog$662] ||
    docElem[$fog$661] ||
    docElem[$fog$660] ||
    docElem[$fog$659] ||
    docElem[$fog$658]) )) ) {

    assert(function( div ) {
      // Check to see if it's possible to do matchesSelector
      // on a disconnected node (IE 9)
      support[$fog$657] = matches[$fog$864]( div, $fog$720 );

      // This should fail with an exception
      // Gecko does not error, returns false instead
      matches[$fog$864]( div, $fog$656 );
      rbuggyMatches[$fog$878]( $fog$655, pseudos );
    });
  }

  rbuggyQSA = rbuggyQSA[$fog$862] && new RegExp( rbuggyQSA[$fog$727]($fog$719) );
  rbuggyMatches = rbuggyMatches[$fog$862] && new RegExp( rbuggyMatches[$fog$727]($fog$719) );

  /* Contains
  ---------------------------------------------------------------------- */
  hasCompare = rnative[$fog$733]( docElem[$fog$654] );

  // Element contains another
  // Purposefully does not implement inclusive descendent
  // As in, an element does not contain itself
  contains = hasCompare || rnative[$fog$733]( docElem[$fog$653] ) ?
    function( a, b ) {
      var adown = a[$fog$836] === $fog$742 ? a[$fog$711] : a,
        bup = b && b[$fog$827];
      return a === bup || !!( bup && bup[$fog$836] === $fog$852 && (
        adown[$fog$653] ?
          adown[$fog$653]( bup ) :
          a[$fog$654] && a[$fog$654]( bup ) & $fog$652
      ));
    } :
    function( a, b ) {
      if ( b ) {
        while ( (b = b[$fog$827]) ) {
          if ( b === a ) {
            return $fog$883;
          }
        }
      }
      return $fog$848;
    };

  /* Sorting
  ---------------------------------------------------------------------- */

  // Document order sorting
  sortOrder = hasCompare ?
  function( a, b ) {

    // Flag for duplicate removal
    if ( a === b ) {
      hasDuplicate = $fog$883;
      return $fog$865;
    }

    // Sort on method existence if only one input has compareDocumentPosition
    var compare = !a[$fog$654] - !b[$fog$654];
    if ( compare ) {
      return compare;
    }

    // Calculate position if both inputs belong to the same document
    compare = ( a[$fog$743] || a ) === ( b[$fog$743] || b ) ?
      a[$fog$654]( b ) :

      // Otherwise we know they are disconnected
      $fog$852;

    // Disconnected nodes
    if ( compare & $fog$852 ||
      (!support[$fog$651] && b[$fog$654]( a ) === compare) ) {

      // Choose the first element that is related to our preferred document
      if ( a === doc || a[$fog$743] === preferredDoc && contains(preferredDoc, a) ) {
        return -$fog$852;
      }
      if ( b === doc || b[$fog$743] === preferredDoc && contains(preferredDoc, b) ) {
        return $fog$852;
      }

      // Maintain original order
      return sortInput ?
        ( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :
        $fog$865;
    }

    return compare & $fog$650 ? -$fog$852 : $fog$852;
  } :
  function( a, b ) {
    // Exit early if the nodes are identical
    if ( a === b ) {
      hasDuplicate = $fog$883;
      return $fog$865;
    }

    var cur,
      i = $fog$865,
      aup = a[$fog$827],
      bup = b[$fog$827],
      ap = [ a ],
      bp = [ b ];

    // Parentless nodes are either documents or disconnected
    if ( !aup || !bup ) {
      return a === doc ? -$fog$852 :
        b === doc ? $fog$852 :
        aup ? -$fog$852 :
        bup ? $fog$852 :
        sortInput ?
        ( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :
        $fog$865;

    // If the nodes are siblings, we can do a quick check
    } else if ( aup === bup ) {
      return siblingCheck( a, b );
    }

    // Otherwise we need full lists of their ancestors for comparison
    cur = a;
    while ( (cur = cur[$fog$827]) ) {
      ap[$fog$649]( cur );
    }
    cur = b;
    while ( (cur = cur[$fog$827]) ) {
      bp[$fog$649]( cur );
    }

    // Walk down the tree looking for a discrepancy
    while ( ap[i] === bp[i] ) {
      i++;
    }

    return i ?
      // Do a sibling check if the nodes have a common ancestor
      siblingCheck( ap[i], bp[i] ) :

      // Otherwise nodes in our document sort first
      ap[i] === preferredDoc ? -$fog$852 :
      bp[i] === preferredDoc ? $fog$852 :
      $fog$865;
  };

  return doc;
};

Sizzle[$fog$662] = function( expr, elements ) {
  return Sizzle( expr, $fog$863, $fog$863, elements );
};

Sizzle[$fog$663] = function( elem, expr ) {
  // Set document vars if needed
  if ( ( elem[$fog$743] || elem ) !== document ) {
    setDocument( elem );
  }

  // Make sure that attribute selectors are quoted
  expr = expr[$fog$842]( rattributeQuotes, $fog$648 );

  if ( support[$fog$663] && documentIsHTML &&
    ( !rbuggyMatches || !rbuggyMatches[$fog$733]( expr ) ) &&
    ( !rbuggyQSA     || !rbuggyQSA[$fog$733]( expr ) ) ) {

    try {
      var ret = matches[$fog$864]( elem, expr );

      // IE 9's matchesSelector returns false on disconnected nodes
      if ( ret || support[$fog$657] ||
          // As well, disconnected nodes are said to be in a document
          // fragment in IE 9
          elem[$fog$884] && elem[$fog$884][$fog$836] !== $fog$741 ) {
        return ret;
      }
    } catch (e) {}
  }

  return Sizzle( expr, document, $fog$863, [ elem ] )[$fog$862] > $fog$865;
};

Sizzle[$fog$653] = function( context, elem ) {
  // Set document vars if needed
  if ( ( context[$fog$743] || context ) !== document ) {
    setDocument( context );
  }
  return contains( context, elem );
};

Sizzle[$fog$647] = function( elem, name ) {
  // Set document vars if needed
  if ( ( elem[$fog$743] || elem ) !== document ) {
    setDocument( elem );
  }

  var fn = Expr[$fog$718][ name[$fog$822]() ],
    // Don't get fooled by Object.prototype properties (jQuery #13807)
    val = fn && hasOwn[$fog$864]( Expr[$fog$718], name[$fog$822]() ) ?
      fn( elem, name, !documentIsHTML ) :
      undefined;

  return val !== undefined ?
    val :
    support[$fog$702] || !documentIsHTML ?
      elem[$fog$732]( name ) :
      (val = elem[$fog$694](name)) && val[$fog$646] ?
        val[$fog$693] :
        $fog$863;
};

Sizzle[$fog$645] = function( msg ) {
  throw new Error( $fog$644 + msg );
};

/**
 * Document sorting and removing duplicates
 * @param {ArrayLike} results
 */
Sizzle[$fog$643] = function( results ) {
  var elem,
    duplicates = [],
    j = $fog$865,
    i = $fog$865;

  // Unless we *know* we can detect duplicates, assume their presence
  hasDuplicate = !support[$fog$642];
  sortInput = !support[$fog$641] && results[$fog$880]( $fog$865 );
  results[$fog$851]( sortOrder );

  if ( hasDuplicate ) {
    while ( (elem = results[i++]) ) {
      if ( elem === results[ i ] ) {
        j = duplicates[$fog$878]( i );
      }
    }
    while ( j-- ) {
      results[$fog$850]( duplicates[ j ], $fog$852 );
    }
  }

  // Clear input after sorting to release objects
  // See https://github.com/jquery/sizzle/pull/225
  sortInput = $fog$863;

  return results;
};

/**
 * Utility function for retrieving the text value of an array of DOM nodes
 * @param {Array|Element} elem
 */
getText = Sizzle[$fog$640] = function( elem ) {
  var node,
    ret = $fog$866,
    i = $fog$865,
    nodeType = elem[$fog$836];

  if ( !nodeType ) {
    // If no nodeType, this is expected to be an array
    while ( (node = elem[i++]) ) {
      // Do not traverse comment nodes
      ret += getText( node );
    }
  } else if ( nodeType === $fog$852 || nodeType === $fog$742 || nodeType === $fog$741 ) {
    // Use textContent for elements
    // innerText usage removed for consistency of new lines (jQuery #11153)
    if ( typeof elem[$fog$639] === $fog$821 ) {
      return elem[$fog$639];
    } else {
      // Traverse its children
      for ( elem = elem[$fog$638]; elem; elem = elem[$fog$716] ) {
        ret += getText( elem );
      }
    }
  } else if ( nodeType === $fog$736 || nodeType === $fog$650 ) {
    return elem[$fog$637];
  }
  // Do not include comment or processing instruction nodes

  return ret;
};

Expr = Sizzle[$fog$636] = {

  // Can be adjusted by the user
  cacheLength: $fog$635,

  createPseudo: markFunction,

  match: matchExpr,

  attrHandle: {},

  find: {},

  relative: {
    ">": { dir: $fog$827, first: $fog$883 },
    " ": { dir: $fog$827 },
    "+": { dir: $fog$634, first: $fog$883 },
    "~": { dir: $fog$634 }
  },

  preFilter: {
    "ATTR": function( match ) {
      match[$fog$852] = match[$fog$852][$fog$842]( runescape, funescape );

      // Move the given value to match[3] whether quoted or unquoted
      match[$fog$736] = ( match[$fog$736] || match[$fog$650] || match[$fog$633] || $fog$866 )[$fog$842]( runescape, funescape );

      if ( match[$fog$820] === $fog$677 ) {
        match[$fog$736] = $fog$815 + match[$fog$736] + $fog$815;
      }

      return match[$fog$880]( $fog$865, $fog$650 );
    },

    "CHILD": function( match ) {
      /* matches from matchExpr["CHILD"]
        1 type (only|nth|...)
        2 what (child|of-type)
        3 argument (even|odd|\d*|\d*n([+-]\d+)?|...)
        4 xn-component of xn+y argument ([+-]?\d*n|)
        5 sign of xn-component
        6 x of xn-component
        7 sign of y-component
        8 y of y-component
      */
      match[$fog$852] = match[$fog$852][$fog$822]();

      if ( match[$fog$852][$fog$880]( $fog$865, $fog$736 ) === $fog$632 ) {
        // nth-* requires argument
        if ( !match[$fog$736] ) {
          Sizzle[$fog$645]( match[$fog$865] );
        }

        // numeric x and y parameters for Expr.filter.CHILD
        // remember that false/true cast respectively to 0/1
        match[$fog$650] = +( match[$fog$650] ? match[$fog$633] + (match[$fog$631] || $fog$852) : $fog$820 * ( match[$fog$736] === $fog$630 || match[$fog$736] === $fog$629 ) );
        match[$fog$633] = +( ( match[$fog$628] + match[$fog$627] ) || match[$fog$736] === $fog$629 );

      // other types prohibit arguments
      } else if ( match[$fog$736] ) {
        Sizzle[$fog$645]( match[$fog$865] );
      }

      return match;
    },

    "PSEUDO": function( match ) {
      var excess,
        unquoted = !match[$fog$631] && match[$fog$820];

      if ( matchExpr[$fog$626][$fog$733]( match[$fog$865] ) ) {
        return $fog$863;
      }

      // Accept quoted arguments as-is
      if ( match[$fog$736] ) {
        match[$fog$820] = match[$fog$650] || match[$fog$633] || $fog$866;

      // Strip excess characters from unquoted arguments
      } else if ( unquoted && rpseudo[$fog$733]( unquoted ) &&
        // Get excess from tokenize (recursively)
        (excess = tokenize( unquoted, $fog$883 )) &&
        // advance to the next closing parenthesis
        (excess = unquoted[$fog$877]( $fog$780, unquoted[$fog$862] - excess ) - unquoted[$fog$862]) ) {

        // excess is a negative index
        match[$fog$865] = match[$fog$865][$fog$880]( $fog$865, excess );
        match[$fog$820] = unquoted[$fog$880]( $fog$865, excess );
      }

      // Return only captures needed by the pseudo filter method (type and argument)
      return match[$fog$880]( $fog$865, $fog$736 );
    }
  },

  filter: {

    "TAG": function( nodeNameSelector ) {
      var nodeName = nodeNameSelector[$fog$842]( runescape, funescape )[$fog$822]();
      return nodeNameSelector === $fog$782 ?
        function() { return $fog$883; } :
        function( elem ) {
          return elem[$fog$823] && elem[$fog$823][$fog$822]() === nodeName;
        };
    },

    "CLASS": function( className ) {
      var pattern = classCache[ className + $fog$815 ];

      return pattern ||
        (pattern = new RegExp( $fog$625 + whitespace + $fog$780 + className + $fog$624 + whitespace + $fog$623 )) &&
        classCache( className, function( elem ) {
          return pattern[$fog$733]( typeof elem[$fog$701] === $fog$821 && elem[$fog$701] || typeof elem[$fog$732] !== $fog$881 && elem[$fog$732]($fog$622) || $fog$866 );
        });
    },

    "ATTR": function( name, operator, check ) {
      return function( elem ) {
        var result = Sizzle[$fog$647]( elem, name );

        if ( result == $fog$863 ) {
          return operator === $fog$655;
        }
        if ( !operator ) {
          return $fog$883;
        }

        result += $fog$866;

        return operator === $fog$779 ? result === check :
          operator === $fog$655 ? result !== check :
          operator === $fog$621 ? check && result[$fog$877]( check ) === $fog$865 :
          operator === $fog$620 ? check && result[$fog$877]( check ) > -$fog$852 :
          operator === $fog$619 ? check && result[$fog$880]( -check[$fog$862] ) === check :
          operator === $fog$677 ? ( $fog$815 + result[$fog$842]( rwhitespace, $fog$815 ) + $fog$815 )[$fog$877]( check ) > -$fog$852 :
          operator === $fog$618 ? result === check || result[$fog$880]( $fog$865, check[$fog$862] + $fog$852 ) === check + $fog$617 :
          $fog$848;
      };
    },

    "CHILD": function( type, what, argument, first, last ) {
      var simple = type[$fog$880]( $fog$865, $fog$736 ) !== $fog$632,
        forward = type[$fog$880]( -$fog$650 ) !== $fog$616,
        ofType = what === $fog$615;

      return first === $fog$852 && last === $fog$865 ?

        // Shortcut for :nth-*(n)
        function( elem ) {
          return !!elem[$fog$827];
        } :

        function( elem, context, xml ) {
          var cache, outerCache, node, diff, nodeIndex, start,
            dir = simple !== forward ? $fog$716 : $fog$634,
            parent = elem[$fog$827],
            name = ofType && elem[$fog$823][$fog$822](),
            useCache = !xml && !ofType;

          if ( parent ) {

            // :(first|last|only)-(child|of-type)
            if ( simple ) {
              while ( dir ) {
                node = elem;
                while ( (node = node[ dir ]) ) {
                  if ( ofType ? node[$fog$823][$fog$822]() === name : node[$fog$836] === $fog$852 ) {
                    return $fog$848;
                  }
                }
                // Reverse direction for :only-* (if we haven't yet done so)
                start = dir = type === $fog$614 && !start && $fog$716;
              }
              return $fog$883;
            }

            start = [ forward ? parent[$fog$638] : parent[$fog$613] ];

            // non-xml :nth-child(...) stores cache data on `parent`
            if ( forward && useCache ) {
              // Seek `elem` from a previously-cached index
              outerCache = parent[ expando ] || (parent[ expando ] = {});
              cache = outerCache[ type ] || [];
              nodeIndex = cache[$fog$865] === dirruns && cache[$fog$852];
              diff = cache[$fog$865] === dirruns && cache[$fog$820];
              node = nodeIndex && parent[$fog$744][ nodeIndex ];

              while ( (node = ++nodeIndex && node && node[ dir ] ||

                // Fallback to seeking `elem` from the start
                (diff = nodeIndex = $fog$865) || start[$fog$808]()) ) {

                // When found, cache indexes on `parent` and break
                if ( node[$fog$836] === $fog$852 && ++diff && node === elem ) {
                  outerCache[ type ] = [ dirruns, nodeIndex, diff ];
                  break;
                }
              }

            // Use previously-cached element index if available
            } else if ( useCache && (cache = (elem[ expando ] || (elem[ expando ] = {}))[ type ]) && cache[$fog$865] === dirruns ) {
              diff = cache[$fog$852];

            // xml :nth-child(...) or :nth-last-child(...) or :nth(-last)?-of-type(...)
            } else {
              // Use the same loop as above to seek `elem` from the start
              while ( (node = ++nodeIndex && node && node[ dir ] ||
                (diff = nodeIndex = $fog$865) || start[$fog$808]()) ) {

                if ( ( ofType ? node[$fog$823][$fog$822]() === name : node[$fog$836] === $fog$852 ) && ++diff ) {
                  // Cache the index of each encountered element
                  if ( useCache ) {
                    (node[ expando ] || (node[ expando ] = {}))[ type ] = [ dirruns, diff ];
                  }

                  if ( node === elem ) {
                    break;
                  }
                }
              }
            }

            // Incorporate the offset, then check against cycle size
            diff -= last;
            return diff === first || ( diff % first === $fog$865 && diff / first >= $fog$865 );
          }
        };
    },

    "PSEUDO": function( pseudo, argument ) {
      // pseudo-class names are case-insensitive
      // http://www.w3.org/TR/selectors/#pseudo-classes
      // Prioritize by case sensitivity in case custom pseudos are added with uppercase letters
      // Remember that setFilters inherits from pseudos
      var args,
        fn = Expr[$fog$612][ pseudo ] || Expr[$fog$611][ pseudo[$fog$822]() ] ||
          Sizzle[$fog$645]( $fog$610 + pseudo );

      // The user may use createPseudo to indicate that
      // arguments are needed to create the filter function
      // just as Sizzle does
      if ( fn[ expando ] ) {
        return fn( argument );
      }

      // But maintain support for old signatures
      if ( fn[$fog$862] > $fog$852 ) {
        args = [ pseudo, pseudo, $fog$866, argument ];
        return Expr[$fog$611][$fog$875]( pseudo[$fog$822]() ) ?
          markFunction(function( seed, matches ) {
            var idx,
              matched = fn( seed, argument ),
              i = matched[$fog$862];
            while ( i-- ) {
              idx = indexOf( seed, matched[i] );
              seed[ idx ] = !( matches[ idx ] = matched[i] );
            }
          }) :
          function( elem ) {
            return fn( elem, $fog$865, args );
          };
      }

      return fn;
    }
  },

  pseudos: {
    // Potentially complex pseudos
    "not": markFunction(function( selector ) {
      // Trim the selector passed to compile
      // to avoid treating leading and trailing
      // spaces as combinators
      var input = [],
        results = [],
        matcher = compile( selector[$fog$842]( rtrim, $fog$723 ) );

      return matcher[ expando ] ?
        markFunction(function( seed, matches, context, xml ) {
          var elem,
            unmatched = matcher( seed, $fog$863, xml, [] ),
            i = seed[$fog$862];

          // Match elements unmatched by `matcher`
          while ( i-- ) {
            if ( (elem = unmatched[i]) ) {
              seed[i] = !(matches[i] = elem);
            }
          }
        }) :
        function( elem, context, xml ) {
          input[$fog$865] = elem;
          matcher( input, $fog$863, xml, results );
          // Don't keep the element (issue #299)
          input[$fog$865] = $fog$863;
          return !results[$fog$808]();
        };
    }),

    "has": markFunction(function( selector ) {
      return function( elem ) {
        return Sizzle( selector, elem )[$fog$862] > $fog$865;
      };
    }),

    "contains": markFunction(function( text ) {
      text = text[$fog$842]( runescape, funescape );
      return function( elem ) {
        return ( elem[$fog$639] || elem[$fog$609] || getText( elem ) )[$fog$877]( text ) > -$fog$852;
      };
    }),

    // "Whether an element is represented by a :lang() selector
    // is based solely on the element's language value
    // being equal to the identifier C,
    // or beginning with the identifier C immediately followed by "-".
    // The matching of C against the element's language value is performed case-insensitively.
    // The identifier C does not have to be a valid language name."
    // http://www.w3.org/TR/selectors/#lang-pseudo
    "lang": markFunction( function( lang ) {
      // lang value must be a valid identifier
      if ( !ridentifier[$fog$733](lang || $fog$866) ) {
        Sizzle[$fog$645]( $fog$608 + lang );
      }
      lang = lang[$fog$842]( runescape, funescape )[$fog$822]();
      return function( elem ) {
        var elemLang;
        do {
          if ( (elemLang = documentIsHTML ?
            elem[$fog$607] :
            elem[$fog$732]($fog$606) || elem[$fog$732]($fog$607)) ) {

            elemLang = elemLang[$fog$822]();
            return elemLang === lang || elemLang[$fog$877]( lang + $fog$617 ) === $fog$865;
          }
        } while ( (elem = elem[$fog$827]) && elem[$fog$836] === $fog$852 );
        return $fog$848;
      };
    }),

    // Miscellaneous
    "target": function( elem ) {
      var hash = window[$fog$605] && window[$fog$605][$fog$604];
      return hash && hash[$fog$880]( $fog$852 ) === elem[$fog$738];
    },

    "root": function( elem ) {
      return elem === docElem;
    },

    "focus": function( elem ) {
      return elem === document[$fog$603] && (!document[$fog$602] || document[$fog$602]()) && !!(elem[$fog$839] || elem[$fog$601] || ~elem[$fog$600]);
    },

    // Boolean properties
    "enabled": function( elem ) {
      return elem[$fog$599] === $fog$848;
    },

    "disabled": function( elem ) {
      return elem[$fog$599] === $fog$883;
    },

    "checked": function( elem ) {
      // In CSS3, :checked should return both checked and selected elements
      // http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
      var nodeName = elem[$fog$823][$fog$822]();
      return (nodeName === $fog$715 && !!elem[$fog$598]) || (nodeName === $fog$597 && !!elem[$fog$596]);
    },

    "selected": function( elem ) {
      // Accessing this property makes selected-by-default
      // options in Safari work properly
      if ( elem[$fog$827] ) {
        elem[$fog$827][$fog$595];
      }

      return elem[$fog$596] === $fog$883;
    },

    // Contents
    "empty": function( elem ) {
      // http://www.w3.org/TR/selectors/#empty-pseudo
      // :empty is negated by element (1) or content nodes (text: 3; cdata: 4; entity ref: 5),
      //   but not by others (comment: 8; processing instruction: 7; etc.)
      // nodeType < 6 works because attributes (2) do not appear as children
      for ( elem = elem[$fog$638]; elem; elem = elem[$fog$716] ) {
        if ( elem[$fog$836] < $fog$631 ) {
          return $fog$848;
        }
      }
      return $fog$883;
    },

    "parent": function( elem ) {
      return !Expr[$fog$612][$fog$594]( elem );
    },

    // Element/input types
    "header": function( elem ) {
      return rheader[$fog$733]( elem[$fog$823] );
    },

    "input": function( elem ) {
      return rinputs[$fog$733]( elem[$fog$823] );
    },

    "button": function( elem ) {
      var name = elem[$fog$823][$fog$822]();
      return name === $fog$715 && elem[$fog$839] === $fog$714 || name === $fog$714;
    },

    "text": function( elem ) {
      var attr;
      return elem[$fog$823][$fog$822]() === $fog$715 &&
        elem[$fog$839] === $fog$829 &&

        // Support: IE<8
        // New HTML5 attribute values (e.g., "search") appear with elem.type === "text"
        ( (attr = elem[$fog$732]($fog$839)) == $fog$863 || attr[$fog$822]() === $fog$829 );
    },

    // Position-in-collection
    "first": createPositionalPseudo(function() {
      return [ $fog$865 ];
    }),

    "last": createPositionalPseudo(function( matchIndexes, length ) {
      return [ length - $fog$852 ];
    }),

    "eq": createPositionalPseudo(function( matchIndexes, length, argument ) {
      return [ argument < $fog$865 ? argument + length : argument ];
    }),

    "even": createPositionalPseudo(function( matchIndexes, length ) {
      var i = $fog$865;
      for ( ; i < length; i += $fog$820 ) {
        matchIndexes[$fog$878]( i );
      }
      return matchIndexes;
    }),

    "odd": createPositionalPseudo(function( matchIndexes, length ) {
      var i = $fog$852;
      for ( ; i < length; i += $fog$820 ) {
        matchIndexes[$fog$878]( i );
      }
      return matchIndexes;
    }),

    "lt": createPositionalPseudo(function( matchIndexes, length, argument ) {
      var i = argument < $fog$865 ? argument + length : argument;
      for ( ; --i >= $fog$865; ) {
        matchIndexes[$fog$878]( i );
      }
      return matchIndexes;
    }),

    "gt": createPositionalPseudo(function( matchIndexes, length, argument ) {
      var i = argument < $fog$865 ? argument + length : argument;
      for ( ; ++i < length; ) {
        matchIndexes[$fog$878]( i );
      }
      return matchIndexes;
    })
  }
};

Expr[$fog$612][$fog$632] = Expr[$fog$612][$fog$853];

// Add button/input type pseudos
for ( i in { radio: $fog$883, checkbox: $fog$883, file: $fog$883, password: $fog$883, image: $fog$883 } ) {
  Expr[$fog$612][ i ] = createInputPseudo( i );
}
for ( i in { submit: $fog$883, reset: $fog$883 } ) {
  Expr[$fog$612][ i ] = createButtonPseudo( i );
}

// Easy API for creating new setFilters
function setFilters() {}
setFilters[$fog$867] = Expr[$fog$593] = Expr[$fog$612];
Expr[$fog$611] = new setFilters();

tokenize = Sizzle[$fog$592] = function( selector, parseOnly ) {
  var matched, match, tokens, type,
    soFar, groups, preFilters,
    cached = tokenCache[ selector + $fog$815 ];

  if ( cached ) {
    return parseOnly ? $fog$865 : cached[$fog$880]( $fog$865 );
  }

  soFar = selector;
  groups = [];
  preFilters = Expr[$fog$591];

  while ( soFar ) {

    // Comma and first run
    if ( !matched || (match = rcomma[$fog$740]( soFar )) ) {
      if ( match ) {
        // Don't consume trailing commas as valid
        soFar = soFar[$fog$880]( match[$fog$865][$fog$862] ) || soFar;
      }
      groups[$fog$878]( (tokens = []) );
    }

    matched = $fog$848;

    // Combinators
    if ( (match = rcombinators[$fog$740]( soFar )) ) {
      matched = match[$fog$721]();
      tokens[$fog$878]({
        value: matched,
        // Cast descendant combinators to space
        type: match[$fog$865][$fog$842]( rtrim, $fog$815 )
      });
      soFar = soFar[$fog$880]( matched[$fog$862] );
    }

    // Filters
    for ( type in Expr[$fog$695] ) {
      if ( (match = matchExpr[ type ][$fog$740]( soFar )) && (!preFilters[ type ] ||
        (match = preFilters[ type ]( match ))) ) {
        matched = match[$fog$721]();
        tokens[$fog$878]({
          value: matched,
          type: type,
          matches: match
        });
        soFar = soFar[$fog$880]( matched[$fog$862] );
      }
    }

    if ( !matched ) {
      break;
    }
  }

  // Return the length of the invalid excess
  // if we're just parsing
  // Otherwise, throw an error or return tokens
  return parseOnly ?
    soFar[$fog$862] :
    soFar ?
      Sizzle[$fog$645]( selector ) :
      // Cache the tokens
      tokenCache( selector, groups )[$fog$880]( $fog$865 );
};

function toSelector( tokens ) {
  var i = $fog$865,
    len = tokens[$fog$862],
    selector = $fog$866;
  for ( ; i < len; i++ ) {
    selector += tokens[i][$fog$693];
  }
  return selector;
}

function addCombinator( matcher, combinator, base ) {
  var dir = combinator[$fog$590],
    checkNonElements = base && dir === $fog$827,
    doneName = done++;

  return combinator[$fog$589] ?
    // Check against closest ancestor/preceding element
    function( elem, context, xml ) {
      while ( (elem = elem[ dir ]) ) {
        if ( elem[$fog$836] === $fog$852 || checkNonElements ) {
          return matcher( elem, context, xml );
        }
      }
    } :

    // Check against all ancestor/preceding elements
    function( elem, context, xml ) {
      var oldCache, outerCache,
        newCache = [ dirruns, doneName ];

      // We can't set arbitrary data on XML nodes, so they don't benefit from dir caching
      if ( xml ) {
        while ( (elem = elem[ dir ]) ) {
          if ( elem[$fog$836] === $fog$852 || checkNonElements ) {
            if ( matcher( elem, context, xml ) ) {
              return $fog$883;
            }
          }
        }
      } else {
        while ( (elem = elem[ dir ]) ) {
          if ( elem[$fog$836] === $fog$852 || checkNonElements ) {
            outerCache = elem[ expando ] || (elem[ expando ] = {});
            if ( (oldCache = outerCache[ dir ]) &&
              oldCache[ $fog$865 ] === dirruns && oldCache[ $fog$852 ] === doneName ) {

              // Assign to newCache so results back-propagate to previous elements
              return (newCache[ $fog$820 ] = oldCache[ $fog$820 ]);
            } else {
              // Reuse newcache so results back-propagate to previous elements
              outerCache[ dir ] = newCache;

              // A match means we're done; a fail means we have to keep checking
              if ( (newCache[ $fog$820 ] = matcher( elem, context, xml )) ) {
                return $fog$883;
              }
            }
          }
        }
      }
    };
}

function elementMatcher( matchers ) {
  return matchers[$fog$862] > $fog$852 ?
    function( elem, context, xml ) {
      var i = matchers[$fog$862];
      while ( i-- ) {
        if ( !matchers[i]( elem, context, xml ) ) {
          return $fog$848;
        }
      }
      return $fog$883;
    } :
    matchers[$fog$865];
}

function multipleContexts( selector, contexts, results ) {
  var i = $fog$865,
    len = contexts[$fog$862];
  for ( ; i < len; i++ ) {
    Sizzle( selector, contexts[i], results );
  }
  return results;
}

function condense( unmatched, map, filter, context, xml ) {
  var elem,
    newUnmatched = [],
    i = $fog$865,
    len = unmatched[$fog$862],
    mapped = map != $fog$863;

  for ( ; i < len; i++ ) {
    if ( (elem = unmatched[i]) ) {
      if ( !filter || filter( elem, context, xml ) ) {
        newUnmatched[$fog$878]( elem );
        if ( mapped ) {
          map[$fog$878]( i );
        }
      }
    }
  }

  return newUnmatched;
}

function setMatcher( preFilter, selector, matcher, postFilter, postFinder, postSelector ) {
  if ( postFilter && !postFilter[ expando ] ) {
    postFilter = setMatcher( postFilter );
  }
  if ( postFinder && !postFinder[ expando ] ) {
    postFinder = setMatcher( postFinder, postSelector );
  }
  return markFunction(function( seed, results, context, xml ) {
    var temp, i, elem,
      preMap = [],
      postMap = [],
      preexisting = results[$fog$862],

      // Get initial elements from seed or context
      elems = seed || multipleContexts( selector || $fog$782, context[$fog$836] ? [ context ] : context, [] ),

      // Prefilter to get matcher input, preserving a map for seed-results synchronization
      matcherIn = preFilter && ( seed || !selector ) ?
        condense( elems, preMap, preFilter, context, xml ) :
        elems,

      matcherOut = matcher ?
        // If we have a postFinder, or filtered seed, or non-seed postFilter or preexisting results,
        postFinder || ( seed ? preFilter : preexisting || postFilter ) ?

          // ...intermediate processing is necessary
          [] :

          // ...otherwise use results directly
          results :
        matcherIn;

    // Find primary matches
    if ( matcher ) {
      matcher( matcherIn, matcherOut, context, xml );
    }

    // Apply postFilter
    if ( postFilter ) {
      temp = condense( matcherOut, postMap );
      postFilter( temp, [], context, xml );

      // Un-match failing elements by moving them back to matcherIn
      i = temp[$fog$862];
      while ( i-- ) {
        if ( (elem = temp[i]) ) {
          matcherOut[ postMap[i] ] = !(matcherIn[ postMap[i] ] = elem);
        }
      }
    }

    if ( seed ) {
      if ( postFinder || preFilter ) {
        if ( postFinder ) {
          // Get the final matcherOut by condensing this intermediate into postFinder contexts
          temp = [];
          i = matcherOut[$fog$862];
          while ( i-- ) {
            if ( (elem = matcherOut[i]) ) {
              // Restore matcherIn since elem is not yet a final match
              temp[$fog$878]( (matcherIn[i] = elem) );
            }
          }
          postFinder( $fog$863, (matcherOut = []), temp, xml );
        }

        // Move matched elements from seed to results to keep them synchronized
        i = matcherOut[$fog$862];
        while ( i-- ) {
          if ( (elem = matcherOut[i]) &&
            (temp = postFinder ? indexOf( seed, elem ) : preMap[i]) > -$fog$852 ) {

            seed[temp] = !(results[temp] = elem);
          }
        }
      }

    // Add elements to results, through postFinder if defined
    } else {
      matcherOut = condense(
        matcherOut === results ?
          matcherOut[$fog$850]( preexisting, matcherOut[$fog$862] ) :
          matcherOut
      );
      if ( postFinder ) {
        postFinder( $fog$863, results, matcherOut, xml );
      } else {
        push[$fog$854]( results, matcherOut );
      }
    }
  });
}

function matcherFromTokens( tokens ) {
  var checkContext, matcher, j,
    len = tokens[$fog$862],
    leadingRelative = Expr[$fog$588][ tokens[$fog$865][$fog$839] ],
    implicitRelative = leadingRelative || Expr[$fog$588][$fog$815],
    i = leadingRelative ? $fog$852 : $fog$865,

    // The foundational matcher ensures that elements are reachable from top-level context(s)
    matchContext = addCombinator( function( elem ) {
      return elem === checkContext;
    }, implicitRelative, $fog$883 ),
    matchAnyContext = addCombinator( function( elem ) {
      return indexOf( checkContext, elem ) > -$fog$852;
    }, implicitRelative, $fog$883 ),
    matchers = [ function( elem, context, xml ) {
      var ret = ( !leadingRelative && ( xml || context !== outermostContext ) ) || (
        (checkContext = context)[$fog$836] ?
          matchContext( elem, context, xml ) :
          matchAnyContext( elem, context, xml ) );
      // Avoid hanging onto element (issue #299)
      checkContext = $fog$863;
      return ret;
    } ];

  for ( ; i < len; i++ ) {
    if ( (matcher = Expr[$fog$588][ tokens[i][$fog$839] ]) ) {
      matchers = [ addCombinator(elementMatcher( matchers ), matcher) ];
    } else {
      matcher = Expr[$fog$695][ tokens[i][$fog$839] ][$fog$854]( $fog$863, tokens[i][$fog$662] );

      // Return special upon seeing a positional matcher
      if ( matcher[ expando ] ) {
        // Find the next relative operator (if any) for proper handling
        j = ++i;
        for ( ; j < len; j++ ) {
          if ( Expr[$fog$588][ tokens[j][$fog$839] ] ) {
            break;
          }
        }
        return setMatcher(
          i > $fog$852 && elementMatcher( matchers ),
          i > $fog$852 && toSelector(
            // If the preceding token was a descendant combinator, insert an implicit any-element `*`
            tokens[$fog$880]( $fog$865, i - $fog$852 )[$fog$879]({ value: tokens[ i - $fog$820 ][$fog$839] === $fog$815 ? $fog$782 : $fog$866 })
          )[$fog$842]( rtrim, $fog$723 ),
          matcher,
          i < j && matcherFromTokens( tokens[$fog$880]( i, j ) ),
          j < len && matcherFromTokens( (tokens = tokens[$fog$880]( j )) ),
          j < len && toSelector( tokens )
        );
      }
      matchers[$fog$878]( matcher );
    }
  }

  return elementMatcher( matchers );
}

function matcherFromGroupMatchers( elementMatchers, setMatchers ) {
  var bySet = setMatchers[$fog$862] > $fog$865,
    byElement = elementMatchers[$fog$862] > $fog$865,
    superMatcher = function( seed, context, xml, results, outermost ) {
      var elem, j, matcher,
        matchedCount = $fog$865,
        i = $fog$587,
        unmatched = seed && [],
        setMatched = [],
        contextBackup = outermostContext,
        // We must always have either seed elements or outermost context
        elems = seed || byElement && Expr[$fog$697][$fog$692]( $fog$782, outermost ),
        // Use integer dirruns iff this is the outermost matcher
        dirrunsUnique = (dirruns += contextBackup == $fog$863 ? $fog$852 : Math[$fog$841]() || $fog$586),
        len = elems[$fog$862];

      if ( outermost ) {
        outermostContext = context !== document && context;
      }

      // Add elements passing elementMatchers directly to results
      // Keep `i` a string if there are no elements so `matchedCount` will be "00" below
      // Support: IE<9, Safari
      // Tolerate NodeList properties (IE: "length"; Safari: <number>) matching elements by id
      for ( ; i !== len && (elem = elems[i]) != $fog$863; i++ ) {
        if ( byElement && elem ) {
          j = $fog$865;
          while ( (matcher = elementMatchers[j++]) ) {
            if ( matcher( elem, context, xml ) ) {
              results[$fog$878]( elem );
              break;
            }
          }
          if ( outermost ) {
            dirruns = dirrunsUnique;
          }
        }

        // Track unmatched elements for set filters
        if ( bySet ) {
          // They will have gone through all possible matchers
          if ( (elem = !matcher && elem) ) {
            matchedCount--;
          }

          // Lengthen the array for every element, matched or not
          if ( seed ) {
            unmatched[$fog$878]( elem );
          }
        }
      }

      // Apply set filters to unmatched elements
      matchedCount += i;
      if ( bySet && i !== matchedCount ) {
        j = $fog$865;
        while ( (matcher = setMatchers[j++]) ) {
          matcher( unmatched, setMatched, context, xml );
        }

        if ( seed ) {
          // Reintegrate element matches to eliminate the need for sorting
          if ( matchedCount > $fog$865 ) {
            while ( i-- ) {
              if ( !(unmatched[i] || setMatched[i]) ) {
                setMatched[i] = pop[$fog$864]( results );
              }
            }
          }

          // Discard index placeholder values to get only actual matches
          setMatched = condense( setMatched );
        }

        // Add matches to results
        push[$fog$854]( results, setMatched );

        // Seedless set matches succeeding multiple successful matchers stipulate sorting
        if ( outermost && !seed && setMatched[$fog$862] > $fog$865 &&
          ( matchedCount + setMatchers[$fog$862] ) > $fog$852 ) {

          Sizzle[$fog$643]( results );
        }
      }

      // Override manipulation of globals by nested matchers
      if ( outermost ) {
        dirruns = dirrunsUnique;
        outermostContext = contextBackup;
      }

      return unmatched;
    };

  return bySet ?
    markFunction( superMatcher ) :
    superMatcher;
}

compile = Sizzle[$fog$585] = function( selector, match /* Internal Use Only */ ) {
  var i,
    setMatchers = [],
    elementMatchers = [],
    cached = compilerCache[ selector + $fog$815 ];

  if ( !cached ) {
    // Generate a function of recursive functions that can be used to check each element
    if ( !match ) {
      match = tokenize( selector );
    }
    i = match[$fog$862];
    while ( i-- ) {
      cached = matcherFromTokens( match[i] );
      if ( cached[ expando ] ) {
        setMatchers[$fog$878]( cached );
      } else {
        elementMatchers[$fog$878]( cached );
      }
    }

    // Cache the compiled function
    cached = compilerCache( selector, matcherFromGroupMatchers( elementMatchers, setMatchers ) );

    // Save selector and tokenization
    cached[$fog$584] = selector;
  }
  return cached;
};

/**
 * A low-level selection function that works with Sizzle's compiled
 *  selector functions
 * @param {String|Function} selector A selector or a pre-compiled
 *  selector function built with Sizzle.compile
 * @param {Element} context
 * @param {Array} [results]
 * @param {Array} [seed] A set of elements to match against
 */
select = Sizzle[$fog$583] = function( selector, context, results, seed ) {
  var i, tokens, token, type, find,
    compiled = typeof selector === $fog$838 && selector,
    match = !seed && tokenize( (selector = compiled[$fog$584] || selector) );

  results = results || [];

  // Try to minimize operations if there is no seed and only one group
  if ( match[$fog$862] === $fog$852 ) {

    // Take a shortcut and set the context if the root selector is an ID
    tokens = match[$fog$865] = match[$fog$865][$fog$880]( $fog$865 );
    if ( tokens[$fog$862] > $fog$820 && (token = tokens[$fog$865])[$fog$839] === $fog$696 &&
        support[$fog$699] && context[$fog$836] === $fog$742 && documentIsHTML &&
        Expr[$fog$588][ tokens[$fog$852][$fog$839] ] ) {

      context = ( Expr[$fog$697][$fog$696]( token[$fog$662][$fog$865][$fog$842](runescape, funescape), context ) || [] )[$fog$865];
      if ( !context ) {
        return results;

      // Precompiled matchers will still verify ancestry, so step up a level
      } else if ( compiled ) {
        context = context[$fog$827];
      }

      selector = selector[$fog$880]( tokens[$fog$721]()[$fog$693][$fog$862] );
    }

    // Fetch a seed set for right-to-left matching
    i = matchExpr[$fog$582][$fog$733]( selector ) ? $fog$865 : tokens[$fog$862];
    while ( i-- ) {
      token = tokens[i];

      // Abort if we hit a combinator
      if ( Expr[$fog$588][ (type = token[$fog$839]) ] ) {
        break;
      }
      if ( (find = Expr[$fog$697][ type ]) ) {
        // Search, expanding context for leading sibling combinators
        if ( (seed = find(
          token[$fog$662][$fog$865][$fog$842]( runescape, funescape ),
          rsibling[$fog$733]( tokens[$fog$865][$fog$839] ) && testContext( context[$fog$827] ) || context
        )) ) {

          // If seed is empty or no tokens remain, we can return early
          tokens[$fog$850]( i, $fog$852 );
          selector = seed[$fog$862] && toSelector( tokens );
          if ( !selector ) {
            push[$fog$854]( results, seed );
            return results;
          }

          break;
        }
      }
    }
  }

  // Compile and execute a filtering function if one is not provided
  // Provide `match` to avoid retokenization if we modified the selector above
  ( compiled || compile( selector, match ) )(
    seed,
    context,
    !documentIsHTML,
    results,
    rsibling[$fog$733]( selector ) && testContext( context[$fog$827] ) || context
  );
  return results;
};

// One-time assignments

// Sort stability
support[$fog$641] = expando[$fog$817]($fog$866)[$fog$851]( sortOrder )[$fog$727]($fog$866) === expando;

// Support: Chrome 14-35+
// Always assume duplicates if they aren't passed to the comparison function
support[$fog$642] = !!hasDuplicate;

// Initialize against the default document
setDocument();

// Support: Webkit<537.32 - Safari 6.0.3/Chrome 25 (fixed in Chrome 27)
// Detached nodes confoundingly follow *each other*
support[$fog$651] = assert(function( div1 ) {
  // Should return 1, but returns 4 (following)
  return div1[$fog$654]( document[$fog$831]($fog$720) ) & $fog$852;
});

// Support: IE<8
// Prevent attribute/property "interpolation"
// http://msdn.microsoft.com/en-us/library/ms536429%28VS.85%29.aspx
if ( !assert(function( div ) {
  div[$fog$690] = $fog$581;
  return div[$fog$638][$fog$732]($fog$601) === $fog$580 ;
}) ) {
  addHandle( $fog$579, function( elem, name, isXML ) {
    if ( !isXML ) {
      return elem[$fog$732]( name, name[$fog$822]() === $fog$839 ? $fog$852 : $fog$820 );
    }
  });
}

// Support: IE<9
// Use defaultValue in place of getAttribute("value")
if ( !support[$fog$702] || !assert(function( div ) {
  div[$fog$690] = $fog$578;
  div[$fog$638][$fog$730]( $fog$693, $fog$866 );
  return div[$fog$638][$fog$732]( $fog$693 ) === $fog$866;
}) ) {
  addHandle( $fog$693, function( elem, name, isXML ) {
    if ( !isXML && elem[$fog$823][$fog$822]() === $fog$715 ) {
      return elem[$fog$577];
    }
  });
}

// Support: IE<9
// Use getAttributeNode to fetch booleans when getAttribute lies
if ( !assert(function( div ) {
  return div[$fog$732]($fog$599) == $fog$863;
}) ) {
  addHandle( booleans, function( elem, name, isXML ) {
    var val;
    if ( !isXML ) {
      return elem[ name ] === $fog$883 ? name[$fog$822]() :
          (val = elem[$fog$694]( name )) && val[$fog$646] ?
          val[$fog$693] :
        $fog$863;
    }
  });
}

return Sizzle;

})( window );



jQuery[$fog$697] = Sizzle;
jQuery[$fog$576] = Sizzle[$fog$636];
jQuery[$fog$576][$fog$575] = jQuery[$fog$576][$fog$612];
jQuery[$fog$574] = Sizzle[$fog$643];
jQuery[$fog$829] = Sizzle[$fog$640];
jQuery[$fog$573] = Sizzle[$fog$712];
jQuery[$fog$653] = Sizzle[$fog$653];



var rneedsContext = jQuery[$fog$576][$fog$572][$fog$582];

var rsingleTag = ($fog$571);



var risSimple = $fog$570;

// Implement the identical functionality for filter and not
function winnow( elements, qualifier, not ) {
  if ( jQuery[$fog$846]( qualifier ) ) {
    return jQuery[$fog$569]( elements, function( elem, i ) {
      /* jshint -W018 */
      return !!qualifier[$fog$864]( elem, i, elem ) !== not;
    });

  }

  if ( qualifier[$fog$836] ) {
    return jQuery[$fog$569]( elements, function( elem ) {
      return ( elem === qualifier ) !== not;
    });

  }

  if ( typeof qualifier === $fog$821 ) {
    if ( risSimple[$fog$733]( qualifier ) ) {
      return jQuery[$fog$695]( qualifier, elements, not );
    }

    qualifier = jQuery[$fog$695]( qualifier, elements );
  }

  return jQuery[$fog$569]( elements, function( elem ) {
    return ( indexOf[$fog$864]( qualifier, elem ) >= $fog$865 ) !== not;
  });
}

jQuery[$fog$695] = function( expr, elems, not ) {
  var elem = elems[ $fog$865 ];

  if ( not ) {
    expr = $fog$568 + expr + $fog$780;
  }

  return elems[$fog$862] === $fog$852 && elem[$fog$836] === $fog$852 ?
    jQuery[$fog$697][$fog$663]( elem, expr ) ? [ elem ] : [] :
    jQuery[$fog$697][$fog$662]( expr, jQuery[$fog$569]( elems, function( elem ) {
      return elem[$fog$836] === $fog$852;
    }));
};

jQuery[$fog$872][$fog$849]({
  find: function( selector ) {
    var i,
      len = this[$fog$862],
      ret = [],
      self = this;

    if ( typeof selector !== $fog$821 ) {
      return this[$fog$856]( jQuery( selector )[$fog$695](function() {
        for ( i = $fog$865; i < len; i++ ) {
          if ( jQuery[$fog$653]( self[ i ], this ) ) {
            return $fog$883;
          }
        }
      }) );
    }

    for ( i = $fog$865; i < len; i++ ) {
      jQuery[$fog$697]( selector, self[ i ], ret );
    }

    // Needed because $( selector, context ) becomes $( context ).find( selector )
    ret = this[$fog$856]( len > $fog$852 ? jQuery[$fog$574]( ret ) : ret );
    ret[$fog$584] = this[$fog$584] ? this[$fog$584] + $fog$815 + selector : selector;
    return ret;
  },
  filter: function( selector ) {
    return this[$fog$856]( winnow(this, selector || [], $fog$848) );
  },
  not: function( selector ) {
    return this[$fog$856]( winnow(this, selector || [], $fog$883) );
  },
  is: function( selector ) {
    return !!winnow(
      this,

      // If this is a positional/relative selector, check membership in the returned set
      // so $("p:first").is("p:last") won't return true for a doc with two "p".
      typeof selector === $fog$821 && rneedsContext[$fog$733]( selector ) ?
        jQuery( selector ) :
        selector || [],
      $fog$848
    )[$fog$862];
  }
});


// Initialize a jQuery object


// A central reference to the root jQuery(document)
var rootjQuery,

  // A simple way to check for HTML strings
  // Prioritize #id over <tag> to avoid XSS via location.hash (#9521)
  // Strict HTML recognition (#11290: must start with <)
  rquickExpr = $fog$567,

  init = jQuery[$fog$872][$fog$873] = function( selector, context ) {
    var match, elem;

    // HANDLE: $(""), $(null), $(undefined), $(false)
    if ( !selector ) {
      return this;
    }

    // Handle HTML strings
    if ( typeof selector === $fog$821 ) {
      if ( selector[$fog$865] === $fog$566 && selector[ selector[$fog$862] - $fog$852 ] === $fog$565 && selector[$fog$862] >= $fog$736 ) {
        // Assume that strings that start and end with <> are HTML and skip the regex check
        match = [ $fog$863, selector, $fog$863 ];

      } else {
        match = rquickExpr[$fog$740]( selector );
      }

      // Match html or make sure no context is specified for #id
      if ( match && (match[$fog$852] || !context) ) {

        // HANDLE: $(html) -> $(array)
        if ( match[$fog$852] ) {
          context = context instanceof jQuery ? context[$fog$865] : context;

          // Option to run scripts is true for back-compat
          // Intentionally let the error be thrown if parseHTML is not present
          jQuery[$fog$861]( this, jQuery[$fog$564](
            match[$fog$852],
            context && context[$fog$836] ? context[$fog$743] || context : document,
            $fog$883
          ) );

          // HANDLE: $(html, props)
          if ( rsingleTag[$fog$733]( match[$fog$852] ) && jQuery[$fog$845]( context ) ) {
            for ( match in context ) {
              // Properties of context are called as methods if possible
              if ( jQuery[$fog$846]( this[ match ] ) ) {
                this[ match ]( context[ match ] );

              // ...and otherwise set as attributes
              } else {
                this[$fog$647]( match, context[ match ] );
              }
            }
          }

          return this;

        // HANDLE: $(#id)
        } else {
          elem = document[$fog$739]( match[$fog$820] );

          // Support: Blackberry 4.6
          // gEBID returns nodes no longer in the document (#6963)
          if ( elem && elem[$fog$827] ) {
            // Inject the element directly into the jQuery object
            this[$fog$862] = $fog$852;
            this[$fog$865] = elem;
          }

          this[$fog$858] = document;
          this[$fog$584] = selector;
          return this;
        }

      // HANDLE: $(expr, $(...))
      } else if ( !context || context[$fog$563] ) {
        return ( context || rootjQuery )[$fog$697]( selector );

      // HANDLE: $(expr, context)
      // (which is just equivalent to: $(context).find(expr)
      } else {
        return this[$fog$860]( context )[$fog$697]( selector );
      }

    // HANDLE: $(DOMElement)
    } else if ( selector[$fog$836] ) {
      this[$fog$858] = this[$fog$865] = selector;
      this[$fog$862] = $fog$852;
      return this;

    // HANDLE: $(function)
    // Shortcut for document ready
    } else if ( jQuery[$fog$846]( selector ) ) {
      return typeof rootjQuery[$fog$562] !== $fog$881 ?
        rootjQuery[$fog$562]( selector ) :
        // Execute immediately if ready is not present
        selector( jQuery );
    }

    if ( selector[$fog$584] !== undefined ) {
      this[$fog$584] = selector[$fog$584];
      this[$fog$858] = selector[$fog$858];
    }

    return jQuery[$fog$561]( selector, this );
  };

// Give the init function the jQuery prototype for later instantiation
init[$fog$867] = jQuery[$fog$872];

// Initialize central reference
rootjQuery = jQuery( document );


var rparentsprev = $fog$560,
  // Methods guaranteed to produce a unique set when starting from a unique set
  guaranteedUnique = {
    children: $fog$883,
    contents: $fog$883,
    next: $fog$883,
    prev: $fog$883
  };

jQuery[$fog$849]({
  dir: function( elem, dir, until ) {
    var matched = [],
      truncate = until !== undefined;

    while ( (elem = elem[ dir ]) && elem[$fog$836] !== $fog$742 ) {
      if ( elem[$fog$836] === $fog$852 ) {
        if ( truncate && jQuery( elem )[$fog$559]( until ) ) {
          break;
        }
        matched[$fog$878]( elem );
      }
    }
    return matched;
  },

  sibling: function( n, elem ) {
    var matched = [];

    for ( ; n; n = n[$fog$716] ) {
      if ( n[$fog$836] === $fog$852 && n !== elem ) {
        matched[$fog$878]( n );
      }
    }

    return matched;
  }
});

jQuery[$fog$872][$fog$849]({
  has: function( target ) {
    var targets = jQuery( target, this ),
      l = targets[$fog$862];

    return this[$fog$695](function() {
      var i = $fog$865;
      for ( ; i < l; i++ ) {
        if ( jQuery[$fog$653]( this, targets[i] ) ) {
          return $fog$883;
        }
      }
    });
  },

  closest: function( selectors, context ) {
    var cur,
      i = $fog$865,
      l = this[$fog$862],
      matched = [],
      pos = rneedsContext[$fog$733]( selectors ) || typeof selectors !== $fog$821 ?
        jQuery( selectors, context || this[$fog$858] ) :
        $fog$865;

    for ( ; i < l; i++ ) {
      for ( cur = this[i]; cur && cur !== context; cur = cur[$fog$827] ) {
        // Always skip document fragments
        if ( cur[$fog$836] < $fog$741 && (pos ?
          pos[$fog$558](cur) > -$fog$852 :

          // Don't pass non-elements to Sizzle
          cur[$fog$836] === $fog$852 &&
            jQuery[$fog$697][$fog$663](cur, selectors)) ) {

          matched[$fog$878]( cur );
          break;
        }
      }
    }

    return this[$fog$856]( matched[$fog$862] > $fog$852 ? jQuery[$fog$574]( matched ) : matched );
  },

  // Determine the position of an element within the set
  index: function( elem ) {

    // No argument, return index in parent
    if ( !elem ) {
      return ( this[ $fog$865 ] && this[ $fog$865 ][$fog$827] ) ? this[$fog$589]()[$fog$557]()[$fog$862] : -$fog$852;
    }

    // Index in selector
    if ( typeof elem === $fog$821 ) {
      return indexOf[$fog$864]( jQuery( elem ), this[ $fog$865 ] );
    }

    // Locate the position of the desired element
    return indexOf[$fog$864]( this,

      // If it receives a jQuery object, the first element is used
      elem[$fog$563] ? elem[ $fog$865 ] : elem
    );
  },

  add: function( selector, context ) {
    return this[$fog$856](
      jQuery[$fog$574](
        jQuery[$fog$861]( this[$fog$556](), jQuery( selector, context ) )
      )
    );
  },

  addBack: function( selector ) {
    return this[$fog$555]( selector == $fog$863 ?
      this[$fog$859] : this[$fog$859][$fog$695](selector)
    );
  }
});

function sibling( cur, dir ) {
  while ( (cur = cur[dir]) && cur[$fog$836] !== $fog$852 ) {}
  return cur;
}

jQuery[$fog$857]({
  parent: function( elem ) {
    var parent = elem[$fog$827];
    return parent && parent[$fog$836] !== $fog$741 ? parent : $fog$863;
  },
  parents: function( elem ) {
    return jQuery[$fog$590]( elem, $fog$827 );
  },
  parentsUntil: function( elem, i, until ) {
    return jQuery[$fog$590]( elem, $fog$827, until );
  },
  next: function( elem ) {
    return sibling( elem, $fog$716 );
  },
  prev: function( elem ) {
    return sibling( elem, $fog$634 );
  },
  nextAll: function( elem ) {
    return jQuery[$fog$590]( elem, $fog$716 );
  },
  prevAll: function( elem ) {
    return jQuery[$fog$590]( elem, $fog$634 );
  },
  nextUntil: function( elem, i, until ) {
    return jQuery[$fog$590]( elem, $fog$716, until );
  },
  prevUntil: function( elem, i, until ) {
    return jQuery[$fog$590]( elem, $fog$634, until );
  },
  siblings: function( elem ) {
    return jQuery[$fog$554]( ( elem[$fog$827] || {} )[$fog$638], elem );
  },
  children: function( elem ) {
    return jQuery[$fog$554]( elem[$fog$638] );
  },
  contents: function( elem ) {
    return elem[$fog$553] || jQuery[$fog$861]( [], elem[$fog$744] );
  }
}, function( name, fn ) {
  jQuery[$fog$872][ name ] = function( until, selector ) {
    var matched = jQuery[$fog$855]( this, fn, until );

    if ( name[$fog$880]( -$fog$633 ) !== $fog$552 ) {
      selector = until;
    }

    if ( selector && typeof selector === $fog$821 ) {
      matched = jQuery[$fog$695]( selector, matched );
    }

    if ( this[$fog$862] > $fog$852 ) {
      // Remove duplicates
      if ( !guaranteedUnique[ name ] ) {
        jQuery[$fog$574]( matched );
      }

      // Reverse order for parents* and prev-derivatives
      if ( rparentsprev[$fog$733]( name ) ) {
        matched[$fog$551]();
      }
    }

    return this[$fog$856]( matched );
  };
});
var rnotwhite = ($fog$550);



// String to Object options format cache
var optionsCache = {};

// Convert String-formatted options into Object-formatted ones and store in cache
function createOptions( options ) {
  var object = optionsCache[ options ] = {};
  jQuery[$fog$857]( options[$fog$572]( rnotwhite ) || [], function( _, flag ) {
    object[ flag ] = $fog$883;
  });
  return object;
}

/*
 * Create a callback list using the following parameters:
 *
 *  options: an optional list of space-separated options that will change how
 *      the callback list behaves or a more traditional option object
 *
 * By default a callback list will act like an event callback list and can be
 * "fired" multiple times.
 *
 * Possible options:
 *
 *  once:     will ensure the callback list can only be fired once (like a Deferred)
 *
 *  memory:     will keep track of previous values and will call any callback added
 *          after the list has been fired right away with the latest "memorized"
 *          values (like a Deferred)
 *
 *  unique:     will ensure a callback can only be added once (no duplicate in the list)
 *
 *  stopOnFalse:  interrupt callings when a callback returns false
 *
 */
jQuery[$fog$549] = function( options ) {

  // Convert options from String-formatted to Object-formatted if needed
  // (we check in cache first)
  options = typeof options === $fog$821 ?
    ( optionsCache[ options ] || createOptions( options ) ) :
    jQuery[$fog$849]( {}, options );

  var // Last fire value (for non-forgettable lists)
    memory,
    // Flag to know if list was already fired
    fired,
    // Flag to know if list is currently firing
    firing,
    // First callback to fire (used internally by add and fireWith)
    firingStart,
    // End of the loop when firing
    firingLength,
    // Index of currently firing callback (modified by remove if needed)
    firingIndex,
    // Actual callback list
    list = [],
    // Stack of fire calls for repeatable lists
    stack = !options[$fog$548] && [],
    // Fire callbacks
    fire = function( data ) {
      memory = options[$fog$547] && data;
      fired = $fog$883;
      firingIndex = firingStart || $fog$865;
      firingStart = $fog$865;
      firingLength = list[$fog$862];
      firing = $fog$883;
      for ( ; list && firingIndex < firingLength; firingIndex++ ) {
        if ( list[ firingIndex ][$fog$854]( data[ $fog$865 ], data[ $fog$852 ] ) === $fog$848 && options[$fog$546] ) {
          memory = $fog$848; // To prevent further calls using add
          break;
        }
      }
      firing = $fog$848;
      if ( list ) {
        if ( stack ) {
          if ( stack[$fog$862] ) {
            fire( stack[$fog$721]() );
          }
        } else if ( memory ) {
          list = [];
        } else {
          self[$fog$545]();
        }
      }
    },
    // Actual Callbacks object
    self = {
      // Add a callback or a collection of callbacks to the list
      add: function() {
        if ( list ) {
          // First, we save the current length
          var start = list[$fog$862];
          (function add( args ) {
            jQuery[$fog$857]( args, function( _, arg ) {
              var type = jQuery[$fog$839]( arg );
              if ( type === $fog$838 ) {
                if ( !options[$fog$574] || !self[$fog$544]( arg ) ) {
                  list[$fog$878]( arg );
                }
              } else if ( arg && arg[$fog$862] && type !== $fog$821 ) {
                // Inspect recursively
                add( arg );
              }
            });
          })( arguments );
          // Do we need to add the callbacks to the
          // current firing batch?
          if ( firing ) {
            firingLength = list[$fog$862];
          // With memory, if we're not firing then
          // we should call right away
          } else if ( memory ) {
            firingStart = start;
            fire( memory );
          }
        }
        return this;
      },
      // Remove a callback from the list
      remove: function() {
        if ( list ) {
          jQuery[$fog$857]( arguments, function( _, arg ) {
            var index;
            while ( ( index = jQuery[$fog$543]( arg, list, index ) ) > -$fog$852 ) {
              list[$fog$850]( index, $fog$852 );
              // Handle firing indexes
              if ( firing ) {
                if ( index <= firingLength ) {
                  firingLength--;
                }
                if ( index <= firingIndex ) {
                  firingIndex--;
                }
              }
            }
          });
        }
        return this;
      },
      // Check if a given callback is in the list.
      // If no argument is given, return whether or not list has callbacks attached.
      has: function( fn ) {
        return fn ? jQuery[$fog$543]( fn, list ) > -$fog$852 : !!( list && list[$fog$862] );
      },
      // Remove all callbacks from the list
      empty: function() {
        list = [];
        firingLength = $fog$865;
        return this;
      },
      // Have the list do nothing anymore
      disable: function() {
        list = stack = memory = undefined;
        return this;
      },
      // Is it disabled?
      disabled: function() {
        return !list;
      },
      // Lock the list in its current state
      lock: function() {
        stack = undefined;
        if ( !memory ) {
          self[$fog$545]();
        }
        return this;
      },
      // Is it locked?
      locked: function() {
        return !stack;
      },
      // Call all callbacks with the given context and arguments
      fireWith: function( context, args ) {
        if ( list && ( !fired || stack ) ) {
          args = args || [];
          args = [ context, args[$fog$880] ? args[$fog$880]() : args ];
          if ( firing ) {
            stack[$fog$878]( args );
          } else {
            fire( args );
          }
        }
        return this;
      },
      // Call all the callbacks with the given arguments
      fire: function() {
        self[$fog$542]( this, arguments );
        return this;
      },
      // To know if the callbacks have already been called at least once
      fired: function() {
        return !!fired;
      }
    };

  return self;
};


jQuery[$fog$849]({

  Deferred: function( func ) {
    var tuples = [
        // action, add listener, listener list, final state
        [ $fog$541, $fog$540, jQuery[$fog$549]($fog$539), $fog$538 ],
        [ $fog$537, $fog$536, jQuery[$fog$549]($fog$539), $fog$535 ],
        [ $fog$534, $fog$533, jQuery[$fog$549]($fog$547) ]
      ],
      state = $fog$532,
      promise = {
        state: function() {
          return state;
        },
        always: function() {
          deferred[$fog$540]( arguments )[$fog$536]( arguments );
          return this;
        },
        then: function( /* fnDone, fnFail, fnProgress */ ) {
          var fns = arguments;
          return jQuery[$fog$530](function( newDefer ) {
            jQuery[$fog$857]( tuples, function( i, tuple ) {
              var fn = jQuery[$fog$846]( fns[ i ] ) && fns[ i ];
              // deferred[ done | fail | progress ] for forwarding actions to newDefer
              deferred[ tuple[$fog$852] ](function() {
                var returned = fn && fn[$fog$854]( this, arguments );
                if ( returned && jQuery[$fog$846]( returned[$fog$531] ) ) {
                  returned[$fog$531]()
                    [$fog$540]( newDefer[$fog$541] )
                    [$fog$536]( newDefer[$fog$537] )
                    [$fog$533]( newDefer[$fog$534] );
                } else {
                  newDefer[ tuple[ $fog$865 ] + $fog$529 ]( this === promise ? newDefer[$fog$531]() : this, fn ? [ returned ] : arguments );
                }
              });
            });
            fns = $fog$863;
          })[$fog$531]();
        },
        // Get a promise for this deferred
        // If obj is provided, the promise aspect is added to the object
        promise: function( obj ) {
          return obj != $fog$863 ? jQuery[$fog$849]( obj, promise ) : promise;
        }
      },
      deferred = {};

    // Keep pipe for back-compat
    promise[$fog$528] = promise[$fog$527];

    // Add list-specific methods
    jQuery[$fog$857]( tuples, function( i, tuple ) {
      var list = tuple[ $fog$820 ],
        stateString = tuple[ $fog$736 ];

      // promise[ done | fail | progress ] = list.add
      promise[ tuple[$fog$852] ] = list[$fog$555];

      // Handle state
      if ( stateString ) {
        list[$fog$555](function() {
          // state = [ resolved | rejected ]
          state = stateString;

        // [ reject_list | resolve_list ].disable; progress_list.lock
        }, tuples[ i ^ $fog$852 ][ $fog$820 ][$fog$545], tuples[ $fog$820 ][ $fog$820 ][$fog$526] );
      }

      // deferred[ resolve | reject | notify ]
      deferred[ tuple[$fog$865] ] = function() {
        deferred[ tuple[$fog$865] + $fog$529 ]( this === deferred ? promise : this, arguments );
        return this;
      };
      deferred[ tuple[$fog$865] + $fog$529 ] = list[$fog$542];
    });

    // Make the deferred a promise
    promise[$fog$531]( deferred );

    // Call given func if any
    if ( func ) {
      func[$fog$864]( deferred, deferred );
    }

    // All done!
    return deferred;
  },

  // Deferred helper
  when: function( subordinate /* , ..., subordinateN */ ) {
    var i = $fog$865,
      resolveValues = slice[$fog$864]( arguments ),
      length = resolveValues[$fog$862],

      // the count of uncompleted subordinates
      remaining = length !== $fog$852 || ( subordinate && jQuery[$fog$846]( subordinate[$fog$531] ) ) ? length : $fog$865,

      // the master Deferred. If resolveValues consist of only a single Deferred, just use that.
      deferred = remaining === $fog$852 ? subordinate : jQuery[$fog$530](),

      // Update function for both resolve and progress values
      updateFunc = function( i, contexts, values ) {
        return function( value ) {
          contexts[ i ] = this;
          values[ i ] = arguments[$fog$862] > $fog$852 ? slice[$fog$864]( arguments ) : value;
          if ( values === progressValues ) {
            deferred[$fog$525]( contexts, values );
          } else if ( !( --remaining ) ) {
            deferred[$fog$524]( contexts, values );
          }
        };
      },

      progressValues, progressContexts, resolveContexts;

    // Add listeners to Deferred subordinates; treat others as resolved
    if ( length > $fog$852 ) {
      progressValues = new Array( length );
      progressContexts = new Array( length );
      resolveContexts = new Array( length );
      for ( ; i < length; i++ ) {
        if ( resolveValues[ i ] && jQuery[$fog$846]( resolveValues[ i ][$fog$531] ) ) {
          resolveValues[ i ][$fog$531]()
            [$fog$540]( updateFunc( i, resolveContexts, resolveValues ) )
            [$fog$536]( deferred[$fog$537] )
            [$fog$533]( updateFunc( i, progressContexts, progressValues ) );
        } else {
          --remaining;
        }
      }
    }

    // If we're not waiting on anything, resolve the master
    if ( !remaining ) {
      deferred[$fog$524]( resolveContexts, resolveValues );
    }

    return deferred[$fog$531]();
  }
});


// The deferred used on DOM ready
var readyList;

jQuery[$fog$872][$fog$562] = function( fn ) {
  // Add the callback
  jQuery[$fog$562][$fog$531]()[$fog$540]( fn );

  return this;
};

jQuery[$fog$849]({
  // Is the DOM ready to be used? Set to true once it occurs.
  isReady: $fog$848,

  // A counter to track how many items to wait for before
  // the ready event fires. See #6781
  readyWait: $fog$852,

  // Hold (or release) the ready event
  holdReady: function( hold ) {
    if ( hold ) {
      jQuery[$fog$523]++;
    } else {
      jQuery[$fog$562]( $fog$883 );
    }
  },

  // Handle when the DOM is ready
  ready: function( wait ) {

    // Abort if there are pending holds or we're already ready
    if ( wait === $fog$883 ? --jQuery[$fog$523] : jQuery[$fog$522] ) {
      return;
    }

    // Remember that the DOM is ready
    jQuery[$fog$522] = $fog$883;

    // If a normal DOM Ready event fired, decrement, and wait if need be
    if ( wait !== $fog$883 && --jQuery[$fog$523] > $fog$865 ) {
      return;
    }

    // If there are functions bound, to execute
    readyList[$fog$524]( document, [ jQuery ] );

    // Trigger any bound ready events
    if ( jQuery[$fog$872][$fog$521] ) {
      jQuery( document )[$fog$521]( $fog$562 );
      jQuery( document )[$fog$520]( $fog$562 );
    }
  }
});

/**
 * The ready event handler and self cleanup method
 */
function completed() {
  document[$fog$519]( $fog$518, completed, $fog$848 );
  window[$fog$519]( $fog$517, completed, $fog$848 );
  jQuery[$fog$562]();
}

jQuery[$fog$562][$fog$531] = function( obj ) {
  if ( !readyList ) {

    readyList = jQuery[$fog$530]();

    // Catch cases where $(document).ready() is called after the browser event has already occurred.
    // We once tried to use readyState "interactive" here, but it caused issues like the one
    // discovered by ChrisS here: http://bugs.jquery.com/ticket/12282#comment:15
    if ( document[$fog$516] === $fog$515 ) {
      // Handle it asynchronously to allow scripts the opportunity to delay ready
      setTimeout( jQuery[$fog$562] );

    } else {

      // Use the handy event callback
      document[$fog$706]( $fog$518, completed, $fog$848 );

      // A fallback to window.onload, that will always work
      window[$fog$706]( $fog$517, completed, $fog$848 );
    }
  }
  return readyList[$fog$531]( obj );
};

// Kick off the DOM ready check even if the user does not
jQuery[$fog$562][$fog$531]();




// Multifunctional method to get and set values of a collection
// The value/s can optionally be executed if it's a function
var access = jQuery[$fog$514] = function( elems, fn, key, value, chainable, emptyGet, raw ) {
  var i = $fog$865,
    len = elems[$fog$862],
    bulk = key == $fog$863;

  // Sets many values
  if ( jQuery[$fog$839]( key ) === $fog$886 ) {
    chainable = $fog$883;
    for ( i in key ) {
      jQuery[$fog$514]( elems, fn, i, key[i], $fog$883, emptyGet, raw );
    }

  // Sets one value
  } else if ( value !== undefined ) {
    chainable = $fog$883;

    if ( !jQuery[$fog$846]( value ) ) {
      raw = $fog$883;
    }

    if ( bulk ) {
      // Bulk operations run against the entire set
      if ( raw ) {
        fn[$fog$864]( elems, value );
        fn = $fog$863;

      // ...except when executing function values
      } else {
        bulk = fn;
        fn = function( elem, key, value ) {
          return bulk[$fog$864]( jQuery( elem ), value );
        };
      }
    }

    if ( fn ) {
      for ( ; i < len; i++ ) {
        fn( elems[i], key, raw ? value : value[$fog$864]( elems[i], i, fn( elems[i], key ) ) );
      }
    }
  }

  return chainable ?
    elems :

    // Gets
    bulk ?
      fn[$fog$864]( elems ) :
      len ? fn( elems[$fog$865], key ) : emptyGet;
};


/**
 * Determines whether an object can have data
 */
jQuery[$fog$513] = function( owner ) {
  // Accepts only:
  //  - Node
  //    - Node.ELEMENT_NODE
  //    - Node.DOCUMENT_NODE
  //  - Object
  //    - Any
  /* jshint -W018 */
  return owner[$fog$836] === $fog$852 || owner[$fog$836] === $fog$742 || !( +owner[$fog$836] );
};


function Data() {
  // Support: Android<4,
  // Old WebKit does not have Object.preventExtensions/freeze method,
  // return new empty object instead with no [[set]] accessor
  Object[$fog$512]( this[$fog$511] = {}, $fog$865, {
    get: function() {
      return {};
    }
  });

  this[$fog$510] = jQuery[$fog$510] + Data[$fog$509]++;
}

Data[$fog$509] = $fog$852;
Data[$fog$508] = jQuery[$fog$513];

Data[$fog$867] = {
  key: function( owner ) {
    // We can accept data for non-element nodes in modern browsers,
    // but we should not, see #8335.
    // Always return the key for a frozen object.
    if ( !Data[$fog$508]( owner ) ) {
      return $fog$865;
    }

    var descriptor = {},
      // Check if the owner object already has a cache key
      unlock = owner[ this[$fog$510] ];

    // If not, create one
    if ( !unlock ) {
      unlock = Data[$fog$509]++;

      // Secure it in a non-enumerable, non-writable property
      try {
        descriptor[ this[$fog$510] ] = { value: unlock };
        Object[$fog$507]( owner, descriptor );

      // Support: Android<4
      // Fallback to a less secure definition
      } catch ( e ) {
        descriptor[ this[$fog$510] ] = unlock;
        jQuery[$fog$849]( owner, descriptor );
      }
    }

    // Ensure the cache object
    if ( !this[$fog$511][ unlock ] ) {
      this[$fog$511][ unlock ] = {};
    }

    return unlock;
  },
  set: function( owner, data, value ) {
    var prop,
      // There may be an unlock assigned to this node,
      // if there is no entry for this "owner", create one inline
      // and set the unlock as though an owner entry had always existed
      unlock = this[$fog$506]( owner ),
      cache = this[$fog$511][ unlock ];

    // Handle: [ owner, key, value ] args
    if ( typeof data === $fog$821 ) {
      cache[ data ] = value;

    // Handle: [ owner, { properties } ] args
    } else {
      // Fresh assignments by object are shallow copied
      if ( jQuery[$fog$505]( cache ) ) {
        jQuery[$fog$849]( this[$fog$511][ unlock ], data );
      // Otherwise, copy the properties one-by-one to the cache object
      } else {
        for ( prop in data ) {
          cache[ prop ] = data[ prop ];
        }
      }
    }
    return cache;
  },
  get: function( owner, key ) {
    // Either a valid cache is found, or will be created.
    // New caches will be created and the unlock returned,
    // allowing direct access to the newly created
    // empty data object. A valid owner object must be provided.
    var cache = this[$fog$511][ this[$fog$506]( owner ) ];

    return key === undefined ?
      cache : cache[ key ];
  },
  access: function( owner, key, value ) {
    var stored;
    // In cases where either:
    //
    //   1. No key was specified
    //   2. A string key was specified, but no value provided
    //
    // Take the "read" path and allow the get method to determine
    // which value to return, respectively either:
    //
    //   1. The entire cache object
    //   2. The data stored at the key
    //
    if ( key === undefined ||
        ((key && typeof key === $fog$821) && value === undefined) ) {

      stored = this[$fog$556]( owner, key );

      return stored !== undefined ?
        stored : this[$fog$556]( owner, jQuery[$fog$504](key) );
    }

    // [*]When the key is not a string, or both a key and value
    // are specified, set or extend (existing objects) with either:
    //
    //   1. An object of properties
    //   2. A key and value
    //
    this[$fog$503]( owner, key, value );

    // Since the "set" path can have two possible entry points
    // return the expected data based on which path was taken[*]
    return value !== undefined ? value : key;
  },
  remove: function( owner, key ) {
    var i, name, camel,
      unlock = this[$fog$506]( owner ),
      cache = this[$fog$511][ unlock ];

    if ( key === undefined ) {
      this[$fog$511][ unlock ] = {};

    } else {
      // Support array or space separated string of keys
      if ( jQuery[$fog$844]( key ) ) {
        // If "name" is an array of keys...
        // When data is initially created, via ("key", "val") signature,
        // keys will be converted to camelCase.
        // Since there is no way to tell _how_ a key was added, remove
        // both plain key and camelCase key. #12786
        // This will only penalize the array argument path.
        name = key[$fog$879]( key[$fog$855]( jQuery[$fog$504] ) );
      } else {
        camel = jQuery[$fog$504]( key );
        // Try the string as a key before any manipulation
        if ( key in cache ) {
          name = [ key, camel ];
        } else {
          // If a key with the spaces exists, use it.
          // Otherwise, create an array by matching non-whitespace
          name = camel;
          name = name in cache ?
            [ name ] : ( name[$fog$572]( rnotwhite ) || [] );
        }
      }

      i = name[$fog$862];
      while ( i-- ) {
        delete cache[ name[ i ] ];
      }
    }
  },
  hasData: function( owner ) {
    return !jQuery[$fog$505](
      this[$fog$511][ owner[ this[$fog$510] ] ] || {}
    );
  },
  discard: function( owner ) {
    if ( owner[ this[$fog$510] ] ) {
      delete this[$fog$511][ owner[ this[$fog$510] ] ];
    }
  }
};
var data_priv = new Data();

var data_user = new Data();



//  Implementation Summary
//
//  1. Enforce API surface and semantic compatibility with 1.9.x branch
//  2. Improve the module's maintainability by reducing the storage
//    paths to a single mechanism.
//  3. Use the same single mechanism to support "private" and "user" data.
//  4. _Never_ expose "private" data to user code (TODO: Drop _data, _removeData)
//  5. Avoid exposing implementation details on user objects (eg. expando properties)
//  6. Provide a clear path for implementation upgrade to WeakMap in 2014

var rbrace = $fog$502,
  rmultiDash = $fog$501;

function dataAttr( elem, key, data ) {
  var name;

  // If nothing was found internally, try to fetch any
  // data from the HTML5 data-* attribute
  if ( data === undefined && elem[$fog$836] === $fog$852 ) {
    name = $fog$500 + key[$fog$842]( rmultiDash, $fog$499 )[$fog$822]();
    data = elem[$fog$732]( name );

    if ( typeof data === $fog$821 ) {
      try {
        data = data === $fog$498 ? $fog$883 :
          data === $fog$497 ? $fog$848 :
          data === $fog$496 ? $fog$863 :
          // Only convert to a number if it doesn't change the string
          +data + $fog$866 === data ? +data :
          rbrace[$fog$733]( data ) ? jQuery[$fog$495]( data ) :
          data;
      } catch( e ) {}

      // Make sure we set the data so it isn't changed later
      data_user[$fog$503]( elem, key, data );
    } else {
      data = undefined;
    }
  }
  return data;
}

jQuery[$fog$849]({
  hasData: function( elem ) {
    return data_user[$fog$494]( elem ) || data_priv[$fog$494]( elem );
  },

  data: function( elem, name, data ) {
    return data_user[$fog$514]( elem, name, data );
  },

  removeData: function( elem, name ) {
    data_user[$fog$493]( elem, name );
  },

  // TODO: Now that all calls to _data and _removeData have been replaced
  // with direct calls to data_priv methods, these can be deprecated.
  _data: function( elem, name, data ) {
    return data_priv[$fog$514]( elem, name, data );
  },

  _removeData: function( elem, name ) {
    data_priv[$fog$493]( elem, name );
  }
});

jQuery[$fog$872][$fog$849]({
  data: function( key, value ) {
    var i, name, data,
      elem = this[ $fog$865 ],
      attrs = elem && elem[$fog$702];

    // Gets all values
    if ( key === undefined ) {
      if ( this[$fog$862] ) {
        data = data_user[$fog$556]( elem );

        if ( elem[$fog$836] === $fog$852 && !data_priv[$fog$556]( elem, $fog$492 ) ) {
          i = attrs[$fog$862];
          while ( i-- ) {

            // Support: IE11+
            // The attrs elements can be null (#14894)
            if ( attrs[ i ] ) {
              name = attrs[ i ][$fog$671];
              if ( name[$fog$877]( $fog$500 ) === $fog$865 ) {
                name = jQuery[$fog$504]( name[$fog$880]($fog$633) );
                dataAttr( elem, name, data[ name ] );
              }
            }
          }
          data_priv[$fog$503]( elem, $fog$492, $fog$883 );
        }
      }

      return data;
    }

    // Sets multiple values
    if ( typeof key === $fog$886 ) {
      return this[$fog$857](function() {
        data_user[$fog$503]( this, key );
      });
    }

    return access( this, function( value ) {
      var data,
        camelKey = jQuery[$fog$504]( key );

      // The calling jQuery object (element matches) is not empty
      // (and therefore has an element appears at this[ 0 ]) and the
      // `value` parameter was not undefined. An empty jQuery object
      // will result in `undefined` for elem = this[ 0 ] which will
      // throw an exception if an attempt to read a data cache is made.
      if ( elem && value === undefined ) {
        // Attempt to get data from the cache
        // with the key as-is
        data = data_user[$fog$556]( elem, key );
        if ( data !== undefined ) {
          return data;
        }

        // Attempt to get data from the cache
        // with the key camelized
        data = data_user[$fog$556]( elem, camelKey );
        if ( data !== undefined ) {
          return data;
        }

        // Attempt to "discover" the data in
        // HTML5 custom data-* attrs
        data = dataAttr( elem, camelKey, undefined );
        if ( data !== undefined ) {
          return data;
        }

        // We tried really hard, but the data doesn't exist.
        return;
      }

      // Set the data...
      this[$fog$857](function() {
        // First, attempt to store a copy or reference of any
        // data that might've been store with a camelCased key.
        var data = data_user[$fog$556]( this, camelKey );

        // For HTML5 data-* attribute interop, we have to
        // store property names with dashes in a camelCase form.
        // This might not apply to all properties...*
        data_user[$fog$503]( this, camelKey, value );

        // *... In the case of properties that might _actually_
        // have dashes, we need to also store a copy of that
        // unchanged property.
        if ( key[$fog$877]($fog$617) !== -$fog$852 && data !== undefined ) {
          data_user[$fog$503]( this, key, value );
        }
      });
    }, $fog$863, value, arguments[$fog$862] > $fog$852, $fog$863, $fog$883 );
  },

  removeData: function( key ) {
    return this[$fog$857](function() {
      data_user[$fog$493]( this, key );
    });
  }
});


jQuery[$fog$849]({
  queue: function( elem, type, data ) {
    var queue;

    if ( elem ) {
      type = ( type || $fog$491 ) + $fog$490;
      queue = data_priv[$fog$556]( elem, type );

      // Speed up dequeue by getting out quickly if this is just a lookup
      if ( data ) {
        if ( !queue || jQuery[$fog$844]( data ) ) {
          queue = data_priv[$fog$514]( elem, type, jQuery[$fog$561](data) );
        } else {
          queue[$fog$878]( data );
        }
      }
      return queue || [];
    }
  },

  dequeue: function( elem, type ) {
    type = type || $fog$491;

    var queue = jQuery[$fog$490]( elem, type ),
      startLength = queue[$fog$862],
      fn = queue[$fog$721](),
      hooks = jQuery[$fog$489]( elem, type ),
      next = function() {
        jQuery[$fog$488]( elem, type );
      };

    // If the fx queue is dequeued, always remove the progress sentinel
    if ( fn === $fog$487 ) {
      fn = queue[$fog$721]();
      startLength--;
    }

    if ( fn ) {

      // Add a progress sentinel to prevent the fx queue from being
      // automatically dequeued
      if ( type === $fog$491 ) {
        queue[$fog$649]( $fog$487 );
      }

      // Clear up the last queue stop function
      delete hooks[$fog$486];
      fn[$fog$864]( elem, next, hooks );
    }

    if ( !startLength && hooks ) {
      hooks[$fog$594][$fog$485]();
    }
  },

  // Not public - generate a queueHooks object, or return the current one
  _queueHooks: function( elem, type ) {
    var key = type + $fog$484;
    return data_priv[$fog$556]( elem, key ) || data_priv[$fog$514]( elem, key, {
      empty: jQuery[$fog$549]($fog$539)[$fog$555](function() {
        data_priv[$fog$493]( elem, [ type + $fog$490, key ] );
      })
    });
  }
});

jQuery[$fog$872][$fog$849]({
  queue: function( type, data ) {
    var setter = $fog$820;

    if ( typeof type !== $fog$821 ) {
      data = type;
      type = $fog$491;
      setter--;
    }

    if ( arguments[$fog$862] < setter ) {
      return jQuery[$fog$490]( this[$fog$865], type );
    }

    return data === undefined ?
      this :
      this[$fog$857](function() {
        var queue = jQuery[$fog$490]( this, type, data );

        // Ensure a hooks for this queue
        jQuery[$fog$489]( this, type );

        if ( type === $fog$491 && queue[$fog$865] !== $fog$487 ) {
          jQuery[$fog$488]( this, type );
        }
      });
  },
  dequeue: function( type ) {
    return this[$fog$857](function() {
      jQuery[$fog$488]( this, type );
    });
  },
  clearQueue: function( type ) {
    return this[$fog$490]( type || $fog$491, [] );
  },
  // Get a promise resolved when queues of a certain type
  // are emptied (fx is the type by default)
  promise: function( type, obj ) {
    var tmp,
      count = $fog$852,
      defer = jQuery[$fog$530](),
      elements = this,
      i = this[$fog$862],
      resolve = function() {
        if ( !( --count ) ) {
          defer[$fog$524]( elements, [ elements ] );
        }
      };

    if ( typeof type !== $fog$821 ) {
      obj = type;
      type = undefined;
    }
    type = type || $fog$491;

    while ( i-- ) {
      tmp = data_priv[$fog$556]( elements[ i ], type + $fog$484 );
      if ( tmp && tmp[$fog$594] ) {
        count++;
        tmp[$fog$594][$fog$555]( resolve );
      }
    }
    resolve();
    return defer[$fog$531]( obj );
  }
});
var pnum = ($fog$482)[$fog$483];

var cssExpand = [ $fog$481, $fog$480, $fog$479, $fog$478 ];

var isHidden = function( elem, el ) {
    // isHidden might be called from jQuery#filter function;
    // in that case, element will be second argument
    elem = el || elem;
    return jQuery[$fog$477]( elem, $fog$476 ) === $fog$475 || !jQuery[$fog$653]( elem[$fog$743], elem );
  };

var rcheckableType = ($fog$474);



(function() {
  var fragment = document[$fog$473](),
    div = fragment[$fog$826]( document[$fog$831]( $fog$720 ) ),
    input = document[$fog$831]( $fog$715 );

  // Support: Safari<=5.1
  // Check state lost if the name is set (#11217)
  // Support: Windows Web Apps (WWA)
  // `name` and `type` must use .setAttribute for WWA (#14901)
  input[$fog$730]( $fog$839, $fog$472 );
  input[$fog$730]( $fog$598, $fog$598 );
  input[$fog$730]( $fog$671, $fog$471 );

  div[$fog$826]( input );

  // Support: Safari<=5.1, Android<4.2
  // Older WebKit doesn't clone checked state correctly in fragments
  support[$fog$470] = div[$fog$469]( $fog$883 )[$fog$469]( $fog$883 )[$fog$613][$fog$598];

  // Support: IE<=11+
  // Make sure textarea (and checkbox) defaultValue is properly cloned
  div[$fog$690] = $fog$468;
  support[$fog$467] = !!div[$fog$469]( $fog$883 )[$fog$613][$fog$577];
})();
var strundefined = typeof undefined;



support[$fog$466] = $fog$465 in window;


var
  rkeyEvent = $fog$464,
  rmouseEvent = $fog$463,
  rfocusMorph = $fog$462,
  rtypenamespace = $fog$461;

function returnTrue() {
  return $fog$883;
}

function returnFalse() {
  return $fog$848;
}

function safeActiveElement() {
  try {
    return document[$fog$603];
  } catch ( err ) { }
}

/*
 * Helper functions for managing events -- not part of the public interface.
 * Props to Dean Edwards' addEvent library for many of the ideas.
 */
jQuery[$fog$460] = {

  global: {},

  add: function( elem, types, handler, data, selector ) {

    var handleObjIn, eventHandle, tmp,
      events, t, handleObj,
      special, handlers, type, namespaces, origType,
      elemData = data_priv[$fog$556]( elem );

    // Don't attach events to noData or text/comment nodes (but allow plain objects)
    if ( !elemData ) {
      return;
    }

    // Caller can pass in an object of custom data in lieu of the handler
    if ( handler[$fog$459] ) {
      handleObjIn = handler;
      handler = handleObjIn[$fog$459];
      selector = handleObjIn[$fog$584];
    }

    // Make sure that the handler has a unique ID, used to find/remove it later
    if ( !handler[$fog$819] ) {
      handler[$fog$819] = jQuery[$fog$819]++;
    }

    // Init the element's event structure and main handler, if this is the first
    if ( !(events = elemData[$fog$458]) ) {
      events = elemData[$fog$458] = {};
    }
    if ( !(eventHandle = elemData[$fog$457]) ) {
      eventHandle = elemData[$fog$457] = function( e ) {
        // Discard the second event of a jQuery.event.trigger() and
        // when an event is called after a page has unloaded
        return typeof jQuery !== strundefined && jQuery[$fog$460][$fog$456] !== e[$fog$839] ?
          jQuery[$fog$460][$fog$455][$fog$854]( elem, arguments ) : undefined;
      };
    }

    // Handle multiple events separated by a space
    types = ( types || $fog$866 )[$fog$572]( rnotwhite ) || [ $fog$866 ];
    t = types[$fog$862];
    while ( t-- ) {
      tmp = rtypenamespace[$fog$740]( types[t] ) || [];
      type = origType = tmp[$fog$852];
      namespaces = ( tmp[$fog$820] || $fog$866 )[$fog$817]( $fog$454 )[$fog$851]();

      // There *must* be a type, no attaching namespace-only handlers
      if ( !type ) {
        continue;
      }

      // If event changes its type, use the special event handlers for the changed type
      special = jQuery[$fog$460][$fog$453][ type ] || {};

      // If selector defined, determine special event api type, otherwise given type
      type = ( selector ? special[$fog$452] : special[$fog$451] ) || type;

      // Update special based on newly reset type
      special = jQuery[$fog$460][$fog$453][ type ] || {};

      // handleObj is passed to all event handlers
      handleObj = jQuery[$fog$849]({
        type: type,
        origType: origType,
        data: data,
        handler: handler,
        guid: handler[$fog$819],
        selector: selector,
        needsContext: selector && jQuery[$fog$576][$fog$572][$fog$582][$fog$733]( selector ),
        namespace: namespaces[$fog$727]($fog$454)
      }, handleObjIn );

      // Init the event handler queue if we're the first
      if ( !(handlers = events[ type ]) ) {
        handlers = events[ type ] = [];
        handlers[$fog$450] = $fog$865;

        // Only use addEventListener if the special events handler returns false
        if ( !special[$fog$449] || special[$fog$449][$fog$864]( elem, data, namespaces, eventHandle ) === $fog$848 ) {
          if ( elem[$fog$706] ) {
            elem[$fog$706]( type, eventHandle, $fog$848 );
          }
        }
      }

      if ( special[$fog$555] ) {
        special[$fog$555][$fog$864]( elem, handleObj );

        if ( !handleObj[$fog$459][$fog$819] ) {
          handleObj[$fog$459][$fog$819] = handler[$fog$819];
        }
      }

      // Add to the element's handler list, delegates in front
      if ( selector ) {
        handlers[$fog$850]( handlers[$fog$450]++, $fog$865, handleObj );
      } else {
        handlers[$fog$878]( handleObj );
      }

      // Keep track of which events have ever been used, for event optimization
      jQuery[$fog$460][$fog$448][ type ] = $fog$883;
    }

  },

  // Detach an event or set of events from an element
  remove: function( elem, types, handler, selector, mappedTypes ) {

    var j, origCount, tmp,
      events, t, handleObj,
      special, handlers, type, namespaces, origType,
      elemData = data_priv[$fog$494]( elem ) && data_priv[$fog$556]( elem );

    if ( !elemData || !(events = elemData[$fog$458]) ) {
      return;
    }

    // Once for each type.namespace in types; type may be omitted
    types = ( types || $fog$866 )[$fog$572]( rnotwhite ) || [ $fog$866 ];
    t = types[$fog$862];
    while ( t-- ) {
      tmp = rtypenamespace[$fog$740]( types[t] ) || [];
      type = origType = tmp[$fog$852];
      namespaces = ( tmp[$fog$820] || $fog$866 )[$fog$817]( $fog$454 )[$fog$851]();

      // Unbind all events (on this namespace, if provided) for the element
      if ( !type ) {
        for ( type in events ) {
          jQuery[$fog$460][$fog$493]( elem, type + types[ t ], handler, selector, $fog$883 );
        }
        continue;
      }

      special = jQuery[$fog$460][$fog$453][ type ] || {};
      type = ( selector ? special[$fog$452] : special[$fog$451] ) || type;
      handlers = events[ type ] || [];
      tmp = tmp[$fog$820] && new RegExp( $fog$447 + namespaces[$fog$727]($fog$446) + $fog$445 );

      // Remove matching events
      origCount = j = handlers[$fog$862];
      while ( j-- ) {
        handleObj = handlers[ j ];

        if ( ( mappedTypes || origType === handleObj[$fog$444] ) &&
          ( !handler || handler[$fog$819] === handleObj[$fog$819] ) &&
          ( !tmp || tmp[$fog$733]( handleObj[$fog$443] ) ) &&
          ( !selector || selector === handleObj[$fog$584] || selector === $fog$442 && handleObj[$fog$584] ) ) {
          handlers[$fog$850]( j, $fog$852 );

          if ( handleObj[$fog$584] ) {
            handlers[$fog$450]--;
          }
          if ( special[$fog$493] ) {
            special[$fog$493][$fog$864]( elem, handleObj );
          }
        }
      }

      // Remove generic event handler if we removed something and no more handlers exist
      // (avoids potential for endless recursion during removal of special event handlers)
      if ( origCount && !handlers[$fog$862] ) {
        if ( !special[$fog$441] || special[$fog$441][$fog$864]( elem, namespaces, elemData[$fog$457] ) === $fog$848 ) {
          jQuery[$fog$440]( elem, type, elemData[$fog$457] );
        }

        delete events[ type ];
      }
    }

    // Remove the expando if it's no longer used
    if ( jQuery[$fog$505]( events ) ) {
      delete elemData[$fog$457];
      data_priv[$fog$493]( elem, $fog$458 );
    }
  },

  trigger: function( event, data, elem, onlyHandlers ) {

    var i, cur, tmp, bubbleType, ontype, handle, special,
      eventPath = [ elem || document ],
      type = hasOwn[$fog$864]( event, $fog$839 ) ? event[$fog$839] : event,
      namespaces = hasOwn[$fog$864]( event, $fog$443 ) ? event[$fog$443][$fog$817]($fog$454) : [];

    cur = tmp = elem = elem || document;

    // Don't do events on text and comment nodes
    if ( elem[$fog$836] === $fog$736 || elem[$fog$836] === $fog$627 ) {
      return;
    }

    // focus/blur morphs to focusin/out; ensure we're not firing them right now
    if ( rfocusMorph[$fog$733]( type + jQuery[$fog$460][$fog$456] ) ) {
      return;
    }

    if ( type[$fog$877]($fog$454) >= $fog$865 ) {
      // Namespaced trigger; create a regexp to match event type in handle()
      namespaces = type[$fog$817]($fog$454);
      type = namespaces[$fog$721]();
      namespaces[$fog$851]();
    }
    ontype = type[$fog$877]($fog$575) < $fog$865 && $fog$439 + type;

    // Caller can pass in a jQuery.Event object, Object, or just an event type string
    event = event[ jQuery[$fog$510] ] ?
      event :
      new jQuery[$fog$438]( type, typeof event === $fog$886 && event );

    // Trigger bitmask: & 1 for native handlers; & 2 for jQuery (always true)
    event[$fog$437] = onlyHandlers ? $fog$820 : $fog$736;
    event[$fog$443] = namespaces[$fog$727]($fog$454);
    event[$fog$436] = event[$fog$443] ?
      new RegExp( $fog$447 + namespaces[$fog$727]($fog$446) + $fog$445 ) :
      $fog$863;

    // Clean up the event in case it is being reused
    event[$fog$435] = undefined;
    if ( !event[$fog$434] ) {
      event[$fog$434] = elem;
    }

    // Clone any incoming data and prepend the event, creating the handler arg list
    data = data == $fog$863 ?
      [ event ] :
      jQuery[$fog$561]( data, [ event ] );

    // Allow special events to draw outside the lines
    special = jQuery[$fog$460][$fog$453][ type ] || {};
    if ( !onlyHandlers && special[$fog$433] && special[$fog$433][$fog$854]( elem, data ) === $fog$848 ) {
      return;
    }

    // Determine event propagation path in advance, per W3C events spec (#9951)
    // Bubble up to document, then to window; watch for a global ownerDocument var (#9724)
    if ( !onlyHandlers && !special[$fog$432] && !jQuery[$fog$835]( elem ) ) {

      bubbleType = special[$fog$452] || type;
      if ( !rfocusMorph[$fog$733]( bubbleType + type ) ) {
        cur = cur[$fog$827];
      }
      for ( ; cur; cur = cur[$fog$827] ) {
        eventPath[$fog$878]( cur );
        tmp = cur;
      }

      // Only add window if we got to document (e.g., not plain obj or detached DOM)
      if ( tmp === (elem[$fog$743] || document) ) {
        eventPath[$fog$878]( tmp[$fog$708] || tmp[$fog$431] || window );
      }
    }

    // Fire handlers on the event path
    i = $fog$865;
    while ( (cur = eventPath[i++]) && !event[$fog$430]() ) {

      event[$fog$839] = i > $fog$852 ?
        bubbleType :
        special[$fog$451] || type;

      // jQuery handler
      handle = ( data_priv[$fog$556]( cur, $fog$458 ) || {} )[ event[$fog$839] ] && data_priv[$fog$556]( cur, $fog$457 );
      if ( handle ) {
        handle[$fog$854]( cur, data );
      }

      // Native handler
      handle = ontype && cur[ ontype ];
      if ( handle && handle[$fog$854] && jQuery[$fog$513]( cur ) ) {
        event[$fog$435] = handle[$fog$854]( cur, data );
        if ( event[$fog$435] === $fog$848 ) {
          event[$fog$429]();
        }
      }
    }
    event[$fog$839] = type;

    // If nobody prevented the default action, do it now
    if ( !onlyHandlers && !event[$fog$428]() ) {

      if ( (!special[$fog$427] || special[$fog$427][$fog$854]( eventPath[$fog$808](), data ) === $fog$848) &&
        jQuery[$fog$513]( elem ) ) {

        // Call a native DOM method on the target with the same name name as the event.
        // Don't do default actions on window, that's where global variables be (#6170)
        if ( ontype && jQuery[$fog$846]( elem[ type ] ) && !jQuery[$fog$835]( elem ) ) {

          // Don't re-trigger an onFOO event when we call its FOO() method
          tmp = elem[ ontype ];

          if ( tmp ) {
            elem[ ontype ] = $fog$863;
          }

          // Prevent re-triggering of the same event, since we already bubbled it above
          jQuery[$fog$460][$fog$456] = type;
          elem[ type ]();
          jQuery[$fog$460][$fog$456] = undefined;

          if ( tmp ) {
            elem[ ontype ] = tmp;
          }
        }
      }
    }

    return event[$fog$435];
  },

  dispatch: function( event ) {

    // Make a writable jQuery.Event from the native event object
    event = jQuery[$fog$460][$fog$426]( event );

    var i, j, ret, matched, handleObj,
      handlerQueue = [],
      args = slice[$fog$864]( arguments ),
      handlers = ( data_priv[$fog$556]( this, $fog$458 ) || {} )[ event[$fog$839] ] || [],
      special = jQuery[$fog$460][$fog$453][ event[$fog$839] ] || {};

    // Use the fix-ed jQuery.Event rather than the (read-only) native event
    args[$fog$865] = event;
    event[$fog$425] = this;

    // Call the preDispatch hook for the mapped type, and let it bail if desired
    if ( special[$fog$424] && special[$fog$424][$fog$864]( this, event ) === $fog$848 ) {
      return;
    }

    // Determine handlers
    handlerQueue = jQuery[$fog$460][$fog$423][$fog$864]( this, event, handlers );

    // Run delegates first; they may want to stop propagation beneath us
    i = $fog$865;
    while ( (matched = handlerQueue[ i++ ]) && !event[$fog$430]() ) {
      event[$fog$422] = matched[$fog$421];

      j = $fog$865;
      while ( (handleObj = matched[$fog$423][ j++ ]) && !event[$fog$420]() ) {

        // Triggered event must either 1) have no namespace, or 2) have namespace(s)
        // a subset or equal to those in the bound event (both can have no namespace).
        if ( !event[$fog$436] || event[$fog$436][$fog$733]( handleObj[$fog$443] ) ) {

          event[$fog$419] = handleObj;
          event[$fog$418] = handleObj[$fog$418];

          ret = ( (jQuery[$fog$460][$fog$453][ handleObj[$fog$444] ] || {})[$fog$457] || handleObj[$fog$459] )
              [$fog$854]( matched[$fog$421], args );

          if ( ret !== undefined ) {
            if ( (event[$fog$435] = ret) === $fog$848 ) {
              event[$fog$429]();
              event[$fog$417]();
            }
          }
        }
      }
    }

    // Call the postDispatch hook for the mapped type
    if ( special[$fog$416] ) {
      special[$fog$416][$fog$864]( this, event );
    }

    return event[$fog$435];
  },

  handlers: function( event, handlers ) {
    var i, matches, sel, handleObj,
      handlerQueue = [],
      delegateCount = handlers[$fog$450],
      cur = event[$fog$434];

    // Find delegate handlers
    // Black-hole SVG <use> instance trees (#13180)
    // Avoid non-left-click bubbling in Firefox (#3861)
    if ( delegateCount && cur[$fog$836] && (!event[$fog$714] || event[$fog$839] !== $fog$415) ) {

      for ( ; cur !== this; cur = cur[$fog$827] || this ) {

        // Don't process clicks on disabled elements (#6911, #8165, #11382, #11764)
        if ( cur[$fog$599] !== $fog$883 || event[$fog$839] !== $fog$415 ) {
          matches = [];
          for ( i = $fog$865; i < delegateCount; i++ ) {
            handleObj = handlers[ i ];

            // Don't conflict with Object.prototype properties (#13203)
            sel = handleObj[$fog$584] + $fog$815;

            if ( matches[ sel ] === undefined ) {
              matches[ sel ] = handleObj[$fog$582] ?
                jQuery( sel, this )[$fog$558]( cur ) >= $fog$865 :
                jQuery[$fog$697]( sel, this, $fog$863, [ cur ] )[$fog$862];
            }
            if ( matches[ sel ] ) {
              matches[$fog$878]( handleObj );
            }
          }
          if ( matches[$fog$862] ) {
            handlerQueue[$fog$878]({ elem: cur, handlers: matches });
          }
        }
      }
    }

    // Add the remaining (directly-bound) handlers
    if ( delegateCount < handlers[$fog$862] ) {
      handlerQueue[$fog$878]({ elem: this, handlers: handlers[$fog$880]( delegateCount ) });
    }

    return handlerQueue;
  },

  // Includes some event props shared by KeyEvent and MouseEvent
  props: $fog$414[$fog$817]($fog$815),

  fixHooks: {},

  keyHooks: {
    props: $fog$413[$fog$817]($fog$815),
    filter: function( event, original ) {

      // Add which for key events
      if ( event[$fog$412] == $fog$863 ) {
        event[$fog$412] = original[$fog$411] != $fog$863 ? original[$fog$411] : original[$fog$410];
      }

      return event;
    }
  },

  mouseHooks: {
    props: $fog$409[$fog$817]($fog$815),
    filter: function( event, original ) {
      var eventDoc, doc, body,
        button = original[$fog$714];

      // Calculate pageX/Y if missing and clientX/Y available
      if ( event[$fog$408] == $fog$863 && original[$fog$407] != $fog$863 ) {
        eventDoc = event[$fog$434][$fog$743] || document;
        doc = eventDoc[$fog$711];
        body = eventDoc[$fog$406];

        event[$fog$408] = original[$fog$407] + ( doc && doc[$fog$405] || body && body[$fog$405] || $fog$865 ) - ( doc && doc[$fog$404] || body && body[$fog$404] || $fog$865 );
        event[$fog$403] = original[$fog$402] + ( doc && doc[$fog$401]  || body && body[$fog$401]  || $fog$865 ) - ( doc && doc[$fog$400]  || body && body[$fog$400]  || $fog$865 );
      }

      // Add which for click: 1 === left; 2 === middle; 3 === right
      // Note: button is not normalized, so don't use it
      if ( !event[$fog$412] && button !== undefined ) {
        event[$fog$412] = ( button & $fog$852 ? $fog$852 : ( button & $fog$820 ? $fog$736 : ( button & $fog$650 ? $fog$820 : $fog$865 ) ) );
      }

      return event;
    }
  },

  fix: function( event ) {
    if ( event[ jQuery[$fog$510] ] ) {
      return event;
    }

    // Create a writable copy of the event object and normalize some properties
    var i, prop, copy,
      type = event[$fog$839],
      originalEvent = event,
      fixHook = this[$fog$399][ type ];

    if ( !fixHook ) {
      this[$fog$399][ type ] = fixHook =
        rmouseEvent[$fog$733]( type ) ? this[$fog$398] :
        rkeyEvent[$fog$733]( type ) ? this[$fog$397] :
        {};
    }
    copy = fixHook[$fog$396] ? this[$fog$396][$fog$879]( fixHook[$fog$396] ) : this[$fog$396];

    event = new jQuery[$fog$438]( originalEvent );

    i = copy[$fog$862];
    while ( i-- ) {
      prop = copy[ i ];
      event[ prop ] = originalEvent[ prop ];
    }

    // Support: Cordova 2.5 (WebKit) (#13255)
    // All events should have a target; Cordova deviceready doesn't
    if ( !event[$fog$434] ) {
      event[$fog$434] = document;
    }

    // Support: Safari 6.0+, Chrome<28
    // Target should not be a text node (#504, #13143)
    if ( event[$fog$434][$fog$836] === $fog$736 ) {
      event[$fog$434] = event[$fog$434][$fog$827];
    }

    return fixHook[$fog$695] ? fixHook[$fog$695]( event, originalEvent ) : event;
  },

  special: {
    load: {
      // Prevent triggered image.load events from bubbling to window.load
      noBubble: $fog$883
    },
    focus: {
      // Fire native event if possible so blur/focus sequence is correct
      trigger: function() {
        if ( this !== safeActiveElement() && this[$fog$395] ) {
          this[$fog$395]();
          return $fog$848;
        }
      },
      delegateType: $fog$394
    },
    blur: {
      trigger: function() {
        if ( this === safeActiveElement() && this[$fog$393] ) {
          this[$fog$393]();
          return $fog$848;
        }
      },
      delegateType: $fog$392
    },
    click: {
      // For checkbox, fire native event so checked state will be right
      trigger: function() {
        if ( this[$fog$839] === $fog$391 && this[$fog$415] && jQuery[$fog$823]( this, $fog$715 ) ) {
          this[$fog$415]();
          return $fog$848;
        }
      },

      // For cross-browser consistency, don't fire native .click() on links
      _default: function( event ) {
        return jQuery[$fog$823]( event[$fog$434], $fog$390 );
      }
    },

    beforeunload: {
      postDispatch: function( event ) {

        // Support: Firefox 20+
        // Firefox doesn't alert if the returnValue field is not set.
        if ( event[$fog$435] !== undefined && event[$fog$389] ) {
          event[$fog$389][$fog$388] = event[$fog$435];
        }
      }
    }
  },

  simulate: function( type, elem, event, bubble ) {
    // Piggyback on a donor event to simulate a different one.
    // Fake originalEvent to avoid donor's stopPropagation, but if the
    // simulated event prevents default then we do the same on the donor.
    var e = jQuery[$fog$849](
      new jQuery[$fog$438](),
      event,
      {
        type: type,
        isSimulated: $fog$883,
        originalEvent: {}
      }
    );
    if ( bubble ) {
      jQuery[$fog$460][$fog$433]( e, $fog$863, elem );
    } else {
      jQuery[$fog$460][$fog$455][$fog$864]( elem, e );
    }
    if ( e[$fog$428]() ) {
      event[$fog$429]();
    }
  }
};

jQuery[$fog$440] = function( elem, type, handle ) {
  if ( elem[$fog$519] ) {
    elem[$fog$519]( type, handle, $fog$848 );
  }
};

jQuery[$fog$438] = function( src, props ) {
  // Allow instantiation without the 'new' keyword
  if ( !(this instanceof jQuery[$fog$438]) ) {
    return new jQuery[$fog$438]( src, props );
  }

  // Event object
  if ( src && src[$fog$839] ) {
    this[$fog$389] = src;
    this[$fog$839] = src[$fog$839];

    // Events bubbling up the document may have been marked as prevented
    // by a handler lower down the tree; reflect the correct value.
    this[$fog$428] = src[$fog$387] ||
        src[$fog$387] === undefined &&
        // Support: Android<4.0
        src[$fog$388] === $fog$848 ?
      returnTrue :
      returnFalse;

  // Event type
  } else {
    this[$fog$839] = src;
  }

  // Put explicitly provided properties onto the event object
  if ( props ) {
    jQuery[$fog$849]( this, props );
  }

  // Create a timestamp if incoming event doesn't have one
  this[$fog$386] = src && src[$fog$386] || jQuery[$fog$818]();

  // Mark it as fixed
  this[ jQuery[$fog$510] ] = $fog$883;
};

// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding
// http://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html
jQuery[$fog$438][$fog$867] = {
  isDefaultPrevented: returnFalse,
  isPropagationStopped: returnFalse,
  isImmediatePropagationStopped: returnFalse,

  preventDefault: function() {
    var e = this[$fog$389];

    this[$fog$428] = returnTrue;

    if ( e && e[$fog$429] ) {
      e[$fog$429]();
    }
  },
  stopPropagation: function() {
    var e = this[$fog$389];

    this[$fog$430] = returnTrue;

    if ( e && e[$fog$417] ) {
      e[$fog$417]();
    }
  },
  stopImmediatePropagation: function() {
    var e = this[$fog$389];

    this[$fog$420] = returnTrue;

    if ( e && e[$fog$385] ) {
      e[$fog$385]();
    }

    this[$fog$417]();
  }
};

// Create mouseenter/leave events using mouseover/out and event-time checks
// Support: Chrome 15+
jQuery[$fog$857]({
  mouseenter: $fog$384,
  mouseleave: $fog$383,
  pointerenter: $fog$382,
  pointerleave: $fog$381
}, function( orig, fix ) {
  jQuery[$fog$460][$fog$453][ orig ] = {
    delegateType: fix,
    bindType: fix,

    handle: function( event ) {
      var ret,
        target = this,
        related = event[$fog$380],
        handleObj = event[$fog$419];

      // For mousenter/leave call the handler if related is outside the target.
      // NB: No relatedTarget if the mouse left/entered the browser window
      if ( !related || (related !== target && !jQuery[$fog$653]( target, related )) ) {
        event[$fog$839] = handleObj[$fog$444];
        ret = handleObj[$fog$459][$fog$854]( this, arguments );
        event[$fog$839] = fix;
      }
      return ret;
    }
  };
});

// Support: Firefox, Chrome, Safari
// Create "bubbling" focus and blur events
if ( !support[$fog$466] ) {
  jQuery[$fog$857]({ focus: $fog$394, blur: $fog$392 }, function( orig, fix ) {

    // Attach a single capturing handler on the document while someone wants focusin/focusout
    var handler = function( event ) {
        jQuery[$fog$460][$fog$379]( fix, event[$fog$434], jQuery[$fog$460][$fog$426]( event ), $fog$883 );
      };

    jQuery[$fog$460][$fog$453][ fix ] = {
      setup: function() {
        var doc = this[$fog$743] || this,
          attaches = data_priv[$fog$514]( doc, fix );

        if ( !attaches ) {
          doc[$fog$706]( orig, handler, $fog$883 );
        }
        data_priv[$fog$514]( doc, fix, ( attaches || $fog$865 ) + $fog$852 );
      },
      teardown: function() {
        var doc = this[$fog$743] || this,
          attaches = data_priv[$fog$514]( doc, fix ) - $fog$852;

        if ( !attaches ) {
          doc[$fog$519]( orig, handler, $fog$883 );
          data_priv[$fog$493]( doc, fix );

        } else {
          data_priv[$fog$514]( doc, fix, attaches );
        }
      }
    };
  });
}

jQuery[$fog$872][$fog$849]({

  on: function( types, selector, data, fn, /*INTERNAL*/ one ) {
    var origFn, type;

    // Types can be a map of types/handlers
    if ( typeof types === $fog$886 ) {
      // ( types-Object, selector, data )
      if ( typeof selector !== $fog$821 ) {
        // ( types-Object, data )
        data = data || selector;
        selector = undefined;
      }
      for ( type in types ) {
        this[$fog$439]( type, selector, data, types[ type ], one );
      }
      return this;
    }

    if ( data == $fog$863 && fn == $fog$863 ) {
      // ( types, fn )
      fn = selector;
      data = selector = undefined;
    } else if ( fn == $fog$863 ) {
      if ( typeof selector === $fog$821 ) {
        // ( types, selector, fn )
        fn = data;
        data = undefined;
      } else {
        // ( types, data, fn )
        fn = data;
        data = selector;
        selector = undefined;
      }
    }
    if ( fn === $fog$848 ) {
      fn = returnFalse;
    } else if ( !fn ) {
      return this;
    }

    if ( one === $fog$852 ) {
      origFn = fn;
      fn = function( event ) {
        // Can use an empty set, since event contains the info
        jQuery()[$fog$520]( event );
        return origFn[$fog$854]( this, arguments );
      };
      // Use same guid so caller can remove using origFn
      fn[$fog$819] = origFn[$fog$819] || ( origFn[$fog$819] = jQuery[$fog$819]++ );
    }
    return this[$fog$857]( function() {
      jQuery[$fog$460][$fog$555]( this, types, fn, data, selector );
    });
  },
  one: function( types, selector, data, fn ) {
    return this[$fog$439]( types, selector, data, fn, $fog$852 );
  },
  off: function( types, selector, fn ) {
    var handleObj, type;
    if ( types && types[$fog$429] && types[$fog$419] ) {
      // ( event )  dispatched jQuery.Event
      handleObj = types[$fog$419];
      jQuery( types[$fog$425] )[$fog$520](
        handleObj[$fog$443] ? handleObj[$fog$444] + $fog$454 + handleObj[$fog$443] : handleObj[$fog$444],
        handleObj[$fog$584],
        handleObj[$fog$459]
      );
      return this;
    }
    if ( typeof types === $fog$886 ) {
      // ( types-object [, selector] )
      for ( type in types ) {
        this[$fog$520]( type, selector, types[ type ] );
      }
      return this;
    }
    if ( selector === $fog$848 || typeof selector === $fog$838 ) {
      // ( types [, fn] )
      fn = selector;
      selector = undefined;
    }
    if ( fn === $fog$848 ) {
      fn = returnFalse;
    }
    return this[$fog$857](function() {
      jQuery[$fog$460][$fog$493]( this, types, fn, selector );
    });
  },

  trigger: function( type, data ) {
    return this[$fog$857](function() {
      jQuery[$fog$460][$fog$433]( type, data, this );
    });
  },
  triggerHandler: function( type, data ) {
    var elem = this[$fog$865];
    if ( elem ) {
      return jQuery[$fog$460][$fog$433]( type, data, elem, $fog$883 );
    }
  }
});


var
  rxhtmlTag = $fog$378,
  rtagName = $fog$377,
  rhtml = $fog$376,
  rnoInnerhtml = $fog$375,
  // checked="checked" or checked
  rchecked = $fog$374,
  rscriptType = $fog$373,
  rscriptTypeMasked = $fog$372,
  rcleanScript = $fog$371,

  // We have to close these tags to support XHTML (#13200)
  wrapMap = {

    // Support: IE9
    option: [ $fog$852, $fog$370, $fog$369 ],

    thead: [ $fog$852, $fog$368, $fog$367 ],
    col: [ $fog$820, $fog$366, $fog$365 ],
    tr: [ $fog$820, $fog$364, $fog$363 ],
    td: [ $fog$736, $fog$362, $fog$361 ],

    _default: [ $fog$865, $fog$866, $fog$866 ]
  };

// Support: IE9
wrapMap[$fog$360] = wrapMap[$fog$597];

wrapMap[$fog$359] = wrapMap[$fog$358] = wrapMap[$fog$357] = wrapMap[$fog$356] = wrapMap[$fog$355];
wrapMap[$fog$354] = wrapMap[$fog$353];

// Support: 1.x compatibility
// Manipulating tables requires a tbody
function manipulationTarget( elem, content ) {
  return jQuery[$fog$823]( elem, $fog$352 ) &&
    jQuery[$fog$823]( content[$fog$836] !== $fog$741 ? content : content[$fog$638], $fog$351 ) ?

    elem[$fog$737]($fog$359)[$fog$865] ||
      elem[$fog$826]( elem[$fog$743][$fog$831]($fog$359) ) :
    elem;
}

// Replace/restore the type attribute of script elements for safe DOM manipulation
function disableScript( elem ) {
  elem[$fog$839] = (elem[$fog$732]($fog$839) !== $fog$863) + $fog$350 + elem[$fog$839];
  return elem;
}
function restoreScript( elem ) {
  var match = rscriptTypeMasked[$fog$740]( elem[$fog$839] );

  if ( match ) {
    elem[$fog$839] = match[ $fog$852 ];
  } else {
    elem[$fog$724]($fog$839);
  }

  return elem;
}

// Mark scripts as having already been evaluated
function setGlobalEval( elems, refElements ) {
  var i = $fog$865,
    l = elems[$fog$862];

  for ( ; i < l; i++ ) {
    data_priv[$fog$503](
      elems[ i ], $fog$349, !refElements || data_priv[$fog$556]( refElements[ i ], $fog$349 )
    );
  }
}

function cloneCopyEvent( src, dest ) {
  var i, l, type, pdataOld, pdataCur, udataOld, udataCur, events;

  if ( dest[$fog$836] !== $fog$852 ) {
    return;
  }

  // 1. Copy private data: events, handlers, etc.
  if ( data_priv[$fog$494]( src ) ) {
    pdataOld = data_priv[$fog$514]( src );
    pdataCur = data_priv[$fog$503]( dest, pdataOld );
    events = pdataOld[$fog$458];

    if ( events ) {
      delete pdataCur[$fog$457];
      pdataCur[$fog$458] = {};

      for ( type in events ) {
        for ( i = $fog$865, l = events[ type ][$fog$862]; i < l; i++ ) {
          jQuery[$fog$460][$fog$555]( dest, type, events[ type ][ i ] );
        }
      }
    }
  }

  // 2. Copy user data
  if ( data_user[$fog$494]( src ) ) {
    udataOld = data_user[$fog$514]( src );
    udataCur = jQuery[$fog$849]( {}, udataOld );

    data_user[$fog$503]( dest, udataCur );
  }
}

function getAll( context, tag ) {
  var ret = context[$fog$737] ? context[$fog$737]( tag || $fog$782 ) :
      context[$fog$725] ? context[$fog$725]( tag || $fog$782 ) :
      [];

  return tag === undefined || tag && jQuery[$fog$823]( context, tag ) ?
    jQuery[$fog$861]( [ context ], ret ) :
    ret;
}

// Fix IE bugs, see support tests
function fixInput( src, dest ) {
  var nodeName = dest[$fog$823][$fog$822]();

  // Fails to persist the checked state of a cloned checkbox or radio button.
  if ( nodeName === $fog$715 && rcheckableType[$fog$733]( src[$fog$839] ) ) {
    dest[$fog$598] = src[$fog$598];

  // Fails to return the selected option to the default selected state when cloning options
  } else if ( nodeName === $fog$715 || nodeName === $fog$348 ) {
    dest[$fog$577] = src[$fog$577];
  }
}

jQuery[$fog$849]({
  clone: function( elem, dataAndEvents, deepDataAndEvents ) {
    var i, l, srcElements, destElements,
      clone = elem[$fog$469]( $fog$883 ),
      inPage = jQuery[$fog$653]( elem[$fog$743], elem );

    // Fix IE cloning issues
    if ( !support[$fog$467] && ( elem[$fog$836] === $fog$852 || elem[$fog$836] === $fog$741 ) &&
        !jQuery[$fog$573]( elem ) ) {

      // We eschew Sizzle here for performance reasons: http://jsperf.com/getall-vs-sizzle/2
      destElements = getAll( clone );
      srcElements = getAll( elem );

      for ( i = $fog$865, l = srcElements[$fog$862]; i < l; i++ ) {
        fixInput( srcElements[ i ], destElements[ i ] );
      }
    }

    // Copy the events from the original to the clone
    if ( dataAndEvents ) {
      if ( deepDataAndEvents ) {
        srcElements = srcElements || getAll( elem );
        destElements = destElements || getAll( clone );

        for ( i = $fog$865, l = srcElements[$fog$862]; i < l; i++ ) {
          cloneCopyEvent( srcElements[ i ], destElements[ i ] );
        }
      } else {
        cloneCopyEvent( elem, clone );
      }
    }

    // Preserve script evaluation history
    destElements = getAll( clone, $fog$830 );
    if ( destElements[$fog$862] > $fog$865 ) {
      setGlobalEval( destElements, !inPage && getAll( elem, $fog$830 ) );
    }

    // Return the cloned set
    return clone;
  },

  buildFragment: function( elems, context, scripts, selection ) {
    var elem, tmp, tag, wrap, contains, j,
      fragment = context[$fog$473](),
      nodes = [],
      i = $fog$865,
      l = elems[$fog$862];

    for ( ; i < l; i++ ) {
      elem = elems[ i ];

      if ( elem || elem === $fog$865 ) {

        // Add nodes directly
        if ( jQuery[$fog$839]( elem ) === $fog$886 ) {
          // Support: QtWebKit, PhantomJS
          // push.apply(_, arraylike) throws on ancient WebKit
          jQuery[$fog$861]( nodes, elem[$fog$836] ? [ elem ] : elem );

        // Convert non-html into a text node
        } else if ( !rhtml[$fog$733]( elem ) ) {
          nodes[$fog$878]( context[$fog$347]( elem ) );

        // Convert html into DOM nodes
        } else {
          tmp = tmp || fragment[$fog$826]( context[$fog$831]($fog$720) );

          // Deserialize a standard representation
          tag = ( rtagName[$fog$740]( elem ) || [ $fog$866, $fog$866 ] )[ $fog$852 ][$fog$822]();
          wrap = wrapMap[ tag ] || wrapMap[$fog$427];
          tmp[$fog$690] = wrap[ $fog$852 ] + elem[$fog$842]( rxhtmlTag, $fog$346 ) + wrap[ $fog$820 ];

          // Descend through wrappers to the right content
          j = wrap[ $fog$865 ];
          while ( j-- ) {
            tmp = tmp[$fog$613];
          }

          // Support: QtWebKit, PhantomJS
          // push.apply(_, arraylike) throws on ancient WebKit
          jQuery[$fog$861]( nodes, tmp[$fog$744] );

          // Remember the top-level container
          tmp = fragment[$fog$638];

          // Ensure the created nodes are orphaned (#12392)
          tmp[$fog$639] = $fog$866;
        }
      }
    }

    // Remove wrapper from fragment
    fragment[$fog$639] = $fog$866;

    i = $fog$865;
    while ( (elem = nodes[ i++ ]) ) {

      // #4087 - If origin and destination elements are the same, and this is
      // that element, do not do anything
      if ( selection && jQuery[$fog$543]( elem, selection ) !== -$fog$852 ) {
        continue;
      }

      contains = jQuery[$fog$653]( elem[$fog$743], elem );

      // Append to fragment
      tmp = getAll( fragment[$fog$826]( elem ), $fog$830 );

      // Preserve script evaluation history
      if ( contains ) {
        setGlobalEval( tmp );
      }

      // Capture executables
      if ( scripts ) {
        j = $fog$865;
        while ( (elem = tmp[ j++ ]) ) {
          if ( rscriptType[$fog$733]( elem[$fog$839] || $fog$866 ) ) {
            scripts[$fog$878]( elem );
          }
        }
      }
    }

    return fragment;
  },

  cleanData: function( elems ) {
    var data, elem, type, key,
      special = jQuery[$fog$460][$fog$453],
      i = $fog$865;

    for ( ; (elem = elems[ i ]) !== undefined; i++ ) {
      if ( jQuery[$fog$513]( elem ) ) {
        key = elem[ data_priv[$fog$510] ];

        if ( key && (data = data_priv[$fog$511][ key ]) ) {
          if ( data[$fog$458] ) {
            for ( type in data[$fog$458] ) {
              if ( special[ type ] ) {
                jQuery[$fog$460][$fog$493]( elem, type );

              // This is a shortcut to avoid jQuery.event.remove's overhead
              } else {
                jQuery[$fog$440]( elem, type, data[$fog$457] );
              }
            }
          }
          if ( data_priv[$fog$511][ key ] ) {
            // Discard any remaining `private` data
            delete data_priv[$fog$511][ key ];
          }
        }
      }
      // Discard any remaining `user` data
      delete data_user[$fog$511][ elem[ data_user[$fog$510] ] ];
    }
  }
});

jQuery[$fog$872][$fog$849]({
  text: function( value ) {
    return access( this, function( value ) {
      return value === undefined ?
        jQuery[$fog$829]( this ) :
        this[$fog$594]()[$fog$857](function() {
          if ( this[$fog$836] === $fog$852 || this[$fog$836] === $fog$741 || this[$fog$836] === $fog$742 ) {
            this[$fog$639] = value;
          }
        });
    }, $fog$863, value, arguments[$fog$862] );
  },

  append: function() {
    return this[$fog$345]( arguments, function( elem ) {
      if ( this[$fog$836] === $fog$852 || this[$fog$836] === $fog$741 || this[$fog$836] === $fog$742 ) {
        var target = manipulationTarget( this, elem );
        target[$fog$826]( elem );
      }
    });
  },

  prepend: function() {
    return this[$fog$345]( arguments, function( elem ) {
      if ( this[$fog$836] === $fog$852 || this[$fog$836] === $fog$741 || this[$fog$836] === $fog$742 ) {
        var target = manipulationTarget( this, elem );
        target[$fog$344]( elem, target[$fog$638] );
      }
    });
  },

  before: function() {
    return this[$fog$345]( arguments, function( elem ) {
      if ( this[$fog$827] ) {
        this[$fog$827][$fog$344]( elem, this );
      }
    });
  },

  after: function() {
    return this[$fog$345]( arguments, function( elem ) {
      if ( this[$fog$827] ) {
        this[$fog$827][$fog$344]( elem, this[$fog$716] );
      }
    });
  },

  remove: function( selector, keepData /* Internal Use Only */ ) {
    var elem,
      elems = selector ? jQuery[$fog$695]( selector, this ) : this,
      i = $fog$865;

    for ( ; (elem = elems[i]) != $fog$863; i++ ) {
      if ( !keepData && elem[$fog$836] === $fog$852 ) {
        jQuery[$fog$343]( getAll( elem ) );
      }

      if ( elem[$fog$827] ) {
        if ( keepData && jQuery[$fog$653]( elem[$fog$743], elem ) ) {
          setGlobalEval( getAll( elem, $fog$830 ) );
        }
        elem[$fog$827][$fog$828]( elem );
      }
    }

    return this;
  },

  empty: function() {
    var elem,
      i = $fog$865;

    for ( ; (elem = this[i]) != $fog$863; i++ ) {
      if ( elem[$fog$836] === $fog$852 ) {

        // Prevent memory leaks
        jQuery[$fog$343]( getAll( elem, $fog$848 ) );

        // Remove any remaining nodes
        elem[$fog$639] = $fog$866;
      }
    }

    return this;
  },

  clone: function( dataAndEvents, deepDataAndEvents ) {
    dataAndEvents = dataAndEvents == $fog$863 ? $fog$848 : dataAndEvents;
    deepDataAndEvents = deepDataAndEvents == $fog$863 ? dataAndEvents : deepDataAndEvents;

    return this[$fog$855](function() {
      return jQuery[$fog$342]( this, dataAndEvents, deepDataAndEvents );
    });
  },

  html: function( value ) {
    return access( this, function( value ) {
      var elem = this[ $fog$865 ] || {},
        i = $fog$865,
        l = this[$fog$862];

      if ( value === undefined && elem[$fog$836] === $fog$852 ) {
        return elem[$fog$690];
      }

      // See if we can take a shortcut and just use innerHTML
      if ( typeof value === $fog$821 && !rnoInnerhtml[$fog$733]( value ) &&
        !wrapMap[ ( rtagName[$fog$740]( value ) || [ $fog$866, $fog$866 ] )[ $fog$852 ][$fog$822]() ] ) {

        value = value[$fog$842]( rxhtmlTag, $fog$346 );

        try {
          for ( ; i < l; i++ ) {
            elem = this[ i ] || {};

            // Remove element nodes and prevent memory leaks
            if ( elem[$fog$836] === $fog$852 ) {
              jQuery[$fog$343]( getAll( elem, $fog$848 ) );
              elem[$fog$690] = value;
            }
          }

          elem = $fog$865;

        // If using innerHTML throws an exception, use the fallback method
        } catch( e ) {}
      }

      if ( elem ) {
        this[$fog$594]()[$fog$341]( value );
      }
    }, $fog$863, value, arguments[$fog$862] );
  },

  replaceWith: function() {
    var arg = arguments[ $fog$865 ];

    // Make the changes, replacing each context element with the new content
    this[$fog$345]( arguments, function( elem ) {
      arg = this[$fog$827];

      jQuery[$fog$343]( getAll( this ) );

      if ( arg ) {
        arg[$fog$340]( elem, this );
      }
    });

    // Force removal if there was no new content (e.g., from empty arguments)
    return arg && (arg[$fog$862] || arg[$fog$836]) ? this : this[$fog$493]();
  },

  detach: function( selector ) {
    return this[$fog$493]( selector, $fog$883 );
  },

  domManip: function( args, callback ) {

    // Flatten any nested arrays
    args = concat[$fog$854]( [], args );

    var fragment, first, scripts, hasScripts, node, doc,
      i = $fog$865,
      l = this[$fog$862],
      set = this,
      iNoClone = l - $fog$852,
      value = args[ $fog$865 ],
      isFunction = jQuery[$fog$846]( value );

    // We can't cloneNode fragments that contain checked, in WebKit
    if ( isFunction ||
        ( l > $fog$852 && typeof value === $fog$821 &&
          !support[$fog$470] && rchecked[$fog$733]( value ) ) ) {
      return this[$fog$857](function( index ) {
        var self = set[$fog$853]( index );
        if ( isFunction ) {
          args[ $fog$865 ] = value[$fog$864]( this, index, self[$fog$339]() );
        }
        self[$fog$345]( args, callback );
      });
    }

    if ( l ) {
      fragment = jQuery[$fog$338]( args, this[ $fog$865 ][$fog$743], $fog$848, this );
      first = fragment[$fog$638];

      if ( fragment[$fog$744][$fog$862] === $fog$852 ) {
        fragment = first;
      }

      if ( first ) {
        scripts = jQuery[$fog$855]( getAll( fragment, $fog$830 ), disableScript );
        hasScripts = scripts[$fog$862];

        // Use the original fragment for the last item instead of the first because it can end up
        // being emptied incorrectly in certain situations (#8070).
        for ( ; i < l; i++ ) {
          node = fragment;

          if ( i !== iNoClone ) {
            node = jQuery[$fog$342]( node, $fog$883, $fog$883 );

            // Keep references to cloned scripts for later restoration
            if ( hasScripts ) {
              // Support: QtWebKit
              // jQuery.merge because push.apply(_, arraylike) throws
              jQuery[$fog$861]( scripts, getAll( node, $fog$830 ) );
            }
          }

          callback[$fog$864]( this[ i ], node, i );
        }

        if ( hasScripts ) {
          doc = scripts[ scripts[$fog$862] - $fog$852 ][$fog$743];

          // Reenable scripts
          jQuery[$fog$855]( scripts, restoreScript );

          // Evaluate executable scripts on first document insertion
          for ( i = $fog$865; i < hasScripts; i++ ) {
            node = scripts[ i ];
            if ( rscriptType[$fog$733]( node[$fog$839] || $fog$866 ) &&
              !data_priv[$fog$514]( node, $fog$349 ) && jQuery[$fog$653]( doc, node ) ) {

              if ( node[$fog$337] ) {
                // Optional AJAX dependency, but won't run scripts if not present
                if ( jQuery[$fog$336] ) {
                  jQuery[$fog$336]( node[$fog$337] );
                }
              } else {
                jQuery[$fog$349]( node[$fog$639][$fog$842]( rcleanScript, $fog$866 ) );
              }
            }
          }
        }
      }
    }

    return this;
  }
});

jQuery[$fog$857]({
  appendTo: $fog$341,
  prependTo: $fog$335,
  insertBefore: $fog$334,
  insertAfter: $fog$333,
  replaceAll: $fog$332
}, function( name, original ) {
  jQuery[$fog$872][ name ] = function( selector ) {
    var elems,
      ret = [],
      insert = jQuery( selector ),
      last = insert[$fog$862] - $fog$852,
      i = $fog$865;

    for ( ; i <= last; i++ ) {
      elems = i === last ? this : this[$fog$342]( $fog$883 );
      jQuery( insert[ i ] )[ original ]( elems );

      // Support: QtWebKit
      // .get() because push.apply(_, arraylike) throws
      push[$fog$854]( ret, elems[$fog$556]() );
    }

    return this[$fog$856]( ret );
  };
});


var iframe,
  elemdisplay = {};

/**
 * Retrieve the actual display of a element
 * @param {String} name nodeName of the element
 * @param {Object} doc Document object
 */
// Called only from within defaultDisplay
function actualDisplay( name, doc ) {
  var style,
    elem = jQuery( doc[$fog$831]( name ) )[$fog$331]( doc[$fog$406] ),

    // getDefaultComputedStyle might be reliably used only on attached element
    display = window[$fog$330] && ( style = window[$fog$330]( elem[ $fog$865 ] ) ) ?

      // Use of this method is a temporary fix (more like optimization) until something better comes along,
      // since it was removed from specification and supported only in FF
      style[$fog$476] : jQuery[$fog$477]( elem[ $fog$865 ], $fog$476 );

  // We don't have any data stored on the element,
  // so use "detach" method as fast way to get rid of the element
  elem[$fog$329]();

  return display;
}

/**
 * Try to determine the default display value of an element
 * @param {String} nodeName
 */
function defaultDisplay( nodeName ) {
  var doc = document,
    display = elemdisplay[ nodeName ];

  if ( !display ) {
    display = actualDisplay( nodeName, doc );

    // If the simple way fails, read from inside an iframe
    if ( display === $fog$475 || !display ) {

      // Use the already-created iframe if possible
      iframe = (iframe || jQuery( $fog$328 ))[$fog$331]( doc[$fog$711] );

      // Always write a new HTML skeleton so Webkit and Firefox don't choke on reuse
      doc = iframe[ $fog$865 ][$fog$553];

      // Support: IE
      doc[$fog$327]();
      doc[$fog$326]();

      display = actualDisplay( nodeName, doc );
      iframe[$fog$329]();
    }

    // Store the correct default display
    elemdisplay[ nodeName ] = display;
  }

  return display;
}
var rmargin = ($fog$325);

var rnumnonpx = new RegExp( $fog$774 + pnum + $fog$324, $fog$767 );

var getStyles = function( elem ) {
    // Support: IE<=11+, Firefox<=30+ (#15098, #14150)
    // IE throws on elements created in popups
    // FF meanwhile throws on frame elements through "defaultView.getComputedStyle"
    if ( elem[$fog$743][$fog$708][$fog$323] ) {
      return elem[$fog$743][$fog$708][$fog$322]( elem, $fog$863 );
    }

    return window[$fog$322]( elem, $fog$863 );
  };



function curCSS( elem, name, computed ) {
  var width, minWidth, maxWidth, ret,
    style = elem[$fog$321];

  computed = computed || getStyles( elem );

  // Support: IE9
  // getPropertyValue is only needed for .css('filter') (#12537)
  if ( computed ) {
    ret = computed[$fog$320]( name ) || computed[ name ];
  }

  if ( computed ) {

    if ( ret === $fog$866 && !jQuery[$fog$653]( elem[$fog$743], elem ) ) {
      ret = jQuery[$fog$321]( elem, name );
    }

    // Support: iOS < 6
    // A tribute to the "awesome hack by Dean Edwards"
    // iOS < 6 (at least) returns percentage for a larger set of values, but width seems to be reliably pixels
    // this is against the CSSOM draft spec: http://dev.w3.org/csswg/cssom/#resolved-values
    if ( rnumnonpx[$fog$733]( ret ) && rmargin[$fog$733]( name ) ) {

      // Remember the original values
      width = style[$fog$319];
      minWidth = style[$fog$318];
      maxWidth = style[$fog$317];

      // Put in the new values to get a computed value out
      style[$fog$318] = style[$fog$317] = style[$fog$319] = ret;
      ret = computed[$fog$319];

      // Revert the changed values
      style[$fog$319] = width;
      style[$fog$318] = minWidth;
      style[$fog$317] = maxWidth;
    }
  }

  return ret !== undefined ?
    // Support: IE
    // IE returns zIndex value as an integer.
    ret + $fog$866 :
    ret;
}


function addGetHookIf( conditionFn, hookFn ) {
  // Define the hook, we'll check on the first run if it's really needed.
  return {
    get: function() {
      if ( conditionFn() ) {
        // Hook not needed (or it's not possible to use it due
        // to missing dependency), remove it.
        delete this[$fog$556];
        return;
      }

      // Hook needed; redefine it so that the support test is not executed again.
      return (this[$fog$556] = hookFn)[$fog$854]( this, arguments );
    }
  };
}


(function() {
  var pixelPositionVal, boxSizingReliableVal,
    docElem = document[$fog$711],
    container = document[$fog$831]( $fog$720 ),
    div = document[$fog$831]( $fog$720 );

  if ( !div[$fog$321] ) {
    return;
  }

  // Support: IE9-11+
  // Style of cloned element affects source element cloned (#8908)
  div[$fog$321][$fog$316] = $fog$315;
  div[$fog$469]( $fog$883 )[$fog$321][$fog$316] = $fog$866;
  support[$fog$314] = div[$fog$321][$fog$316] === $fog$315;

  container[$fog$321][$fog$313] = $fog$312 +
    $fog$311;
  container[$fog$826]( div );

  // Executing both pixelPosition & boxSizingReliable tests require only one layout
  // so they're executed at the same time to save the second computation.
  function computePixelPositionAndBoxSizingReliable() {
    div[$fog$321][$fog$313] =
      // Support: Firefox<29, Android 2.3
      // Vendor-prefix box-sizing
      $fog$310 +
      $fog$309 +
      $fog$308;
    div[$fog$690] = $fog$866;
    docElem[$fog$826]( container );

    var divStyle = window[$fog$322]( div, $fog$863 );
    pixelPositionVal = divStyle[$fog$707] !== $fog$307;
    boxSizingReliableVal = divStyle[$fog$319] === $fog$306;

    docElem[$fog$828]( container );
  }

  // Support: node.js jsdom
  // Don't assume that getComputedStyle is a property of the global object
  if ( window[$fog$322] ) {
    jQuery[$fog$849]( support, {
      pixelPosition: function() {

        // This test is executed only once but we still do memoizing
        // since we can use the boxSizingReliable pre-computing.
        // No need to check if the test was already performed, though.
        computePixelPositionAndBoxSizingReliable();
        return pixelPositionVal;
      },
      boxSizingReliable: function() {
        if ( boxSizingReliableVal == $fog$863 ) {
          computePixelPositionAndBoxSizingReliable();
        }
        return boxSizingReliableVal;
      },
      reliableMarginRight: function() {

        // Support: Android 2.3
        // Check if div with explicit width and no margin-right incorrectly
        // gets computed margin-right based on width of container. (#3333)
        // WebKit Bug 13343 - getComputedStyle returns wrong value for margin-right
        // This support function is only executed once so no memoizing is needed.
        var ret,
          marginDiv = div[$fog$826]( document[$fog$831]( $fog$720 ) );

        // Reset CSS: box-sizing; display; margin; border; padding
        marginDiv[$fog$321][$fog$313] = div[$fog$321][$fog$313] =
          // Support: Firefox<29, Android 2.3
          // Vendor-prefix box-sizing
          $fog$305 +
          $fog$304;
        marginDiv[$fog$321][$fog$303] = marginDiv[$fog$321][$fog$319] = $fog$587;
        div[$fog$321][$fog$319] = $fog$302;
        docElem[$fog$826]( container );

        ret = !parseFloat( window[$fog$322]( marginDiv, $fog$863 )[$fog$303] );

        docElem[$fog$828]( container );
        div[$fog$828]( marginDiv );

        return ret;
      }
    });
  }
})();


// A method for quickly swapping in/out CSS properties to get correct calculations.
jQuery[$fog$301] = function( elem, options, callback, args ) {
  var ret, name,
    old = {};

  // Remember the old values, and insert the new ones
  for ( name in options ) {
    old[ name ] = elem[$fog$321][ name ];
    elem[$fog$321][ name ] = options[ name ];
  }

  ret = callback[$fog$854]( elem, args || [] );

  // Revert the old values
  for ( name in options ) {
    elem[$fog$321][ name ] = old[ name ];
  }

  return ret;
};


var
  // Swappable if display is none or starts with table except "table", "table-cell", or "table-caption"
  // See here for display values: https://developer.mozilla.org/en-US/docs/CSS/display
  rdisplayswap = $fog$300,
  rnumsplit = new RegExp( $fog$774 + pnum + $fog$299, $fog$767 ),
  rrelNum = new RegExp( $fog$298 + pnum + $fog$780, $fog$767 ),

  cssShow = { position: $fog$297, visibility: $fog$672, display: $fog$296 },
  cssNormalTransform = {
    letterSpacing: $fog$587,
    fontWeight: $fog$295
  },

  cssPrefixes = [ $fog$294, $fog$293, $fog$292, $fog$291 ];

// Return a css property mapped to a potentially vendor prefixed property
function vendorPropName( style, name ) {

  // Shortcut for names that are not vendor prefixed
  if ( name in style ) {
    return name;
  }

  // Check for vendor prefixed names
  var capName = name[$fog$865][$fog$868]() + name[$fog$880]($fog$852),
    origName = name,
    i = cssPrefixes[$fog$862];

  while ( i-- ) {
    name = cssPrefixes[ i ] + capName;
    if ( name in style ) {
      return name;
    }
  }

  return origName;
}

function setPositiveNumber( elem, value, subtract ) {
  var matches = rnumsplit[$fog$740]( value );
  return matches ?
    // Guard against undefined "subtract", e.g., when used as in cssHooks
    Math[$fog$290]( $fog$865, matches[ $fog$852 ] - ( subtract || $fog$865 ) ) + ( matches[ $fog$820 ] || $fog$289 ) :
    value;
}

function augmentWidthOrHeight( elem, name, extra, isBorderBox, styles ) {
  var i = extra === ( isBorderBox ? $fog$288 : $fog$287 ) ?
    // If we already have the right measurement, avoid augmentation
    $fog$650 :
    // Otherwise initialize for horizontal or vertical properties
    name === $fog$319 ? $fog$852 : $fog$865,

    val = $fog$865;

  for ( ; i < $fog$650; i += $fog$820 ) {
    // Both box models exclude margin, so add it if we want it
    if ( extra === $fog$286 ) {
      val += jQuery[$fog$477]( elem, extra + cssExpand[ i ], $fog$883, styles );
    }

    if ( isBorderBox ) {
      // border-box includes padding, so remove it if we want content
      if ( extra === $fog$287 ) {
        val -= jQuery[$fog$477]( elem, $fog$285 + cssExpand[ i ], $fog$883, styles );
      }

      // At this point, extra isn't border nor margin, so remove border
      if ( extra !== $fog$286 ) {
        val -= jQuery[$fog$477]( elem, $fog$288 + cssExpand[ i ] + $fog$284, $fog$883, styles );
      }
    } else {
      // At this point, extra isn't content, so add padding
      val += jQuery[$fog$477]( elem, $fog$285 + cssExpand[ i ], $fog$883, styles );

      // At this point, extra isn't content nor padding, so add border
      if ( extra !== $fog$285 ) {
        val += jQuery[$fog$477]( elem, $fog$288 + cssExpand[ i ] + $fog$284, $fog$883, styles );
      }
    }
  }

  return val;
}

function getWidthOrHeight( elem, name, extra ) {

  // Start with offset property, which is equivalent to the border-box value
  var valueIsBorderBox = $fog$883,
    val = name === $fog$319 ? elem[$fog$283] : elem[$fog$282],
    styles = getStyles( elem ),
    isBorderBox = jQuery[$fog$477]( elem, $fog$281, $fog$848, styles ) === $fog$280;

  // Some non-html elements return undefined for offsetWidth, so check for null/undefined
  // svg - https://bugzilla.mozilla.org/show_bug.cgi?id=649285
  // MathML - https://bugzilla.mozilla.org/show_bug.cgi?id=491668
  if ( val <= $fog$865 || val == $fog$863 ) {
    // Fall back to computed then uncomputed css if necessary
    val = curCSS( elem, name, styles );
    if ( val < $fog$865 || val == $fog$863 ) {
      val = elem[$fog$321][ name ];
    }

    // Computed unit is not pixels. Stop here and return.
    if ( rnumnonpx[$fog$733](val) ) {
      return val;
    }

    // Check for style in case a browser which returns unreliable values
    // for getComputedStyle silently falls back to the reliable elem.style
    valueIsBorderBox = isBorderBox &&
      ( support[$fog$279]() || val === elem[$fog$321][ name ] );

    // Normalize "", auto, and prepare for extra
    val = parseFloat( val ) || $fog$865;
  }

  // Use the active box-sizing model to add/subtract irrelevant styles
  return ( val +
    augmentWidthOrHeight(
      elem,
      name,
      extra || ( isBorderBox ? $fog$288 : $fog$287 ),
      valueIsBorderBox,
      styles
    )
  ) + $fog$289;
}

function showHide( elements, show ) {
  var display, elem, hidden,
    values = [],
    index = $fog$865,
    length = elements[$fog$862];

  for ( ; index < length; index++ ) {
    elem = elements[ index ];
    if ( !elem[$fog$321] ) {
      continue;
    }

    values[ index ] = data_priv[$fog$556]( elem, $fog$278 );
    display = elem[$fog$321][$fog$476];
    if ( show ) {
      // Reset the inline display of this element to learn if it is
      // being hidden by cascaded rules or not
      if ( !values[ index ] && display === $fog$475 ) {
        elem[$fog$321][$fog$476] = $fog$866;
      }

      // Set elements which have been overridden with display: none
      // in a stylesheet to whatever the default browser style is
      // for such an element
      if ( elem[$fog$321][$fog$476] === $fog$866 && isHidden( elem ) ) {
        values[ index ] = data_priv[$fog$514]( elem, $fog$278, defaultDisplay(elem[$fog$823]) );
      }
    } else {
      hidden = isHidden( elem );

      if ( display !== $fog$475 || !hidden ) {
        data_priv[$fog$503]( elem, $fog$278, hidden ? display : jQuery[$fog$477]( elem, $fog$476 ) );
      }
    }
  }

  // Set the display of most of the elements in a second loop
  // to avoid the constant reflow
  for ( index = $fog$865; index < length; index++ ) {
    elem = elements[ index ];
    if ( !elem[$fog$321] ) {
      continue;
    }
    if ( !show || elem[$fog$321][$fog$476] === $fog$475 || elem[$fog$321][$fog$476] === $fog$866 ) {
      elem[$fog$321][$fog$476] = show ? values[ index ] || $fog$866 : $fog$475;
    }
  }

  return elements;
}

jQuery[$fog$849]({

  // Add in style property hooks for overriding the default
  // behavior of getting and setting a style property
  cssHooks: {
    opacity: {
      get: function( elem, computed ) {
        if ( computed ) {

          // We should always get a number back from opacity
          var ret = curCSS( elem, $fog$277 );
          return ret === $fog$866 ? $fog$276 : ret;
        }
      }
    }
  },

  // Don't automatically add "px" to these possibly-unitless properties
  cssNumber: {
    "columnCount": $fog$883,
    "fillOpacity": $fog$883,
    "flexGrow": $fog$883,
    "flexShrink": $fog$883,
    "fontWeight": $fog$883,
    "lineHeight": $fog$883,
    "opacity": $fog$883,
    "order": $fog$883,
    "orphans": $fog$883,
    "widows": $fog$883,
    "zIndex": $fog$883,
    "zoom": $fog$883
  },

  // Add in properties whose names you wish to fix before
  // setting or getting the value
  cssProps: {
    "float": $fog$275
  },

  // Get and set the style property on a DOM Node
  style: function( elem, name, value, extra ) {

    // Don't set styles on text and comment nodes
    if ( !elem || elem[$fog$836] === $fog$736 || elem[$fog$836] === $fog$627 || !elem[$fog$321] ) {
      return;
    }

    // Make sure that we're working with the right name
    var ret, type, hooks,
      origName = jQuery[$fog$504]( name ),
      style = elem[$fog$321];

    name = jQuery[$fog$274][ origName ] || ( jQuery[$fog$274][ origName ] = vendorPropName( style, origName ) );

    // Gets hook for the prefixed version, then unprefixed version
    hooks = jQuery[$fog$273][ name ] || jQuery[$fog$273][ origName ];

    // Check if we're setting a value
    if ( value !== undefined ) {
      type = typeof value;

      // Convert "+=" or "-=" to relative numbers (#7345)
      if ( type === $fog$821 && (ret = rrelNum[$fog$740]( value )) ) {
        value = ( ret[$fog$852] + $fog$852 ) * ret[$fog$820] + parseFloat( jQuery[$fog$477]( elem, name ) );
        // Fixes bug #9237
        type = $fog$811;
      }

      // Make sure that null and NaN values aren't set (#7116)
      if ( value == $fog$863 || value !== value ) {
        return;
      }

      // If a number, add 'px' to the (except for certain CSS properties)
      if ( type === $fog$811 && !jQuery[$fog$272][ origName ] ) {
        value += $fog$289;
      }

      // Support: IE9-11+
      // background-* props affect original clone's values
      if ( !support[$fog$314] && value === $fog$866 && name[$fog$877]( $fog$271 ) === $fog$865 ) {
        style[ name ] = $fog$270;
      }

      // If a hook was provided, use that value, otherwise just set the specified value
      if ( !hooks || !($fog$503 in hooks) || (value = hooks[$fog$503]( elem, value, extra )) !== undefined ) {
        style[ name ] = value;
      }

    } else {
      // If a hook was provided get the non-computed value from there
      if ( hooks && $fog$556 in hooks && (ret = hooks[$fog$556]( elem, $fog$848, extra )) !== undefined ) {
        return ret;
      }

      // Otherwise just get the value from the style object
      return style[ name ];
    }
  },

  css: function( elem, name, extra, styles ) {
    var val, num, hooks,
      origName = jQuery[$fog$504]( name );

    // Make sure that we're working with the right name
    name = jQuery[$fog$274][ origName ] || ( jQuery[$fog$274][ origName ] = vendorPropName( elem[$fog$321], origName ) );

    // Try prefixed name followed by the unprefixed name
    hooks = jQuery[$fog$273][ name ] || jQuery[$fog$273][ origName ];

    // If a hook was provided get the computed value from there
    if ( hooks && $fog$556 in hooks ) {
      val = hooks[$fog$556]( elem, $fog$883, extra );
    }

    // Otherwise, if a way to get the computed value exists, use that
    if ( val === undefined ) {
      val = curCSS( elem, name, styles );
    }

    // Convert "normal" to computed value
    if ( val === $fog$269 && name in cssNormalTransform ) {
      val = cssNormalTransform[ name ];
    }

    // Make numeric if forced or a qualifier was provided and val looks numeric
    if ( extra === $fog$866 || extra ) {
      num = parseFloat( val );
      return extra === $fog$883 || jQuery[$fog$268]( num ) ? num || $fog$865 : val;
    }
    return val;
  }
});

jQuery[$fog$857]([ $fog$267, $fog$319 ], function( i, name ) {
  jQuery[$fog$273][ name ] = {
    get: function( elem, computed, extra ) {
      if ( computed ) {

        // Certain elements can have dimension info if we invisibly show them
        // but it must have a current display style that would benefit
        return rdisplayswap[$fog$733]( jQuery[$fog$477]( elem, $fog$476 ) ) && elem[$fog$283] === $fog$865 ?
          jQuery[$fog$301]( elem, cssShow, function() {
            return getWidthOrHeight( elem, name, extra );
          }) :
          getWidthOrHeight( elem, name, extra );
      }
    },

    set: function( elem, value, extra ) {
      var styles = extra && getStyles( elem );
      return setPositiveNumber( elem, value, extra ?
        augmentWidthOrHeight(
          elem,
          name,
          extra,
          jQuery[$fog$477]( elem, $fog$281, $fog$848, styles ) === $fog$280,
          styles
        ) : $fog$865
      );
    }
  };
});

// Support: Android 2.3
jQuery[$fog$273][$fog$303] = addGetHookIf( support[$fog$266],
  function( elem, computed ) {
    if ( computed ) {
      return jQuery[$fog$301]( elem, { "display": $fog$265 },
        curCSS, [ elem, $fog$303 ] );
    }
  }
);

// These hooks are used by animate to expand properties
jQuery[$fog$857]({
  margin: $fog$866,
  padding: $fog$866,
  border: $fog$284
}, function( prefix, suffix ) {
  jQuery[$fog$273][ prefix + suffix ] = {
    expand: function( value ) {
      var i = $fog$865,
        expanded = {},

        // Assumes a single number if not a string
        parts = typeof value === $fog$821 ? value[$fog$817]($fog$815) : [ value ];

      for ( ; i < $fog$650; i++ ) {
        expanded[ prefix + cssExpand[ i ] + suffix ] =
          parts[ i ] || parts[ i - $fog$820 ] || parts[ $fog$865 ];
      }

      return expanded;
    }
  };

  if ( !rmargin[$fog$733]( prefix ) ) {
    jQuery[$fog$273][ prefix + suffix ][$fog$503] = setPositiveNumber;
  }
});

jQuery[$fog$872][$fog$849]({
  css: function( name, value ) {
    return access( this, function( elem, name, value ) {
      var styles, len,
        map = {},
        i = $fog$865;

      if ( jQuery[$fog$844]( name ) ) {
        styles = getStyles( elem );
        len = name[$fog$862];

        for ( ; i < len; i++ ) {
          map[ name[ i ] ] = jQuery[$fog$477]( elem, name[ i ], $fog$848, styles );
        }

        return map;
      }

      return value !== undefined ?
        jQuery[$fog$321]( elem, name, value ) :
        jQuery[$fog$477]( elem, name );
    }, name, value, arguments[$fog$862] > $fog$852 );
  },
  show: function() {
    return showHide( this, $fog$883 );
  },
  hide: function() {
    return showHide( this );
  },
  toggle: function( state ) {
    if ( typeof state === $fog$847 ) {
      return state ? this[$fog$264]() : this[$fog$263]();
    }

    return this[$fog$857](function() {
      if ( isHidden( this ) ) {
        jQuery( this )[$fog$264]();
      } else {
        jQuery( this )[$fog$263]();
      }
    });
  }
});


function Tween( elem, options, prop, end, easing ) {
  return new Tween[$fog$867][$fog$873]( elem, options, prop, end, easing );
}
jQuery[$fog$262] = Tween;

Tween[$fog$867] = {
  constructor: Tween,
  init: function( elem, options, prop, end, easing, unit ) {
    this[$fog$421] = elem;
    this[$fog$261] = prop;
    this[$fog$260] = easing || $fog$259;
    this[$fog$258] = options;
    this[$fog$257] = this[$fog$818] = this[$fog$256]();
    this[$fog$255] = end;
    this[$fog$254] = unit || ( jQuery[$fog$272][ prop ] ? $fog$866 : $fog$289 );
  },
  cur: function() {
    var hooks = Tween[$fog$253][ this[$fog$261] ];

    return hooks && hooks[$fog$556] ?
      hooks[$fog$556]( this ) :
      Tween[$fog$253][$fog$427][$fog$556]( this );
  },
  run: function( percent ) {
    var eased,
      hooks = Tween[$fog$253][ this[$fog$261] ];

    if ( this[$fog$258][$fog$252] ) {
      this[$fog$251] = eased = jQuery[$fog$260][ this[$fog$260] ](
        percent, this[$fog$258][$fog$252] * percent, $fog$865, $fog$852, this[$fog$258][$fog$252]
      );
    } else {
      this[$fog$251] = eased = percent;
    }
    this[$fog$818] = ( this[$fog$255] - this[$fog$257] ) * eased + this[$fog$257];

    if ( this[$fog$258][$fog$250] ) {
      this[$fog$258][$fog$250][$fog$864]( this[$fog$421], this[$fog$818], this );
    }

    if ( hooks && hooks[$fog$503] ) {
      hooks[$fog$503]( this );
    } else {
      Tween[$fog$253][$fog$427][$fog$503]( this );
    }
    return this;
  }
};

Tween[$fog$867][$fog$873][$fog$867] = Tween[$fog$867];

Tween[$fog$253] = {
  _default: {
    get: function( tween ) {
      var result;

      if ( tween[$fog$421][ tween[$fog$261] ] != $fog$863 &&
        (!tween[$fog$421][$fog$321] || tween[$fog$421][$fog$321][ tween[$fog$261] ] == $fog$863) ) {
        return tween[$fog$421][ tween[$fog$261] ];
      }

      // Passing an empty string as a 3rd parameter to .css will automatically
      // attempt a parseFloat and fallback to a string if the parse fails.
      // Simple values such as "10px" are parsed to Float;
      // complex values such as "rotate(1rad)" are returned as-is.
      result = jQuery[$fog$477]( tween[$fog$421], tween[$fog$261], $fog$866 );
      // Empty strings, null, undefined and "auto" are converted to 0.
      return !result || result === $fog$249 ? $fog$865 : result;
    },
    set: function( tween ) {
      // Use step hook for back compat.
      // Use cssHook if its there.
      // Use .style if available and use plain properties where available.
      if ( jQuery[$fog$491][$fog$250][ tween[$fog$261] ] ) {
        jQuery[$fog$491][$fog$250][ tween[$fog$261] ]( tween );
      } else if ( tween[$fog$421][$fog$321] && ( tween[$fog$421][$fog$321][ jQuery[$fog$274][ tween[$fog$261] ] ] != $fog$863 || jQuery[$fog$273][ tween[$fog$261] ] ) ) {
        jQuery[$fog$321]( tween[$fog$421], tween[$fog$261], tween[$fog$818] + tween[$fog$254] );
      } else {
        tween[$fog$421][ tween[$fog$261] ] = tween[$fog$818];
      }
    }
  }
};

// Support: IE9
// Panic based approach to setting things on disconnected nodes
Tween[$fog$253][$fog$401] = Tween[$fog$253][$fog$405] = {
  set: function( tween ) {
    if ( tween[$fog$421][$fog$836] && tween[$fog$421][$fog$827] ) {
      tween[$fog$421][ tween[$fog$261] ] = tween[$fog$818];
    }
  }
};

jQuery[$fog$260] = {
  linear: function( p ) {
    return p;
  },
  swing: function( p ) {
    return $fog$248 - Math[$fog$247]( p * Math[$fog$246] ) / $fog$820;
  }
};

jQuery[$fog$491] = Tween[$fog$867][$fog$873];

// Back Compat <1.8 extension point
jQuery[$fog$491][$fog$250] = {};




var
  fxNow, timerId,
  rfxtypes = $fog$245,
  rfxnum = new RegExp( $fog$244 + pnum + $fog$243, $fog$767 ),
  rrun = $fog$242,
  animationPrefilters = [ defaultPrefilter ],
  tweeners = {
    "*": [ function( prop, value ) {
      var tween = this[$fog$241]( prop, value ),
        target = tween[$fog$256](),
        parts = rfxnum[$fog$740]( value ),
        unit = parts && parts[ $fog$736 ] || ( jQuery[$fog$272][ prop ] ? $fog$866 : $fog$289 ),

        // Starting value computation is required for potential unit mismatches
        start = ( jQuery[$fog$272][ prop ] || unit !== $fog$289 && +target ) &&
          rfxnum[$fog$740]( jQuery[$fog$477]( tween[$fog$421], prop ) ),
        scale = $fog$852,
        maxIterations = $fog$240;

      if ( start && start[ $fog$736 ] !== unit ) {
        // Trust units reported by jQuery.css
        unit = unit || start[ $fog$736 ];

        // Make sure we update the tween properties later on
        parts = parts || [];

        // Iteratively approximate from a nonzero starting point
        start = +target || $fog$852;

        do {
          // If previous iteration zeroed out, double until we get *something*.
          // Use string for doubling so we don't accidentally see scale as unchanged below
          scale = scale || $fog$239;

          // Adjust and apply
          start = start / scale;
          jQuery[$fog$321]( tween[$fog$421], prop, start + unit );

        // Update scale, tolerating zero or NaN from tween.cur(),
        // break the loop if scale is unchanged or perfect, or if we've just had enough
        } while ( scale !== (scale = tween[$fog$256]() / target) && scale !== $fog$852 && --maxIterations );
      }

      // Update tween properties
      if ( parts ) {
        start = tween[$fog$257] = +start || +target || $fog$865;
        tween[$fog$254] = unit;
        // If a +=/-= token was provided, we're doing a relative animation
        tween[$fog$255] = parts[ $fog$852 ] ?
          start + ( parts[ $fog$852 ] + $fog$852 ) * parts[ $fog$820 ] :
          +parts[ $fog$820 ];
      }

      return tween;
    } ]
  };

// Animations created synchronously will run synchronously
function createFxNow() {
  setTimeout(function() {
    fxNow = undefined;
  });
  return ( fxNow = jQuery[$fog$818]() );
}

// Generate parameters to create a standard animation
function genFx( type, includeWidth ) {
  var which,
    i = $fog$865,
    attrs = { height: type };

  // If we include width, step value is 1 to do all cssExpand values,
  // otherwise step value is 2 to skip over Left and Right
  includeWidth = includeWidth ? $fog$852 : $fog$865;
  for ( ; i < $fog$650 ; i += $fog$820 - includeWidth ) {
    which = cssExpand[ i ];
    attrs[ $fog$286 + which ] = attrs[ $fog$285 + which ] = type;
  }

  if ( includeWidth ) {
    attrs[$fog$277] = attrs[$fog$319] = type;
  }

  return attrs;
}

function createTween( value, prop, animation ) {
  var tween,
    collection = ( tweeners[ prop ] || [] )[$fog$879]( tweeners[ $fog$782 ] ),
    index = $fog$865,
    length = collection[$fog$862];
  for ( ; index < length; index++ ) {
    if ( (tween = collection[ index ][$fog$864]( animation, prop, value )) ) {

      // We're done with this property
      return tween;
    }
  }
}

function defaultPrefilter( elem, props, opts ) {
  /* jshint validthis: true */
  var prop, value, toggle, tween, hooks, oldfire, display, checkDisplay,
    anim = this,
    orig = {},
    style = elem[$fog$321],
    hidden = elem[$fog$836] && isHidden( elem ),
    dataShow = data_priv[$fog$556]( elem, $fog$238 );

  // Handle queue: false promises
  if ( !opts[$fog$490] ) {
    hooks = jQuery[$fog$489]( elem, $fog$491 );
    if ( hooks[$fog$237] == $fog$863 ) {
      hooks[$fog$237] = $fog$865;
      oldfire = hooks[$fog$594][$fog$485];
      hooks[$fog$594][$fog$485] = function() {
        if ( !hooks[$fog$237] ) {
          oldfire();
        }
      };
    }
    hooks[$fog$237]++;

    anim[$fog$236](function() {
      // Ensure the complete handler is called before this completes
      anim[$fog$236](function() {
        hooks[$fog$237]--;
        if ( !jQuery[$fog$490]( elem, $fog$491 )[$fog$862] ) {
          hooks[$fog$594][$fog$485]();
        }
      });
    });
  }

  // Height/width overflow pass
  if ( elem[$fog$836] === $fog$852 && ( $fog$267 in props || $fog$319 in props ) ) {
    // Make sure that nothing sneaks out
    // Record all 3 overflow attributes because IE9-10 do not
    // change the overflow attribute when overflowX and
    // overflowY are set to the same value
    opts[$fog$235] = [ style[$fog$235], style[$fog$234], style[$fog$233] ];

    // Set display property to inline-block for height/width
    // animations on inline elements that are having width/height animated
    display = jQuery[$fog$477]( elem, $fog$476 );

    // Test default display if display is currently "none"
    checkDisplay = display === $fog$475 ?
      data_priv[$fog$556]( elem, $fog$278 ) || defaultDisplay( elem[$fog$823] ) : display;

    if ( checkDisplay === $fog$232 && jQuery[$fog$477]( elem, $fog$231 ) === $fog$475 ) {
      style[$fog$476] = $fog$265;
    }
  }

  if ( opts[$fog$235] ) {
    style[$fog$235] = $fog$672;
    anim[$fog$236](function() {
      style[$fog$235] = opts[$fog$235][ $fog$865 ];
      style[$fog$234] = opts[$fog$235][ $fog$852 ];
      style[$fog$233] = opts[$fog$235][ $fog$820 ];
    });
  }

  // show/hide pass
  for ( prop in props ) {
    value = props[ prop ];
    if ( rfxtypes[$fog$740]( value ) ) {
      delete props[ prop ];
      toggle = toggle || value === $fog$230;
      if ( value === ( hidden ? $fog$263 : $fog$264 ) ) {

        // If there is dataShow left over from a stopped hide or show and we are going to proceed with show, we should pretend to be hidden
        if ( value === $fog$264 && dataShow && dataShow[ prop ] !== undefined ) {
          hidden = $fog$883;
        } else {
          continue;
        }
      }
      orig[ prop ] = dataShow && dataShow[ prop ] || jQuery[$fog$321]( elem, prop );

    // Any non-fx value stops us from restoring the original display value
    } else {
      display = undefined;
    }
  }

  if ( !jQuery[$fog$505]( orig ) ) {
    if ( dataShow ) {
      if ( $fog$672 in dataShow ) {
        hidden = dataShow[$fog$672];
      }
    } else {
      dataShow = data_priv[$fog$514]( elem, $fog$238, {} );
    }

    // Store state if its toggle - enables .stop().toggle() to "reverse"
    if ( toggle ) {
      dataShow[$fog$672] = !hidden;
    }
    if ( hidden ) {
      jQuery( elem )[$fog$264]();
    } else {
      anim[$fog$540](function() {
        jQuery( elem )[$fog$263]();
      });
    }
    anim[$fog$540](function() {
      var prop;

      data_priv[$fog$493]( elem, $fog$238 );
      for ( prop in orig ) {
        jQuery[$fog$321]( elem, prop, orig[ prop ] );
      }
    });
    for ( prop in orig ) {
      tween = createTween( hidden ? dataShow[ prop ] : $fog$865, prop, anim );

      if ( !( prop in dataShow ) ) {
        dataShow[ prop ] = tween[$fog$257];
        if ( hidden ) {
          tween[$fog$255] = tween[$fog$257];
          tween[$fog$257] = prop === $fog$319 || prop === $fog$267 ? $fog$852 : $fog$865;
        }
      }
    }

  // If this is a noop like .hide().hide(), restore an overwritten display value
  } else if ( (display === $fog$475 ? defaultDisplay( elem[$fog$823] ) : display) === $fog$232 ) {
    style[$fog$476] = display;
  }
}

function propFilter( props, specialEasing ) {
  var index, name, easing, value, hooks;

  // camelCase, specialEasing and expand cssHook pass
  for ( index in props ) {
    name = jQuery[$fog$504]( index );
    easing = specialEasing[ name ];
    value = props[ index ];
    if ( jQuery[$fog$844]( value ) ) {
      easing = value[ $fog$852 ];
      value = props[ index ] = value[ $fog$865 ];
    }

    if ( index !== name ) {
      props[ name ] = value;
      delete props[ index ];
    }

    hooks = jQuery[$fog$273][ name ];
    if ( hooks && $fog$229 in hooks ) {
      value = hooks[$fog$229]( value );
      delete props[ name ];

      // Not quite $.extend, this won't overwrite existing keys.
      // Reusing 'index' because we have the correct "name"
      for ( index in value ) {
        if ( !( index in props ) ) {
          props[ index ] = value[ index ];
          specialEasing[ index ] = easing;
        }
      }
    } else {
      specialEasing[ name ] = easing;
    }
  }
}

function Animation( elem, properties, options ) {
  var result,
    stopped,
    index = $fog$865,
    length = animationPrefilters[$fog$862],
    deferred = jQuery[$fog$530]()[$fog$236]( function() {
      // Don't match elem in the :animated selector
      delete tick[$fog$421];
    }),
    tick = function() {
      if ( stopped ) {
        return $fog$848;
      }
      var currentTime = fxNow || createFxNow(),
        remaining = Math[$fog$290]( $fog$865, animation[$fog$228] + animation[$fog$252] - currentTime ),
        // Support: Android 2.3
        // Archaic crash bug won't allow us to use `1 - ( 0.5 || 0 )` (#12497)
        temp = remaining / animation[$fog$252] || $fog$865,
        percent = $fog$852 - temp,
        index = $fog$865,
        length = animation[$fog$227][$fog$862];

      for ( ; index < length ; index++ ) {
        animation[$fog$227][ index ][$fog$226]( percent );
      }

      deferred[$fog$525]( elem, [ animation, percent, remaining ]);

      if ( percent < $fog$852 && length ) {
        return remaining;
      } else {
        deferred[$fog$524]( elem, [ animation ] );
        return $fog$848;
      }
    },
    animation = deferred[$fog$531]({
      elem: elem,
      props: jQuery[$fog$849]( {}, properties ),
      opts: jQuery[$fog$849]( $fog$883, { specialEasing: {} }, options ),
      originalProperties: properties,
      originalOptions: options,
      startTime: fxNow || createFxNow(),
      duration: options[$fog$252],
      tweens: [],
      createTween: function( prop, end ) {
        var tween = jQuery[$fog$262]( elem, animation[$fog$225], prop, end,
            animation[$fog$225][$fog$224][ prop ] || animation[$fog$225][$fog$260] );
        animation[$fog$227][$fog$878]( tween );
        return tween;
      },
      stop: function( gotoEnd ) {
        var index = $fog$865,
          // If we are going to the end, we want to run all the tweens
          // otherwise we skip this part
          length = gotoEnd ? animation[$fog$227][$fog$862] : $fog$865;
        if ( stopped ) {
          return this;
        }
        stopped = $fog$883;
        for ( ; index < length ; index++ ) {
          animation[$fog$227][ index ][$fog$226]( $fog$852 );
        }

        // Resolve when we played the last frame; otherwise, reject
        if ( gotoEnd ) {
          deferred[$fog$524]( elem, [ animation, gotoEnd ] );
        } else {
          deferred[$fog$223]( elem, [ animation, gotoEnd ] );
        }
        return this;
      }
    }),
    props = animation[$fog$396];

  propFilter( props, animation[$fog$225][$fog$224] );

  for ( ; index < length ; index++ ) {
    result = animationPrefilters[ index ][$fog$864]( animation, elem, props, animation[$fog$225] );
    if ( result ) {
      return result;
    }
  }

  jQuery[$fog$855]( props, createTween, animation );

  if ( jQuery[$fog$846]( animation[$fog$225][$fog$257] ) ) {
    animation[$fog$225][$fog$257][$fog$864]( elem, animation );
  }

  jQuery[$fog$491][$fog$222](
    jQuery[$fog$849]( tick, {
      elem: elem,
      anim: animation,
      queue: animation[$fog$225][$fog$490]
    })
  );

  // attach callbacks from options
  return animation[$fog$533]( animation[$fog$225][$fog$533] )
    [$fog$540]( animation[$fog$225][$fog$540], animation[$fog$225][$fog$515] )
    [$fog$536]( animation[$fog$225][$fog$536] )
    [$fog$236]( animation[$fog$225][$fog$236] );
}

jQuery[$fog$221] = jQuery[$fog$849]( Animation, {

  tweener: function( props, callback ) {
    if ( jQuery[$fog$846]( props ) ) {
      callback = props;
      props = [ $fog$782 ];
    } else {
      props = props[$fog$817]($fog$815);
    }

    var prop,
      index = $fog$865,
      length = props[$fog$862];

    for ( ; index < length ; index++ ) {
      prop = props[ index ];
      tweeners[ prop ] = tweeners[ prop ] || [];
      tweeners[ prop ][$fog$649]( callback );
    }
  },

  prefilter: function( callback, prepend ) {
    if ( prepend ) {
      animationPrefilters[$fog$649]( callback );
    } else {
      animationPrefilters[$fog$878]( callback );
    }
  }
});

jQuery[$fog$220] = function( speed, easing, fn ) {
  var opt = speed && typeof speed === $fog$886 ? jQuery[$fog$849]( {}, speed ) : {
    complete: fn || !fn && easing ||
      jQuery[$fog$846]( speed ) && speed,
    duration: speed,
    easing: fn && easing || easing && !jQuery[$fog$846]( easing ) && easing
  };

  opt[$fog$252] = jQuery[$fog$491][$fog$520] ? $fog$865 : typeof opt[$fog$252] === $fog$811 ? opt[$fog$252] :
    opt[$fog$252] in jQuery[$fog$491][$fog$219] ? jQuery[$fog$491][$fog$219][ opt[$fog$252] ] : jQuery[$fog$491][$fog$219][$fog$427];

  // Normalize opt.queue - true/undefined/null -> "fx"
  if ( opt[$fog$490] == $fog$863 || opt[$fog$490] === $fog$883 ) {
    opt[$fog$490] = $fog$491;
  }

  // Queueing
  opt[$fog$218] = opt[$fog$515];

  opt[$fog$515] = function() {
    if ( jQuery[$fog$846]( opt[$fog$218] ) ) {
      opt[$fog$218][$fog$864]( this );
    }

    if ( opt[$fog$490] ) {
      jQuery[$fog$488]( this, opt[$fog$490] );
    }
  };

  return opt;
};

jQuery[$fog$872][$fog$849]({
  fadeTo: function( speed, to, easing, callback ) {

    // Show any hidden elements after setting opacity to 0
    return this[$fog$695]( isHidden )[$fog$477]( $fog$277, $fog$865 )[$fog$264]()

      // Animate to the value specified
      [$fog$255]()[$fog$217]({ opacity: to }, speed, easing, callback );
  },
  animate: function( prop, speed, easing, callback ) {
    var empty = jQuery[$fog$505]( prop ),
      optall = jQuery[$fog$220]( speed, easing, callback ),
      doAnimation = function() {
        // Operate on a copy of prop so per-property easing won't be lost
        var anim = Animation( this, jQuery[$fog$849]( {}, prop ), optall );

        // Empty animations, or finishing resolves immediately
        if ( empty || data_priv[$fog$556]( this, $fog$216 ) ) {
          anim[$fog$486]( $fog$883 );
        }
      };
      doAnimation[$fog$216] = doAnimation;

    return empty || optall[$fog$490] === $fog$848 ?
      this[$fog$857]( doAnimation ) :
      this[$fog$490]( optall[$fog$490], doAnimation );
  },
  stop: function( type, clearQueue, gotoEnd ) {
    var stopQueue = function( hooks ) {
      var stop = hooks[$fog$486];
      delete hooks[$fog$486];
      stop( gotoEnd );
    };

    if ( typeof type !== $fog$821 ) {
      gotoEnd = clearQueue;
      clearQueue = type;
      type = undefined;
    }
    if ( clearQueue && type !== $fog$848 ) {
      this[$fog$490]( type || $fog$491, [] );
    }

    return this[$fog$857](function() {
      var dequeue = $fog$883,
        index = type != $fog$863 && type + $fog$484,
        timers = jQuery[$fog$215],
        data = data_priv[$fog$556]( this );

      if ( index ) {
        if ( data[ index ] && data[ index ][$fog$486] ) {
          stopQueue( data[ index ] );
        }
      } else {
        for ( index in data ) {
          if ( data[ index ] && data[ index ][$fog$486] && rrun[$fog$733]( index ) ) {
            stopQueue( data[ index ] );
          }
        }
      }

      for ( index = timers[$fog$862]; index--; ) {
        if ( timers[ index ][$fog$421] === this && (type == $fog$863 || timers[ index ][$fog$490] === type) ) {
          timers[ index ][$fog$214][$fog$486]( gotoEnd );
          dequeue = $fog$848;
          timers[$fog$850]( index, $fog$852 );
        }
      }

      // Start the next in the queue if the last step wasn't forced.
      // Timers currently will call their complete callbacks, which
      // will dequeue but only if they were gotoEnd.
      if ( dequeue || !gotoEnd ) {
        jQuery[$fog$488]( this, type );
      }
    });
  },
  finish: function( type ) {
    if ( type !== $fog$848 ) {
      type = type || $fog$491;
    }
    return this[$fog$857](function() {
      var index,
        data = data_priv[$fog$556]( this ),
        queue = data[ type + $fog$490 ],
        hooks = data[ type + $fog$484 ],
        timers = jQuery[$fog$215],
        length = queue ? queue[$fog$862] : $fog$865;

      // Enable finishing flag on private data
      data[$fog$216] = $fog$883;

      // Empty the queue first
      jQuery[$fog$490]( this, type, [] );

      if ( hooks && hooks[$fog$486] ) {
        hooks[$fog$486][$fog$864]( this, $fog$883 );
      }

      // Look for any active animations, and finish them
      for ( index = timers[$fog$862]; index--; ) {
        if ( timers[ index ][$fog$421] === this && timers[ index ][$fog$490] === type ) {
          timers[ index ][$fog$214][$fog$486]( $fog$883 );
          timers[$fog$850]( index, $fog$852 );
        }
      }

      // Look for any animations in the old queue and finish them
      for ( index = $fog$865; index < length; index++ ) {
        if ( queue[ index ] && queue[ index ][$fog$216] ) {
          queue[ index ][$fog$216][$fog$864]( this );
        }
      }

      // Turn off finishing flag
      delete data[$fog$216];
    });
  }
});

jQuery[$fog$857]([ $fog$230, $fog$264, $fog$263 ], function( i, name ) {
  var cssFn = jQuery[$fog$872][ name ];
  jQuery[$fog$872][ name ] = function( speed, easing, callback ) {
    return speed == $fog$863 || typeof speed === $fog$847 ?
      cssFn[$fog$854]( this, arguments ) :
      this[$fog$217]( genFx( name, $fog$883 ), speed, easing, callback );
  };
});

// Generate shortcuts for custom animations
jQuery[$fog$857]({
  slideDown: genFx($fog$264),
  slideUp: genFx($fog$263),
  slideToggle: genFx($fog$230),
  fadeIn: { opacity: $fog$264 },
  fadeOut: { opacity: $fog$263 },
  fadeToggle: { opacity: $fog$230 }
}, function( name, props ) {
  jQuery[$fog$872][ name ] = function( speed, easing, callback ) {
    return this[$fog$217]( props, speed, easing, callback );
  };
});

jQuery[$fog$215] = [];
jQuery[$fog$491][$fog$213] = function() {
  var timer,
    i = $fog$865,
    timers = jQuery[$fog$215];

  fxNow = jQuery[$fog$818]();

  for ( ; i < timers[$fog$862]; i++ ) {
    timer = timers[ i ];
    // Checks the timer has not already been removed
    if ( !timer() && timers[ i ] === timer ) {
      timers[$fog$850]( i--, $fog$852 );
    }
  }

  if ( !timers[$fog$862] ) {
    jQuery[$fog$491][$fog$486]();
  }
  fxNow = undefined;
};

jQuery[$fog$491][$fog$222] = function( timer ) {
  jQuery[$fog$215][$fog$878]( timer );
  if ( timer() ) {
    jQuery[$fog$491][$fog$257]();
  } else {
    jQuery[$fog$215][$fog$808]();
  }
};

jQuery[$fog$491][$fog$212] = $fog$211;

jQuery[$fog$491][$fog$257] = function() {
  if ( !timerId ) {
    timerId = setInterval( jQuery[$fog$491][$fog$213], jQuery[$fog$491][$fog$212] );
  }
};

jQuery[$fog$491][$fog$486] = function() {
  clearInterval( timerId );
  timerId = $fog$863;
};

jQuery[$fog$491][$fog$219] = {
  slow: $fog$210,
  fast: $fog$209,
  // Default speed
  _default: $fog$208
};


// Based off of the plugin by Clint Helfers, with permission.
// http://blindsignals.com/index.php/2009/07/jquery-delay/
jQuery[$fog$872][$fog$207] = function( time, type ) {
  time = jQuery[$fog$491] ? jQuery[$fog$491][$fog$219][ time ] || time : time;
  type = type || $fog$491;

  return this[$fog$490]( type, function( next, hooks ) {
    var timeout = setTimeout( next, time );
    hooks[$fog$486] = function() {
      clearTimeout( timeout );
    };
  });
};


(function() {
  var input = document[$fog$831]( $fog$715 ),
    select = document[$fog$831]( $fog$583 ),
    opt = select[$fog$826]( document[$fog$831]( $fog$597 ) );

  input[$fog$839] = $fog$391;

  // Support: iOS<=5.1, Android<=4.2+
  // Default value for a checkbox should be "on"
  support[$fog$206] = input[$fog$693] !== $fog$866;

  // Support: IE<=11+
  // Must access selectedIndex to make default options select
  support[$fog$205] = opt[$fog$596];

  // Support: Android<=2.3
  // Options inside disabled selects are incorrectly marked as disabled
  select[$fog$599] = $fog$883;
  support[$fog$204] = !opt[$fog$599];

  // Support: IE<=11+
  // An input loses its value after becoming a radio
  input = document[$fog$831]( $fog$715 );
  input[$fog$693] = $fog$471;
  input[$fog$839] = $fog$472;
  support[$fog$203] = input[$fog$693] === $fog$471;
})();


var nodeHook, boolHook,
  attrHandle = jQuery[$fog$576][$fog$718];

jQuery[$fog$872][$fog$849]({
  attr: function( name, value ) {
    return access( this, jQuery[$fog$647], name, value, arguments[$fog$862] > $fog$852 );
  },

  removeAttr: function( name ) {
    return this[$fog$857](function() {
      jQuery[$fog$202]( this, name );
    });
  }
});

jQuery[$fog$849]({
  attr: function( elem, name, value ) {
    var hooks, ret,
      nType = elem[$fog$836];

    // don't get/set attributes on text, comment and attribute nodes
    if ( !elem || nType === $fog$736 || nType === $fog$627 || nType === $fog$820 ) {
      return;
    }

    // Fallback to prop when attributes are not supported
    if ( typeof elem[$fog$732] === strundefined ) {
      return jQuery[$fog$261]( elem, name, value );
    }

    // All attributes are lowercase
    // Grab necessary hook if one is defined
    if ( nType !== $fog$852 || !jQuery[$fog$573]( elem ) ) {
      name = name[$fog$822]();
      hooks = jQuery[$fog$201][ name ] ||
        ( jQuery[$fog$576][$fog$572][$fog$200][$fog$733]( name ) ? boolHook : nodeHook );
    }

    if ( value !== undefined ) {

      if ( value === $fog$863 ) {
        jQuery[$fog$202]( elem, name );

      } else if ( hooks && $fog$503 in hooks && (ret = hooks[$fog$503]( elem, value, name )) !== undefined ) {
        return ret;

      } else {
        elem[$fog$730]( name, value + $fog$866 );
        return value;
      }

    } else if ( hooks && $fog$556 in hooks && (ret = hooks[$fog$556]( elem, name )) !== $fog$863 ) {
      return ret;

    } else {
      ret = jQuery[$fog$697][$fog$647]( elem, name );

      // Non-existent attributes return null, we normalize to undefined
      return ret == $fog$863 ?
        undefined :
        ret;
    }
  },

  removeAttr: function( elem, value ) {
    var name, propName,
      i = $fog$865,
      attrNames = value && value[$fog$572]( rnotwhite );

    if ( attrNames && elem[$fog$836] === $fog$852 ) {
      while ( (name = attrNames[i++]) ) {
        propName = jQuery[$fog$199][ name ] || name;

        // Boolean attributes get special treatment (#10870)
        if ( jQuery[$fog$576][$fog$572][$fog$200][$fog$733]( name ) ) {
          // Set corresponding property to false
          elem[ propName ] = $fog$848;
        }

        elem[$fog$724]( name );
      }
    }
  },

  attrHooks: {
    type: {
      set: function( elem, value ) {
        if ( !support[$fog$203] && value === $fog$472 &&
          jQuery[$fog$823]( elem, $fog$715 ) ) {
          var val = elem[$fog$693];
          elem[$fog$730]( $fog$839, value );
          if ( val ) {
            elem[$fog$693] = val;
          }
          return value;
        }
      }
    }
  }
});

// Hooks for boolean attributes
boolHook = {
  set: function( elem, value, name ) {
    if ( value === $fog$848 ) {
      // Remove boolean attributes when set to false
      jQuery[$fog$202]( elem, name );
    } else {
      elem[$fog$730]( name, name );
    }
    return name;
  }
};
jQuery[$fog$857]( jQuery[$fog$576][$fog$572][$fog$200][$fog$483][$fog$572]( $fog$198 ), function( i, name ) {
  var getter = attrHandle[ name ] || jQuery[$fog$697][$fog$647];

  attrHandle[ name ] = function( elem, name, isXML ) {
    var ret, handle;
    if ( !isXML ) {
      // Avoid an infinite loop by temporarily removing this function from the getter
      handle = attrHandle[ name ];
      attrHandle[ name ] = ret;
      ret = getter( elem, name, isXML ) != $fog$863 ?
        name[$fog$822]() :
        $fog$863;
      attrHandle[ name ] = handle;
    }
    return ret;
  };
});




var rfocusable = $fog$761;

jQuery[$fog$872][$fog$849]({
  prop: function( name, value ) {
    return access( this, jQuery[$fog$261], name, value, arguments[$fog$862] > $fog$852 );
  },

  removeProp: function( name ) {
    return this[$fog$857](function() {
      delete this[ jQuery[$fog$199][ name ] || name ];
    });
  }
});

jQuery[$fog$849]({
  propFix: {
    "for": $fog$197,
    "class": $fog$701
  },

  prop: function( elem, name, value ) {
    var ret, hooks, notxml,
      nType = elem[$fog$836];

    // Don't get/set properties on text, comment and attribute nodes
    if ( !elem || nType === $fog$736 || nType === $fog$627 || nType === $fog$820 ) {
      return;
    }

    notxml = nType !== $fog$852 || !jQuery[$fog$573]( elem );

    if ( notxml ) {
      // Fix name and attach hooks
      name = jQuery[$fog$199][ name ] || name;
      hooks = jQuery[$fog$253][ name ];
    }

    if ( value !== undefined ) {
      return hooks && $fog$503 in hooks && (ret = hooks[$fog$503]( elem, value, name )) !== undefined ?
        ret :
        ( elem[ name ] = value );

    } else {
      return hooks && $fog$556 in hooks && (ret = hooks[$fog$556]( elem, name )) !== $fog$863 ?
        ret :
        elem[ name ];
    }
  },

  propHooks: {
    tabIndex: {
      get: function( elem ) {
        return elem[$fog$196]( $fog$195 ) || rfocusable[$fog$733]( elem[$fog$823] ) || elem[$fog$601] ?
          elem[$fog$600] :
          -$fog$852;
      }
    }
  }
});

if ( !support[$fog$205] ) {
  jQuery[$fog$253][$fog$596] = {
    get: function( elem ) {
      var parent = elem[$fog$827];
      if ( parent && parent[$fog$827] ) {
        parent[$fog$827][$fog$595];
      }
      return $fog$863;
    }
  };
}

jQuery[$fog$857]([
  $fog$600,
  $fog$194,
  $fog$193,
  $fog$192,
  $fog$191,
  $fog$190,
  $fog$189,
  $fog$188,
  $fog$187,
  $fog$186
], function() {
  jQuery[$fog$199][ this[$fog$822]() ] = this;
});




var rclass = $fog$185;

jQuery[$fog$872][$fog$849]({
  addClass: function( value ) {
    var classes, elem, cur, clazz, j, finalValue,
      proceed = typeof value === $fog$821 && value,
      i = $fog$865,
      len = this[$fog$862];

    if ( jQuery[$fog$846]( value ) ) {
      return this[$fog$857](function( j ) {
        jQuery( this )[$fog$184]( value[$fog$864]( this, j, this[$fog$701] ) );
      });
    }

    if ( proceed ) {
      // The disjunction here is for better compressibility (see removeClass)
      classes = ( value || $fog$866 )[$fog$572]( rnotwhite ) || [];

      for ( ; i < len; i++ ) {
        elem = this[ i ];
        cur = elem[$fog$836] === $fog$852 && ( elem[$fog$701] ?
          ( $fog$815 + elem[$fog$701] + $fog$815 )[$fog$842]( rclass, $fog$815 ) :
          $fog$815
        );

        if ( cur ) {
          j = $fog$865;
          while ( (clazz = classes[j++]) ) {
            if ( cur[$fog$877]( $fog$815 + clazz + $fog$815 ) < $fog$865 ) {
              cur += clazz + $fog$815;
            }
          }

          // only assign if different to avoid unneeded rendering.
          finalValue = jQuery[$fog$833]( cur );
          if ( elem[$fog$701] !== finalValue ) {
            elem[$fog$701] = finalValue;
          }
        }
      }
    }

    return this;
  },

  removeClass: function( value ) {
    var classes, elem, cur, clazz, j, finalValue,
      proceed = arguments[$fog$862] === $fog$865 || typeof value === $fog$821 && value,
      i = $fog$865,
      len = this[$fog$862];

    if ( jQuery[$fog$846]( value ) ) {
      return this[$fog$857](function( j ) {
        jQuery( this )[$fog$183]( value[$fog$864]( this, j, this[$fog$701] ) );
      });
    }
    if ( proceed ) {
      classes = ( value || $fog$866 )[$fog$572]( rnotwhite ) || [];

      for ( ; i < len; i++ ) {
        elem = this[ i ];
        // This expression is here for better compressibility (see addClass)
        cur = elem[$fog$836] === $fog$852 && ( elem[$fog$701] ?
          ( $fog$815 + elem[$fog$701] + $fog$815 )[$fog$842]( rclass, $fog$815 ) :
          $fog$866
        );

        if ( cur ) {
          j = $fog$865;
          while ( (clazz = classes[j++]) ) {
            // Remove *all* instances
            while ( cur[$fog$877]( $fog$815 + clazz + $fog$815 ) >= $fog$865 ) {
              cur = cur[$fog$842]( $fog$815 + clazz + $fog$815, $fog$815 );
            }
          }

          // Only assign if different to avoid unneeded rendering.
          finalValue = value ? jQuery[$fog$833]( cur ) : $fog$866;
          if ( elem[$fog$701] !== finalValue ) {
            elem[$fog$701] = finalValue;
          }
        }
      }
    }

    return this;
  },

  toggleClass: function( value, stateVal ) {
    var type = typeof value;

    if ( typeof stateVal === $fog$847 && type === $fog$821 ) {
      return stateVal ? this[$fog$184]( value ) : this[$fog$183]( value );
    }

    if ( jQuery[$fog$846]( value ) ) {
      return this[$fog$857](function( i ) {
        jQuery( this )[$fog$182]( value[$fog$864](this, i, this[$fog$701], stateVal), stateVal );
      });
    }

    return this[$fog$857](function() {
      if ( type === $fog$821 ) {
        // Toggle individual class names
        var className,
          i = $fog$865,
          self = jQuery( this ),
          classNames = value[$fog$572]( rnotwhite ) || [];

        while ( (className = classNames[ i++ ]) ) {
          // Check each className given, space separated list
          if ( self[$fog$181]( className ) ) {
            self[$fog$183]( className );
          } else {
            self[$fog$184]( className );
          }
        }

      // Toggle whole class name
      } else if ( type === strundefined || type === $fog$847 ) {
        if ( this[$fog$701] ) {
          // store className if set
          data_priv[$fog$503]( this, $fog$180, this[$fog$701] );
        }

        // If the element has a class name or if we're passed `false`,
        // then remove the whole classname (if there was one, the above saved it).
        // Otherwise bring back whatever was previously saved (if anything),
        // falling back to the empty string if nothing was stored.
        this[$fog$701] = this[$fog$701] || value === $fog$848 ? $fog$866 : data_priv[$fog$556]( this, $fog$180 ) || $fog$866;
      }
    });
  },

  hasClass: function( selector ) {
    var className = $fog$815 + selector + $fog$815,
      i = $fog$865,
      l = this[$fog$862];
    for ( ; i < l; i++ ) {
      if ( this[i][$fog$836] === $fog$852 && ($fog$815 + this[i][$fog$701] + $fog$815)[$fog$842](rclass, $fog$815)[$fog$877]( className ) >= $fog$865 ) {
        return $fog$883;
      }
    }

    return $fog$848;
  }
});




var rreturn = $fog$179;

jQuery[$fog$872][$fog$849]({
  val: function( value ) {
    var hooks, ret, isFunction,
      elem = this[$fog$865];

    if ( !arguments[$fog$862] ) {
      if ( elem ) {
        hooks = jQuery[$fog$178][ elem[$fog$839] ] || jQuery[$fog$178][ elem[$fog$823][$fog$822]() ];

        if ( hooks && $fog$556 in hooks && (ret = hooks[$fog$556]( elem, $fog$693 )) !== undefined ) {
          return ret;
        }

        ret = elem[$fog$693];

        return typeof ret === $fog$821 ?
          // Handle most common string cases
          ret[$fog$842](rreturn, $fog$866) :
          // Handle cases where value is null/undef or number
          ret == $fog$863 ? $fog$866 : ret;
      }

      return;
    }

    isFunction = jQuery[$fog$846]( value );

    return this[$fog$857](function( i ) {
      var val;

      if ( this[$fog$836] !== $fog$852 ) {
        return;
      }

      if ( isFunction ) {
        val = value[$fog$864]( this, i, jQuery( this )[$fog$177]() );
      } else {
        val = value;
      }

      // Treat null/undefined as ""; convert numbers to string
      if ( val == $fog$863 ) {
        val = $fog$866;

      } else if ( typeof val === $fog$811 ) {
        val += $fog$866;

      } else if ( jQuery[$fog$844]( val ) ) {
        val = jQuery[$fog$855]( val, function( value ) {
          return value == $fog$863 ? $fog$866 : value + $fog$866;
        });
      }

      hooks = jQuery[$fog$178][ this[$fog$839] ] || jQuery[$fog$178][ this[$fog$823][$fog$822]() ];

      // If set returns undefined, fall back to normal setting
      if ( !hooks || !($fog$503 in hooks) || hooks[$fog$503]( this, val, $fog$693 ) === undefined ) {
        this[$fog$693] = val;
      }
    });
  }
});

jQuery[$fog$849]({
  valHooks: {
    option: {
      get: function( elem ) {
        var val = jQuery[$fog$697][$fog$647]( elem, $fog$693 );
        return val != $fog$863 ?
          val :
          // Support: IE10-11+
          // option.text throws exceptions (#14686, #14858)
          jQuery[$fog$833]( jQuery[$fog$829]( elem ) );
      }
    },
    select: {
      get: function( elem ) {
        var value, option,
          options = elem[$fog$258],
          index = elem[$fog$595],
          one = elem[$fog$839] === $fog$176 || index < $fog$865,
          values = one ? $fog$863 : [],
          max = one ? index + $fog$852 : options[$fog$862],
          i = index < $fog$865 ?
            max :
            one ? index : $fog$865;

        // Loop through all the selected options
        for ( ; i < max; i++ ) {
          option = options[ i ];

          // IE6-9 doesn't update selected after form reset (#2551)
          if ( ( option[$fog$596] || i === index ) &&
              // Don't return options that are disabled or in a disabled optgroup
              ( support[$fog$204] ? !option[$fog$599] : option[$fog$732]( $fog$599 ) === $fog$863 ) &&
              ( !option[$fog$827][$fog$599] || !jQuery[$fog$823]( option[$fog$827], $fog$360 ) ) ) {

            // Get the specific value for the option
            value = jQuery( option )[$fog$177]();

            // We don't need an array for one selects
            if ( one ) {
              return value;
            }

            // Multi-Selects return an array
            values[$fog$878]( value );
          }
        }

        return values;
      },

      set: function( elem, value ) {
        var optionSet, option,
          options = elem[$fog$258],
          values = jQuery[$fog$561]( value ),
          i = options[$fog$862];

        while ( i-- ) {
          option = options[ i ];
          if ( (option[$fog$596] = jQuery[$fog$543]( option[$fog$693], values ) >= $fog$865) ) {
            optionSet = $fog$883;
          }
        }

        // Force browsers to behave consistently when non-matching value is set
        if ( !optionSet ) {
          elem[$fog$595] = -$fog$852;
        }
        return values;
      }
    }
  }
});

// Radios and checkboxes getter/setter
jQuery[$fog$857]([ $fog$472, $fog$391 ], function() {
  jQuery[$fog$178][ this ] = {
    set: function( elem, value ) {
      if ( jQuery[$fog$844]( value ) ) {
        return ( elem[$fog$598] = jQuery[$fog$543]( jQuery(elem)[$fog$177](), value ) >= $fog$865 );
      }
    }
  };
  if ( !support[$fog$206] ) {
    jQuery[$fog$178][ this ][$fog$556] = function( elem ) {
      return elem[$fog$732]($fog$693) === $fog$863 ? $fog$439 : elem[$fog$693];
    };
  }
});




// Return jQuery for attributes-only inclusion


jQuery[$fog$857]( ($fog$175 +
  $fog$174 +
  $fog$173)[$fog$817]($fog$815), function( i, name ) {

  // Handle event binding
  jQuery[$fog$872][ name ] = function( data, fn ) {
    return arguments[$fog$862] > $fog$865 ?
      this[$fog$439]( name, $fog$863, data, fn ) :
      this[$fog$433]( name );
  };
});

jQuery[$fog$872][$fog$849]({
  hover: function( fnOver, fnOut ) {
    return this[$fog$171]( fnOver )[$fog$172]( fnOut || fnOver );
  },

  bind: function( types, data, fn ) {
    return this[$fog$439]( types, $fog$863, data, fn );
  },
  unbind: function( types, fn ) {
    return this[$fog$520]( types, $fog$863, fn );
  },

  delegate: function( selector, types, data, fn ) {
    return this[$fog$439]( types, selector, data, fn );
  },
  undelegate: function( selector, types, fn ) {
    // ( namespace ) or ( selector, types [, fn] )
    return arguments[$fog$862] === $fog$852 ? this[$fog$520]( selector, $fog$442 ) : this[$fog$520]( types, selector || $fog$442, fn );
  }
});


var nonce = jQuery[$fog$818]();

var rquery = ($fog$170);



// Support: Android 2.3
// Workaround failure to string-cast null input
jQuery[$fog$495] = function( data ) {
  return JSON[$fog$169]( data + $fog$866 );
};


// Cross-browser xml parsing
jQuery[$fog$168] = function( data ) {
  var xml, tmp;
  if ( !data || typeof data !== $fog$821 ) {
    return $fog$863;
  }

  // Support: IE9
  try {
    tmp = new DOMParser();
    xml = tmp[$fog$167]( data, $fog$166 );
  } catch ( e ) {
    xml = undefined;
  }

  if ( !xml || xml[$fog$737]( $fog$165 )[$fog$862] ) {
    jQuery[$fog$645]( $fog$164 + data );
  }
  return xml;
};


var
  rhash = $fog$163,
  rts = $fog$162,
  rheaders = $fog$161,
  // #7653, #8125, #8152: local protocol detection
  rlocalProtocol = $fog$160,
  rnoContent = $fog$159,
  rprotocol = $fog$158,
  rurl = $fog$157,

  /* Prefilters
   * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)
   * 2) These are called:
   *    - BEFORE asking for a transport
   *    - AFTER param serialization (s.data is a string if s.processData is true)
   * 3) key is the dataType
   * 4) the catchall symbol "*" can be used
   * 5) execution will start with transport dataType and THEN continue down to "*" if needed
   */
  prefilters = {},

  /* Transports bindings
   * 1) key is the dataType
   * 2) the catchall symbol "*" can be used
   * 3) selection will start with transport dataType and THEN go to "*" if needed
   */
  transports = {},

  // Avoid comment-prolog char sequence (#10098); must appease lint and evade compression
  allTypes = $fog$156[$fog$879]( $fog$782 ),

  // Document location
  ajaxLocation = window[$fog$605][$fog$601],

  // Segment location into parts
  ajaxLocParts = rurl[$fog$740]( ajaxLocation[$fog$822]() ) || [];

// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport
function addToPrefiltersOrTransports( structure ) {

  // dataTypeExpression is optional and defaults to "*"
  return function( dataTypeExpression, func ) {

    if ( typeof dataTypeExpression !== $fog$821 ) {
      func = dataTypeExpression;
      dataTypeExpression = $fog$782;
    }

    var dataType,
      i = $fog$865,
      dataTypes = dataTypeExpression[$fog$822]()[$fog$572]( rnotwhite ) || [];

    if ( jQuery[$fog$846]( func ) ) {
      // For each dataType in the dataTypeExpression
      while ( (dataType = dataTypes[i++]) ) {
        // Prepend if requested
        if ( dataType[$fog$865] === $fog$788 ) {
          dataType = dataType[$fog$880]( $fog$852 ) || $fog$782;
          (structure[ dataType ] = structure[ dataType ] || [])[$fog$649]( func );

        // Otherwise append
        } else {
          (structure[ dataType ] = structure[ dataType ] || [])[$fog$878]( func );
        }
      }
    }
  };
}

// Base inspection function for prefilters and transports
function inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR ) {

  var inspected = {},
    seekingTransport = ( structure === transports );

  function inspect( dataType ) {
    var selected;
    inspected[ dataType ] = $fog$883;
    jQuery[$fog$857]( structure[ dataType ] || [], function( _, prefilterOrFactory ) {
      var dataTypeOrTransport = prefilterOrFactory( options, originalOptions, jqXHR );
      if ( typeof dataTypeOrTransport === $fog$821 && !seekingTransport && !inspected[ dataTypeOrTransport ] ) {
        options[$fog$155][$fog$649]( dataTypeOrTransport );
        inspect( dataTypeOrTransport );
        return $fog$848;
      } else if ( seekingTransport ) {
        return !( selected = dataTypeOrTransport );
      }
    });
    return selected;
  }

  return inspect( options[$fog$155][ $fog$865 ] ) || !inspected[ $fog$782 ] && inspect( $fog$782 );
}

// A special extend for ajax options
// that takes "flat" options (not to be deep extended)
// Fixes #9887
function ajaxExtend( target, src ) {
  var key, deep,
    flatOptions = jQuery[$fog$153][$fog$154] || {};

  for ( key in src ) {
    if ( src[ key ] !== undefined ) {
      ( flatOptions[ key ] ? target : ( deep || (deep = {}) ) )[ key ] = src[ key ];
    }
  }
  if ( deep ) {
    jQuery[$fog$849]( $fog$883, target, deep );
  }

  return target;
}

/* Handles responses to an ajax request:
 * - finds the right dataType (mediates between content-type and expected dataType)
 * - returns the corresponding response
 */
function ajaxHandleResponses( s, jqXHR, responses ) {

  var ct, type, finalDataType, firstDataType,
    contents = s[$fog$152],
    dataTypes = s[$fog$155];

  // Remove auto dataType and get content-type in the process
  while ( dataTypes[ $fog$865 ] === $fog$782 ) {
    dataTypes[$fog$721]();
    if ( ct === undefined ) {
      ct = s[$fog$151] || jqXHR[$fog$150]($fog$149);
    }
  }

  // Check if we're dealing with a known content-type
  if ( ct ) {
    for ( type in contents ) {
      if ( contents[ type ] && contents[ type ][$fog$733]( ct ) ) {
        dataTypes[$fog$649]( type );
        break;
      }
    }
  }

  // Check to see if we have a response for the expected dataType
  if ( dataTypes[ $fog$865 ] in responses ) {
    finalDataType = dataTypes[ $fog$865 ];
  } else {
    // Try convertible dataTypes
    for ( type in responses ) {
      if ( !dataTypes[ $fog$865 ] || s[$fog$148][ type + $fog$815 + dataTypes[$fog$865] ] ) {
        finalDataType = type;
        break;
      }
      if ( !firstDataType ) {
        firstDataType = type;
      }
    }
    // Or just use first one
    finalDataType = finalDataType || firstDataType;
  }

  // If we found a dataType
  // We add the dataType to the list if needed
  // and return the corresponding response
  if ( finalDataType ) {
    if ( finalDataType !== dataTypes[ $fog$865 ] ) {
      dataTypes[$fog$649]( finalDataType );
    }
    return responses[ finalDataType ];
  }
}

/* Chain conversions given the request and the original response
 * Also sets the responseXXX fields on the jqXHR instance
 */
function ajaxConvert( s, response, jqXHR, isSuccess ) {
  var conv2, current, conv, tmp, prev,
    converters = {},
    // Work with a copy of dataTypes in case we need to modify it for conversion
    dataTypes = s[$fog$155][$fog$880]();

  // Create converters map with lowercased keys
  if ( dataTypes[ $fog$852 ] ) {
    for ( conv in s[$fog$148] ) {
      converters[ conv[$fog$822]() ] = s[$fog$148][ conv ];
    }
  }

  current = dataTypes[$fog$721]();

  // Convert to each sequential dataType
  while ( current ) {

    if ( s[$fog$147][ current ] ) {
      jqXHR[ s[$fog$147][ current ] ] = response;
    }

    // Apply the dataFilter if provided
    if ( !prev && isSuccess && s[$fog$146] ) {
      response = s[$fog$146]( response, s[$fog$145] );
    }

    prev = current;
    current = dataTypes[$fog$721]();

    if ( current ) {

    // There's only work to do if current dataType is non-auto
      if ( current === $fog$782 ) {

        current = prev;

      // Convert response if prev dataType is non-auto and differs from current
      } else if ( prev !== $fog$782 && prev !== current ) {

        // Seek a direct converter
        conv = converters[ prev + $fog$815 + current ] || converters[ $fog$144 + current ];

        // If none found, seek a pair
        if ( !conv ) {
          for ( conv2 in converters ) {

            // If conv2 outputs current
            tmp = conv2[$fog$817]( $fog$815 );
            if ( tmp[ $fog$852 ] === current ) {

              // If prev can be converted to accepted input
              conv = converters[ prev + $fog$815 + tmp[ $fog$865 ] ] ||
                converters[ $fog$144 + tmp[ $fog$865 ] ];
              if ( conv ) {
                // Condense equivalence converters
                if ( conv === $fog$883 ) {
                  conv = converters[ conv2 ];

                // Otherwise, insert the intermediate dataType
                } else if ( converters[ conv2 ] !== $fog$883 ) {
                  current = tmp[ $fog$865 ];
                  dataTypes[$fog$649]( tmp[ $fog$852 ] );
                }
                break;
              }
            }
          }
        }

        // Apply converter (if not an equivalence)
        if ( conv !== $fog$883 ) {

          // Unless errors are allowed to bubble, catch and return them
          if ( conv && s[ $fog$143 ] ) {
            response = conv( response );
          } else {
            try {
              response = conv( response );
            } catch ( e ) {
              return { state: $fog$165, error: conv ? e : $fog$142 + prev + $fog$141 + current };
            }
          }
        }
      }
    }
  }

  return { state: $fog$140, data: response };
}

jQuery[$fog$849]({

  // Counter for holding the number of active queries
  active: $fog$865,

  // Last-Modified header cache for next request
  lastModified: {},
  etag: {},

  ajaxSettings: {
    url: ajaxLocation,
    type: $fog$139,
    isLocal: rlocalProtocol[$fog$733]( ajaxLocParts[ $fog$852 ] ),
    global: $fog$883,
    processData: $fog$883,
    async: $fog$883,
    contentType: $fog$138,
    /*
    timeout: 0,
    data: null,
    dataType: null,
    username: null,
    password: null,
    cache: null,
    throws: false,
    traditional: false,
    headers: {},
    */

    accepts: {
      "*": allTypes,
      text: $fog$137,
      html: $fog$136,
      xml: $fog$135,
      json: $fog$134
    },

    contents: {
      xml: $fog$133,
      html: $fog$132,
      json: $fog$131
    },

    responseFields: {
      xml: $fog$130,
      text: $fog$129,
      json: $fog$128
    },

    // Data converters
    // Keys separate source (or catchall "*") and destination types with a single space
    converters: {

      // Convert anything to text
      "* text": String,

      // Text to html (true = no transformation)
      "text html": $fog$883,

      // Evaluate text as a json expression
      "text json": jQuery[$fog$495],

      // Parse text as xml
      "text xml": jQuery[$fog$168]
    },

    // For options that shouldn't be deep extended:
    // you can add your own custom options here if
    // and when you create one that shouldn't be
    // deep extended (see ajaxExtend)
    flatOptions: {
      url: $fog$883,
      context: $fog$883
    }
  },

  // Creates a full fledged settings object into target
  // with both ajaxSettings and settings fields.
  // If target is omitted, writes into ajaxSettings.
  ajaxSetup: function( target, settings ) {
    return settings ?

      // Building a settings object
      ajaxExtend( ajaxExtend( target, jQuery[$fog$153] ), settings ) :

      // Extending ajaxSettings
      ajaxExtend( jQuery[$fog$153], target );
  },

  ajaxPrefilter: addToPrefiltersOrTransports( prefilters ),
  ajaxTransport: addToPrefiltersOrTransports( transports ),

  // Main method
  ajax: function( url, options ) {

    // If url is an object, simulate pre-1.5 signature
    if ( typeof url === $fog$886 ) {
      options = url;
      url = undefined;
    }

    // Force options to be an object
    options = options || {};

    var transport,
      // URL without anti-cache param
      cacheURL,
      // Response headers
      responseHeadersString,
      responseHeaders,
      // timeout handle
      timeoutTimer,
      // Cross-domain detection vars
      parts,
      // To know if global events are to be dispatched
      fireGlobals,
      // Loop variable
      i,
      // Create the final options object
      s = jQuery[$fog$127]( {}, options ),
      // Callbacks context
      callbackContext = s[$fog$858] || s,
      // Context for global events is callbackContext if it is a DOM node or jQuery collection
      globalEventContext = s[$fog$858] && ( callbackContext[$fog$836] || callbackContext[$fog$563] ) ?
        jQuery( callbackContext ) :
        jQuery[$fog$460],
      // Deferreds
      deferred = jQuery[$fog$530](),
      completeDeferred = jQuery[$fog$549]($fog$539),
      // Status-dependent callbacks
      statusCode = s[$fog$126] || {},
      // Headers (they are sent all at once)
      requestHeaders = {},
      requestHeadersNames = {},
      // The jqXHR state
      state = $fog$865,
      // Default abort message
      strAbort = $fog$125,
      // Fake xhr
      jqXHR = {
        readyState: $fog$865,

        // Builds headers hashtable if needed
        getResponseHeader: function( key ) {
          var match;
          if ( state === $fog$820 ) {
            if ( !responseHeaders ) {
              responseHeaders = {};
              while ( (match = rheaders[$fog$740]( responseHeadersString )) ) {
                responseHeaders[ match[$fog$852][$fog$822]() ] = match[ $fog$820 ];
              }
            }
            match = responseHeaders[ key[$fog$822]() ];
          }
          return match == $fog$863 ? $fog$863 : match;
        },

        // Raw string
        getAllResponseHeaders: function() {
          return state === $fog$820 ? responseHeadersString : $fog$863;
        },

        // Caches the header
        setRequestHeader: function( name, value ) {
          var lname = name[$fog$822]();
          if ( !state ) {
            name = requestHeadersNames[ lname ] = requestHeadersNames[ lname ] || name;
            requestHeaders[ name ] = value;
          }
          return this;
        },

        // Overrides response content-type header
        overrideMimeType: function( type ) {
          if ( !state ) {
            s[$fog$151] = type;
          }
          return this;
        },

        // Status-dependent callbacks
        statusCode: function( map ) {
          var code;
          if ( map ) {
            if ( state < $fog$820 ) {
              for ( code in map ) {
                // Lazy-add the new callback in a way that preserves old ones
                statusCode[ code ] = [ statusCode[ code ], map[ code ] ];
              }
            } else {
              // Execute the appropriate callbacks
              jqXHR[$fog$236]( map[ jqXHR[$fog$124] ] );
            }
          }
          return this;
        },

        // Cancel the request
        abort: function( statusText ) {
          var finalText = statusText || strAbort;
          if ( transport ) {
            transport[$fog$123]( finalText );
          }
          done( $fog$865, finalText );
          return this;
        }
      };

    // Attach deferreds
    deferred[$fog$531]( jqXHR )[$fog$515] = completeDeferred[$fog$555];
    jqXHR[$fog$140] = jqXHR[$fog$540];
    jqXHR[$fog$645] = jqXHR[$fog$536];

    // Remove hash character (#7531: and string promotion)
    // Add protocol if not provided (prefilters might expect it)
    // Handle falsy url in the settings object (#10093: consistency with old signature)
    // We also use the url parameter if available
    s[$fog$122] = ( ( url || s[$fog$122] || ajaxLocation ) + $fog$866 )[$fog$842]( rhash, $fog$866 )
      [$fog$842]( rprotocol, ajaxLocParts[ $fog$852 ] + $fog$121 );

    // Alias method option to type as per ticket #12004
    s[$fog$839] = options[$fog$120] || options[$fog$839] || s[$fog$120] || s[$fog$839];

    // Extract dataTypes list
    s[$fog$155] = jQuery[$fog$833]( s[$fog$145] || $fog$782 )[$fog$822]()[$fog$572]( rnotwhite ) || [ $fog$866 ];

    // A cross-domain request is in order when we have a protocol:host:port mismatch
    if ( s[$fog$119] == $fog$863 ) {
      parts = rurl[$fog$740]( s[$fog$122][$fog$822]() );
      s[$fog$119] = !!( parts &&
        ( parts[ $fog$852 ] !== ajaxLocParts[ $fog$852 ] || parts[ $fog$820 ] !== ajaxLocParts[ $fog$820 ] ||
          ( parts[ $fog$736 ] || ( parts[ $fog$852 ] === $fog$118 ? $fog$117 : $fog$116 ) ) !==
            ( ajaxLocParts[ $fog$736 ] || ( ajaxLocParts[ $fog$852 ] === $fog$118 ? $fog$117 : $fog$116 ) ) )
      );
    }

    // Convert data if not already a string
    if ( s[$fog$418] && s[$fog$115] && typeof s[$fog$418] !== $fog$821 ) {
      s[$fog$418] = jQuery[$fog$114]( s[$fog$418], s[$fog$113] );
    }

    // Apply prefilters
    inspectPrefiltersOrTransports( prefilters, s, options, jqXHR );

    // If request was aborted inside a prefilter, stop there
    if ( state === $fog$820 ) {
      return jqXHR;
    }

    // We can fire global events as of now if asked to
    // Don't fire events if jQuery.event is undefined in an AMD-usage scenario (#15118)
    fireGlobals = jQuery[$fog$460] && s[$fog$448];

    // Watch for a new set of requests
    if ( fireGlobals && jQuery[$fog$112]++ === $fog$865 ) {
      jQuery[$fog$460][$fog$433]($fog$111);
    }

    // Uppercase the type
    s[$fog$839] = s[$fog$839][$fog$868]();

    // Determine if request has content
    s[$fog$110] = !rnoContent[$fog$733]( s[$fog$839] );

    // Save the URL in case we're toying with the If-Modified-Since
    // and/or If-None-Match header later on
    cacheURL = s[$fog$122];

    // More options handling for requests with no content
    if ( !s[$fog$110] ) {

      // If data is available, append data to url
      if ( s[$fog$418] ) {
        cacheURL = ( s[$fog$122] += ( rquery[$fog$733]( cacheURL ) ? $fog$109 : $fog$108 ) + s[$fog$418] );
        // #9682: remove data so that it's not used in an eventual retry
        delete s[$fog$418];
      }

      // Add anti-cache in url if needed
      if ( s[$fog$511] === $fog$848 ) {
        s[$fog$122] = rts[$fog$733]( cacheURL ) ?

          // If there is already a '_' parameter, set its value
          cacheURL[$fog$842]( rts, $fog$107 + nonce++ ) :

          // Otherwise add one to the end
          cacheURL + ( rquery[$fog$733]( cacheURL ) ? $fog$109 : $fog$108 ) + $fog$106 + nonce++;
      }
    }

    // Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
    if ( s[$fog$105] ) {
      if ( jQuery[$fog$104][ cacheURL ] ) {
        jqXHR[$fog$103]( $fog$102, jQuery[$fog$104][ cacheURL ] );
      }
      if ( jQuery[$fog$101][ cacheURL ] ) {
        jqXHR[$fog$103]( $fog$100, jQuery[$fog$101][ cacheURL ] );
      }
    }

    // Set the correct header, if data is being sent
    if ( s[$fog$418] && s[$fog$110] && s[$fog$99] !== $fog$848 || options[$fog$99] ) {
      jqXHR[$fog$103]( $fog$149, s[$fog$99] );
    }

    // Set the Accepts header for the server, depending on the dataType
    jqXHR[$fog$103](
      $fog$98,
      s[$fog$155][ $fog$865 ] && s[$fog$508][ s[$fog$155][$fog$865] ] ?
        s[$fog$508][ s[$fog$155][$fog$865] ] + ( s[$fog$155][ $fog$865 ] !== $fog$782 ? $fog$97 + allTypes + $fog$96 : $fog$866 ) :
        s[$fog$508][ $fog$782 ]
    );

    // Check for headers option
    for ( i in s[$fog$95] ) {
      jqXHR[$fog$103]( i, s[$fog$95][ i ] );
    }

    // Allow custom headers/mimetypes and early abort
    if ( s[$fog$94] && ( s[$fog$94][$fog$864]( callbackContext, jqXHR, s ) === $fog$848 || state === $fog$820 ) ) {
      // Abort if not done already and return
      return jqXHR[$fog$123]();
    }

    // Aborting is no longer a cancellation
    strAbort = $fog$123;

    // Install callbacks on deferreds
    for ( i in { success: $fog$852, error: $fog$852, complete: $fog$852 } ) {
      jqXHR[ i ]( s[ i ] );
    }

    // Get transport
    transport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );

    // If no transport, we auto-abort
    if ( !transport ) {
      done( -$fog$852, $fog$93 );
    } else {
      jqXHR[$fog$516] = $fog$852;

      // Send global event
      if ( fireGlobals ) {
        globalEventContext[$fog$433]( $fog$92, [ jqXHR, s ] );
      }
      // Timeout
      if ( s[$fog$91] && s[$fog$90] > $fog$865 ) {
        timeoutTimer = setTimeout(function() {
          jqXHR[$fog$123]($fog$90);
        }, s[$fog$90] );
      }

      try {
        state = $fog$852;
        transport[$fog$89]( requestHeaders, done );
      } catch ( e ) {
        // Propagate exception as error if not done
        if ( state < $fog$820 ) {
          done( -$fog$852, e );
        // Simply rethrow otherwise
        } else {
          throw e;
        }
      }
    }

    // Callback for when everything is done
    function done( status, nativeStatusText, responses, headers ) {
      var isSuccess, success, error, response, modified,
        statusText = nativeStatusText;

      // Called once
      if ( state === $fog$820 ) {
        return;
      }

      // State is "done" now
      state = $fog$820;

      // Clear timeout if it exists
      if ( timeoutTimer ) {
        clearTimeout( timeoutTimer );
      }

      // Dereference transport for early garbage collection
      // (no matter how long the jqXHR object will be used)
      transport = undefined;

      // Cache response headers
      responseHeadersString = headers || $fog$866;

      // Set readyState
      jqXHR[$fog$516] = status > $fog$865 ? $fog$650 : $fog$865;

      // Determine if successful
      isSuccess = status >= $fog$209 && status < $fog$88 || status === $fog$87;

      // Get response data
      if ( responses ) {
        response = ajaxHandleResponses( s, jqXHR, responses );
      }

      // Convert no matter what (that way responseXXX fields are always set)
      response = ajaxConvert( s, response, jqXHR, isSuccess );

      // If successful, handle type chaining
      if ( isSuccess ) {

        // Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
        if ( s[$fog$105] ) {
          modified = jqXHR[$fog$150]($fog$86);
          if ( modified ) {
            jQuery[$fog$104][ cacheURL ] = modified;
          }
          modified = jqXHR[$fog$150]($fog$101);
          if ( modified ) {
            jQuery[$fog$101][ cacheURL ] = modified;
          }
        }

        // if no content
        if ( status === $fog$85 || s[$fog$839] === $fog$84 ) {
          statusText = $fog$83;

        // if not modified
        } else if ( status === $fog$87 ) {
          statusText = $fog$82;

        // If we have data, let's convert it
        } else {
          statusText = response[$fog$81];
          success = response[$fog$418];
          error = response[$fog$645];
          isSuccess = !error;
        }
      } else {
        // Extract error from statusText and normalize for non-aborts
        error = statusText;
        if ( status || !statusText ) {
          statusText = $fog$645;
          if ( status < $fog$865 ) {
            status = $fog$865;
          }
        }
      }

      // Set data for the fake xhr object
      jqXHR[$fog$124] = status;
      jqXHR[$fog$80] = ( nativeStatusText || statusText ) + $fog$866;

      // Success/Error
      if ( isSuccess ) {
        deferred[$fog$524]( callbackContext, [ success, statusText, jqXHR ] );
      } else {
        deferred[$fog$223]( callbackContext, [ jqXHR, statusText, error ] );
      }

      // Status-dependent callbacks
      jqXHR[$fog$126]( statusCode );
      statusCode = undefined;

      if ( fireGlobals ) {
        globalEventContext[$fog$433]( isSuccess ? $fog$79 : $fog$78,
          [ jqXHR, s, isSuccess ? success : error ] );
      }

      // Complete
      completeDeferred[$fog$542]( callbackContext, [ jqXHR, statusText ] );

      if ( fireGlobals ) {
        globalEventContext[$fog$433]( $fog$77, [ jqXHR, s ] );
        // Handle the global AJAX counter
        if ( !( --jQuery[$fog$112] ) ) {
          jQuery[$fog$460][$fog$433]($fog$76);
        }
      }
    }

    return jqXHR;
  },

  getJSON: function( url, data, callback ) {
    return jQuery[$fog$556]( url, data, callback, $fog$75 );
  },

  getScript: function( url, callback ) {
    return jQuery[$fog$556]( url, undefined, callback, $fog$830 );
  }
});

jQuery[$fog$857]( [ $fog$556, $fog$74 ], function( i, method ) {
  jQuery[ method ] = function( url, data, callback, type ) {
    // Shift arguments if data argument was omitted
    if ( jQuery[$fog$846]( data ) ) {
      type = type || callback;
      callback = data;
      data = undefined;
    }

    return jQuery[$fog$73]({
      url: url,
      type: method,
      dataType: type,
      data: data,
      success: callback
    });
  };
});


jQuery[$fog$336] = function( url ) {
  return jQuery[$fog$73]({
    url: url,
    type: $fog$139,
    dataType: $fog$830,
    async: $fog$848,
    global: $fog$848,
    "throws": $fog$883
  });
};


jQuery[$fog$872][$fog$849]({
  wrapAll: function( html ) {
    var wrap;

    if ( jQuery[$fog$846]( html ) ) {
      return this[$fog$857](function( i ) {
        jQuery( this )[$fog$72]( html[$fog$864](this, i) );
      });
    }

    if ( this[ $fog$865 ] ) {

      // The elements to wrap the target around
      wrap = jQuery( html, this[ $fog$865 ][$fog$743] )[$fog$853]( $fog$865 )[$fog$342]( $fog$883 );

      if ( this[ $fog$865 ][$fog$827] ) {
        wrap[$fog$344]( this[ $fog$865 ] );
      }

      wrap[$fog$855](function() {
        var elem = this;

        while ( elem[$fog$71] ) {
          elem = elem[$fog$71];
        }

        return elem;
      })[$fog$341]( this );
    }

    return this;
  },

  wrapInner: function( html ) {
    if ( jQuery[$fog$846]( html ) ) {
      return this[$fog$857](function( i ) {
        jQuery( this )[$fog$70]( html[$fog$864](this, i) );
      });
    }

    return this[$fog$857](function() {
      var self = jQuery( this ),
        contents = self[$fog$152]();

      if ( contents[$fog$862] ) {
        contents[$fog$72]( html );

      } else {
        self[$fog$341]( html );
      }
    });
  },

  wrap: function( html ) {
    var isFunction = jQuery[$fog$846]( html );

    return this[$fog$857](function( i ) {
      jQuery( this )[$fog$72]( isFunction ? html[$fog$864](this, i) : html );
    });
  },

  unwrap: function() {
    return this[$fog$69]()[$fog$857](function() {
      if ( !jQuery[$fog$823]( this, $fog$406 ) ) {
        jQuery( this )[$fog$332]( this[$fog$744] );
      }
    })[$fog$255]();
  }
});


jQuery[$fog$576][$fog$593][$fog$672] = function( elem ) {
  // Support: Opera <= 12.12
  // Opera reports offsetWidths and offsetHeights less than zero on some elements
  return elem[$fog$283] <= $fog$865 && elem[$fog$282] <= $fog$865;
};
jQuery[$fog$576][$fog$593][$fog$68] = function( elem ) {
  return !jQuery[$fog$576][$fog$593][$fog$672]( elem );
};




var r20 = $fog$67,
  rbracket = $fog$66,
  rCRLF = $fog$65,
  rsubmitterTypes = $fog$64,
  rsubmittable = $fog$63;

function buildParams( prefix, obj, traditional, add ) {
  var name;

  if ( jQuery[$fog$844]( obj ) ) {
    // Serialize array item.
    jQuery[$fog$857]( obj, function( i, v ) {
      if ( traditional || rbracket[$fog$733]( prefix ) ) {
        // Treat each array item as a scalar.
        add( prefix, v );

      } else {
        // Item is non-scalar (array or object), encode its numeric index.
        buildParams( prefix + $fog$62 + ( typeof v === $fog$886 ? i : $fog$866 ) + $fog$813, v, traditional, add );
      }
    });

  } else if ( !traditional && jQuery[$fog$839]( obj ) === $fog$886 ) {
    // Serialize object item.
    for ( name in obj ) {
      buildParams( prefix + $fog$62 + name + $fog$813, obj[ name ], traditional, add );
    }

  } else {
    // Serialize scalar item.
    add( prefix, obj );
  }
}

// Serialize an array of form elements or a set of
// key/values into a query string
jQuery[$fog$114] = function( a, traditional ) {
  var prefix,
    s = [],
    add = function( key, value ) {
      // If value is a function, invoke it and return its value
      value = jQuery[$fog$846]( value ) ? value() : ( value == $fog$863 ? $fog$866 : value );
      s[ s[$fog$862] ] = encodeURIComponent( key ) + $fog$779 + encodeURIComponent( value );
    };

  // Set traditional to true for jQuery <= 1.3.2 behavior.
  if ( traditional === undefined ) {
    traditional = jQuery[$fog$153] && jQuery[$fog$153][$fog$113];
  }

  // If an array was passed in, assume that it is an array of form elements.
  if ( jQuery[$fog$844]( a ) || ( a[$fog$563] && !jQuery[$fog$845]( a ) ) ) {
    // Serialize the form elements
    jQuery[$fog$857]( a, function() {
      add( this[$fog$671], this[$fog$693] );
    });

  } else {
    // If traditional, encode the "old" way (the way 1.3.2 or older
    // did it), otherwise encode params recursively.
    for ( prefix in a ) {
      buildParams( prefix, a[ prefix ], traditional, add );
    }
  }

  // Return the resulting serialization
  return s[$fog$727]( $fog$109 )[$fog$842]( r20, $fog$788 );
};

jQuery[$fog$872][$fog$849]({
  serialize: function() {
    return jQuery[$fog$114]( this[$fog$61]() );
  },
  serializeArray: function() {
    return this[$fog$855](function() {
      // Can add propHook for "elements" to filter or add form elements
      var elements = jQuery[$fog$261]( this, $fog$60 );
      return elements ? jQuery[$fog$561]( elements ) : this;
    })
    [$fog$695](function() {
      var type = this[$fog$839];

      // Use .is( ":disabled" ) so that fieldset[disabled] works
      return this[$fog$671] && !jQuery( this )[$fog$559]( $fog$666 ) &&
        rsubmittable[$fog$733]( this[$fog$823] ) && !rsubmitterTypes[$fog$733]( type ) &&
        ( this[$fog$598] || !rcheckableType[$fog$733]( type ) );
    })
    [$fog$855](function( i, elem ) {
      var val = jQuery( this )[$fog$177]();

      return val == $fog$863 ?
        $fog$863 :
        jQuery[$fog$844]( val ) ?
          jQuery[$fog$855]( val, function( val ) {
            return { name: elem[$fog$671], value: val[$fog$842]( rCRLF, $fog$59 ) };
          }) :
          { name: elem[$fog$671], value: val[$fog$842]( rCRLF, $fog$59 ) };
    })[$fog$556]();
  }
});


jQuery[$fog$153][$fog$58] = function() {
  try {
    return new XMLHttpRequest();
  } catch( e ) {}
};

var xhrId = $fog$865,
  xhrCallbacks = {},
  xhrSuccessStatus = {
    // file protocol always yields status code 0, assume 200
    0: $fog$209,
    // Support: IE9
    // #1450: sometimes IE returns 1223 when it should be 204
    1223: $fog$85
  },
  xhrSupported = jQuery[$fog$153][$fog$58]();

// Support: IE9
// Open requests must be manually aborted on unload (#5280)
// See https://support.microsoft.com/kb/2856746 for more info
if ( window[$fog$704] ) {
  window[$fog$704]( $fog$703, function() {
    for ( var key in xhrCallbacks ) {
      xhrCallbacks[ key ]();
    }
  });
}

support[$fog$57] = !!xhrSupported && ( $fog$56 in xhrSupported );
support[$fog$73] = xhrSupported = !!xhrSupported;

jQuery[$fog$55](function( options ) {
  var callback;

  // Cross domain only allowed if supported through XMLHttpRequest
  if ( support[$fog$57] || xhrSupported && !options[$fog$119] ) {
    return {
      send: function( headers, complete ) {
        var i,
          xhr = options[$fog$58](),
          id = ++xhrId;

        xhr[$fog$54]( options[$fog$839], options[$fog$122], options[$fog$91], options[$fog$53], options[$fog$52] );

        // Apply custom fields if provided
        if ( options[$fog$51] ) {
          for ( i in options[$fog$51] ) {
            xhr[ i ] = options[$fog$51][ i ];
          }
        }

        // Override mime type if needed
        if ( options[$fog$151] && xhr[$fog$50] ) {
          xhr[$fog$50]( options[$fog$151] );
        }

        // X-Requested-With header
        // For cross-domain requests, seeing as conditions for a preflight are
        // akin to a jigsaw puzzle, we simply never set it to be sure.
        // (it can always be set on a per-request basis or even using ajaxSetup)
        // For same-domain requests, won't change header if already provided.
        if ( !options[$fog$119] && !headers[$fog$49] ) {
          headers[$fog$49] = $fog$48;
        }

        // Set headers
        for ( i in headers ) {
          xhr[$fog$103]( i, headers[ i ] );
        }

        // Callback
        callback = function( type ) {
          return function() {
            if ( callback ) {
              delete xhrCallbacks[ id ];
              callback = xhr[$fog$47] = xhr[$fog$46] = $fog$863;

              if ( type === $fog$123 ) {
                xhr[$fog$123]();
              } else if ( type === $fog$645 ) {
                complete(
                  // file: protocol always yields status 0; see #8605, #14207
                  xhr[$fog$124],
                  xhr[$fog$80]
                );
              } else {
                complete(
                  xhrSuccessStatus[ xhr[$fog$124] ] || xhr[$fog$124],
                  xhr[$fog$80],
                  // Support: IE9
                  // Accessing binary-data responseText throws an exception
                  // (#11426)
                  typeof xhr[$fog$129] === $fog$821 ? {
                    text: xhr[$fog$129]
                  } : undefined,
                  xhr[$fog$45]()
                );
              }
            }
          };
        };

        // Listen to events
        xhr[$fog$47] = callback();
        xhr[$fog$46] = callback($fog$645);

        // Create the abort callback
        callback = xhrCallbacks[ id ] = callback($fog$123);

        try {
          // Do send the request (this may raise an exception)
          xhr[$fog$89]( options[$fog$110] && options[$fog$418] || $fog$863 );
        } catch ( e ) {
          // #14683: Only rethrow if this hasn't been notified as an error yet
          if ( callback ) {
            throw e;
          }
        }
      },

      abort: function() {
        if ( callback ) {
          callback();
        }
      }
    };
  }
});




// Install script dataType
jQuery[$fog$127]({
  accepts: {
    script: $fog$44
  },
  contents: {
    script: $fog$43
  },
  converters: {
    "text script": function( text ) {
      jQuery[$fog$349]( text );
      return text;
    }
  }
});

// Handle cache's special case and crossDomain
jQuery[$fog$42]( $fog$830, function( s ) {
  if ( s[$fog$511] === undefined ) {
    s[$fog$511] = $fog$848;
  }
  if ( s[$fog$119] ) {
    s[$fog$839] = $fog$139;
  }
});

// Bind script tag hack transport
jQuery[$fog$55]( $fog$830, function( s ) {
  // This transport only deals with cross domain requests
  if ( s[$fog$119] ) {
    var script, callback;
    return {
      send: function( _, complete ) {
        script = jQuery($fog$41)[$fog$261]({
          async: $fog$883,
          charset: s[$fog$40],
          src: s[$fog$122]
        })[$fog$439](
          $fog$39,
          callback = function( evt ) {
            script[$fog$493]();
            callback = $fog$863;
            if ( evt ) {
              complete( evt[$fog$839] === $fog$645 ? $fog$38 : $fog$209, evt[$fog$839] );
            }
          }
        );
        document[$fog$825][$fog$826]( script[ $fog$865 ] );
      },
      abort: function() {
        if ( callback ) {
          callback();
        }
      }
    };
  }
});




var oldCallbacks = [],
  rjsonp = $fog$37;

// Default jsonp settings
jQuery[$fog$127]({
  jsonp: $fog$36,
  jsonpCallback: function() {
    var callback = oldCallbacks[$fog$808]() || ( jQuery[$fog$510] + $fog$35 + ( nonce++ ) );
    this[ callback ] = $fog$883;
    return callback;
  }
});

// Detect, normalize options and install callbacks for jsonp requests
jQuery[$fog$42]( $fog$34, function( s, originalSettings, jqXHR ) {

  var callbackName, overwritten, responseContainer,
    jsonProp = s[$fog$33] !== $fog$848 && ( rjsonp[$fog$733]( s[$fog$122] ) ?
      $fog$122 :
      typeof s[$fog$418] === $fog$821 && !( s[$fog$99] || $fog$866 )[$fog$877]($fog$32) && rjsonp[$fog$733]( s[$fog$418] ) && $fog$418
    );

  // Handle iff the expected data type is "jsonp" or we have a parameter to set
  if ( jsonProp || s[$fog$155][ $fog$865 ] === $fog$33 ) {

    // Get callback name, remembering preexisting value associated with it
    callbackName = s[$fog$31] = jQuery[$fog$846]( s[$fog$31] ) ?
      s[$fog$31]() :
      s[$fog$31];

    // Insert callback into url or form data
    if ( jsonProp ) {
      s[ jsonProp ] = s[ jsonProp ][$fog$842]( rjsonp, $fog$723 + callbackName );
    } else if ( s[$fog$33] !== $fog$848 ) {
      s[$fog$122] += ( rquery[$fog$733]( s[$fog$122] ) ? $fog$109 : $fog$108 ) + s[$fog$33] + $fog$779 + callbackName;
    }

    // Use data converter to retrieve json after script execution
    s[$fog$148][$fog$30] = function() {
      if ( !responseContainer ) {
        jQuery[$fog$645]( callbackName + $fog$29 );
      }
      return responseContainer[ $fog$865 ];
    };

    // force json dataType
    s[$fog$155][ $fog$865 ] = $fog$75;

    // Install callback
    overwritten = window[ callbackName ];
    window[ callbackName ] = function() {
      responseContainer = arguments;
    };

    // Clean-up function (fires after converters)
    jqXHR[$fog$236](function() {
      // Restore preexisting value
      window[ callbackName ] = overwritten;

      // Save back as free
      if ( s[ callbackName ] ) {
        // make sure that re-using the options doesn't screw things around
        s[$fog$31] = originalSettings[$fog$31];

        // save the callback name for future use
        oldCallbacks[$fog$878]( callbackName );
      }

      // Call if it was a function and we have a response
      if ( responseContainer && jQuery[$fog$846]( overwritten ) ) {
        overwritten( responseContainer[ $fog$865 ] );
      }

      responseContainer = overwritten = undefined;
    });

    // Delegate to script
    return $fog$830;
  }
});




// data: string of html
// context (optional): If specified, the fragment will be created in this context, defaults to document
// keepScripts (optional): If true, will include scripts passed in the html string
jQuery[$fog$564] = function( data, context, keepScripts ) {
  if ( !data || typeof data !== $fog$821 ) {
    return $fog$863;
  }
  if ( typeof context === $fog$847 ) {
    keepScripts = context;
    context = $fog$848;
  }
  context = context || document;

  var parsed = rsingleTag[$fog$740]( data ),
    scripts = !keepScripts && [];

  // Single tag
  if ( parsed ) {
    return [ context[$fog$831]( parsed[$fog$852] ) ];
  }

  parsed = jQuery[$fog$338]( [ data ], context, scripts );

  if ( scripts && scripts[$fog$862] ) {
    jQuery( scripts )[$fog$493]();
  }

  return jQuery[$fog$861]( [], parsed[$fog$744] );
};


// Keep a copy of the old load method
var _load = jQuery[$fog$872][$fog$517];

/**
 * Load a url into a page
 */
jQuery[$fog$872][$fog$517] = function( url, params, callback ) {
  if ( typeof url !== $fog$821 && _load ) {
    return _load[$fog$854]( this, arguments );
  }

  var selector, type, response,
    self = this,
    off = url[$fog$877]($fog$815);

  if ( off >= $fog$865 ) {
    selector = jQuery[$fog$833]( url[$fog$880]( off ) );
    url = url[$fog$880]( $fog$865, off );
  }

  // If it's a function
  if ( jQuery[$fog$846]( params ) ) {

    // We assume that it's the callback
    callback = params;
    params = undefined;

  // Otherwise, build a param string
  } else if ( params && typeof params === $fog$886 ) {
    type = $fog$28;
  }

  // If we have elements to modify, make the request
  if ( self[$fog$862] > $fog$865 ) {
    jQuery[$fog$73]({
      url: url,

      // if "type" variable is undefined, then "GET" method will be used
      type: type,
      dataType: $fog$339,
      data: params
    })[$fog$540](function( responseText ) {

      // Save response for use in complete callback
      response = arguments;

      self[$fog$339]( selector ?

        // If a selector was specified, locate the right elements in a dummy div
        // Exclude scripts to avoid IE 'Permission Denied' errors
        jQuery($fog$27)[$fog$341]( jQuery[$fog$564]( responseText ) )[$fog$697]( selector ) :

        // Otherwise use the full result
        responseText );

    })[$fog$515]( callback && function( jqXHR, status ) {
      self[$fog$857]( callback, response || [ jqXHR[$fog$129], status, jqXHR ] );
    });
  }

  return this;
};




// Attach a bunch of functions for handling common AJAX events
jQuery[$fog$857]( [ $fog$111, $fog$76, $fog$77, $fog$78, $fog$79, $fog$92 ], function( i, type ) {
  jQuery[$fog$872][ type ] = function( fn ) {
    return this[$fog$439]( type, fn );
  };
});




jQuery[$fog$576][$fog$593][$fog$26] = function( elem ) {
  return jQuery[$fog$569](jQuery[$fog$215], function( fn ) {
    return elem === fn[$fog$421];
  })[$fog$862];
};




var docElem = window[$fog$884][$fog$711];

/**
 * Gets a window from an element
 */
function getWindow( elem ) {
  return jQuery[$fog$835]( elem ) ? elem : elem[$fog$836] === $fog$742 && elem[$fog$708];
}

jQuery[$fog$25] = {
  setOffset: function( elem, options, i ) {
    var curPosition, curLeft, curCSSTop, curTop, curOffset, curCSSLeft, calculatePosition,
      position = jQuery[$fog$477]( elem, $fog$24 ),
      curElem = jQuery( elem ),
      props = {};

    // Set position first, in-case top/left are set even on static elem
    if ( position === $fog$23 ) {
      elem[$fog$321][$fog$24] = $fog$588;
    }

    curOffset = curElem[$fog$25]();
    curCSSTop = jQuery[$fog$477]( elem, $fog$707 );
    curCSSLeft = jQuery[$fog$477]( elem, $fog$22 );
    calculatePosition = ( position === $fog$297 || position === $fog$21 ) &&
      ( curCSSTop + curCSSLeft )[$fog$877]($fog$249) > -$fog$852;

    // Need to be able to calculate position if either
    // top or left is auto and position is either absolute or fixed
    if ( calculatePosition ) {
      curPosition = curElem[$fog$24]();
      curTop = curPosition[$fog$707];
      curLeft = curPosition[$fog$22];

    } else {
      curTop = parseFloat( curCSSTop ) || $fog$865;
      curLeft = parseFloat( curCSSLeft ) || $fog$865;
    }

    if ( jQuery[$fog$846]( options ) ) {
      options = options[$fog$864]( elem, i, curOffset );
    }

    if ( options[$fog$707] != $fog$863 ) {
      props[$fog$707] = ( options[$fog$707] - curOffset[$fog$707] ) + curTop;
    }
    if ( options[$fog$22] != $fog$863 ) {
      props[$fog$22] = ( options[$fog$22] - curOffset[$fog$22] ) + curLeft;
    }

    if ( $fog$20 in options ) {
      options[$fog$20][$fog$864]( elem, props );

    } else {
      curElem[$fog$477]( props );
    }
  }
};

jQuery[$fog$872][$fog$849]({
  offset: function( options ) {
    if ( arguments[$fog$862] ) {
      return options === undefined ?
        this :
        this[$fog$857](function( i ) {
          jQuery[$fog$25][$fog$19]( this, options, i );
        });
    }

    var docElem, win,
      elem = this[ $fog$865 ],
      box = { top: $fog$865, left: $fog$865 },
      doc = elem && elem[$fog$743];

    if ( !doc ) {
      return;
    }

    docElem = doc[$fog$711];

    // Make sure it's not a disconnected DOM node
    if ( !jQuery[$fog$653]( docElem, elem ) ) {
      return box;
    }

    // Support: BlackBerry 5, iOS 3 (original iPhone)
    // If we don't have gBCR, just use 0,0 rather than error
    if ( typeof elem[$fog$18] !== strundefined ) {
      box = elem[$fog$18]();
    }
    win = getWindow( doc );
    return {
      top: box[$fog$707] + win[$fog$17] - docElem[$fog$400],
      left: box[$fog$22] + win[$fog$16] - docElem[$fog$404]
    };
  },

  position: function() {
    if ( !this[ $fog$865 ] ) {
      return;
    }

    var offsetParent, offset,
      elem = this[ $fog$865 ],
      parentOffset = { top: $fog$865, left: $fog$865 };

    // Fixed elements are offset from window (parentOffset = {top:0, left: 0}, because it is its only offset parent
    if ( jQuery[$fog$477]( elem, $fog$24 ) === $fog$21 ) {
      // Assume getBoundingClientRect is there when computed position is fixed
      offset = elem[$fog$18]();

    } else {
      // Get *real* offsetParent
      offsetParent = this[$fog$15]();

      // Get correct offsets
      offset = this[$fog$25]();
      if ( !jQuery[$fog$823]( offsetParent[ $fog$865 ], $fog$339 ) ) {
        parentOffset = offsetParent[$fog$25]();
      }

      // Add offsetParent borders
      parentOffset[$fog$707] += jQuery[$fog$477]( offsetParent[ $fog$865 ], $fog$14, $fog$883 );
      parentOffset[$fog$22] += jQuery[$fog$477]( offsetParent[ $fog$865 ], $fog$13, $fog$883 );
    }

    // Subtract parent offsets and element margins
    return {
      top: offset[$fog$707] - parentOffset[$fog$707] - jQuery[$fog$477]( elem, $fog$12, $fog$883 ),
      left: offset[$fog$22] - parentOffset[$fog$22] - jQuery[$fog$477]( elem, $fog$11, $fog$883 )
    };
  },

  offsetParent: function() {
    return this[$fog$855](function() {
      var offsetParent = this[$fog$15] || docElem;

      while ( offsetParent && ( !jQuery[$fog$823]( offsetParent, $fog$339 ) && jQuery[$fog$477]( offsetParent, $fog$24 ) === $fog$23 ) ) {
        offsetParent = offsetParent[$fog$15];
      }

      return offsetParent || docElem;
    });
  }
});

// Create scrollLeft and scrollTop methods
jQuery[$fog$857]( { scrollLeft: $fog$16, scrollTop: $fog$17 }, function( method, prop ) {
  var top = $fog$17 === prop;

  jQuery[$fog$872][ method ] = function( val ) {
    return access( this, function( elem, method, val ) {
      var win = getWindow( elem );

      if ( val === undefined ) {
        return win ? win[ prop ] : elem[ method ];
      }

      if ( win ) {
        win[$fog$10](
          !top ? val : window[$fog$16],
          top ? val : window[$fog$17]
        );

      } else {
        elem[ method ] = val;
      }
    }, method, val, arguments[$fog$862], $fog$863 );
  };
});

// Support: Safari<7+, Chrome<37+
// Add the top/left cssHooks using jQuery.fn.position
// Webkit bug: https://bugs.webkit.org/show_bug.cgi?id=29084
// Blink bug: https://code.google.com/p/chromium/issues/detail?id=229280
// getComputedStyle returns percent when specified for top/left/bottom/right;
// rather than make the css module depend on the offset module, just check for it here
jQuery[$fog$857]( [ $fog$707, $fog$22 ], function( i, prop ) {
  jQuery[$fog$273][ prop ] = addGetHookIf( support[$fog$9],
    function( elem, computed ) {
      if ( computed ) {
        computed = curCSS( elem, prop );
        // If curCSS returns percentage, fallback to offset
        return rnumnonpx[$fog$733]( computed ) ?
          jQuery( elem )[$fog$24]()[ prop ] + $fog$289 :
          computed;
      }
    }
  );
});


// Create innerHeight, innerWidth, height, width, outerHeight and outerWidth methods
jQuery[$fog$857]( { Height: $fog$267, Width: $fog$319 }, function( name, type ) {
  jQuery[$fog$857]( { padding: $fog$8 + name, content: type, "": $fog$7 + name }, function( defaultExtra, funcName ) {
    // Margin is only for outerHeight, outerWidth
    jQuery[$fog$872][ funcName ] = function( margin, value ) {
      var chainable = arguments[$fog$862] && ( defaultExtra || typeof margin !== $fog$847 ),
        extra = defaultExtra || ( margin === $fog$883 || value === $fog$883 ? $fog$286 : $fog$288 );

      return access( this, function( elem, type, value ) {
        var doc;

        if ( jQuery[$fog$835]( elem ) ) {
          // As of 5/8/2012 this will yield incorrect results for Mobile Safari, but there
          // isn't a whole lot we can do. See pull request at this URL for discussion:
          // https://github.com/jquery/jquery/pull/764
          return elem[$fog$884][$fog$711][ $fog$6 + name ];
        }

        // Get document width or height
        if ( elem[$fog$836] === $fog$742 ) {
          doc = elem[$fog$711];

          // Either scroll[Width/Height] or offset[Width/Height] or client[Width/Height],
          // whichever is greatest
          return Math[$fog$290](
            elem[$fog$406][ $fog$5 + name ], doc[ $fog$5 + name ],
            elem[$fog$406][ $fog$25 + name ], doc[ $fog$25 + name ],
            doc[ $fog$6 + name ]
          );
        }

        return value === undefined ?
          // Get width or height on the element, requesting but not forcing parseFloat
          jQuery[$fog$477]( elem, type, extra ) :

          // Set width or height on the element
          jQuery[$fog$321]( elem, type, value, extra );
      }, type, chainable ? margin : undefined, chainable, $fog$863 );
    };
  });
});


// The number of elements contained in the matched element set
jQuery[$fog$872][$fog$4] = function() {
  return this[$fog$862];
};

jQuery[$fog$872][$fog$3] = jQuery[$fog$872][$fog$2];




// Register as a named AMD module, since jQuery can be concatenated with other
// files that may use define, but not via a proper concatenation script that
// understands anonymous AMD modules. A named AMD is safest and most robust
// way to register. Lowercase jquery is used because AMD module names are
// derived from file names, and jQuery is normally delivered in a lowercase
// file name. Do this after creating the global so that if an AMD module wants
// to call noConflict to hide this version of jQuery, it will work.

// Note that for maximum portability, libraries that are not jQuery should
// declare themselves as anonymous modules, and avoid setting a global if an
// AMD loader is present. jQuery is a special case. For more information, see
// https://github.com/jrburke/requirejs/wiki/Updating-existing-libraries#wiki-anon

if ( typeof define === $fog$838 && define[$fog$1] ) {
  define( $fog$563, [], function() {
    return jQuery;
  });
}




var
  // Map over jQuery in case of overwrite
  _jQuery = window[$fog$843],

  // Map over the $ in case of overwrite
  _$ = window[$fog$777];

jQuery[$fog$0] = function( deep ) {
  if ( window[$fog$777] === jQuery ) {
    window[$fog$777] = _$;
  }

  if ( deep && window[$fog$843] === jQuery ) {
    window[$fog$843] = _jQuery;
  }

  return jQuery;
};

// Expose jQuery and $ identifiers, even in AMD
// (#7102#comment:10, https://github.com/jquery/jquery/pull/557)
// and CommonJS for browser emulators (#13566)
if ( typeof noGlobal === strundefined ) {
  window[$fog$843] = window[$fog$777] = jQuery;
}




return jQuery;

}));
})();
})("tcejbo", "stropxe", "tnemucod", true, "tnemucod a htiw wodniw a seriuqer yreuQj", "denifednu", "ecils", "tacnoc", "hsup", "fOxedni", "gnirtSot", "ytreporPnwOsah", "4.1.2", "tini", "nf", /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, /^-ms-/, /-([\da-z])/gi, "esaCreppUot", "epytotorp", "", 0, "llac", null, "htgnel", "egrem", "rotcurtsnoc", "tcejbOverp", "txetnoc", "hcae", "kcatShsup", "pam", "ylppa", "qe", 1, "tros", "ecilps", "dnetxe", false, "naeloob", "noitcnuFsi", "tcejbOnialPsi", "yarrAsi", "yreuQj", "ecalper", "modnar", /\D/g, "epyt", "noitcnuf", "wodniw", "epyTedon", "wodniWsi", "fOepytotorPsi", "mirt", "tcirts esu", "tnemelEetaerc", "tpircs", "txet", "dlihCevomer", "edoNtnerap", "dlihCdneppa", "daeh", "-sm", "emaNedon", "esaCrewoLot", "gnirts", 2, "diug", "won", "tilps", "rorrE tcejbO pxEgeR etaD yarrA noitcnuF gnirtS rebmuN naelooB", " ", " tcejbo[", "]", "yarra", "rebmun", "elzzis", 31, "pop", "depocs|deriuqer|ylnodaer|nepo|elpitlum|pool|pamsi|neddih|delbasid|refed|slortnoc|yalpotua|sucofotua|cnysa|detceles|dekcehc", "]f\\n\\r\\t\\02x\\[", "+)]0ax\\-00x\\^[|]-w\\[|.\\\\:?(", "w", "#w", "[\\", "(*", ":?()", ")=?]~!|$^*[(*", "(|\")*)]\"\\\\^[|.\\\\:?((\"|')*)]'\\\\^[|.\\\\:?((':?(*", ")|))", "]\\*", "(:", "((\\:?()", "|)\")*)]\"\\\\^[|.\\\\:?((\"|')*)]'\\\\^[|.\\\\:?(('(", "|]]\\[)(\\\\^[|.\\\\:?((", "|)*)", "*.", ")|)\\)", "+", "g", "^", ")*).\\\\:?()]\\\\^[|^:?((|+", "$+", ",*", "*", "|]~+>[(*", ")", "=", ")?*]\"']\\^[(*", "$", "(#^", "(.\\^", "(^", "*w", "(\\:?()epyt-fo|dlihc(-)tsal-htn|htn|tsal|tsrif|ylno(:^", ")|n)*d\\()|]-+[((|ddo|neve(*", ")|]-+[(:?(*", "))|)+d\\(*", ")|)\\*", "i", ":?(^", "$)", "(\\:?()tsal|tsrif|htn|tl|tg|qe|ddo|neve(:|]~+>[*", ")*d\\?)d\\-:?((*", ")$|]-^[=?()|)\\*", /^(?:input|select|textarea|button)$/i, /^h\d$/i, /^[^{]+\{\s*\[native \w/, /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/, /[+~]/, /'|\\/g, "}6,1{]f-ad\\[(\\\\", "(|?", ").|)", "gi", "x0", 65536, "edoCrahCmorf", 10, 55296, 1023, 56320, "sedoNdlihc", "tnemucoDrenwo", 9, 11, "cexe", "dIyBtnemelEteg", "di", "emaNgaTyBstnemelEteg", 3, "emaNssalCyBstnemelEteg", "asq", "tset", "etubirttAteg", "&$\\", "etubirttAtes", "'=di[", " ]'", "nioj", ",", "llArotceleSyreuq", "etubirttAevomer", "1$", "htgneLehcac", "tfihs", "vid", "|", "eldnaHrtta", "xednIecruos", "gnilbiStxen", "tupni", "nottub", "troppus", "LMXsi", "tnemelEtnemucod", "LMTH", "tnemucoDtes", "weiVtluafed", "pot", "renetsiLtnevEdda", "daolnu", "tnevEhcatta", "daolnuno", "setubirtta", "emaNssalc", "tnemmoCetaerc", "dIyBteg", "emaNyBstnemelEteg", "dnif", "DI", "retlif", "edoNetubirttAteg", "eulav", "GAT", "SSALC", "LMTHrenni", "'=di a<", ">a/<>'", "'=di tceles<", ">''=erutpacwollasm ']\f-", ">tceles/<>noitpo/<>''=detceles noitpo<", "]''=^erutpacwollasm[", "=]$^*[", ")\"\"|'':?(*", "]detceles[", "|eulav:?(*", "=~di[", "]-", "=~", "dekcehc:", "#a", "*+", "]~+[+.#.", "neddih", "eman", "D", "]d=eman[", "=?]~!|$^*[*", "delbane:", "delbasid:", "x:,*", ":*.,", "rotceleSsehctam", "sehctam", "rotceleSsehctaMtikbew", "rotceleSsehctaMzom", "rotceleSsehctaMo", "rotceleSsehctaMsm", "hctaMdetcennocsid", "x:]''=!s[", "=!", "noitisoPtnemucoDerapmoc", "sniatnoc", 16, "dehcateDtros", 4, "tfihsnu", "]'1$'=", "rtta", "deificeps", "rorre", " :noisserpxe dezingocernu ,rorre xatnyS", "troSeuqinu", "setacilpuDtceted", "elbatStros", "txeTteg", "tnetnoCtxet", "dlihCtsrif", "eulaVedon", "srotceles", 50, "gnilbiSsuoiverp", 5, "htn", 6, "neve", "ddo", 7, 8, "DLIHC", "|^(", "(", ")$|", "ssalc", "=^", "=*", "=$", "=|", "-", "tsal", "epyt-fo", "ylno", "dlihCtsal", "soduesp", "sretliFtes", " :oduesp detroppusnu", "txeTrenni", " :gnal detroppusnu", "gnal", "gnal:lmx", "noitacol", "hsah", "tnemelEevitca", "sucoFsah", "ferh", "xednIbat", "delbasid", "dekcehc", "noitpo", "detceles", "xednIdetceles", "ytpme", "sretlif", "ezinekot", "retliFerp", "rid", "tsrif", "evitaler", "0", 0.1, "elipmoc", "rotceles", "tceles", "txetnoCsdeen", ">a/<>'#'=ferh a<", "#", "htdiw|thgieh|ferh|epyt", ">/tupni<", "eulaVtluafed", "rpxe", ":", "euqinu", "coDLMXsi", "hctam", /^<(\w+)\s*\/?>(?:<\/\1>|)$/, /^.[^:#\[\.,]*$/, "perg", "(ton:", /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]*))$/, "<", ">", "LMTHesrap", "yreuqj", "ydaer", "yarrAekam", /^(?:parents|prev(?:Until|All))/, "si", "xedni", "llAverp", "teg", "dda", "gnilbis", "tnemucoDtnetnoc", "litnU", "esrever", /\S+/g, "skcabllaC", "ecno", "yromem", "eslaFnOpots", "elbasid", "sah", "yarrAni", "htiWerif", "evloser", "enod", "yromem ecno", "devloser", "tcejer", "liaf", "detcejer", "yfiton", "ssergorp", "gnidnep", "esimorp", "derrefeD", "htiW", "epip", "neht", "kcol", "htiWyfiton", "htiWevloser", "tiaWydaer", "ydaeRsi", "reldnaHreggirt", "ffo", "renetsiLtnevEevomer", "dedaoLtnetnoCMOD", "daol", "etatSydaer", "etelpmoc", "ssecca", "ataDtpecca", "ytreporPenifed", "ehcac", "odnapxe", "diu", "stpecca", "seitreporPenifed", "yek", "tcejbOytpmEsi", "esaClemac", "tes", /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/, /([A-Z])/g, "-atad", "1$-", "eurt", "eslaf", "llun", "NOSJesrap", "ataDsah", "evomer", "srttAataDsah", "xf", "eueuq", "skooHeueuq_", "eueuqed", "ssergorpni", "pots", "erif", "skooHeueuq", "ecruos", /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/, "poT", "thgiR", "mottoB", "tfeL", "ssc", "yalpsid", "enon", /^(?:checkbox|radio)$/i, "tnemgarFtnemucoDetaerc", "oidar", "t", "enolCkcehc", "edoNenolc", ">aeratxet/<x>aeratxet<", "dekcehCenolCon", "selbbuBnisucof", "nisucofno", /^key/, /^(?:mouse|pointer|contextmenu)|click/, /^(?:focusinfocus|focusoutblur)$/, /^([^.]*)(?:\.(.+)|)$/, "tneve", "reldnah", "stneve", "eldnah", "dereggirt", "hctapsid", ".", "laiceps", "epyTetageled", "epyTdnib", "tnuoCetageled", "putes", "labolg", ").\\|^(", ")|.\\*.:?(.\\", ")$|.\\(", "epyTgiro", "ecapseman", "**", "nwodraet", "tnevEevomer", "no", "tnevE", "reggirTsi", "er_ecapseman", "tluser", "tegrat", "reggirt", "elbbuBon", "wodniWtnerap", "deppotSnoitagaporPsi", "tluafeDtneverp", "detneverPtluafeDsi", "tluafed_", "xif", "tegraTetageled", "hctapsiDerp", "sreldnah", "tegraTtnerruc", "mele", "deppotSnoitagaporPetaidemmIsi", "jbOeldnah", "atad", "noitagaporPpots", "hctapsiDtsop", "kcilc", "hcihw weiv pmatSemit tegrat yeKtfihs tegraTdetaler yeKatem esahPtneve tegraTtnerruc yeKlrtc elbalecnac selbbub yeKtla", "edoCyek yek edoCrahc rahc", "hcihw", "edoCrahc", "edoCyek", "tnemelEot Yneercs Xneercs Yegap Xegap Ytesffo Xtesffo Ytneilc Xtneilc snottub nottub", "Xegap", "Xtneilc", "ydob", "tfeLllorcs", "tfeLtneilc", "Yegap", "Ytneilc", "poTllorcs", "poTtneilc", "skooHxif", "skooHesuom", "skooHyek", "sporp", "sucof", "nisucof", "rulb", "tuosucof", "xobkcehc", "a", "tnevElanigiro", "eulaVnruter", "detneverPtluafed", "pmatSemit", "noitagaporPetaidemmIpots", "revoesuom", "tuoesuom", "revoretniop", "tuoretniop", "tegraTdetaler", "etalumis", /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([\w:]+)[^>]*)\/>/gi, /<([\w:]+)/, /<|&#?\w+;/, /<(?:script|style|link)/i, /checked\s*(?:[^=]|=\s*.checked.)/i, /^$|\/(?:java|ecma)script/i, /^true\/(.*)/, /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g, ">'elpitlum'=elpitlum tceles<", ">tceles/<", ">elbat<", ">elbat/<", ">puorgloc<>elbat<", ">elbat/<>puorgloc/<", ">ydobt<>elbat<", ">elbat/<>ydobt/<", ">rt<>ydobt<>elbat<", ">elbat/<>ydobt/<>rt/<", "puorgtpo", "ydobt", "tooft", "puorgloc", "noitpac", "daeht", "ht", "dt", "elbat", "rt", "/", "lavElabolg", "aeratxet", "edoNtxeTetaerc", ">2$/<>1$<", "pinaMmod", "erofeBtresni", "ataDnaelc", "enolc", "dneppa", "dlihCecalper", "lmth", "tnemgarFdliub", "crs", "lrUlave_", "dneperp", "erofeb", "retfa", "htiWecalper", "oTdneppa", "elytSdetupmoCtluafeDteg", "hcated", ">/'0'=thgieh '0'=htdiw '0'=redrobemarf emarfi<", "etirw", "esolc", /^margin/, "$+]%z-a[)xp!?()", "renepo", "elytSdetupmoCteg", "elyts", "eulaVytreporPteg", "htdiw", "htdiWnim", "htdiWxam", "pilCdnuorgkcab", "xob-tnetnoc", "elytSenolCraelc", "txeTssc", ";xp1:pot-nigram;xp9999-:tfel;0:pot;0:thgieh;0:htdiw;0:redrob", "etulosba:noitisop", ";xob-redrob:gnizis-xob-zom-;xob-redrob:gnizis-xob-tikbew-", ";%1:pot;%1:pot-nigram;kcolb:yalpsid;xob-redrob:gnizis-xob", "etulosba:noitisop;xp4:htdiw;xp1:gniddap;xp1:redrob", "%1", "xp4", ";xob-tnetnoc:gnizis-xob-zom-;xob-tnetnoc:gnizis-xob-tikbew-", "0:gniddap;0:redrob;0:nigram;kcolb:yalpsid;xob-tnetnoc:gnizis-xob", "thgiRnigram", "xp1", "paws", /^(none|table(?!-c[ea]).+)/, "$)*.()", "(=)]-+[(^", "etulosba", "kcolb", "004", "tikbeW", "O", "zoM", "sm", "xam", "xp", "redrob", "tnetnoc", "nigram", "gniddap", "htdiW", "htdiWtesffo", "thgieHtesffo", "gniziSxob", "xob-redrob", "elbaileRgniziSxob", "yalpsiddlo", "yticapo", "1", "taolFssc", "sporPssc", "skooHssc", "rebmuNssc", "dnuorgkcab", "tirehni", "lamron", "ciremuNsi", "thgieh", "thgiRnigraMelbailer", "kcolb-enilni", "wohs", "edih", "neewT", "porp", "gnisae", "gniws", "snoitpo", "trats", "ruc", "dne", "tinu", "skooHporp", "noitarud", "sop", "pets", "otua", 0.5, "soc", "IP", /^(?:toggle|show|hide)$/, "()|=)]-+[(:?(^", "$)*]%z-a[()", /queueHooks$/, "neewTetaerc", 20, "5.", "wohsxf", "deueuqnu", "syawla", "wolfrevo", "Xwolfrevo", "Ywolfrevo", "enilni", "taolf", "elggot", "dnapxe", "emiTtrats", "sneewt", "nur", "stpo", "gnisaElaiceps", "htiWtcejer", "remit", "noitaminA", "deeps", "sdeeps", "dlo", "etamina", "hsinif", "sremit", "mina", "kcit", "lavretni", 13, 600, 200, 400, "yaled", "nOkcehc", "detceleStpo", "delbasiDtpo", "eulaVoidar", "rttAevomer", "skooHrtta", "loob", "xiFporp", /\w+/g, "roFlmth", "etubirttAsah", "xednibat", "ylnOdaer", "htgneLxam", "gnicapSllec", "gniddaPllec", "napSwor", "napSloc", "paMesu", "redroBemarf", "elbatidEtnetnoc", /[\t\r\n\f]/g, "ssalCdda", "ssalCevomer", "ssalCelggot", "ssalCsah", "__emaNssalc__", /\r/g, "skooHlav", "lav", "eno-tceles", " kcilclbd kcilc daolnu llorcs eziser daol tuosucof nisucof sucof rulb", " evaelesuom retneesuom tuoesuom revoesuom evomesuom puesuom nwodesuom", "unemtxetnoc rorre puyek sserpyek nwodyek timbus tceles egnahc", "evaelesuom", "retneesuom", /\?/, "esrap", "LMXesrap", "gnirtSmorFesrap", "lmx/txet", "rorreresrap", " :LMX dilavnI", /#.*$/, /([?&])_=[^&]*/, /^(.*?):[ \t]*([^\r\n]*)$/mg, /^(?:about|app|app-storage|.+-extension|file|res|widget):$/, /^(?:GET|HEAD)$/, /^\/\//, /^([\w.+-]+:)(?:\/\/(?:[^\/?#]*@|)([^\/?#:]*)(?::(\d+)|)|)/, "/*", "sepyTatad", "snoitpOtalf", "sgnitteSxaja", "stnetnoc", "epyTemim", "redaeHesnopseRteg", "epyT-tnetnoC", "sretrevnoc", "sdleiFesnopser", "retliFatad", "epyTatad", " *", "sworht", " morf noisrevnoc oN", " ot ", "sseccus", "TEG", "8-FTU=tesrahc ;dedocnelru-mrof-www-x/noitacilppa", "nialp/txet", "lmth/txet", "lmx/txet ,lmx/noitacilppa", "tpircsavaj/txet ,nosj/noitacilppa", /xml/, /html/, /json/, "LMXesnopser", "txeTesnopser", "NOSJesnopser", "puteSxaja", "edoCsutats", "delecnac", "sutats", "troba", "lru", "//", "dohtem", "niamoDssorc", ":ptth", "08", "344", "ataDssecorp", "marap", "lanoitidart", "evitca", "tratSxaja", "tnetnoCsah", "&", "?", "=_1$", "=_", "deifidoMfi", "deifidoMtsal", "redaeHtseuqeRtes", "ecniS-deifidoM-fI", "gate", "hctaM-enoN-fI", "epyTtnetnoc", "tpeccA", " ,", "10.0=q ;", "sredaeh", "dneSerofeb", "tropsnarT oN", "dneSxaja", "cnysa", "tuoemit", "dnes", 300, 304, "deifidoM-tsaL", 204, "DAEH", "tnetnocon", "deifidomton", "etats", "txeTsutats", "sseccuSxaja", "rorrExaja", "etelpmoCxaja", "potSxaja", "nosj", "tsop", "xaja", "llAparw", "dlihCtnemelEtsrif", "rennIparw", "tnerap", "elbisiv", /%20/g, /\[\]$/, /\r?\n/g, /^(?:submit|button|image|reset|file)$/i, /^(?:input|select|textarea|keygen)/i, "[", "yarrAezilaires", "stnemele", "\n\r", "rhx", "sroc", "slaitnederChtiw", "tropsnarTxaja", "nepo", "emanresu", "drowssap", "sdleiFrhx", "epyTemiMedirrevo", "htiW-detseuqeR-X", "tseuqeRpttHLMX", "daolno", "rorreno", "sredaeHesnopseRllAteg", "tpircsamce-x/noitacilppa ,tpircsamce/noitacilppa ,tpircsavaj/noitacilppa ,tpircsavaj/txet", /(?:java|ecma)script/, "retliferPxaja", ">tpircs<", "tesrahCtpircs", "rorre daol", 404, /(=)\?(?=&|$)|\?\?/, "kcabllac", "_", "pnosj nosj", "pnosj", "dedocnelru-mrof-www-x/noitacilppa", "kcabllaCpnosj", "nosj tpircs", "dellac ton saw ", "TSOP", ">vid<", "detamina", "tesffo", "noitisop", "citats", "tfel", "dexif", "gnisu", "tesffOtes", "tceRtneilCgnidnuoBteg", "tesffOYegap", "tesffOXegap", "tneraPtesffo", "htdiWpoTredrob", "htdiWtfeLredrob", "poTnigram", "tfeLnigram", "oTllorcs", "noitisoPlexip", "renni", "retuo", "tneilc", "llorcs", "ezis", "fleSdna", "kcaBdda", "dma", "tcilfnoCon", "‮", 887, "", 0, "string", "split", "reverse", "join", 1, 2);
     